# 8.22.0 电话提醒与独立翻译验收

本轮按用户最新要求取消8.21的电话拦截规则：电话由人工核对，翻译独立处理聊天正文。保留现有界面、后台连接和历史记录，不重做其他功能。

## 最终行为

- 助手显示实际关联的聊天号码。客户电话缺失、格式无法判断或号码有差异时，仅在号码后显示红色圈叹号；悬停、点击或键盘聚焦查看两个号码，Escape关闭提示，不新增提示行。
- 可识别的本地与国际号码等价格式不产生差异提示。系统不自动改客户电话、不自动换绑；关联、同步、刷新、提取、拟回复和复制不因电话提醒阻断。
- 点击翻译，将当前显示、已经存储的消息正文交给AI生成中文，英文、阿拉伯文及其他模型可识别语言均可处理。历史不完整、订单阶段、电话差异不成为翻译前提；可见引用/编辑正文和较早历史也可翻译。
- 无正文、图片占位和待解密占位跳过自身。逐条缓存、每次最多20条新译文、原文保留、失败批次不部分写入；不会自动调用AI。
- 保留聊天身份、账号、消息归属、关联版本及异步结果过期保护。提取、拟回复使用业务上下文时原有完整性检查继续适用；它们与逐条翻译的要求不同。

## 验收依据

完整单元测试349/349通过，项目检查通过。源码Electron界面6/6通过。最终ZIP新解压副本11/11通过，零失败、零跳过，耗时186.25秒；已验证App实际启动、包版本8.22.0及界面8.22，深度签名检查通过。结果记录在 `artifacts/assistant-advisory-8.22/final-e2e.log`，包装元数据、校验值及源码一致性见同目录的 `package-target.json`、`package-integrity.json` 和 `ZIP-SHA256.txt`。包内130个源码、界面与资源文件逐字节一致，WhatsApp连接库仍锁定1.34.7。

关键新增检验同时设置电话不一致和历史不完整：英文、阿拉伯文、法文正文提交到模拟翻译接口；引用及较早历史有译文，图片占位不请求翻译。电话图标同行、红色、浮层显示两个号码且不改变行高，翻译/复制按钮可用。修改电话后后台仍接收明确关联聊天的事件，草稿保留；重启后缓存和草稿保留，改成等价号码后提醒消失。既有订单隔离、同客多单待关联、去重、断网恢复、图片重试和备份检查继续运行。

所有故障注入、数据库写入及模型响应来自隔离虚构数据。模拟接口证明调用链和缓存行为，不证明真实模型翻译准确率；本轮未新增真实AI请求、未发送真实WhatsApp消息、未修改正式订单。有效模型、密钥、网络及额度仍决定真实翻译服务是否成功。本轮未重新做两小时真实连接观察，8.20的既有真实验收记录保留，不作为8.22新增验收宣称。

## 使用与交付

安装包：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.22.0-x64.zip`，大小161,521,544字节。SHA-256：`9c49eba35a52d2b6b582b8e0ad00c4b49ca6427993db98f45b5c775a80fd6fdf`。先在菜单栏旧应用图标选择“退出”，解压ZIP，双击 `KDocs订单助手 8.22.0.app`。正常使用无需命令启动器或测试资料；沿用现有本机资料与登录目录。版本应显示“本地业务助手8.22”。登录有效时无需重新扫码；翻译无需先修改客户电话。

本轮提交只含任务变更与虚构验收证据，未推送。原工作区其他修改、原HEAD及暂存区保持原样，记录见 `artifacts/assistant-advisory-8.22/workspace-preservation.json` 与 `git-delivery.json`。

接下来做什么、由谁做、是否需要用户操作：开发方已完成修改、检查、打包和最终副本复验；用户仅退出旧版并打开8.22 App，正常使用翻译。无需用户运行测试或处理电话校验拦截。
