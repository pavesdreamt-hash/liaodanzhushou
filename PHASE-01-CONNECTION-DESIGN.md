# 阶段 0、1：基线、连接设计与人工关口 A

> 2026-09-13 最新核验：用户已完成密钥保存、登录、指定目标和两条新虚构消息发送；有限真实读取及刷新验证通过。当前完整结果见 [WHATSAPP-CONNECTION-ACCEPTANCE.md](WHATSAPP-CONNECTION-ACCEPTANCE.md)。App 内读取接入、通用消息覆盖及 AI 语义验收尚待开发，不能把有限探针视为完整读取器。

日期：2026-09-12；检查对象：当前工作区 8.5.0。

2026-09-13 更新：用户已确认网页入口、双向自用测试账号及本轮新消息范围；随后要求先交付设置基础。8.6 实现及验收见 ASSISTANT-SETTINGS-ACCEPTANCE.md，首次密钥输入改用应用“助手设置”的系统安全输入窗口，本文第 5 节的手工钥匙串方案不再作为当前操作步骤。

这是 2026-09-12 的接入设计与执行记录，不是新版本发布说明。本文标为“设计”的入口、状态和适配逻辑尚未写入 8.5 App，不能在旧 App 中寻找它们。当前阶段未改动应用功能。

## 1. 2026-09-12 历史结论

- 阶段 0 的基线与开发环境检查通过，证据见 PHASE-01-ACCEPTANCE.md。
- 阶段 1 的接入设计与可独立执行的前置检查完成；真实连接验收仍为 BLOCKED / 等待人工关口 A。
- 已使用全新 Chrome 会话访问 WhatsApp 官方网页：HTTP 200，登录说明可见。只证明登录入口可达，不证明登录、账号识别或聊天读取成功。
- 当前应用专用配置文件不存在；专用钥匙串的 `ai` 和 `whatsapp` 项目均未找到；本次真实 AI 调用为 0，真实聊天读取为 0。
- 工具连接中只有无标签页的 Codex 内置浏览器，未连接用户已登录的 WhatsApp 标签页。项目中的现有浏览器模块用于 KDocs，不是 WhatsApp 读取器。
- 不能要求用户仅填写一个 WhatsApp 网页地址，就让当前 `/resolve`、`/messages` 适配器变成真实消息服务。缺失的适配实现由开发负责。

## 2. 阶段 0 的可恢复基线

基线目录：`artifacts/phase01-8.5/`。

- `baseline.json`：版本、分支、提交、原暂存区指纹，以及 8.5、7.6 包的 SHA-256。
- `source-manifest.json`、`source-baseline.tar.gz`：150 个代码与文档文件的快照和逐文件指纹。
- `git-status-before.txt`、`git-unstaged-before.patch`、`git-staged-before.patch`：原工作区和暂存区记录。
- 快照刻意排除凭据资源、运行数据、客户资料、依赖和构建产物；不等同于包含全部资源的可独立安装源码包。现有 8.5 提交和回溯包继续保留。
- 当前 `main` 指向历史提交 `c511f84`，已交付 8.5 的代码在 `codex/assistant-half-layout-8.5` / `e1087b9`。不能用 `main` 覆盖当前工作区。
- 后续只在明确列出的任务文件中修改；不能用重置、清空正式库、删除旧包解决尚未定位的问题。

数据身份：

| 用途 | 数据目录 | 端口 | 本次动作 |
|---|---|---|---|
| 正常 8.5 订单助手 | `~/Library/Application Support/KDocs Order Assistant` | 43874 | 只检查专用配置文件是否存在；未打开订单库 |
| 原库存同步应用 | `~/Library/Application Support/KDocs Inventory Sync` | 43873 为检查候选，非本次实测运行结论 | 只检查专用配置文件是否存在 |
| 7.6 回溯应用 | `~/Library/Application Support/KDocs Inventory Sync Rollback 7.6` | 43872 | 保留源码与 App 存档，未启动 |
| 本次自动测试 | 系统临时目录，测试结束清理 | 随机回环端口 | 仅虚构数据；不作为用户使用方式 |

## 3. WhatsApp 技术路线与决策条件

### 优先验证：应用控制的独立 Chrome 登录会话

适用前提：用户通过手机 WhatsApp / WhatsApp Business 配对网页，愿意在一次连接关口中登录并选择指定聊天。

设计步骤：

1. 由应用使用现有 Playwright 和 Chrome 打开专用 WhatsApp 会话。会话目录独立于 KDocs 浏览器资料，不复制或读取用户其他 Chrome 会话。
2. 只显示登录入口，不自动选择聊天；用户扫码后，在该窗口手动打开专用测试聊天。
3. 登录后先核验账号身份和目标身份；显示姓名仅供辨认，不能成为关联主键。主键必须来自能够复核的真实账号/聊天标识。
4. 用户确认目标及本单时间范围后，仅读取这个聊天。无法取得稳定消息 ID、方向或可靠时间时，停止该读取能力，不使用随机 ID、当前时间或姓名拼凑。
5. 先限定一对一文本消息；附件、语音、图片、无法解释的编辑/删除事件明确显示未覆盖，不做推断。不得把局部加载的历史片段称为完整聊天。
6. 读取能力作为内部 `chatAdapter.resolve/read` 实现接入订单服务，不要求用户自己搭建 HTTP 服务，也不增加独立客服平台。

已证明：本机 Chrome 可以打开匿名登录页。尚未证明：真实登录后目标身份、稳定消息标识及范围读取是否能可靠取得。**这是待验证技术路线，不是“扫码后保证接通”的承诺。**

受控页面读取也不等于 WhatsApp 官方支持的消息 API。若页面无法提供满足验收的稳定依据，该路线必须停止；不靠未核实的内部接口或猜测字段完成验收。

### 已有官方接口时：优先复用现有能力

只有用户确有已配置的 WhatsApp Business Platform 或现成只读消息服务时才采用。需要核对实际可读取的消息范围、商家消息来源和历史记录覆盖，不能把发送接口当成历史查询接口。

Meta 的官方 Cloud API 资料要求商业账号、业务号码等资产；接收消息事件涉及 Webhook。新建账号、接入回调服务、迁移号码或同步历史均不是“填一个 token”就完成。本阶段不注册业务账号、不迁移现有号码、不订阅整个账号的无关聊天。参考：[Meta 官方 Cloud API 文档](https://www.postman.com/meta/whatsapp-business-platform/documentation/wlk6lh4/whatsapp-cloud-api)、[Meta 官方 Webhook 订阅说明](https://www.postman.com/meta/whatsapp-business-platform/folder/ozgs3jn/webhook-subscriptions)。

如已有服务能满足指定范围，只增加映射适配；若没有且浏览器路线未通过，说明真实 BLOCKED 的原因及选择，不擅自安装第三方平台。聊天导出文件属于另一种离线范围，未经用户接受不作为 NEW-04 的替代通过。

## 4. 接口与状态设计

8.5 现有 HTTP 适配契约仅供复用，不是 WhatsApp 官方端点：

```text
resolve(accountId, chatId) -> { accountId, chatId }
read(accountId, chatId, from, to)
  -> [{ id, accountId, chatId, direction, sentAt, text }]
```

内部读取器与 HTTP 读取器使用同一验证边界。读取入口固定只读，没有发送消息方法。传输只允许 HTTPS 或回环 HTTP；不接受带凭据/查询串的服务基址。

设计状态分别保存，避免把一个布尔值叫“连接成功”：

| 状态 | 所需证据 | 允许的下一步 |
|---|---|---|
| 未配置 | 缺少服务/模型/凭据或读取实现 | 配置或准备连接 |
| 待登录 / 凭据不可用 | 登录页、凭据缺失或认证失败 | 用户在安全入口处理 |
| 登录或认证已验证 | 实际身份读取/服务响应成功 | 选择并确认目标 |
| 目标待确认 | 已识别账号和候选聊天，未获用户确认 | 确认目标及时间范围 |
| 目标已确认 | 身份匹配及用户确认记录 | 读取指定范围 |
| 读取成功 | 已取得有效消息，所有身份/时间/方向检查通过 | 显示范围及数量；AI 可用时允许提取 |
| 范围内无消息 | 成功读取但结果为空 | 提示核对范围；禁止提取 |
| AI 请求成功 | 虚构样本的真实请求及响应结构验证通过 | 进入后续语义验证，不能直接宣布自动补全可用 |
| 连接失效 / 错误 / 覆盖不全 | 认证、网络、身份或历史覆盖错误 | 展示具体原因，禁止错误补全 |

每次检查绑定配置版本；每次读取绑定 `orderId + bindingRevision + accountId + chatId + scope`。修改关联、切单、修改配置或取消操作后，旧结果不得更新新上下文。

读取完整性：先校验整批，再在一个事务内保存。重复读取以真实消息 ID 去重；同一 ID 出现不同内容时，必须标记编辑/冲突，不能当新商品追加或静默丢掉变化。跨页历史必须明确是否覆盖用户指定范围。

首次连接时只验证最多 20 条文本消息；超出后缩小时间范围，不偷偷截断成“全部成功”。已有服务的 500 条上限不等于首次核验必须读取 500 条。

当前数据库代码最高迁移版本为 17。连接设计本身不迁移数据库；后续确需新增表时重新检查下一可用版本，不在本轮预留或假定已迁移到 18。

## 5. AI 连接与密钥

当前已实现客户端要求服务兼容 Chat Completions JSON 响应，并接受当前请求参数。未选择服务商和模型；待用户提供非秘密信息后查阅该服务官方文档，按真实接口适配，不假定任意服务兼容。

本机安全配置入口已经存在：

- 应用：`/System/Library/CoreServices/Applications/Keychain Access.app`。
- 在“登录”钥匙串内新建密码项目，项目名称：`KDocs Order Assistant`，账号：`ai`，密码为 API 密钥。
- 密钥不贴到对话，不保存在 JSON、Git、日志、截图或浏览器页面，不作为命令行参数传递。
- 非秘密的服务基址和模型名由开发写入 `~/Library/Application Support/KDocs Order Assistant/config/order-assistant.json`；保留已有配置项。用户无需编辑 JSON。
- 使用浏览器读取路线无需创建 `whatsapp` API token；只有实际采用已存在的 HTTP 服务时才需要对应服务凭据。不要提前要求无用密钥。

后续应用内“连接检查”仅显示非秘密状态和模型信息；不在正常浏览器 renderer 中收集 API 密钥。若增加安全输入窗口，必须明确凭据由本机安全组件处理，不能把普通网页密码框当成主进程安全配置。

真实 AI 最小验证：

1. 使用纯虚构消息，不读取订单库或真实客户聊天。例：客户声明 `Full name: Avery Example`，商家报价 `KY02 AED 75 each`，客户仅询问运费。预期姓名有正确消息来源，报价不能成为客户接受的成交金额。
2. 一次实际请求，20 秒超时，无自动重试；无配置时不发请求。累计上限 5 次，不因重启脚本或刷新页面重新获得预算。认证/额度错误停止同类调用。
3. 先检查真实响应、JSON 结构、字段类型和消息来源；在虚构数据中执行校验，不写正式订单。预期答案不发给模型。
4. 保存服务商、模型、耗时、调用序号、脱敏错误及断言结果；不记录请求凭据，不把固定 fixture 返回值当真实响应。
5. 此处通过仅代表连接和最小样本可用；阶段 3 仍须验证完整的自然语言场景。

## 6. 人工关口 A：休息回来后集中处理

现在不需要用户反复打开安装包测试。先集中确认以下非秘密信息：

1. 平时在什么入口使用 WhatsApp：Chrome 网页、WhatsApp 桌面版，还是已有 Business Platform 接口。
2. 使用哪个 AI 服务商和模型；如有自定义服务地址，只提供不含凭据的基址。没有 API 账号或密钥就如实标记待配置，不代购服务。
3. 指定一个允许读取的小范围聊天，优先专门准备的虚构测试聊天。完整联合验收需要双向虚构消息；不能把真实客户资料发送给 AI，也不由助手发送 WhatsApp 消息。

按实际路线准备后，在同一轮中完成：

| 窗口 | 用户动作 | 为什么需要 | 成功标准 |
|---|---|---|---|
| 由开发准备的专用 Chrome WhatsApp 窗口（待准备登录后验证） | 必要时扫码；手动打开指定聊天；确认目标及时间范围 | 开发不能代替账号登录，也不能猜目标 | 能核对真实账号/聊天标识；用户明确确认 |
| 钥匙串访问 | 仅缺少时创建上述 `ai` 密码项目；必要时允许应用读取专用项目 | 密钥由用户在本机安全输入 | 主进程取得凭据且真实虚构样本请求成功 |
| 订单助手的目标核验结果（读取实现通过后准备） | 核对少量消息的归属、方向和时间 | 这些事实需要与用户的实际聊天对应 | 无错账号、错聊天、错时间；不要求用户逐条跑测试 |

这些未来窗口没有准备好之前，不让用户到处寻找入口或配置不存在的服务。第一次需要用户提供的信息集中收集；后续只是登录、选聊、密钥输入及核对同一结果，不把开发调试拆成多轮用户验收。

## 7. 停止与恢复规则

- 当前停在 A：缺账号登录、指定目标及 AI 服务配置；浏览器读取实现还需在真实授权页面上验证可行性。
- 在 A 未通过时，不宣称 NEW-04、NEW-05、NEW-07 真实通过。本轮用户指定 0、1 阶段，不启动阶段 2–6 的功能改动。
- 用户不回复不是授权或验收通过。窗口超时、连接失效时保留状态，不持续轮询客户聊天或付费 API。
- 用户回来提供集中信息后，从本文件继续，不重做已通过的环境检查；只有环境变化或出现新失败才复验。
- 技术路线取得不了稳定标识或范围覆盖时，明确指出哪项做不到及可选范围，不交一个只能模拟的“已接通版”。
