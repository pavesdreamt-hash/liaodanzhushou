# 8.20 自动消息同步验收

本版接入 whatsapp-web.js 1.34.7＋LocalAuth。本地自动接收与业务接入已实现；最终 ZIP 的真实新消息接收仍等待指定聊天产生新消息，不能标成整个真实链路通过。真实 AI 请求与开发代发消息均为0，未操作正式订单或库存同步。

## 已实现及本地验收

- 独立无头 Chrome、软件内扫码、受保护的本机登录目录、有效会话恢复；普通使用双击 App，不需要 .command 或额外服务。
- 消息事件接收客户消息及人工发出的消息，先按已核对 PN/LID 与账号过滤，再入库；原生消息编号和旧 PN/LID 编号的同一消息去重。
- 启动、重连及在线恢复旧备份后有限补漏：500/1000/2000/5000条阶梯，每次公开库读取45秒上限，批次100条，轮次在请求之间检查120秒预算（正在执行的一次读取仍受独立超时限制）；找不到已有记录的连续衔接依据则保留缺口提示。当前连接在线与全部历史完整分别判断。
- 每个新连接代次先核对账号才接受事件，初始化成功/失败晚于退出均再次清理自有客户端；进程正常退出只向自有浏览器发送关闭信号，不扫描或抢占别的会话。连接状态变更日志仅记录固定提示及状态，不记录密钥或正文。
- 同一账号共用一条连接；订单绑定范围和草稿独立，同客多单范围重叠进入待关联区，明确确认后关联指定订单。
- 新消息仅更新聊天，不自动更新客户资料、确认订单或触发 AI；已有自动 AI 设置也不由本接入触发。
- 图片最多两条下载并发、20秒超时，失败不阻塞文字，可人工重试；支持 PNG/JPEG/WebP，单张约1MB、缓存100MB、默认30天。备份含聊天、图片缓存与草稿，登录目录与普通订单备份分开。
- 阅读旧消息时保留位置并提示有新消息，底部跟随；退出前保存失败或版本冲突保留输入并阻止不安全退出。
- 上游 PR #201848 的只读兼容修复支持消息编号 `$1` 更名，解决真实 getChatById 的空键 IndexedDB 异常。锁定原包1.34.7及原/补丁文件 SHA256，安装和构建重现补丁，文件偏离则拒绝构建；没有加入消息发送能力。许可见 THIRD-PARTY-NOTICES.md。

证据目录：artifacts/assistant-live-8.20/final-release/。

| 项目 | 实际结果及范围 |
|---|---|
| 全量单元、项目检查 | 345/345 PASS，check PASS |
| 最终ZIP新解压App Electron/Chrome | 最终准确ZIP全量20个不同案例复验：19 PASS，1被钥匙串授权阻塞，0已知未修复应用代码FAIL；完整日志保留测试运行器的1项失败，未删测试或跳过 |
| 自动接收、去重、图片失败恢复、断网恢复、三次重启、关闭全部网页仍接收、SSE | PASS-FICTIONAL，真实Electron操作＋虚构连接客户端，不能当成真实网络结果 |
| 两单隔离、输入、引用、正文转义、分页、剪贴板、关闭重开、失败及并发保护 | PASS-LOCAL，最终App与虚构订单 |
| 虚构订单完整流程、阶段提醒、TXT、地图及返回列表 | PASS-LOCAL；模拟AI，不是新增真实AI语义验收 |
| XLSX / XLS / CSV | 各1份在最终App正常生产启动隔离目录经文件选择器实际导入，PASS |
| 备份迁移17→24（含20）、回复草稿、原生图片、恢复后继续接收 | PASS-FICTIONAL；凭据哨兵未进入备份且恢复不改登录目录 |
| 900×700 / 1280×850 / 1440×900 | 已保存并实际查看虚构数据截图，无横向溢出 |
| 真实扫码、账号和授权聊天映射 | PASS-REAL，隔离数据，仅尾号4771 |
| 真实近期文字、图片入库和界面解码显示 | PASS-REAL-READABLE，36条记录、5张缓存图片；不是手机全历史保证 |
| 真实最终准确ZIP App三次重启 | PASS-REAL，每次无需扫码，36条/5图及隔离草稿保留；5张实际解码显示 |
| 两小时连续运行 | PASS-REAL-CANDIDATE：实际120.05分钟，App持续运行；112次非在线样本，约4分钟后自动恢复，无需扫码。连接模块与最终包逐字节相同；最终准确ZIP另做三次真实重启及图片解码复验，不冒称该准确App已运行两小时 |
| 不点刷新收到真实新文字/新图片 | 文字：WAIT-FRESH-MESSAGE；图片：WAIT-FRESH-MESSAGE。缺少新消息刺激，不能记真实PASS；开发不代发 |
| 密钥更换加密存储 | BLOCKED-OS-AUTH：macOS钥匙串等待系统授权；已验证此等待不阻塞订单操作。与WhatsApp登录是两件事 |

## 失败证据与修复

1. 沙箱内全量测试的OAuth回环监听受限，外部重跑最终345/345通过。最终包图形进程也受MachPort沙箱限制；修正验收脚本环境变量后在沙箱外完整复跑，保留环境/前置检查失败日志，不算应用代码FAIL。
2. 旧纯浏览器测试漏加载新增连接模块，补上实际模块资源后原断言通过。
3. 连续消息时缓存译文重绘可能扰乱底部跟随，修复位置保护并保留旧消息不跳动断言复验；没有删除业务检查。最终关键复验一次因程序化滚动尚未保存就注入事件而超时，改为先等待两帧布局和工作区 scrollTop=0 的实际保存，再保留91条、原位置不变及新消息提示原断言复验通过。
4. 最终生产启动测试沿用旧“每次读取到最新”标签，改成实际“自动接收到最新”，保留状态、导航、退出保存断言后通过。
5. 真实隔离脚本最初未滚动到懒加载图片；后续改为滚动、等待实际 naturalWidth。脚本外部直接保存工作区造成版本冲突，被应用保护阻止退出；改成界面输入和等待保存。脚本异常退出遗留的专用 Chrome 导致目录占用，只关闭核实属于本轮临时目录、父进程已退出的浏览器，保留登录资料后完成三次真实重启。未清理普通Chrome或正式数据。

6. 把解压验收App放在源码.cache时，原密钥扫描将App内必要的OAuth资源副本视为重复凭据，阻止构建。只把本轮生成副本移至独立临时目录并从那里验收，保留原扫描规则，未删除测试或放宽安全检查。

7. 追加虚构案例确认：在线恢复较旧备份后，备份以后的已读消息不会再次产生新事件，原实现漏补。恢复组件时若连接已在线，触发现有有限补漏；保留图片、草稿及凭据断言，同时要求补回备份以后记录，源码及最终ZIP Electron复验通过。成功时间也包含事件实际保存时间。

## 使用与未完成部分

解压 dist/KDocs库存同步8.20.0-x64.zip，双击“KDocs订单助手 8.20.0.app”。进入订单助手→连接状态，首次扫码；聊天关联输入国际区号电话并核对，选择本单范围，确认后自动接收。关闭管理页面仍可接收；退出App或电脑休眠后停止，重开尝试有限补齐。

本版只完成最新消息自动接入。手机全部历史保证、所有媒体语义理解、AI自动回复及S2/S3整个业务闭环未完成。已有翻译和拟回复仍人工操作，真实AI追加额度未授权，不新增调用。

ZIP校验、源文件一致性、签名、准确启动副本见 final-integrity.json；该App为本机ad-hoc签名，未公证。若macOS阻止首次启动，在Finder右键App→打开，不需要关闭系统安全设置。旧安装版、原HEAD、原暂存区和212个既有非任务源码文件保留；本地任务提交详情见 git-delivery.json，不推送。

保护核验覆盖233个开工源码快照，其中212个非任务源码原样，以及原HEAD/暂存区字节。本轮早期部分测试沿用旧默认输出目录，旧生成测试截图/日志曾被重新写出；开工快照未覆盖这些生成文件，不能宣称它们全部逐字节不变。没有回滚这些文件，也不把它们纳入本任务提交；正式业务数据未改动。见 workspace-protection.json。

接下来做什么、由谁做、是否需要我操作：开发已完成连续运行记录、最终准确ZIP复验和报告，本地任务提交见git-delivery.json；用户可解压并双击App，常用资料目录首次扫码。真实新消息若仍WAIT，仅需用户在可控制的指定聊天中手动产生少量虚构文字/图片，开发负责复验。钥匙串授权仅在需要保存AI密钥时由用户处理，其他调试由开发完成。
