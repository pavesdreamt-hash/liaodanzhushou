# 8.15 本地在线同步与关闭入口验收

2026-09-17。承接8.14增量开发，状态MIXED / PASS-LOCAL；不宣称S2/S3或全部项目完成。遵循AGENTS.md升级次版本至8.15/8.15.0，元数据、界面、锁文件、使用说明、发布记录一致。

## 交付内容

- 助手设置右上角×、Esc、外部点击、再次齿轮关闭；设置读取中也可以关闭。关闭后旧异步返回不重建浮层，监听清理、焦点恢复。模型/服务商字段仍只读；密钥使用已有系统安全输入，不写renderer或日志。
- 流程与缓存设置增加×、外部和Esc关闭及焦点循环，未保存编辑关闭时确认放弃；保存失败保留窗口和输入。
- 当前及下一阶段编号前各一枚细→，颜色随阶段，纯方向说明；保留两行目标、顺序配置及事实判断，不通过箭头跳过业务条件。
- 主进程拥有独立在线只读同步，每15秒检查当前已核对且明确开启的客户；关闭助手、切单、管理页隐藏或全部管理页面关闭仍继续。需要电脑联网、App与专用Chrome WhatsApp连接运行。
- 彻底退出、休眠/关机期间停止；重开App恢复已保存的本单开关与专用登录资料，并尝试补取授权起点至最新的可获取记录。连接断开后继续检查并恢复，不请求AI。登录失效需扫码；仅等待专用窗口当前匹配的已授权聊天，不自动遍历/切换其他联系人。
- 从最新位置向较早批次补取，到已保存原生消息锚点或授权起点停止；每轮最多45秒/30批。页面稳定、未找到锚点、超时均提示补取待核对，成功批次保留且不推进未确认游标，不报全历史完成。不支持的消息格式会显示失败并保留既有数据。
- 同一账号/客户聊天共同归档去重，订单草稿、引用、阅读位置、资料、阶段及同步授权独立。只有本单时间归属唯一时自动进入订单；同客多单范围重叠的消息仅进客户归档，标待关联订单，不能作为本单引用或提取依据。齿轮人工确认关联仅采用尚未归属其他订单且处于本单范围内的消息，最多500条，更新选定单，不自动改地址、客户确认或发送。
- 查看最新时增量显示；阅读旧消息时保持位置，显示“有新消息”供手动跳转。手动意图和英文草稿保留。无新消息不反复提醒；增量刷新保持一张隐藏流程卡。
- 新迁移24保存每单授权目标、绑定版本、开关、修订和状态。旧绑定/固定时间范围授权不能生效；数据库恢复先停止在途同步，重新装载后恢复服务；退出等保存及同步停止后再关库。

## 操作方法与安装

先从菜单栏退出旧版App。双击ZIP解压，再双击 `KDocs订单助手 8.15.0.app`；正常使用无需.command、终端或测试数据库，不覆盖已安装App。首次macOS阻止时在Finder右键App→打开，不关闭系统安全功能。

订单助手→聊天关联→打开专用WhatsApp网页→必要时扫码→核对号码→本单开始时间及“每次读取到最新”→确认关联。齿轮→开启本单在线同步→确认自动读取范围。以后软件主进程运行就可继续同步当前匹配聊天；退出后重开补取，失效时重新扫码。齿轮可关闭同步。旧的前台自动处理设置不等同于后台在线开关，自动AI仍默认关闭。

ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.15.0-x64.zip`。

SHA256：`67c234d697344000ee7894dc897783efd5658b4848aa9c86af1a5d5ebd4deb94`。

## 实际验收证据

所有数据为隔离虚构订单、消息及表格。真实Electron/浏览器DOM/最终App操作是真实界面测试，聊天内容及AI返回为模拟，不冒称真实业务链路全部通过。新增真实聊天读取0、真实AI请求0、发送/粘贴WhatsApp0；正式订单、库存与Google Sheets写入0。

| 验收 | 实际结果与证据 |
|---|---|
| 全量单元与检查 | 308/308，0跳过；npm run check PASS。`artifacts/assistant-online-build-release.log` |
| 新增9项单元 | 开关确认、受限连接不扩大、重叠多单与独立草稿、唯一时间归属、错目标与断线、部分失败不推进游标、切换绑定丢弃、范围/缺口、迁移24失败回滚与损坏备份、恢复固定范围授权不生效。`assistant-online-range-guard.log` |
| 新源版界面与DOM | 3/3通过：真实Electron双单、关闭所有管理页面的菜单栏App，以及真实浏览器虚构DOM。新增状态积累断言失败后原断言2/2复验通过，见`assistant-online-state-guard.log`、`assistant-online-state-recheck.log` |
| 最终ZIP新解压完整回归 | 32项：30 PASS、2 FAIL、0跳过。两项FAIL均为密钥加密保存断言超时，系统消息为macOS钥匙串等待授权，交付状态WAIT-HUMAN；保留原29项全部业务断言及新增同步3项，不删断言凑通过。`assistant-online-delivery-zip-tests.log` |
| 钥匙串集中复验 | 原两项单独重跑0/2，仍FAIL；系统授权等待不算PASS。等待授权时普通订单操作另已PASS；未用真实密钥/真实AI。`assistant-online-keychain-followup.log`、`keychain-followup-result.json` |
| 在线新消息 | 同客两单不自动归属、手动仅关联第二单、两份英文保留；关闭助手和切模块继续、断线恢复、错聊天不读、退出重开补取、65条增量展示、阅读旧记录不跳动、点击跳到最新、关闭开关停止。`online-result.json` |
| 所有管理页面关闭 | 菜单栏主进程零管理页继续保存第二条，退出后重开补第三条并恢复有效授权；未访问真实账号。`online-closed-page-result.json` |
| 浏览器读取 | 最新位置、三页9条到原生锚点、授权起点以前正文getter一读即失败而未被读、目标改变丢弃、取消、受限连接保护、缺口保留原游标。`online-browser-result.json` |
| 原业务 | 独立回复、分页转义引用、剪贴板校验、失败/并发/退出保存、冲突与确认、分包TXT、阶段与利润、同客第二单及重启：完整最终日志与原workspace/stages/experience等JSON |
| 三格式导入 | 最终App实际.xlsx/.xls/.csv各1/1，未知成本NULL、重复保护、重启，见`import-results.json`与三份日志 |
| 备份恢复 | 17→24有预迁移备份；回复/中文/缓存、历史范围/阅读锚点/同步进度、阶段历史与修订及独立同步授权原始行逐字段恢复并重启比对。固定范围中恢复的旧开关无效；有效开启后重启补取另由在线测试验证。`packaged-assistant-backup.json` |
| 生产启动 | 最终App在生产模式及隔离虚构目录打开管理页，显示8.15/8.15.0，手动输入和流程重启保留、无密钥AI禁用；不启动正式资料库。`production-assistant-result.json` |
| 三尺寸截图 | 900×700、1280×850、1440×900，设置×在视口内可点击、长菜单自身滚动、两箭头与阶段目标；既有14px同排布局回归保留，截图实际查看见`visual-review.json` |
| 包源码与旧交付保护 | 最终ZIP新解压，x86_64、ad-hoc签名校验，106个src/renderer/shared及主文档完全一致；8.13/8.14 ZIP哈希未变。最终目录中8.11/8.12 ZIP不存在，开工快照未采集dist清单，无法断言其保留或消失原因。`source-match.json`、`final-integrity.json` |

初始新单元7项6通过1失败为虚构空电话期望类型错误，改为精确NULL后复验；初始新界面因同名导航设置按钮选择及详情页返回步骤缺失失败，限定助手范围并补充真实返回操作，保留所有业务断言。新增“仅一张流程卡”断言确实复现3张，修复后原断言通过。初始候选ZIP和记录另行保留，最终回归仅使用最终修复后的新解压副本，不拿初始候选结果替代最终包。

## 可用与人工关口

可直接使用：关闭入口、箭头、本地归档分页、明确开启后的后台只读同步、重开补取、断线等待恢复、未确认缺口提示、同客多单人工关联、草稿隔离、备份恢复。本地功能已通过上述虚构数据的实际App验收。

待系统授权：AI密钥更换/加密保存相关两项测试仍FAIL，不能宣称安全设置全部通过。使用时若出现macOS钥匙串系统授权窗口，需要用户完成该系统窗口；在软件完整助手设置中点击“重新检查安全存储”。系统授权不能由开发绕过。授权后由开发复验密钥保存、删除与重启，用户无需运行测试。消息只读同步不依赖AI密钥。

仅模拟/待真实验证：扫码后真实账号身份识别、在线新消息延迟、离线补取覆盖及全部媒体。当前实现按专用网页实际加载内容读取，只同步当前已核对匹配的客户，不能声称全部联系人的后台收件箱。删除或页面不提供的记录无法保证补回；原图、音频、文档、转发/引用等全部类型未接通。真实AI语义无新增额度，本轮不申请或调用；自动回复/发送、云服务器托管及App退出后继续运行不纳入。NEW-04在线本地PASS-LOCAL、真实联验待登录与范围；NEW-05真实语义BLOCKED。S2/S3剩余项目不整体勾完。

原HEAD c511f84保留，开工214原文件快照完整；只提交本轮允许范围，原暂存内容和标记保留，不重置、不恢复覆盖索引、不git add全部。通过临时Git索引提交独立本地分支，未推送。提交和索引字节证明见`artifacts/assistant-online-8.15/git-delivery.json`与`index-metadata-check.json`。

接下来做什么、由谁做、是否需要我操作：你解压打开；真实联验需要你扫码、打开指定聊天并明确读取范围，密钥功能遇系统窗口时需要你完成钥匙串授权，随后由开发集中复验。无需你运行测试或调试，也不需要新增真实AI额度来验证消息同步。
