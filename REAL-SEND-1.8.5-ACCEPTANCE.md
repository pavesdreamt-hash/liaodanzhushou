# 真实人工发送与会话未读数核验 — 1.8.5

日期：2026-09-24。交付方式：现有本地 Electron 操作窗口；不制作安装包，不提交或重置 Git，保留全部既有未提交改动。

## 本次修复目标

聊天工作台的人工输入区增加等尺寸回形针入口，支持发送文字、Unicode 表情、网址、JPEG/PNG/WebP 图片及常用文件。发送前核对当前账号、目标号码、会话身份、文件类型、文件名和大小；发送结果不确定时停止后续部分、保留原稿且不自动重发。会话列表日期下方显示 WhatsApp 返回的真实未读消息数红圈，0 隐藏，1～99 原样显示，超过 99 显示 `99+`。不改订单、AI、翻译规则和其他页面。

## 真实发送验收

真实验收始终锁定用户授权的测试号码（已脱敏）。每次调用前均检查候选号码和绑定号码必须精确等于该号码；没有向其他号码发送。验收内容均明确标注为聊单助手 1.8.5 测试，不含业务资料。

| 内容 | 真实结果 |
| --- | --- |
| 文字、表情、网址 | 首条 `聊单助手 1.8.5 真实发送验收：文字、表情 😊、链接 https://example.com/liaodan-send` 由只读回查确认存在，时间 `2026-09-23T23:51:58Z`，回执摘要 `ae9cc4f182bc`。|
| 图片 | 项目 1024×1024 PNG 真实发送后，由只读回查确认目标会话存在 `image` 消息，时间 `2026-09-24T00:24:29Z`，回执摘要 `da00d92cfcfc`。|
| 文件 | 验收 TXT 真实发送后，由只读回查确认目标会话存在 `document` 消息，时间 `2026-09-24T01:06:54Z`，回执摘要 `d4c91f8cd249`。|
| 最终即时回执 | 修复回执查询后，唯一测试文字直接返回 `status=sent`、`part.status=sent`，回执摘要 `adfedf5e0663`。|

诊断期间同一测试号码收到数条不同文案的无业务验收文字。它们用于定位 WhatsApp 即时回执为空的问题；所有状态不确定的请求均没有自动重发，相同指纹会被本机幂等记录阻止。图片和文件各只在只读回查确认前一次未送达或确认当前部分后才继续下一步。

## 根因与修复

`whatsapp-web.js 1.34.7` 在当前 WhatsApp Web 中存在两个实际兼容问题：MediaData 模型的私有 `__x_id` 被展开到外层消息对象，覆盖正确 MsgKey，导致图片发送抛出“Data passed to getter must include an id property”；修复该冲突后消息能够发出，但库仍用 `newMsgKey._serialized` 查回刚发出的消息，而当前键字段已经变化，导致 `sendMessage` 返回 `undefined`。受哈希保护的 `scripts/whatsapp-compat.mjs` 仅对审核过的 1.34.7 源码删除冲突私有字段，并用已有兼容读取器查询新消息。依赖源码版本或内容变化时构建会失败，要求重新审核，不会静默套用补丁。

发送适配器同时验证附件白名单、文件名、单项 16 MB、合计 32 MB、最多 6 个；文字、表情和网址保持原文。文件使用 WhatsApp 文档模式，图片使用媒体模式。合法我方回执支持完整序列化 ID和结构化 `fromMe/remote/id` 形式；格式异常仍停止并提示人工核对。

## 会话未读红圈与界面验收

隔离 Electron 窗口向三条会话提供 `unreadCount` 3、120、0，实际 DOM 分别显示 `3`、`99+`、无红圈；筛选栏“全部／未读／未回”的既有统计仍居中且数值不变。已亲自查看截图 [conversation-unread-badges.png](artifacts/send-1.8.5/conversation-unread-badges.png)，SHA-256 为 `85a54659a7d06996b1d981c7322246dc1bd6a601c0f163dd4c951bf0709cc7cf`；红圈位于对应会话日期下方，未改变头像、号码、摘要、接待状态、排序和其他页面。发送界面隔离回归确认回形针、图片预览、TXT 文件条、逐项移除、文字/表情/网址原文和发送成功后的清空行为。

## 自动核验

- `npm run check` 通过：TypeScript、UI 构建、共享模块及项目检查均成功。
- 36 项聊天历史、发送、媒体、身份、通知与 WhatsApp 兼容单元测试通过。
- 6 项串行 Electron 回归通过：未读红圈、人工发送、可见图片、隐藏会话、系统通知、可视翻译。
- `git diff --check` 通过。
- 完整 `npm test` 未通过：本轮范围外的既有订单测试仍按旧调用方式缺少新增的“订单确认依据”；本环境对 `127.0.0.1` 监听返回 `EPERM`，使本地服务和 OAuth 回环测试失败。本条不把完整测试集标为通过，也没有借本轮修改这些订单或 OAuth 功能。
- 正式本地窗口已由 `npm run start` 启动；启动日志确认版本 1.8.5 构建完成、主窗口显示并获得焦点、既有 WhatsApp 登录恢复为“自动消息连接在线”。

## 核验文档与规则

实施和交付按 `AGENTS.md`、`CONFIRMED-UI-BASELINE.md` D-099/D-100、`USER-GUIDE.md` 和本文件执行。用户明确授权真实发送验收只可使用其指定测试号码（已脱敏）；此授权优先于旧文档“只读／不发送”的历史限制。用户要求直接使用本地操作窗口，不制作安装包；此要求优先于通用桌面打包规则。
