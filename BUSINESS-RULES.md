# 库存同步业务规则

## 商品映射

- `商品映射` 必须按 KDocs `sourceRow` 升序保存，不排序、不去重。
- A 列商品编号由用户人工填写，始终按纯文本处理；`049` 与 `49` 是不同编号。
- 来源名称前缀和型号只用于来源匹配，绝不能自动转换为业务商品编号。
- 同名来源保留独立的 occurrence sourceKey 和独立行。

## KDocs G 列

- G 列是任意“附加信息”显示字符串，不是数值型汇率字段。
- 采集快照必须原样保存 `additionalInfo`；禁止使用 `Number`、`parseFloat` 或数值差计算。
- 商品编号全部确认后再统一迁移 `库存情况`：F 列名称改为“附加信息变化”，只在本次与上一正式版本不同时显示“旧值 → 新值”，无变化留空。
- 每个未来历史版本保存成本、建议售价、库存、附加信息四项。
- 当前商品映射整理阶段不得修改现有 `库存情况`。

## 订单运输与利润

- 一个订单内同一SKU系列为一个实际包裹，每个实际包裹独立计费。
- 签收和拒收使用相同的“快递确认实际包裹总费用”录入流程，不再按地区或状态自动套用固定金额。
- 出库费暂未确定；系统不自动加收AED 2。已出库的包裹（包括出库后取消）必须等快递确认总费用。
- 出库前取消没有发生运输或出库费，总费用为0，可直接结算。
- 实际费用未录入前，`unshipped`、`outbound_processing`、`shipped_pending`、`signed`、`refused`、`cancelled_after_outbound`均不生成最终利润；因没有可靠的预计运费，也不用0运费生成虚假的预计利润。
- 签收在确认费用后按“收入-成本-实际费用”结算；拒收和出库后取消的实现收入、销售成本均为0，利润为负的实际费用。
- 费用和AED/CNY汇率均使用不可变版本记录；订单和包裹保存当时快照，后续新规则不得改写历史利润。
- 所有金额以整数fils或人民币分保存，禁止使用JavaScript浮点数累计。
