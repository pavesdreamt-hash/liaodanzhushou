# 非文字消息第二步核验 — 1.7.8

日期：2026-09-23。此次交付是系统通知分类修复；全部图片读取问题尚未完成。

## 本次修复目标

修复已确认的 notification_template 被误当图片问题，并正确处理撤回消息。通知没有附件时不进入图片下载、不显示图片占位、不进入自动或手动翻译，也不显示旧缓存译文。未知通知子类型使用中性“WhatsApp 系统通知”，不猜测具体内容。已知账号变更通知显示“WhatsApp 商业账号类型已变更”。消息 ID、方向、时间、顺序保留；一般通知已有正文保留，撤回内容不暴露。客户列表相应通知摘要同步使用中文提示。

## 真实验收内容

1. 复用原有登录，对上一步两条真实通知再次调用 getMessageById 和修复后的 normalize。均确认 hasMedia=false、subtype=biz_account_type_changed_to_hosted；结果为中文通知、media=[]、原消息 ID 保留。未调用下载、发送、AI 或重新认证。脱敏证据：artifacts/media-step2/real-notice-check.json。
2. 构建完成后正常退出旧应用，沿用正式用户资料直接启动本地 1.7.8。实际读取新窗口 DOM：主窗口及内嵌工作台 visibility=visible、版本 1.7.8、WhatsApp 已连接。证据：artifacts/media-step2/local-window-check.json。
3. 新窗口已置于前台，可直接使用。现有会话登录保留，无需安装包。重新打开可在项目目录运行 npm run start；不要单独打开 Electron 引擎。

真实通知检查与真实应用启动均已完成，但没有在正式工作台滚动到两条历史通知进行完整显示验收；不能把适配器检查与隔离窗口检查合称真实端到端验收通过。

## 自动与隔离窗口验收

- npm run check 通过：TypeScript、UI 构建、共享模块兼容及项目检查。
- 11 项单元测试通过：test/manual-chat.test.mjs、test/whatsapp-system-notices.test.mjs。测试覆盖旧媒体缓存不污染通知、通知不触发下载、未知子类型、保留正文、撤回处理以及原有图片/贴纸/GIF路径。均为隔离虚构数据。
- 三项 Electron 回归最终通过：通知显示、图片可见区域读取、翻译可见区域与阅读位置稳定。
- 通知回归在 1280×820、1440×1000 检查正确通知、旧错误缓存及撤回消息无图片卡/翻译按钮，通知 ID 未进入媒体或翻译请求；普通测试图片继续显示。已亲自查看 artifacts/media-step2/system-notices-fixture.png，该图是隔离数据，不能冒充真实客户界面。
- 通知回归前两次分别因消息重绘造成测试节点失效、启动自动选中加手动重新选中导致两次合法图片读取而失败；修正测试为即时节点操作和“所有媒体请求必须属于图片”的范围断言后通过。未用修改应用逻辑掩盖失败。
- git diff --check 通过。

## 保留与未验收

本轮只改 src/orders/whatsapp-web-client.mjs、ui/src/chat-workbench-desktop.ts 中的通知处理及关联版本、测试、文档；没有修改 CSS、布局、订单逻辑、图片下载大小门槛或队列。旧未提交工作保留，无 Git 提交。版本按用户要求 1.6.9→1.7.0 进位；历史 1.6.17 对应 1.7.7，本次为 1.7.8，历史发布说明不重命名。

约 1 MB 大图限制、真正失败图片的个别原因、下载队列、可重试状态及真实图片全流程仍待后续步骤。没有进行真实发送、付费 AI、全页面回归或安装包验收；用户要求只运行本地窗口，本次没有制作安装包。

## 核验文档和规则

实际复读 AGENTS.md、CONFIRMED-UI-BASELINE.md，按 D-075、D-090、D-091 与本轮 D-092 核对范围、数据保护、真实与虚构验收的分离。第一步依据为 MEDIA-STEP1-DIAGNOSIS.md。遵循用户最新范围，保留其他元素与现有未提交改动。旧手机样式不属于本轮，不以其历史偏差代表本次通知修复失败，也不称整页已验收。
