# 聊单助手 1.9.3｜聊天工作台原生拖动验收记录

## 本次修复目标

修复聊天工作台不能移动窗口的问题：恢复 macOS 原生窗口拖动，同时不拦截 WhatsApp 连接状态、人工／AI辅助／AI自动顶栏控件或工作台模态弹窗。

## 根因与最小修复

- `ui/src/main.tsx` 先前以 `page !== 'workbench'` 排除了工作台的父级原生拖动层；工作台 iframe 内的 `-webkit-app-region: drag` 不能可靠承担 macOS 窗口命中。
- 1.9.3 仅将工作台顶栏 x=96 到右边缘、y=0 到 56 px 的原生拖动层恢复为父级片段。片段根据 `#cwb-connection-status` 和 `.cwb-top-modes` 的实时几何范围留出 4 px 安全孔位，因而不会覆盖可点击控件。
- 片段在窗口尺寸、iframe 尺寸、状态文字或类、顶栏控件样式与工作台 DOM 改变时重算；工作台模态弹窗打开时移除片段，关闭后恢复。其它页面的顶栏拖动方式未改变。

## 受保护范围与规则

- 已重新核对 `AGENTS.md`、`CONFIRMED-UI-BASELINE.md` 的 D-035、D-106、D-107、D-108，以及上一版 `QUIT-LIFECYCLE-1.9.2-ACCEPTANCE.md`。
- 保持 56 px 顶栏、四列位置、导航、AI 辅助流程、WhatsApp 状态、聊天和订单业务、其它页面顶栏与用户资料不变。
- 自动化和最终成品验收均使用临时隔离资料目录与虚构会话；最终报告记录 `fictionalClient=true`、`realMessagesSent=0`、`realAiCalls=0`，没有读取或写入正式 WhatsApp、客户或订单资料。

## 真实执行的检查

- `npm run check`：通过 TypeScript、UI 构建、共享兼容性和项目检查；`git diff --check`：通过。
- `node --test --test-concurrency=1 --test-timeout=90000 test/workbench-window-drag.e2e.mjs`：1/1 通过，覆盖父级物理片段、连接状态与顶部模式孔位、AI辅助点击不被吞掉、导航展开/收起、客户聊天列与订单列宽度变化、虚构连接状态文本变化、1280×820、820×640、业务页往返及模态弹窗开关。
- `test/main-window-lifecycle.e2e.mjs`：3/3 通过（关闭隐藏／激活恢复、正常退出、原生菜单栏回调退出），确认本轮没有回归 1.9.2 的退出修复。
- `test/chat-workbench-ai-assist.e2e.mjs` 与 `test/chat-workbench-send.e2e.mjs`：2/2 通过，确认 AI 辅助与人工发送确认入口仍可用。

## 最终成品验收

- 最终交付为独立 Mac x64 App，不生成 ZIP：[`dist/聊单助手1.9.3-x64/mac/聊单助手 1.9.3.app`](dist/聊单助手1.9.3-x64/mac/聊单助手%201.9.3.app)。已核实包内版本 `1.9.3`，可执行文件为 `Mach-O 64-bit executable x86_64`。
- 从该最终 App 直接启动生产模式隔离实例，报告 [`artifacts/workbench-drag-1.9.3/app-verification.json`](artifacts/workbench-drag-1.9.3/app-verification.json) 记录 `packaged=true`、版本 `1.9.3`、`arch=x64`、`errors=[]`。
- 最终包通过真实 macOS `CGEvent` 原生鼠标拖动验收：1280×820 左／右分别移动窗口 `+44,+20`；820×640 左／右分别为 `+44,+31`；1440×1000 从连接与设置页返回工作台后为 `+44,+31`；关闭工作台模态弹窗后为 `+44,+31`。每次窗口宽高变化均为 `0,0`。模态弹窗打开期间，原生拖动实际保持不移动，关闭后才恢复。
- 成品验收还实际改变导航、客户聊天列宽度、订单列宽度和虚构连接状态文本，并逐次确认连接状态和三种顶部模式控件不与父级拖动片段相交；AI辅助可点击且在未配置的虚构资料中只显示准确配置提示；没有发送或 AI 调用。1280×820 与 820×640 的最终包截图已实际查看，未见父级拖动片段的视觉覆盖；820×640 继续使用已有的横向滚动策略。
- 业务页往返选用“连接与设置”页，以验证切出再返回工作台后的重新挂载；订单管理页现有内嵌旧脚本的空元素 `onclick` 异常不属于 D-108，未为本轮局部拖动修复改动。

## 工作台整页基准状态

- **D-108 顶部原生拖动：通过。** 最终 App 的真实原生拖动、控件孔位、动态重算、业务页往返和模态开关均以上述虚构资料成品验收通过。
- **820×640 顶栏品牌／WhatsApp 状态文字：未通过。** 最终包截图中两者仍有可见文字重叠；这是本轮前已存在的窄窗口视觉问题，D-108 只授权恢复拖动，未修改这两个元素的布局。
- **工作台其余完整基准：未核验。** 历史手机底部 P1–P4 也未在本轮重新检查或转为通过。

## 未扩大范围

- 本轮只验收工作台顶部原生拖动局部修复，不代表工作台整页、订单管理或其他页面完整基准通过；历史手机页 P1–P4 等基准偏差未在本轮修改或重验。
- 工作区原有大量已修改和未跟踪文件均保留；未执行 `reset`、`clean`、覆盖或删除。
- 按用户的每五个版本一次 GitHub 功能更新规则，1.9.3 是当前批次第 3 个交付版本；本轮未创建 GitHub 提交或推送。
