# 8.14 阶段目标、电话身份与聊天刷新验收

2026-09-17。承接8.13继续修改，不重新开发既有功能。按AGENTS.md升次版本至8.14.0；状态MIXED / PASS-LOCAL。S2/S3和整个项目没有整体完成。

## 完成内容与使用方法

1. 顶部当前与下一阶段显示动作目标，收货资料只列尚缺的姓名、电话、地址；如姓名电话已有，显示“确认收货地址”。完整配置动作悬停可看，较窄区域最多两行；配送异常、等待和明确结束仍优先显示。流程名称、动作、順序、必需条件与阶段事实判断保持有效。
2. 头像改为电话图标，旁边使用已核对关联的WhatsApp号码；不采用客户姓名或左侧收货电话。未关联显示“待关联聊天”，非电话渠道标识显示“已关联聊天”，不伪造电话号码。
3. 三个选项间距12px，未选项和翻译按钮增加浅灰底、圆角与悬停反馈；选中继续浅蓝底，字体14px与客户字段一致，三个验收尺寸仍同排。
4. “刷新聊天”先到最新位置读取，再向前分批补齐可获取历史，每轮最多45秒/60批；成功批次即时保存，去重、取消、失败保留及分页。可用“停止同步”；网页连接可点“继续补齐更早记录”，从当前较早加载位置继续，不先跳回最新。若手动改变专用窗口位置，以页面实际加载位置为准。首次网页全历史刷新需在软件内确认当前客户范围；受限连接始终只读原授权范围。
5. 历史在主聊天区可分页查看，范围外消息标为“历史消息·本单范围外”，不能引用为本单回复依据，也不进入本单AI提取或翻译请求。最新客户回复依据仍选本单消息，阅读位置可保存历史锚点，回复及阅读位置各订单独立。切单时取消旧同步，旧界面异步返回不覆盖第二单输入。
6. 沿用schema23，不新增迁移；完整备份保留同客历史、阅读位置、同步进度/确认标记、回复、双语缓存及阶段记录。新版从正常App打开，无.command要求。

## 实际验证证据

全部使用隔离虚构临时订单、聊天、表格及模拟AI；真实AI请求0、真实聊天读取0、WhatsApp发送0、正式订单/库存/Google Sheets写入0。测试操作真实Electron、浏览器DOM和最终App；模拟语义不是实际业务链路全部通过。

| 项目 | 实际结果 |
|---|---|
| npm test / npm run check | 299/299，0跳过；项目检查PASS。`artifacts/assistant-refresh-build.log` |
| 七项新增单元测试 | 阶段目标缺失项、自定义动作、刷新去重与范围、166条分页和游标、受限连接不扩大、失败重试保留、目标改变拒绝与取消。`assistant-refresh-new-unit.log` |
| 最终ZIP完整回归 | 最终全部29/29通过，包括虚构密钥加密保存、齿轮与独立设置回归。 `artifacts/assistant-refresh-delivery-zip-tests.log` |
| Electron新功能 | 106条首次分页→107条增量→失败仍保留108条；本单范围外不能引用；关联电话与收货电话分别验证；停止和刷新时切单保留第二单输入；英文草稿及历史重启保留。`refresh-result.json`；独立补验2/2见`assistant-refresh-final-followup.log` |
| 实际浏览器DOM | 四页12条正文、最新位置、继续更早、游标上下界、稳定停止、取消，始终不伪称全部历史。`refresh-browser-result.json` |
| 三尺寸实际查看 | 900×700、1280×850、1440×900，14px、选项间距12px、底纹、同排与无横向溢出；阶段目标和号码可读。`visual-review.json`与三份`refresh-尺寸.png` |
| 业务及既有回归 | 独立草稿、两单、引用/复制、分页、失败并发、TXT、分包、地址冲突、客户确认、重启及同客第二单；沿用原业务断言。完整最终日志、`workspace-result.json`、`stages-result.json`、`experience-result.json` |
| 三种表格导入 | 最终App实际导入.xlsx/.xls/.csv各1/1，未知成本NULL、重复保护和重启。`import-results.json`及三份导入日志 |
| 备份恢复 | 17→23自动迁移备份，恢复并重启精确比对历史范围标记、历史滚动锚点、同步进度、双语、回复、阶段版本和业务成本快照。`packaged-assistant-backup.json` |
| 生产启动与源码 | 最终新解压副本生产模式在隔离虚构资料目录启动，显示8.14/8.14.0，手动草稿重启且未配置AI禁用；没有启动正式资料目录。104个源/界面/共享和主文档文件匹配，元数据匹配、x86_64、签名校验通过。`production-assistant-result.json`、`source-match.json`、`final-integrity.json` |

初次沙箱单元测试299项中7项因回环端口EPERM失败，沙箱外完整打包前重跑299/299通过，不将沙箱结果冒称PASS。源版初次5项3通过2失败，修复数量拼接及新增测试的订单行选择后复验3/3，停止补验1/1、浏览器DOM1/1。首轮最终包29项27通过2失败：旧浏览器测试缺少新接口、齿轮钥匙串等待；补齐桥接且保留受限范围正文保护断言后2/2补验，再执行最终完整29项，以最终日志为准。初始日志与JSON保留在`assistant-refresh-initial-zip-tests.log`和`artifacts/assistant-refresh-8.14/initial-run/`。

## 可用、模拟和未验收

可直接使用：阶段目标、号码显示、工具栏样式、本地历史归档分页/去重、范围保护、停止、失败保留、切单保护、重启与备份恢复。网页历史获取代码已接入，但没有实际读取真实旧聊天。

仅模拟验证：AI翻译、提取、拟回复及商业语义判断；实际浏览器DOM测试为虚构内容，不能作为真实WhatsApp网页覆盖验收。

尚未验收：真实账号从最早到最新的全历史覆盖、原图/语音/附件完整性、真实AI语义。页面稳定、没提供更早记录、到达本轮上限都仅记录未确认完整，不能报“全部完成”。已删除或渠道不提供的记录不能恢复；长历史可能多轮，导出TXT全部导入也不能证明账号全部历史。本单范围最多5000条沿用保护，过大要求缩小订单范围，客户历史仍保存可看。

未实现/不纳入：自动发送、退出后后台提醒、模型训练、桃园提交与回执。原有可选自动处理默认不开启；本轮不申请新增真实AI调用。NEW-04本地获取/分页增强为PASS-LOCAL，真实覆盖仍未证明；NEW-05真实语义仍BLOCKED（无新增额度）。

系统授权：虚构密钥回归通过，无需为本地功能做额外授权。真实AI仍需有效配置和明确额度；真实旧历史联验需你明确客户与时间范围、登录并核对当前号码，再由助手集中复验。

## 安装与工作区保护

ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.14.0-x64.zip`。

SHA256：`17e763fdeefa8f39ded82e52cdbf08343f055014ac162cdf96f9ac4f57d3d0ef`。

双击ZIP解压，然后双击`KDocs订单助手 8.14.0.app`。正常使用无需.command、终端或测试参数。首次被macOS阻止时，在Finder右键App选择“打开”，不关闭系统安全功能。本轮没有覆盖已安装App，8.11、8.12、8.13 ZIP校验原值保留。

开工基线`.cache/assistant-refresh-baseline/files/`保存210个原文件。只提交本轮明确范围改动，保留原HEAD、原暂存内容和标记、无关修改；索引字节是否与开工相同详见`index-metadata-check.json`，不恢复覆盖索引。使用临时Git索引建立独立本地分支，不git add全部、不切换工作树、不推送。提交与保护证据见`artifacts/assistant-refresh-8.14/git-delivery.json`。

接下来做什么、由谁做、是否需要我操作：你解压并打开本地功能即可，无需参与常规调试；真实旧历史及AI联合验收，需要你明确范围/额度并完成必要登录或系统授权，再由助手集中复验。
