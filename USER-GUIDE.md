# 聊单助手 1.9.5 — 当前本地运行版

在聊单工作台填写中文后，点击输入区顶部的“转为英文”即可生成可编辑英文草稿；也可继续使用 Cmd/Ctrl+Enter。快捷回复仍先只填入中文，必须由你再点击“转为英文”才调用翻译，绝不会自动发送。翻译期间不能重复提交；若你修改中文、英文、快捷回复或表情，较晚返回的旧结果不会覆盖当前草稿。翻译失败时中文和已编辑的英文会保留，请核对提示后重试。

聊天工作台顶部的窗口拖动已恢复。按住 macOS 窗口按钮右侧的顶部空白区域即可移动窗口；WhatsApp 状态和“人工 / AI辅助 / AI自动”按钮仍可直接点击。切换页面、调整窗口或打开再关闭工作台弹窗后，拖动区会自动重新对齐。

窗口宽度处于既有的窄窗口范围时，顶栏会保留“聊单助手”、WhatsApp 图标、状态圆点和简短的“已连接／未连接”，并自动隐藏重复的“WhatsApp”前缀和顶栏版本徽标，避免文字重叠。鼠标悬停或辅助功能仍会获得完整的“WhatsApp 已连接／未连接”状态；在常用宽度下，完整状态文案与版本徽标照常显示。

订单管理页在小窗口下会保持每一列的独立宽度；当窗口不足以同时显示九列时，可使用表格底部的横向滚动条查看右侧内容。常用窗口尺寸会直接显示全部列。多商品订单仍在同一订单行中逐项显示，商品之间不再出现多余横线，订单之间仍保留一条分隔线。

订单详情页已恢复最初确认的成品业务卡样式。页面从上到下显示订单阶段和当前目标、客户信息、商品与金额、配送与包裹；顶部履约状态统一显示中文。客户和商品卡在常用桌面宽度下并排，商品数量、价格、订单合计和完整收货资料均读取当前订单，资料缺失时显示“待核对”，不会使用设计稿里的示例内容。

订单详情页已移除“快捷回复”和“客户聊天”两个区域，页面只保留订单资料与相关操作。客户聊天、快捷回复、翻译和真实发送继续在聊单工作台中使用。

订单详情页左侧“我的工作台”标题旁现在有折叠按钮。点击后导航缩为仅图标，按钮会变成展开入口；再次点击恢复。这个状态会在工作台、订单管理、商品库存、利润核算、助手配置、连接与设置、商品详情和利润详情之间保持一致。各页侧栏底部显示当前版本号。

把鼠标移到任意聊天消息上，消息气泡朝对话区中央的一侧会出现一排“翻译、话术、识图、回复”按钮。“翻译”只翻译这一条文字；“话术”打开现有快捷话术；图片已实际显示时可点“识图”，识别结果会显示在该图片消息内，这要求当前配置的 AI 模型支持图片输入；点“回复”后，输入区会显示被引用消息，发送时会以这条消息作为 WhatsApp 引用对象。点引用提示右侧的关闭键可取消；切换客户会话会自动清除引用和临时识图结果。

聊天工作台顶部的“AI辅助”现已可用。先在“助手配置”保存服务商、模型和 API 密钥，再从左侧选中并核对当前 WhatsApp 会话，点击“AI辅助”，最后明确点击“生成 AI 回复”。系统只在这一次点击时将当前已选会话最多 20 条文字、可选的被引用客户消息和你输入的中文意图交给已配置服务商；它不会自动请求、发送消息、更新订单或客户资料。生成的英文草稿可直接编辑，下方会显示 AI 中文核对；请人工核对后，仍通过原有“发送确认”取得 WhatsApp 回执。切回“人工”不会清空草稿、附件或引用回复。“AI自动”仍为待启用，不能自动发送。

聊天工作台处于“人工”模式并选中真实 WhatsApp 会话后，可在输入框输入文字、表情或网址原文；点击回形针可添加 JPEG/PNG/WebP 图片、PDF、TXT、CSV、Word、Excel 或 ZIP 文件。附件会先显示在输入区，可逐项移除；单个附件不得超过 16 MB，一次最多 6 个、合计不得超过 32 MB。点击“发送确认”后，只有取得 WhatsApp 合法回执才显示发送成功；回执不确定时保留文字和附件，且不会自动重发。

会话列表每一行的日期下方会显示该会话的真实未读消息红圈。没有未读时不显示，1～99 显示实际条数，超过 99 显示 `99+`；该数字来自 WhatsApp 当前会话的 `unreadCount`，不会根据订单或历史消息总数猜测。

在聊天工作台的会话行点击“三点”→“隐藏此会话”，该会话会从聊单助手的正常列表移除。点击“已隐藏”可查看隐藏列表，再用该会话的“三点”→“恢复会话”放回正常列表。隐藏按当前 WhatsApp 登录账号保存；不会删除 WhatsApp 原始聊天或本机已存消息，也不会拉黑、结束会话。之前误选的会话已在本机精确标记为隐藏；如需找回，可按上述步骤恢复。

聊天工作台现在只列出最近会话，不会自动打开排第一的客户。请从左侧点击要查看的号码；程序会核对列表号码与实际会话号码。只有两者明确矛盾时才停止显示，并提示重新选择。WhatsApp 暂时无法复核 LID 号码映射时，仍允许打开该会话，但在标题处标明“号码映射暂未复核”。这个规则不根据订单有无、消息条数、头像或系统通知拦截会话，也不使用 AI 判断身份。

本版会把已核对的一对一 WhatsApp 会话新消息保存在本机，即使没有关联订单、尚未打开该聊天或窗口已隐藏；打开会话后还会保存 WhatsApp Web 实际返回的可用记录，不只保存屏幕最后 20 条。刷新会保留已经看到的内容；来源暂时读取失败时继续显示本机已保存记录。若提示“较早记录尚未核实”，表示 WhatsApp Web 当前没有提供那些消息，可稍后点击“刷新”重试。助手不会依据手机截图编造未读取的聊天内容；本功能不能追溯补存升级前未读到的历史。

此前已修复系统通知被误当作图片的问题：账号变更通知显示中文说明，撤回消息显示“此消息已撤回”，不再显示假图片卡或翻译按钮。图片现在按当前可见区域排队，最多同时读取两张；点击已显示的缩略图可放大，点击大图可切换原尺寸，按 Esc 或“关闭”退出。读取失败时可在图片卡片点击“重试读取”；图片超过本机 16 MB 安全上限或格式不支持时，应在 WhatsApp 查看原图。

本版交付 Mac x64 App，不生成 ZIP。在 `dist/聊单助手1.9.5-x64/mac/` 中双击“聊单助手 1.9.5.app”即可启动；首次被 macOS 阻止时，在 Finder 中右键 App 选择“打开”。左上角红色关闭键只会把窗口隐藏到菜单栏，点击菜单栏的聊单助手图标即可重新显示；菜单栏里的“退出”会先完成安全保存和关闭，再真实结束应用。

在聊天工作台选择已关联订单的会话后，点击客户信息卡的“AI 识别当前会话”可获取建议。点击“更新”后，只有再次选择“是，更新”才会写入本机。

右侧“地址核对”卡会逐行显示国家/地区、省/州、城市、街道和详细收货地址。客户发来 WhatsApp 位置描述，或 Google Maps 链接中带有明确地址文字时，系统会在该消息旁显示“! 可更新”，并在地址卡显示更新项。点击“更新”后再次确认，才会改本机订单；短链、只有地点名或经纬度的地图链接不会猜测成详细地址。

聊天工作台只在图片消息进入当前可视区域时读取该条安全附件，读取完成后直接显示原图片；向上或向下滚动到其他图片后才读取其他图片，未显示的历史图片不预加载，排队中的视口外图片会停止排队。自动翻译同样只处理当前可见区域；滚动到其他位置后才处理该位置的未译消息，译文写入时会固定正在阅读的位置。网址可以点击打开。语音、文件、位置和联系人会显示消息类型；若 WhatsApp 未提供可安全缓存的原文件，按提示回到 WhatsApp 查看。

在“订单管理”点击“同步最新订单”，首次使用选择“配置 ShopPlus API”，在系统安全输入窗口依次输入 App Key 和 API Secret。应用会先只读验证订单接口；验证后点击“同步最新订单”即可把后台最新订单加入本机列表。相同 ShopPlus 订单号会跳过，不会覆盖本地订单，也不会修改网站后台。即使不同订单使用了相同的 ShopPlus 商品编号，也会按订单分别保存。同步结果会显示读取、新加入、重复跳过和待核对数量。

消息读取完成后，紫色提示点会隐藏；向上滚动阅读时立即取消提示。读取连续失败 6 秒会短暂显示“信息暂时无法获取”，3 秒后消失；同一次故障不会反复提示。

下方为历史 1.2.1 打包版说明，不代表当前版本的交付文件。

# 聊单助手 1.2.1 — 顶部拖动修复

## 打开

1. 退出正在运行的旧版，解压 `聊单助手1.2.1-x64.zip`。
2. 双击其中的 `聊单助手 1.2.1.app`。首次被 macOS 阻止时，在 Finder 中右键 App，选择“打开”。
3. 1.2.1 与 1.1.4、1.1.3、1.1.2、1.1.1、1.1.0 使用同一个 `~/Library/Application Support/Liaodan Assistant Live` 资料目录；本版不清空已有资料。1.0.12 使用另一套资料目录，仍可保留。

## 移动聊天窗口

按住顶部空白栏（macOS 窗口按钮右侧）的任意位置，向左或向右拖动即可移动窗口。右半区域也可拖动；下方业务控件的操作不变。

## 导入你自己的订单

1. 从 ShopPlus 导出订单文件（Excel 或 CSV），在应用左侧打开“订单管理”，点击“导入订单”，选择该文件。
2. 应用先显示导入预览，尚未写入订单。查看可导入、需要核对、重复和被阻断的订单数。需要核对的单据，请逐单检查商品编号、成本和优惠；缺失成本可明确选择暂缺，利润会保持待核对。
3. 确认资料后点击“确认导入”。重复订单会跳过；存在阻断错误时不会提交整批文件。完成后可在订单列表搜索编号、电话或商品编号，并打开详情。
4. 没有文件时，可点“新建订单”建立空白草稿，手工填写真实资料并保存。应用不会预填虚拟客户或订单。

工作台“近期订单”中，点击卡片只会选中并高亮该订单；点击卡片上的“订单详情”才进入详情页。状态颜色与“需注意”圈数依据实际订单资料显示。工作台不会凭空生成未读数或聊天预览。

## 将所选订单关联真实 WhatsApp 聊天

1. 在订单列表打开该订单详情，在右侧“客户聊天”点击“关联真实聊天”，然后连接 WhatsApp 并用手机扫码。
2. 输入含国家区号的客户号码，点击“核对号码”，检查发送账号与目标号码。若订单已有电话，应用会按订单国家／地区识别本地号码（例如阿联酋的 `05…`）与国际写法是否为同一个完整号码。真正不一致或无法核实的号码仍会被拦截；确需更正订单电话时，在订单详情点“核对／更正联系电话”并填写修改依据。
3. 明确点击“确认关联并读取最近 24 小时”后，才读取该订单已核对聊天最近 24 小时、最多 100 条消息。需要时点击“刷新消息”。切换订单不会沿用上一单的聊天关联。
4. 发送前在左侧核对英文内容和附件。点击“发送”会向真实客户发送，不能用于试发。本次开发验收没有读取你的聊天，也没有发送消息。

## 恢复的页面

- “商品库存”显示本机库存同步记录和商品档案；可搜索、筛选、同步库存、打开商品详情并编辑商品档案。无资料时显示空状态，不能把“未同步”当作有货。商品图片从产品详情的“商品图片”入口管理。
- “利润核算”显示本机订单与已登记的每日成本；可选择日期保存投流、汇率和账号成本，打开每日利润详情和导出利润。还没有结算资料时显示空状态。
- “助手配置”保留说明和页签，可看真实服务配置与调用记录。自动发送未启用；原稿中没有服务接口的策略保存按钮会明确禁用。
- “连接与设置”可查看连接状态，在安全输入窗口设置 API 密钥、保存模型、按限制测试服务、打开 WhatsApp 登录窗口，以及备份、选择恢复订单数据库。恢复前会显示文件内容摘要并再次确认；原稿中未实现的开关或导出明确禁用。

订单来源可为 ShopPlus 文件导入或 ShopPlus API 同步。真实 WhatsApp 扫码、消息读取和真实订单利润仍需在你的资料环境中单独核对。手机底部仍保留旧版的大英文框与“待发送”行，尚未达到已确认的短白色输入框和紫色纸飞机基准。
