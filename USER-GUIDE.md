# 聊单助手 1.1.5 — 恢复业务页面

## 打开

1. 退出正在运行的旧版，解压 `聊单助手1.1.5-x64.zip`。
2. 双击其中的 `聊单助手 1.1.5.app`。首次被 macOS 阻止时，在 Finder 中右键 App，选择“打开”。
3. 1.1.5 与 1.1.4、1.1.3、1.1.2、1.1.1、1.1.0 使用同一个 `~/Library/Application Support/Liaodan Assistant Live` 资料目录；本版不清空已有资料。1.0.12 使用另一套资料目录，仍可保留。

## 导入你自己的订单

1. 从 ShopPlus 导出订单文件（Excel 或 CSV），在应用左侧打开“订单管理”，点击“导入订单”，选择该文件。
2. 应用先显示导入预览，尚未写入订单。查看可导入、需要核对、重复和被阻断的订单数。需要核对的单据，请逐单检查商品编号、成本和优惠；缺失成本可明确选择暂缺，利润会保持待核对。
3. 确认资料后点击“确认导入”。重复订单会跳过；存在阻断错误时不会提交整批文件。完成后可在订单列表搜索编号、电话或商品编号，并打开详情。
4. 没有文件时，可点“新建订单”建立空白草稿，手工填写真实资料并保存。应用不会预填虚拟客户或订单。

工作台“近期订单”中，点击卡片只会选中并高亮该订单；点击卡片上的“订单详情”才进入详情页。状态颜色与“需注意”圈数依据实际订单资料显示。工作台不会凭空生成未读数或聊天预览。

## 将所选订单关联真实 WhatsApp 聊天

1. 在订单列表打开该订单详情，在右侧“客户聊天”点击“关联真实聊天”，然后连接 WhatsApp 并用手机扫码。
2. 输入含国家区号的客户号码，点击“核对号码”，检查发送账号与目标号码。若订单已有电话，应用会按订单国家／地区识别本地号码（例如阿联酋的 `05…`）与国际写法是否为同一个完整号码。真正不一致或无法核实的号码仍会被拦截；确需更正订单电话时，在订单详情点“核对／更正联系电话”并填写修改依据。
3. 明确点击“确认关联并读取最近 24 小时”后，才读取该订单已核对聊天最近 24 小时、最多 100 条消息。需要时点击“刷新消息”。切换订单不会沿用上一单的聊天关联。
4. 发送前在左侧核对英文内容和附件。点击“发送”会向真实客户发送，不能用于试发。本次开发验收没有读取你的聊天，也没有发送消息。

## 恢复的页面

- “商品库存”显示本机库存同步记录和商品档案；可搜索、筛选、同步库存、打开商品详情并编辑商品档案。无资料时显示空状态，不能把“未同步”当作有货。商品图片从产品详情的“商品图片”入口管理。
- “利润核算”显示本机订单与已登记的每日成本；可选择日期保存投流、汇率和账号成本，打开每日利润详情和导出利润。还没有结算资料时显示空状态。
- “助手配置”保留说明和页签，可看真实服务配置与调用记录。自动发送未启用；原稿中没有服务接口的策略保存按钮会明确禁用。
- “连接与设置”可查看连接状态，在安全输入窗口设置 API 密钥、保存模型、按限制测试服务、打开 WhatsApp 登录窗口，以及备份、选择恢复订单数据库。恢复前会显示文件内容摘要并再次确认；原稿中未实现的开关或导出明确禁用。

订单来源为 ShopPlus 文件导入，尚未实现 ShopPlus API 自动同步。真实 WhatsApp 扫码、消息读取和真实订单利润仍需在你的资料环境中单独核对。手机底部仍保留旧版的大英文框与“待发送”行，尚未达到已确认的短白色输入框和紫色纸飞机基准。
