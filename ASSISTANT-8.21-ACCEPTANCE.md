# 8.21.0 电话核对与逐条翻译验收

日期：2026-09-18。范围限本次电话一致性保护和逐条翻译修复，继续8.20现成连接，不重做界面或协议。

## 交付

- ZIP：`dist/KDocs库存同步8.21.0-x64.zip`。
- SHA256：`4c2f23985a286c520308e7d9bebe949c91e3a0165e86181e3071c6168bba11da`。
- 解压后双击“KDocs订单助手 8.21.0.app”，无需.command、Docker或额外后台服务；保留Google Chrome依赖。
- 从最终ZIP新解压副本启动；包内版本8.21.0、页面8.21；本地签名验证通过，130个运行源码/资源文件与当前源码逐字节一致。

## 电话：PASS-LOCAL

- 读取左侧客户信息的有效电话，包括人工收货资料覆盖值，与本单真实保存的WhatsApp聊天号码核对，不把标题换成客户电话制造一致假象。
- +、00、空格、括号和横线统一；按明确国家/地区转换本地号码，例如阿联酋0500000002与+971500000002一致。支持项目常见国家别名；未识别国家的本地号码不猜区号，要求完整国际号码。分机等不明确格式不自动放行，不比较尾号。
- 未填、无法确定区号或确实不一致时，显示提示。首次/重新关联在解析前及保存前验证；刷新、历史保存/应用、自动归入、手工关联待处理、上下文AI及复制去WhatsApp均有服务端保护。
- 已保存的错误关联不删除、不自动修改客户电话，不自动猜测正确聊天。旧消息和每单草稿保留；修正客户资料或核对正确聊天后恢复符合条件的订单。
- 客户电话变更后，不符合的订单不再接纳消息；同客多单和归属不明处理保留。号码一致仅核对号码，不能代替客户身份与订单内容的人工语义确认。

## 翻译：PASS-LOCAL / 模拟服务验证

- 聊天窗口已显示的正文逐条翻译，包括已关联账号/客户的范围外历史；不要求历史完整，不因其他图片或待解密消息拦整单。
- 引用、编辑等标记不阻断其已显示正文的字面翻译。图片、待解密、撤回等无正文占位跳过自身；实际未提供正文不生成内容。
- 请求不带订单上下文，不推进订单。每次最多20条新译文，逐条缓存；已缓存的批次不会让后续已显示的未翻译消息永久无法触达。
- 原消息编号、关联账号/客户与绑定版本验证保留，拒绝其他聊天ID；响应格式错误、输入/绑定变化、存储失败不会留部分缓存或覆盖草稿。
- 显示中文译文开关只控制缓存显示，翻译按钮触发AI调用。电话错误关联仍禁止新AI请求；需要完整上下文的拟回复和提取继续保留独立校验。
- 本轮真实AI请求0：使用明确标识的虚构模拟服务验收按钮→IPC→选取原文→服务请求→缓存→气泡显示→重启，不宣称模型真实输出、余额或Keychain授权已复验。正常使用仍需有效服务商、模型、密钥、网络及额度。

## 检验结果

| 检验 | 结果 | 证据 |
| --- | --- | --- |
| npm test | 349/349 PASS，未跳过 | artifacts/assistant-fixes-8.21/unit-delivery.log |
| npm run check | PASS；打包前再次通过 | check-final.log、build.log |
| 源码Electron及Chrome回归 | 6/6 PASS | source-e2e-final.log |
| 最终ZIP新解压副本回归 | 11/11 PASS，未跳过 | final-e2e.log、final-test-invocation.json |
| 本地号码匹配、错误旧关联阻止、改电话后归入保护 | PASS-LOCAL | final/phone-translation-result.json、单元测试 |
| 正文翻译、引用正文、较早历史、占位跳过、缓存、重启 | PASS-模拟服务 | final/phone-translation-result.json |
| 不刷新自动接收、关闭全部管理网页、SSE、同客多单、断网、三次重启 | PASS-虚构事件适配器 | final/live-ui-result.json、final/live-closed-pages-result.json |
| 迁移17→24（含20）、图片/聊天/草稿备份恢复及恢复后补漏 | PASS-虚构数据 | final/live-backup-result.json、final-e2e.log |
| 消息转义、分页、引用、输入保存失败、并发、剪贴板、关闭重开 | PASS-LOCAL | final-e2e.log、final/workspace-result.json |
| 900×700、1280×850、1440×900 | PASS-LOCAL，虚构截图已查看 | final/ai-chat-*.png、final/polish-*.png |
| 包内源文件一致与签名 | PASS | package-integrity.json、package-target.json |

初始失败记录保留：沙箱阻止OAuth回环端口属于环境限制，沙箱外完整重跑通过；新提示加载中的引用错误已修复；旧测试缺少电话的初始化改为显式填写虚构匹配电话，原有接收、隔离、恢复业务断言保留。没有删测试或跳过失败用例。

## 边界与保护

- 正式订单写入0、真实消息发送0、新增真实AI请求0；不操作库存同步、Google Sheets，不复制隔离凭据到常用目录。
- 新增运行模块一个；没有新增依赖或升级whatsapp-web.js。原HEAD和本轮开始时暂存区字节保留，247个非本任务基线源文件不变。全部本任务修改在明确文件清单上提交，不git add全部，不推送。
- 8.20 ZIP与已安装App保留。本轮测试输出写入独立assistant-fixes-8.21目录；常用资料目录没有参与破坏、迁移或故障测试。
- 8.20真实连接观察的适用范围见旧验收表，本轮没有新增两小时真实观察，不把模拟消息冒称真实新消息事件验收。
- 图片大小/类型/失败原因完善、AI自动回复、S2/S3剩余能力及手机全部历史保证未在本次完成。

接下来做什么、由谁做、是否需要我操作：开发方完成本次修复和安装包验收；用户退出旧版、解压打开8.21，遇到旧错误关联时核对本单正确WhatsApp电话后重新关联。有效登录无需重复扫码；正常翻译由用户点击按钮使用自己的AI配置与额度，无需用户进行调试。
