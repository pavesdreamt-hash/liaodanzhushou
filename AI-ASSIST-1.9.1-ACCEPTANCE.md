# 聊单助手 1.9.1｜聊天工作台 AI 辅助验收记录

## 本次修复目标

启用聊天工作台顶部“AI辅助”，但只提供人工明确点击后的可编辑草稿能力。AI 自动发送继续不启用；不得恢复订单详情中已经移除的聊天区域。

## 已核验规则与范围

- `AGENTS.md`：交付必须是可直接使用的 Mac x64 App，且最终 App 必须实际启动。
- `CONFIRMED-UI-BASELINE.md`：D-106 规定了本轮许可范围、受保护邻近元素、当前会话令牌限制、显式请求、无自动发送/更新/重试，以及独立 Mac x64 App 的直接启动验收要求。
- `ORDERS-RESPONSIVE-1.9.0-ACCEPTANCE.md`：仅作为上一版订单管理局部验收记录；不将其扩大解释为本轮工作台 AI 功能已验收。

## 实现边界

- 顶部顺序和布局仍为“人工、AI辅助、AI自动”；只有 AI辅助可选，AI自动保持禁用及“待启用”。
- 选择 AI辅助本身不请求 AI、不清空中文/英文草稿、附件或引用回复，也不发送 WhatsApp。
- 只有点击“生成 AI 回复”才调用 `manual-reply-translation/assist-draft`；页面只提交当前绑定令牌、中文意图和可选引用 ID。
- 主进程重新验证该令牌，并自行读取当前绑定会话最多 20 条文字；页面提交的聊天正文不参与请求。
- 返回内容仅填入可编辑英文草稿并显示中文核对；不调用发送、订单或客户资料写入接口。输入/引用在等待中变化、配置缺失或服务失败时保留现有草稿。
- 所有开发和验收数据均为虚构号码、虚构会话、虚构 API key 和模拟服务商结果；未读取真实客户聊天、未调用真实 AI、未发送真实 WhatsApp 消息。

## 实际验收结果

### 最终成品与启动

- 最终交付为独立 Mac x64 App，不生成 ZIP：`dist/聊单助手1.9.1-x64/mac/聊单助手 1.9.1.app`。
- 最终一次构建使用 `electron-builder --mac dir --x64`，输出目录独立于仍保留的 1.9.0 成品；没有覆盖、清理或重置既有工作区内容。
- 从该最终 `.app` 直接启动了生产模式隔离实例。验收报告 [`artifacts/ai-assist-1.9.1/app-verification.json`](artifacts/ai-assist-1.9.1/app-verification.json) 记录 `packaged=true`、版本 `1.9.1`、`arch=x64`、`errors=[]`。
- 应用目录稳定清单 SHA-256 为 `91bfc2d2f7ac667745c67bb107653d8eee78ae942adbe3f6b212d12a3707e6d9`；方法和校验值见 [`dist/聊单助手1.9.1-x64/APP-SHA256.txt`](dist/聊单助手1.9.1-x64/APP-SHA256.txt)。

### 虚构数据的成品流程

- 使用临时隔离资料目录、虚构发送账号 `+971500000001`、虚构客户 `+971500000002`、虚构消息 “Can you confirm the fictional delivery address?”、虚构 API key 和模拟服务商回复。最终 App 顶栏实际显示 WhatsApp 已连接。
- 未配置密钥时，点击 AI辅助后准确提示“AI辅助需要先在助手配置保存服务商、模型和 API 密钥。”，仍停留人工模式，`reply` 调用记录为 0。
- 保存虚构密钥并输入“请礼貌确认虚构地址”后，选择 AI辅助本身仍不产生 `reply` 请求；只有明确点击“生成 AI 回复”才产生 1 次模拟草稿请求。打包 App 的主进程重新读取当前令牌绑定会话，页面不能注入会话正文。
- 返回的英文草稿为可编辑文本，并显示“AI 中文核对：模拟中文对照”；切回人工后英文草稿、附件与引用回复状态保持。AI自动仍禁用并显示“待启用”，人工“发送确认”入口保持可用。
- 验收 fixture 禁止人工发送，最终报告记录 `realMessagesSent: 0`、`realAiCalls: 0`；没有读取真实客户聊天、没有调用真实 AI、没有发送 WhatsApp、没有写入订单或客户资料。

### 真实执行的检查

- `node --test --test-timeout=30000 test/manual-translation.test.mjs`：7/7 通过，包含“最多 20 条已核验消息、无发送”和“无效引用/缺依据时不请求 AI”。
- `npm run build:ui && node --test --test-concurrency=1 --test-timeout=60000 test/chat-workbench-ai-assist.e2e.mjs test/chat-workbench-send.e2e.mjs`：2/2 通过；前者验证显式生成、零自动发送，后者回归既有人工文字/图片/文件发送链路，均使用隔离虚构测试数据。
- `npm run check`：通过 TypeScript、UI 构建、共享兼容性和项目检查；`git diff --check` 通过。
- 最终打包 App 的生产模式验收覆盖了未配置失败、配置后显式草稿、主进程 `assist-draft` IPC、草稿保留、AI自动禁用、无发送，以及 1440×1000、1280×820、820×640 三个窗口截图。三张最终成品截图已实际查看：[`1440×1000`](artifacts/ai-assist-1.9.1/ai-assist-1440x1000.png)、[`1280×820`](artifacts/ai-assist-1.9.1/ai-assist-1280x820.png)、[`820×640`](artifacts/ai-assist-1.9.1/ai-assist-820x640.png)。820×640 沿用已确认的横向滚动行为，不借本轮 AI 功能改变现有小窗口布局。

### 未扩大范围与交付状态

- 本轮没有重新验收订单详情、订单管理或其他页面的完整业务/视觉基准；订单详情已删除的快捷回复和客户聊天区域未恢复。
- 完整 `npm test` 未作为本轮通过项：既有脏工作区中的历史订单测试与更严格的订单确认依据规则不一致，且非提权环境的旧本地 Web/OAuth 测试会因回环端口权限失败；本轮未修改这些无关规则或测试来掩盖结果。
- 工作区中原有的大量已修改和未跟踪文件均保持原样；未执行 `reset`、`clean`、覆盖或删除。
- 按用户的新节奏，1.9.0 → 1.9.1 只是当前五个版本批次中的第一个版本；本次不创建 GitHub 提交或推送，累计到第五个交付版本时再进行 GitHub 功能更新。
