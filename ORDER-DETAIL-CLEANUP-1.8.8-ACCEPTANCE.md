# 订单详情精简 1.8.8 验收

## 本次修复目标

只在订单详情页删除用户标记的两个完整区域：“快捷回复”和“客户聊天”。同时删除该页上的关联真实聊天入口、手机聊天框和聊天输入区。订单资料正文使用释放后的宽度。

## 保护范围

- 订单概览、客户信息、商品与金额、配送与包裹保留。
- 左侧导航、导航折叠、顶栏和布局设置入口保留。
- 聊单工作台的聊天、输入和快捷回复保留。
- 本轮不改订单资料和其他业务页的功能。

## 真实验收内容

1. 使用隔离资料目录和虚构订单 `DETAIL-CLEAN-001` 启动已打包的 Mac x64 App。
2. 从“订单管理”进入订单详情，检查 `.reply-rail`、`.reply-chat-workspace`、`.od-chat-column`、`.od-phone`、`.manual-composer` 的 DOM 数量均为 0，并确认不存在“关联真实聊天”按钮。
3. 实际检查订单正文为单列，“订单概览”“客户信息”“商品与金额”“配送与包裹”均只有一个且可见。
4. 实际检查导航折叠入口和布局设置入口仍可见。
5. 返回聊单工作台，检查聊天区、输入区和快捷回复仍各有一个。
6. 九个可进入页面的导航回归通过，每页仍只有一个折叠／展开入口，状态跨页保留。
7. 已人工查看 `artifacts/order-detail-1.8.8/order-detail-without-chat.png`：指定两区已消失，订单正文占用页面剩余宽度，未发现重叠、截断或空白占位。

## 自动检查

- `npm run check`：通过。
- `git diff --check`：通过。
- `test/order-detail-no-chat.e2e.mjs`：1/1 通过，在已打包 1.8.8 App 上执行。
- `test/navigation-collapse-audit.e2e.mjs`：1/1 通过，在已打包 1.8.8 App 上执行。

## 最终交付物

- ZIP：`dist/聊单助手1.8.8-x64.zip`
- SHA-256：`1c23877ebd160a1966a7f3f42147d9b92c854d8d0210690b91cd061b68708106`
- 从 ZIP 重新解压后，目标验收和九页导航回归共 2/2 通过。
- 正式资料窗口启动日志确认 `packaged: true`、`version: 1.8.8`、Mac x64，主窗口为可见并已获得焦点；WhatsApp 自动消息连接在启动后进入 `online`。

## 核验文档和规则

- `CONFIRMED-UI-BASELINE.md` D-103：本轮允许范围与保护项。
- `CONFIRMED-UI-BASELINE.md` D-102：订单详情导航折叠与全站共享状态。
- `AGENTS.md`：界面基准、局部修复边界、真实图形验收、Mac x64 ZIP 交付与最终包启动验证规则。

## 未扩大声明

本次是订单详情页的局部精简验收，不代表其他页面的完整 UI 基准全部重验。历史“手机底部 P1/P2”属于本轮按用户要求整体删除的订单详情客户聊天区，不再是该页可验收元素。
