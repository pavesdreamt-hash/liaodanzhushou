# 订单助手：已确认设计、实施顺序与验收清单

最新阶段：8.20本机自动消息接入及最终App本地验收通过；真实扫码、36条/5图及三次重启通过，候选App实际两小时观察和最终准确ZIP三次真实重启通过；中途自动重连约4分钟。真实新消息仍等待产生。见ASSISTANT-8.20-ACCEPTANCE.md。

整理日期：2026-09-13。原始设计基线：8.8.0；2026-09-16 已交付8.9.0 S0/S1本地阶段，详细证据见 ASSISTANT-S1-ACCEPTANCE.md。打包版钥匙串授权未完成，S2/S3仍待开发，不是项目总PASS。

本文是后续实施依据，不是功能完成报告。勾选表示已有对应证据；没有执行的新功能测试一律不记为 PASS。旧发布记录继续保留，不能替代下一候选版本的验收。

## 1. 本轮已完成与后续范围

- [x] 对照当前 AGENTS.md、Git 状态、package.json、客户信息样式及订单助手/浏览器代码。
- [x] 汇总用户已确认的布局、按钮位置、阅读范围、回复区与资料核对边界。
- [x] 将四张设计依据复制到项目中，保留生成原图。
- [x] 列出分阶段任务、自动验收、真实人工关口和交付条件。
- [ ] 后续应用实现与真实验收。此项未因用户确认草图而完成，也不在本轮文档任务中启动。

保留现有工作区和全部无关修改；不重做已完成的 REQ-01～REQ-07。资料核对按基础版接通，深入交互研究留到实际使用后。删除/隐藏订单、独立客户管理、自动发送消息不加入本次实施范围。

## 2. 设计依据及冲突时的采用顺序

文字决定优先于图片里的偶然文字、数字和排版误差。示例姓名、报价、已读数量、时间及“成功”提示均为虚构展示，不代表应用实测结果。

| 依据 | 用途 | 采用边界 |
|---|---|---|
| [统一卡片风格](design/assistant-ui-2026-09-13/01-card-style.png) | 订单助手与左侧客户信息的视觉统一 | 白底、圆角、轻阴影、标题、留白；区域通过卡片间距分开 |
| [用户选定的整体比例](design/assistant-ui-2026-09-13/02-approved-proportions.png) | 整体比例、关联浮层大小、按钮位置 | 是先后生成的两张完整设计图中的第一张；不采用随后放大的浮层 |
| [设置布局](design/assistant-ui-2026-09-13/03-settings-layout.png) | 齿轮内设置与密钥控件 | 只采用设置部分；图中的简化回复栏和旧时间默认值已经被后续决定替代 |
| [资料核对基础方案](design/assistant-ui-2026-09-13/04-basic-review.png) | 本次新增、冲突、缺失项和保存到同一订单 | 功能保留基础版；视觉服从最新卡片风格，不继续复制完整客户资料表 |

原始生成图标识：卡片风格 `exec-a1f2214e-7887-473c-a198-adf92d7badcd.png`；选定比例 `exec-cb837129-e885-4c0e-971f-f98a3251db96.png`；设置 `exec-1953cbbf-7df2-4bd7-867e-507014abd585.png`；资料核对 `exec-3178e76e-cf41-4c7f-9c0e-d38a0c76c7be.png`。

### 2.1 固定的视觉和布局规则

- [x] U01：左侧保留现有完整订单详情。宽屏打开助手后按已确认方式占右半栏，原右栏业务内容排列到左栏下方；关闭助手恢复布局。
- [x] U02：“订单助手”与“客户信息”标题使用相同字号、字重、颜色及内边距；当前客户信息样式参考为 19px 标题、20px 内边距、14px 圆角、白底及轻阴影，实施时复查实际 CSS 层叠结果。
- [x] U03：聊天、资料提示、回复编辑区使用独立卡片和间距；不使用横线划分功能区，不顺带修改左侧原有表格和业务样式。输入框边界、气泡轮廓仍可保留。
- [x] U04：对话区域保持聊天软件形式：客户在左、我方在右、气泡、时间、中文译文和针对单条消息拟回复；不会变成资料表。
- [x] U05：“刷新聊天”在“聊天关联”左侧且同一行。“今天14:00－最新”等真实范围摘要在下一行“显示中文译文”右侧且同一行；样例时间不能写死。
- [x] U06：保留“聊天 / 资料核对 / 操作记录”。待处理数字来自当前订单实际未解决事项；同一事项不因重复提取累加。
- [x] U07：保留完整回复区：回复客户/主动联系、语气、中文输入、AI 拟回复、转成英文、可编辑英文草稿、重新生成、复制并去 WhatsApp。两个模式文字前保留对话气泡/纸飞机图标。
- [x] U08：采用已选比例图的关联浮层尺寸；不再放大成后来的第二张，也不为了排更多字段缩小字号。
- [x] U09：900×700、1280×850、1440×900 实测无页面横向滚动、裁切或无法点击的控件。窄窗口按原响应规则重排，用内部滚动保持可达，不强行缩小所有控件。

视觉验收：开发用虚构数据在实际 Electron 页面截图，与上述依据对照；测试打开、收起、滚动、输入、切换标签，不能只检查静态首屏。卡片风格和单条消息气泡应同时满足。

## 3. 已有代码与本次需要补齐的能力

以下来自本轮只读检查；尚未重新运行应用、访问正式订单、读取真实聊天或调用 AI。

| 能力 | 当前依据 | 后续责任 |
|---|---|---|
| 版本及工作区 | package.json 为 8.8.0，存在大量既有修改 | 实施前再核对版本及迁移，不重置工作区 |
| 客户信息卡片 | renderer/orders.css 的当前详情样式；renderer/renderer.js 的 compactRecipientSection | 复用视觉和订单对象，避免再做一套完整客户资料 |
| 已保存的聊天 | order-assistant.mjs 保存消息正文、方向、时间；status 主要返回数量和提取状态 | 增加按当前订单关联限定的消息读取接口及可见聊天界面 |
| 打开 WhatsApp | whatsapp-browser.mjs 的 open 仍依赖单个 page 引用，首次取 pages()[0] | 修复多标签、关闭后重开、已有会话占用；不能只再增加错误文案 |
| 阅读范围 | 有固定/最新结束模式；read 每次仍从 scope_start 请求 | 增加成功进度、连续补读、历史覆盖和分批读取 |
| 真实页面限制 | 当前按已加载消息校验范围；每次最多 20 条；时间解析依赖中文网页格式 | 验证受支持范围，不能宣称可任意读取全部历史 |
| 重新关联 | bind 会增加关联版本并清空当前读取状态 | 明确同一关联重复确认与真正修改关联，避免看起来“消息消失” |
| AI | 有主进程配置、安全存储和提取客户端 | 新增翻译/回复草稿能力；不能将“配置已保存”当作真实能力通过 |
| 资料核对 | 已有草稿补全、冲突、撤销及确认后只出建议的路径 | 接入基础核对界面及同一订单刷新，复查事务与并发保护 |

候选修改位置：renderer/draft-orders.js、renderer/orders.css、renderer/renderer.js、renderer/assistant-settings.js/css、必要的 index.html；src/orders/whatsapp-browser.mjs、order-assistant.mjs、assistant-range.mjs、assistant-connectors.mjs、assistant-settings.mjs、order-app-service.mjs，以及对应 IPC/本地网页接口。仅在实际需要时新增模块或迁移，文件名单不是预先批准覆盖整文件。

## 4. 聊天关联、可见记录和连续刷新

### 4.1 连接和目标

- [ ] C01：连续点击打开 WhatsApp 复用当前有效窗口和标签。关闭被追踪标签但另有可用标签时正确恢复；应用重启/浏览器重启后也有明确恢复路径。
- [ ] C02：旧版本占用浏览器资料时，不通过反复启动制造空标签，不删除登录资料、不强行关闭用户进程；给出具体占用/恢复提示。自动测试覆盖真实可见 Chrome 生命周期。
- [ ] C03：首次关联由用户核对当前账号、联系人及本单范围。内部标识自动取得，正常界面不要求手填 accountId/chatId。
- [ ] C04：绑定使用经核对的账号与稳定聊天身份，不仅凭姓名。现有号码组合不能在报告中冒称平台 JID；遇到身份变化必须重新核对。
- [ ] C05：关联状态、已读数量、读取中、无消息、读取失败分别显示。关联成功不显示为读取成功；已保存记录不会因新读取失败全部消失。
- [ ] C06：同一账号/聊天/范围的重复确认不无故重置记录。真正变更关联留下记录，并明确切换显示范围；旧异步结果不能进入新上下文。

### 4.2 时间与历史

- [ ] R01：按天选择时，开始为所选日 00:00，固定结束为所选日 24:00；可按需展开时分。24:00 在内部表示次日 00:00 的不包含上界，测试当天末分钟及次日边界。
- [ ] R02：新关联默认勾选“每次读取到最新”；既有订单保持原确认范围，不在升级或重新打开时自动扩大。取消勾选后显示固定结束日期。
- [ ] R03：当天固定结束尚未来到时，本次只读取到当前可取得的最新位置；已保存的固定截止日仍保留。开始晚于当前、结束早于开始、无效日期要明确报错。
- [ ] R04：增加可选“从当前聊天最早记录开始”，默认不勾选。必须实际加载并证明覆盖起点；不能把当前已加载最早一条冒充整个聊天的第一条。
- [ ] R05：历史查看范围与本单提取范围分开。扩大历史查看不自动扩大本单购买识别范围；不能混入客户以前的购买。
- [ ] R06：历史读取可分批、可取消，有有限时长/批次数和进度；到达限制仍未覆盖时显示“尚未读全”和继续入口。具体限额在实施时记录并测试，不用无限滚动或静默截断达到通过。

### 4.3 增量、正文显示和消息变化

- [ ] R07：刷新从上次成功保存的消息进度继续到本次最新位置，保存消息与进度保持一致；不把点击按钮的时间当作成功进度。
- [ ] R08：同一分钟多条消息、边界重叠、迟到或乱序消息使用稳定消息 ID 去重/排序；不能只用时间戳跳过尚未取得的消息。
- [ ] R09：网络失败、取消、验证失败或保存回滚不推进未完成区段；先前成功页可以保留并标出剩余范围。重试从真实成功位置继续。
- [ ] R10：超过当前 20 条限制的选定范围采用受控分批方案；至少测试 0/1/20/21 条和多批内容。真实授权上限与产品分批能力分开计算。
- [x] R11：提供当前订单消息列表，显示正文、方向、本机时间、读取范围；“查看原消息”可定位并高亮来源，返回后仍保留核对上下文。
- [ ] R12：重复刷新不重复显示气泡，也不重复添加商品/优惠。相同消息 ID 内容变化、编辑/删除或不支持类型有明确状态，不静默当作新购买。
- [ ] R13：用户正在看旧消息时刷新不强制滚到底部；原来就在底部时跟随新消息。切单和重启恢复各自上下文及已成功的读取位置。
- [ ] R14：只分析已关联范围。图片、附件、语音、引用、转发等当前未可靠支持的类型说明限制；不能制造正文、时间或消息 ID。

本节自动验收：虚构页面和本地数据库测试覆盖失败/重复/乱序/切单/重启；真实可见 Chrome 验证标签复用。真实 WhatsApp 的正文、身份和完整性另在人工关口 A 限定范围核验，模拟 PASS 不能代替。

## 5. 翻译、拟回复与安全设置

- [ ] A01：显示中文译文保留原文；翻译按消息 ID、正文版本、语言和模型配置缓存。刷新/切换标签不无理由重复收费；有新消息只处理新增或改变的内容。
- [ ] A02：“回复客户”基于选中客户消息及本单必要上下文；没有选中时清楚显示引用的最近客户消息，不混用其他订单。
- [ ] A03：“主动联系”以人工输入的中文意图为主，不要求先有客户问题。两个模式只切换草稿生成方式，不表示发送。
- [ ] A04：“AI 拟回复”结合本单上下文、中文意图与语气生成可编辑英文草稿；“转成英文”忠实转换中文输入，不能擅自增加库存、价格、时效承诺。
- [ ] A05：“重新生成”只在明确点击后请求，遵守相同上下文和预算；生成失败保留已写草稿。人工编辑期间返回的旧请求不能覆盖新输入。
- [x] A06：中文输入、英文草稿、模式、语气和引用消息按订单独立保存；切单、切标签、关闭助手和重启不串单、不丢失已保存草稿。
- [ ] A07：“复制并去 WhatsApp”复制当前可见英文草稿并复用正确窗口；剪贴板/打开失败分别提示，保留草稿。程序不点击发送，也不默认粘贴到未知聊天。
- [ ] A08：聊天内容作为待分析资料处理，恶意指令不能改变系统规则、提取范围或触发外部操作。
- [ ] A09：齿轮内保留服务商、模型、密钥和语言/语气偏好。密钥显示框与同页其他框同宽；“更换 / 删除 / 测试连接”同一行。继续使用已有本机安全输入入口。
- [ ] A10：DeepSeek 优先复用已保存配置，未来保留 OpenAI/GPT 适配入口；模型从实际配置核验，不凭旧报告写死。设置保存、凭据可用、真实请求成功分别标识。
- [ ] A11：renderer、Git、普通日志及截图中不出现密钥。系统钥匙串等待期间应用可操作；超时恢复不反复启动系统授权请求。
- [ ] A12：提取、翻译、拟回复、重新生成及测试连接共享开发调用账本；记录脱敏的用途、计数和耗时。重启不能重置本轮授权预算。

真实 AI 约束：历史账本为初始 5/5 已使用；额外最多 3 次的请求仍待明确答复，本轮新增调用为 0。后续先复查已有账本与授权，禁止把设计确认视为新的调用预算。每次真实开发请求 20 秒超时、无自动重试；认证/配额错误停止同类请求。若有限调用不足以验证全部功能，逐项记待验收，不能复用固定结果冒充新请求通过。

自动验收：虚构双向聊天覆盖准确翻译、选中消息、主动联系、空输入、错误响应、超时、人工编辑竞态、切单、重启及复制失败。真实 AI 对虚构资料分别验证已计划的用途；用户只集中核对少量语义结果，不负责调试接口。

## 6. 资料核对基础版与同一订单

此部分按用户要求留待使用后深入研究。当前只保证以下基础行为，不增加完整客户管理或另一套商品/履约页面。

- [ ] D01：左侧显示完整订单并承载业务操作；助手“资料核对”仅显示本次新增、冲突、缺失和依据。两边使用同一订单对象及版本，不保存两个各自独立的订单副本。
- [ ] D02：有明确依据、无冲突的空字段只自动写入草稿。保存成功后刷新左侧；失败不显示同步成功，也不覆盖左侧未保存的人工输入。
- [ ] D03：冲突展示原值/新值/来源，默认不代选；保存人工选择后更新左侧同一字段。未解决项可继续留在草稿，不强迫一次填完。
- [ ] D04：询价不创建确认购买；商家报价未经客户接受不变成成交金额；换商品、改数量、已含优惠按原业务规则处理。未知金额/成本保持 NULL，明确 0 才为 0。
- [ ] D05：已确认/履约订单仅生成建议，不被助手静默修改；客户确认及记录继续独立操作。
- [ ] D06：客户字段、商品变更、来源和审计一次事务保存，失败全部回滚；保存后左右显示一致。
- [ ] D07：撤销最近一次补全校验之后的人工修改及订单版本；冲突时阻止覆盖。撤销成功同步左侧并保留审计。
- [ ] D08：重复查看、提取、刷新或打开不重算历史成本快照；相关订单资料改变使旧报单失效；已确定的变动物流费规则保持不变。
- [ ] D09：操作记录只列本单关联、读取、提取、核对、撤销等必要事件；正文通过限定来源入口查看，不进入普通日志。

自动验收：虚构草稿中完成“补空字段→左侧更新→一个地址冲突→人工选择→左侧同步→撤销保护”，并验证已确认订单只出建议。资料核对界面不因本节暂缓深入设计而跳过业务正确性。

## 7. 实施阶段、通过条件和人工关口

状态统一使用：TODO / RUNNING / PASS-LOCAL / WAIT-HUMAN / BLOCKED / FAIL / PASS。每项结论都附本轮证据；无证据的既有报告只作参考。

| 阶段 | 开发需要完成 | 通过条件及证据 | 人工参与和停止位置 |
|---|---|---|---|
| S0 基线与环境 | 重新核对代码/版本/迁移、保存差异指纹、识别进程和资料目录、复用现有凭据配置 | 既有修改不变；虚构测试可运行；新问题及权限需要已列明 | 通常无；系统权限缺口仅暂停依赖项 |
| S1 本地界面与订单状态 | U01～U09、消息显示接口、订单独立草稿、基础核对接通 | 实际 Electron 虚构界面在三种尺寸可操作，左右更新/切单/关闭/重启通过 | 开发自行截图复核，不让用户逐按钮试错 |
| S2 WhatsApp 读取链路 | C01～C06、R01～R14，先解决重复标签、关联和可见聊天，再做连续/历史补读 | 本地边界测试通过；实际 WhatsApp 在许可范围内读到可见消息并验证方向/时间/去重 | 到关口 A 做真实核验；不足的范围或登录条件明确 BLOCKED |
| S3 AI 和基础核对 | A01～A12、D01～D09；主进程接口、缓存、预算、错误恢复 | 本地失败/语义/并发样本通过；获准的真实虚构请求有相应证据 | 复用关口 A 的配置；预算未批准时只推进本地工作 |
| S4 完整流程与回归 | NEW 矩阵、导入、库存回归、迁移、备份恢复、最终候选包 | 从新建到 TXT 的虚构全流程，源代码及打包版回归均通过；无代码 FAIL | 由开发完成；准备好可直接使用的候选产物再请用户核验 |
| S5 集中体验与发布 | 用户反馈一次收集，开发修复并复验，核对最终产物 | 人工 B 的视觉/真实操作通过；最终包与源码版本一致，直接打开可用 | 用户只核对实际体验；代码缺陷由开发定位，修复引入实质变化时再集中复核 |

S2 与 S3 的不依赖外部连接部分可继续推进，不需要每完成一步询问用户。真实连接与依赖该连接的验收必须停在实际关口，不能将模拟链路结论扩大为总体 PASS。

### 人工关口 A：必要连接集中处理

实施阶段开始后先准备可操作窗口、虚构样本和具体校验步骤，再一次列出仍需用户完成的事项。已有有效登录/配置不重复要求操作。

| 窗口/位置 | 仅在确实需要时的动作 | 成功标准 |
|---|---|---|
| App 打开的专用 WhatsApp 窗口 | 重新登录或打开已指定自用目标；确认新一轮测试范围 | 程序核对正确账号/目标，App 显示许可范围内的消息 |
| 同一测试聊天 | 为尚无足够样本的方向/时间场景发送少量指定虚构内容；由用户发送 | 来源、方向和时间符合样本；开发不发送任何 WhatsApp 消息 |
| 本机安全输入/钥匙串提示 | 仅在凭据不可用时授权当前 App；不向对话粘贴密码或密钥 | 主进程能取得已有配置，重启复用成功 |
| 对话中的一次范围/调用额度说明 | 明确额外消息元数据、历史范围或真实 AI 的有限调用预算 | 只在获得明确答复的范围内执行；不把沉默当许可 |

现有真实授权仅限已指定自用账号与本轮新虚构消息（最多 20 条）；“从最早记录开始”的设计选项不是读取真实旧历史的授权。历史报告记录过范围外时间元数据审批拒绝、旧 App 占用及钥匙串等待；这些不能直接当作当前仍存在的故障，需在相关阶段复查，也不能绕过。此文档阶段不重新触发这些权限。

### 人工关口 B：可用候选包集中核验

用户只需在准备好的候选 App 中完成一次短流程：打开指定聊天、刷新看到消息、看一条译文、拟定/修改回复草稿并复制、核对一处资料同步和页面舒适度。发送始终由用户自行决定。开发已在此前完成导入、异常、迁移和恢复等重复测试。

目标为两轮集中参与，而非保证永远只需要两次。若平台要求重新登录、系统拒绝或用户新增范围，开发说明新的具体原因，只请求必要动作。

## 8. NEW-01～NEW-07 回归矩阵

下表均为下一候选版待执行项。历史本地通过记录保留在原报告中，不在这里预先勾选。

| 编号 | 本轮必须保持/接通的行为 | 必要验证 | 当前状态 |
|---|---|---|---|
| NEW-01 | 空白草稿、唯一 ID/递增号、空网站号、NULL、无空包裹、防连点、独立草稿状态 | 两条草稿、关启持久化、统计排除；网站导入及去重继续可用 | PASS-LOCAL：261单元/完整虚构流程 |
| NEW-02 | 原文姓名、电话原文和去空格展示、商品数量、成本快照、系列分包、优惠规则 | 单品多数量、同/跨系列、NUMERIC/KY/YB及现有系列、缺成本、恢复后快照一致 | PASS-LOCAL：业务回归/最终包恢复 |
| NEW-03 | 订单独立助手、设置安全、卡片界面、聊天与回复上下文 | 两单往返、同客第二单、关闭、三尺寸、长输入、日志无密钥 | PASS-LOCAL；打包密钥写入WAIT-HUMAN |
| NEW-04 | 正确目标关联、实际正文可见、连续刷新、去重、历史范围和旧请求隔离 | 模拟与真实分别报告；首读/重复/重启/切单/失败恢复/多标签 | 本地消息通过；S2读取扩展TODO |
| NEW-05 | 有依据提取、空字段补全、人工冲突保护、报价/购买/替换区分 | 明确/缺失/地址冲突/询价/未接受报价/改数量/换商品/重复/恶意文本；真实模型另计 | 本地核对通过；真实AI语义待S3 |
| NEW-06 | 原子保存/回滚、撤销、独立确认、报单失效、迁移/备份 | 故障注入、后续人工编辑保护、未确认不发货、成本/运费/利润规则回归 | PASS-LOCAL：事务/迁移20/备份恢复 |
| NEW-07 | 新建→关联→读聊天→提取→解决冲突→确认商品金额→成本分包→客户确认→TXT→重启→同客第二单 | 虚构完整流程和真实接口流程分开；核对 TXT 内容及第一单不受影响 | 虚构流程PASS-LOCAL；真实联合仍BLOCKED |

### 最低回归集合

- [ ] `npm test`、`npm run check`、`npm run test:electron-orders`；执行前审阅对应脚本，保持测试使用虚构资料，记录实际命令和通过/失败/跳过数量。
- [ ] .xlsx/.xls/.csv 经实际页面选择→预览→核对→导入→列表→重启；取消、重选、坏文件、重复导入、缺成本均覆盖。测试不能只传固定文件路径绕过文件选择环节。
- [ ] 新增的聊天、范围、刷新、回复和安全存储测试；既有相关 Electron/浏览器/备份测试按本轮改动纳入，不以测试名字里的“real”推断已获正式数据许可。
- [ ] 在虚构备份上验证升级、失败回滚、恢复及重新打开。迁移使用实施时下一个可用编号（本次只读看到最高 19），不预先锁定新编号，不修改已发布迁移。
- [ ] 不读取正式订单做破坏性测试，不运行真实 KDocs 采集或 Google Sheets 写入。正常交付 App 不强制空白隔离库，也不要求用户从 .command 启动。
- [ ] 关键截图只使用虚构数据；真实连接证据以计数、方向、时段、脱敏错误和校验结论保存，完整真实聊天不进入截图、报告及普通日志。

## 9. 包装、交付和回溯

- [ ] 应用实现完成后按 AGENTS.md 更新次版本，重新核对实际基线；同步 package.json、锁文件、界面、使用说明、发布说明。仅整理本文不升版本。
- [ ] 采用当前实际交付格式 ZIP：`dist/KDocs库存同步<版本号>-x64.zip`；双击解压后的带版本 App 即可打开，不依赖启动KDocs.command。若用户后来明确要求 DMG，按其新要求执行，不把格式切换当作功能修复。
- [ ] 从最终 ZIP 新解压一份 macOS x64 App，验证签名/架构、启动、显示版本、源码与包内资源一致；通过该副本完成关键导入/聊天/回复/核对回归。
- [ ] 正常启动使用稳定的应用数据身份；测试仅在内部使用虚构目录。保留回溯及备份，不自动覆盖已安装 App、不清空正式库。
- [ ] 只提交本任务文件/修改块，本地提交不推送，保留原 HEAD/暂存区相关约束和用户无关修改，交付时说明实际 Git 状态。
- [ ] 最终提供唯一对应版本的验收报告：每个 NEW 的实际文件、实现内容、测试操作和证据、PASS/FAIL/BLOCKED；同时给出 ZIP 绝对路径、SHA-256、准确打开方式。
- [ ] 有代码 FAIL 时继续修复并复验；外部接入未完成时交付范围如实标 MIXED/BLOCKED，说明什么能用、什么仅模拟、缺少什么。打包成功不等于全部功能成功。

## 10. 2026-09-13 设计交接原记录（最新状态见第11节）

当前完成：清单与设计依据归档。应用源代码仍为 8.8.0；本轮未读取真实聊天、未调用 AI、未启动正式订单、未生成新版安装包。

下一步由开发负责：收到开始实施的指令后从 S0/S1 开始，优先打通“打开/复用正确聊天窗口→App 内看见正文→完整回复区”；随后按 S2/S3 完成刷新、AI 和资料同步，按 S4/S5 回归交付。只在关口 A/B 或实际新增必要权限处请用户参与，不在每一步结束后索要“继续”。

每次阶段交接固定记录：已完成项与证据、未完成项及原因、下一动作与责任方、是否需要用户操作、正在运行的进程/窗口及调用账本。中断后据此继续，不用历史“通过”覆盖当前待验收项。


## 11. 8.9 本阶段实际交接

S0/S1本地实现、三尺寸实操、虚构完整流程、备份恢复及最终ZIP启动已完成；安全设置入口源码通过，打包版钥匙串写入仍WAIT-HUMAN。未勾选的S2/S3项目不能从历史截图或本地模拟推定通过。唯一当前报告为 ASSISTANT-S1-ACCEPTANCE.md。

接下来做什么、由谁做、是否需要我操作：用户仅在需要AI配置时处理系统钥匙串授权，开发负责随后复验；后续S2/S3由开发继续，新增真实AI仍需明确预算。无需用户运行测试，无后台持续任务。

## 12. 8.10本轮增量交接

沿用8.9 S1，不重新实现。C01标签复用恢复、C06相同关联保留、A01～A05客户端/界面/缓存、A12共享预算与脱敏加密账本已实现并本地验收。S1原业务回归继续通过，三尺寸控件重叠已修复。真实AI/聊天本轮调用0。

本轮报告 `ASSISTANT-S23-LOCAL-ACCEPTANCE.md`，全套单元271/271、订单Electron8/8；打包最终证据写入报告。S2历史/连续读取、全天范围及受控多批仍TODO，S3真实语义BLOCKED（额度未追加），打包安全存储已用虚构密钥验收通过，不能整体勾选S2/S3 PASS。

接下来做什么、由谁做、是否需要我操作：开发负责剩余S2与真实复验；用户仅在真实核验打开指定自用聊天和明确有限AI额度，系统实际要求时再授权，不负责运行测试。无后台持续任务。

## 13. 2026-09-17 文字修订与8.11

最新文字优先：聊天、资料核对、操作记录与短“翻译”按钮同一行，16px；译文开关和时间范围下一行；译文去前缀。回复这条有选中、引用和焦点反馈，不立即请求AI。文本框随内容和宽度增高或收缩，聊天可拖动高度并按单保存。

新增译文期限、图片缓存容量限制、英文中文对照、按意图起草、可编辑流程与完成条件及批准范例。独立历史查看、TXT导入与本单提取范围分离；自动AI默认关闭，自动处理须明确日常额度。自动发送继续排除。

以ASSISTANT-8.11-ACCEPTANCE.md和最终ZIP证据为准。网页无法证明账号全部历史；其他附件、原图及真实AI和聊天语义不宣称PASS。

最终本地验收通过：281单元、项目检查、18源版Electron、最终ZIP22项中的20项，以及XLSX/XLS/CSV实际导入；三尺寸截图已实际打开查看，迁移22和全部新回复/历史/流程状态备份恢复通过。最终包另2项安全设置测试为macOS钥匙串授权等待，保留原失败断言，状态WAIT-HUMAN。NEW-01/02/06与虚构NEW-07本地通过；NEW-03本地通过/安全设置待授权；NEW-04历史与媒体仅本地通过/全部历史覆盖未证明；NEW-05真实语义仍BLOCKED。整体MIXED，不将S2/S3剩余清单勾为完成。

接下来做什么、由谁做、是否需要我操作：开发已完成本地复验、打包和独立提交，用户无需跑测试。后续仅用户处理系统钥匙串授权、明确有限AI预算和指定聊天范围，开发负责真实复验，无Codex后台持续任务。


## 14. 2026-09-17：8.12最新讨论增量验收

承接8.11，不重复开发。最新文字覆盖旧布局：聊天/资料核对/操作记录/译文开关/短翻译全部同一行，14px匹配客户姓名标签；时间/历史移入聊天关联菜单，保留范围和覆盖保护。移除常驻本单聊天、复杂核对条与推进卡，客户卡入口打开宽补充与核对浮层，更新同一订单。小圈说明浮动显示，保存✓并入标题，普通通知6秒消失，错误不隐藏；底部四操作同一行。聊单与正常订单共享客户/概览表头/报单/利润/回款/记录及地图结构，缺失输入、已有明确编辑，保留确认依据、邮箱完整性、NULL成本与报单规则。

实际结果：281/281单元、项目检查PASS、源版21/21 Electron，最终浮层与通知补验1/1；最终ZIP新解压25项23通过2系统钥匙串等待，0跳过。XLSX/XLS/CSV最终App实际导入各1/1，迁移17→22含迁移20回复/双语/历史/流程备份恢复通过；三尺寸和核对浮层截图已实际查看。全部为隔离虚构数据，真实AI/聊天读取/发送0。证据与ZIP哈希以ASSISTANT-8.12-ACCEPTANCE.md及artifacts/assistant-polish-8.12/final-verified/为准，旧报告保留。

NEW-01/02/06及虚构NEW-07 PASS-LOCAL；NEW-03本地PASS/打包密钥WAIT-HUMAN；NEW-04本地历史/媒体PASS但账号全历史覆盖未证明；NEW-05真实语义BLOCKED（未追加额度）。本阶段MIXED，不宣称S2/S3整体或全项目完工，自动发送排除。

接下来做什么、由谁做、是否需要我操作：开发完成本地复验、ZIP和独立本地提交交付；用户只需解压打开App，无需运行测试。安全设置待用户处理系统钥匙串授权；真实联合验收另需明确有限AI额度和指定聊天范围，开发负责后续复验。原HEAD、索引原字节、200份原快照与无关修改保留，不推送，不启动Codex后台持续任务。


## 2026-09-17：8.13八阶段提醒增量交接

承接8.12，默认八阶段、顶部当前/下一步、事实跳过、配送异常与结束/恢复、七天收货提醒、人工纠正、关联关闭与三尺寸视口滚动修复；迁移23及独立阶段记录备份恢复。292单元/项目检查通过；最终27/27通过，0跳过；本地安全设置为虚构密钥验证，实际账号与真实AI语义仍待条件。 XLSX/XLS/CSV最终App各1/1，17→23回复/双语/历史/阶段恢复及生产隔离启动通过。最终报告ASSISTANT-8.13-ACCEPTANCE.md与artifacts/assistant-stages-8.13/final-verified/为准。

NEW-01/02/06与虚构NEW-07 PASS-LOCAL；NEW-03本地通过、虚构密钥通过；NEW-04真实全历史覆盖未证明；NEW-05真实语义BLOCKED（无新增额度）。阶段关键词是人工核对线索，不自动确认、签收或发送；桃园/回执不纳入。S2/S3剩余项目不标整体完成，工作区原HEAD/index及既有修改保留，不推送。


## 2026-09-17：8.14聊天刷新与目标文字增量交接

承接8.13：阶段目标按缺失项显示、关联电话号码、14px/12px间距/浅色选项；刷新统一最新和历史分批获取，停止、继续更早、去重分页、失败保留、切单保护与阅读位置。299单元及项目检查PASS；最终全部29/29通过，包括虚构密钥加密保存、齿轮与独立设置回归。 最终App三格式导入各1/1、17→23历史范围/锚点/同步进度/双语/回复/阶段备份恢复通过。

NEW-01/02/06与虚构NEW-07 PASS-LOCAL；NEW-03本地通过，虚构密钥通过；NEW-04本地历史刷新和分页增强PASS-LOCAL，真实从最早到最新覆盖未证明；NEW-05真实语义BLOCKED（无追加额度）。页面停止或文件完整不当作账号全部历史；无新增真实聊天读取/AI请求/发送。S2/S3不整体勾完，报告ASSISTANT-8.14-ACCEPTANCE.md及final-verified证据为准。原HEAD、暂存内容、无关源文件与旧ZIP保留；临时索引本地提交，不推送。


## 2026-09-17：8.15本地在线同步与关闭入口增量交接

承接8.14：设置×/Esc/外部/齿轮关闭、流程×与放弃保护、阶段细箭头；主进程明确范围后每15秒同步当前已核对匹配聊天，全部管理页关闭仍运行，退出重开补取；去重、失败保留、缺口与绑定保护、同客多单归属不明只归档/人工关联、独立草稿和阅读位置。迁移24与授权原始行备份恢复，固定范围旧授权不生效。308单元/检查PASS；最终新解压32项30PASS、2FAIL、0跳过；2项密钥测试因macOS钥匙串等待授权，集中复验仍0/2，保留原断言和FAIL，交付WAIT-HUMAN，3格式导入各1/1、17→24恢复与生产隔离启动通过，三个尺寸截图实际查看。

NEW-01/02/06与虚构NEW-07 PASS-LOCAL；NEW-03关闭入口PASS-LOCAL，密钥安全设置2项WAIT-HUMAN；NEW-04本地在线增强PASS-LOCAL，真实新消息延迟、离线完整覆盖和全部联系人/媒体待登录及范围，不能说全历史全类型通过；NEW-05真实语义BLOCKED（无额外额度）。真实聊天/AI/发送0，自动发送、云托管、退出后运行不纳入；S2/S3不整体勾完。ASSISTANT-8.15-ACCEPTANCE.md及final-verified为证据，旧报告与8.13/8.14 ZIP哈希保留；8.11/8.12 ZIP最终目录中不存在，原因未核实。原HEAD、暂存内容和无关修改保留，临时索引独立本地提交不推送。

接下来做什么、由谁做、是否需要我操作：用户解压打开，真实联验需用户扫码并明确读取范围，密钥设置需用户完成macOS钥匙串系统授权，开发集中复验，用户不负责调试。


## 2026-09-17：8.16取消自动读取、首次关联补取与人工刷新

按最新用户决定停用8.15后台在线及助手定时读取，旧开关重启/恢复不生效且不能开启；首次明确确认关联后分批读取可获取历史（180秒/500批、有进度和停止），之后只人工刷新，不自动打开WhatsApp。修复逐批完整核对重选聊天打断历史加载：开始时一次核对，期间原生聊天标识/页面变化保护，慢加载等待，缺口不报全历史。归档、回复、迁移24原始授权行和无关修改保留。314单元/检查PASS；最终ZIP首轮29/30，旧关联提示文字更新后原业务断言及两项人工流程复验3/3，31个不同案例均PASS；三格式导入、17→24备份恢复、生产隔离启动和三尺寸查看通过。

NEW-04改为人工历史PASS-LOCAL，真实覆盖/全部媒体待登录与确认范围；NEW-03密钥两项已知钥匙串关口，本轮不重复触发、8.16未验收WAIT-HUMAN；真实AI无额外额度NEW-05 BLOCKED。真实聊天/AI/发送0，S2/S3不整体勾完。见ASSISTANT-8.16-ACCEPTANCE.md和final-verified；本地临时索引提交，不推送，原HEAD/暂存和无关修改保留。

接下来做什么、由谁做、是否需要我操作：用户退出旧版打开8.16，确认聊天读取范围并按需扫码；开发集中联验与系统授权后复验，用户不负责调试。


## 2026-09-17：8.17一次历史补取与界面增量验收

承接8.16：一次人工操作持续读取网页可获取历史，重新遍历防位置复位缺页，慢加载/虚拟列表重读、原生目标校验及媒体占位；30分钟/10000批保护，逐批保存、停止与人工恢复，覆盖未确认不报全账号读完。阶段显示最近完成✓/当前/下一步，窄时折叠后续；客户确认紧凑入口及依据弹窗，设置按钮分离；修复本地网页新增模块漏供与关联浮层遮挡按钮。自动读取仍停用。

最终319单元/检查PASS；第一个ZIP回归29/37、8FAIL保留，修复后重新构建新解压首轮36/37，客户确认✓符号计数适配及滚动按钮复验2/2，37个不同案例均PASS、0跳过；三格式实际虚构导入、17→24备份恢复、生产隔离启动和三个尺寸查看通过。原业务断言保留。NEW-01/02/06及虚构NEW-07 PASS-LOCAL；NEW-03真实密钥两项WAIT-HUMAN，本轮未运行；NEW-04本地历史PASS-LOCAL，真实全历史待具体聊天/范围与登录，网页未提供历史不可保证；NEW-05真实AI无追加额度BLOCKED。真实聊天/AI/发送0，S2/S3不整体勾完。见ASSISTANT-8.17-ACCEPTANCE.md及final-verified证据。224个原文件快照、原HEAD/暂存及无关修改保留，独立本地临时索引提交，不推送。

接下来做什么、由谁做、是否需要我操作：用户退出旧版打开8.17，确认具体历史读取范围并按需扫码；开发集中只读联验，密钥系统授权后复验，用户不负责测试或调试。


## 2026-09-18：8.18结构化历史与附件阶段验收

承接8.17，不重做已有界面和业务。接入已选客户原生消息模型，一次人工操作持续读取网页可获取历史；账号/聊天/原生ID校验、慢加载与停止续取、等待占位真实恢复、图片附件缓存和预览、旧缩略图升级、同步期间AI上下文失效及并发保护。只首次明确关联及人工刷新读取，自动读取保持关闭；网页未提供/未解密的历史不宣称完整。

最终328/328单元及检查、最终ZIP新解压副本42/42 Electron/Chrome回归（0跳过）、XLSX/XLS/CSV各1/1实际虚构导入、迁移17→24（含20）草稿/占位/附件备份恢复与重启、三尺寸查看全部通过。外部连接/AI均为虚构模型，真实聊天读取/真实AI/发送/正式写入0。NEW-01/02/06及虚构NEW-07 PASS-LOCAL；NEW-03本地入口和授权等待隔离通过，真实密钥2项WAIT-HUMAN；NEW-04结构化历史与图片PASS-LOCAL，集成版真实读取WAIT-HUMAN（尾号4771历史图片授权已明确，手机不在无法扫码）；NEW-05无额外AI额度BLOCKED。S2/S3尚未全部完成。

见ASSISTANT-8.18-ACCEPTANCE.md和artifacts/assistant-structured-8.18/final-release。8.18.0版本同步、macOS x64 ZIP直接双击App；107程序文件与最终包一致、签名及启动通过。229原文件快照、原HEAD/暂存内容/无关修改及8.17 ZIP保留，临时索引独立本地任务分支提交，不推送。

接下来做什么、由谁做、是否需要我操作：本地阶段已完成，用户可解压使用；开发负责后续真实联验，手机在手时用户扫码，密钥测试按需完成macOS钥匙串授权，用户无需调试或反复确认。


## 2026-09-18：8.19真实扫码读取阶段

承接8.18，修复真实WhatsApp短消息ID和消息加载顶点核验，号码与每条短ID原生归属交叉验证，不降低身份/并发保护。最终328单元/检查、46回归、三格式导入、迁移20备份恢复和三尺寸通过。尾号4771私有隔离实际界面35条（24文字、6等待、5图片）、分页、两个人工读取按钮、零重载/缓存复用、35/2条两单范围/草稿、退出/重启、真实缓存私有备份恢复及免二次扫码重连通过。正文/图片/凭据仅私有目录，真实AI/发送/正式写入0。

NEW-01/02/06及虚构NEW-07 PASS-LOCAL；NEW-04指定网页可获取历史/图片PASS-REAL-READABLE，6未解密/手机全历史/其他联系人媒体/长期稳定性仍未验收；NEW-03真实密钥2项需钥匙串，不是扫码条件；NEW-05无额外额度BLOCKED，S2/S3不整体完成。见ASSISTANT-8.19-ACCEPTANCE.md及artifacts/assistant-real-8.19/final-release。235基线、原HEAD/暂存字节、无关修改和8.18 ZIP保留，独立临时索引任务提交不推送。

接下来做什么、由谁做、是否需要我操作：扫码验收完成，用户可打开8.19并在常用资料目录登录/关联；开发负责S2/S3，真实AI需额外额度、钥匙串按需授权，当前不用再确认或调试。

## 2026-09-18：8.20事件式自动接收阶段

最新阶段：8.20本机自动消息接入及最终App本地验收通过；真实扫码、36条/5图及三次重启通过，候选App实际两小时观察和最终准确ZIP三次真实重启通过；中途自动重连约4分钟。真实新消息仍等待产生。见ASSISTANT-8.20-ACCEPTANCE.md。

NEW-01/02/06和虚构NEW-07继续PASS-LOCAL，不重复实现既有界面。NEW-03本地设置及等待授权不阻塞订单通过，真实加密密钥保存仍需macOS钥匙串；NEW-04新增whatsapp-web.js＋LocalAuth最新消息事件接入、受限补漏、PN/LID去重、同客多单待关联、图片单独重试和网页关闭仍接收：PASS-FICTIONAL。指定4771真实可获取文字图片入库显示及最终App三次登录恢复PASS-REAL，实时新消息与连续运行记录见新报告，不能用模拟代替。NEW-05真实AI无追加额度，不发起新请求，S2/S3全项目不整体勾完。

最终345/345单元与check通过；最终ZIP新解压20个不同案例19PASS、1钥匙串BLOCKED，0已知代码FAIL；XLSX/XLS/CSV实际虚构导入、17→24含20备份恢复和原生图片草稿保留通过，登录目录不进入订单备份。原工作区基线、原HEAD和暂存字节保持，独立临时索引本地任务提交，不推送。

接下来做什么、由谁做、是否需要我操作：开发已记录实际连续运行结果并完成最终ZIP复验，负责本地提交和交付；用户在常用资料目录首次扫码，真实新消息若仍待验收仅需手动产生测试文字/图片，钥匙串授权仅在需要AI密钥时处理，其他调试由开发完成。
