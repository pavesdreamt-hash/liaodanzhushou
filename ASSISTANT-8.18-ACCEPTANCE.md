# 8.18 阶段验收与交付（开发2026-09-17，验收2026-09-18）

当前状态：本地328/328单元、最终ZIP 42/42回归、三格式实际导入、备份恢复及三尺寸界面均PASS-LOCAL。真实新版连接复验 WAIT-HUMAN。S2/S3 尚未全部完成。

## 本轮增量

承接已完成的 8.17，不重做界面与订单服务。本轮将历史读取接入现有专用 Chrome 登录窗口的当前客户消息模型，按原生 ID、秒级时间、方向与正文逐批归档。不会枚举其他客户、监听全部账号、自动刷新、发送或粘贴。

- 首次明确关联后一次补取网页可获取记录；以后只人工刷新。慢加载等待，缺口只请求已选客户一次；保留停止、成功批次及人工继续。
- 保存前再次核验账号。切换账号或聊天、网页结构无法适配、超时、原生标识无效均停止；不猜时间，不跳过校验凑成功。
- 等待解密保留明确占位；只有平台真正提供对应原生正文时才补齐同一记录。引用仍不完整时正文可以显示，AI 上下文继续暂停。已有正文发生编辑、方向或分钟变化仍报冲突。
- 图片附件最大1MB（PNG/JPEG/WebP），保留附件来源、可点击预览、去重缓存、重启及备份恢复。旧缩略图可升级成附件；图片缓存总量100MB，默认30天，可在流程与缓存设置调整。
- 同步开始使旧 AI 上下文和在途结果失效。同步中禁止导入或应用半成品；停止、失败或尚未覆盖本单起点时，记录可查看、人工草稿保留，依赖完整上下文的提取和拟回复暂停。
- 顶部区分“内容待核对”与“更新失败”，避免将图片语义待核对误报为读取失败。
- 原有统一聊单/正常订单结构及地图、同排14px工具栏、阶段完成✓/当前/下一步与折叠、设置/关联关闭、浮动帮助、自动高度、资料补充弹窗、每单独立回复、缓存和剪贴板继续保留。

## 交付

ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.18.0-x64.zip`

SHA256：`75ec7b6d5760468660faf598419c3ac1f853c5e9de9b5e8cf0ae1f6b2d9f50b6`

大小：151486651字节；macOS x64；新解压 App 签名验证、显示元数据8.18.0及107个程序文件逐字节一致。

先从菜单退出旧版，双击 ZIP 解压，进入文件夹，直接双击 **KDocs订单助手 8.18.0.app**。无需.command，不覆盖已安装App。若macOS阻止，Finder右键App→打开；无需关闭系统安全设置。

## 实际证据

| 检查 | 结果及范围 |
|---|---|
| npm test / npm run check / npm run dist | 冻结后的源码328/328单元、0跳过；共享模块和项目检查通过，当前ZIP已生成。见 assistant-structured-8.18-build-release.log。 |
| 最终 ZIP 回归 | 42/42通过，0失败、0跳过；实际Electron/Chrome，外部连接和AI为虚构模型。见assistant-structured-8.18-final-release-tests.log和final-release/overall-result.json。 |
| 新结构化浏览器测试 | 虚构消息模型，实际Chrome操作：60页242条、无DOM时间前缀仍读取、原生占位恢复、图片字节读取及刷新复用、慢到达补取、停止、账号切换前后拒绝保存。非真实WhatsApp。 |
| Electron 增量界面 | 实际App UI，虚构连接：占位可见、正文转义、人工刷新原生补齐、图片预览、关闭重开、重启缓存和两单独立回复。 |
| 三格式实际导入 | 最终新解压App的.xlsx/.xls/.csv各1/1通过，虚构1单1商品，实际文件选择/预览/NULL成本确认/导入/重启及重复保护。 |
| 打包备份恢复 | PASS。迁移17→24（含20回复工作区）后等待占位、附件来源与缓存、独立草稿、双语缓存、阶段、历史锚点逐字段恢复和重启断言。 |
| 三尺寸截图 | PASS-LOCAL。最终ZIP三尺寸聊天和图片预览4张已实际查看；阶段/设置6张来自前次同版本包，分列于visual-review.json。几何和操作断言另行通过。 |
| 工作区保护 | 原HEAD c511f84与原暂存区字节保持；229个开工原文件快照保留，8.17 ZIP哈希保持。不覆盖正式库或已安装App，不推送。临时索引、前次任务提交为父提交和独立本地分支；准确提交见final-release/git-delivery.json。 |

证据目录：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/artifacts/assistant-structured-8.18/final-release`。本轮应用集成及最终验收使用隔离虚构数据，真实聊天读取0、真实AI请求0、发送0、自动粘贴0、正式订单/库存/Google Sheets写入0。

## 可用与限制

可直接使用的本地功能：独立手动回复、保存/复制、消息显示与归档分页搜索、资料编辑和核对、订单结构/地图、阶段提醒、TXT、备份与三格式导入。缓存译文会复用；未配置或无额度的AI按钮不会制造输出。

仅虚构验证的新功能：本次接入的原生历史遍历、等待正文恢复、图片附件同步与并发保护。指定客户历史及图片的只读授权已明确，不需要再次确认范围；但当前没有装WhatsApp的手机，无法扫码完成新版本真实连接复验。以前的隔离原理验证不能替代这次App实测。

网页耗尽与手机账号完整历史是不同结论。本软件只报告网页当前可获取记录；已删除、未同步或尚未解密的正文无法凭空补齐。WhatsApp网页内部模块改变时会明确报适配失败，保留已存批次，不把空结果报告为全历史成功。

图片可查看仍不等于图片语义理解。引用详情、非文字完整理解、真实AI语义和真实长期业务联验属于未完成S2/S3；额外AI额度未授权，不能新增真实请求。自动回复/发送暂不实施，后台自动读取保持关闭。

NEW-01/02/06及虚构NEW-07：本地能力PASS-LOCAL；NEW-03：关闭入口和等待授权不阻塞订单操作可本地验收，真实密钥两项仍需macOS钥匙串授权，本轮不重复触发，不标PASS；NEW-04：结构化读取本地验收，真实新版全历史/媒体尚未通过；NEW-05：真实AI语义BLOCKED，无额外额度。

接下来做什么、由谁做、是否需要我操作：本地开发、验收与安装包已完成，用户可解压使用；后续真实连接联验由开发负责，手机在手时用户扫码，密钥测试按需完成钥匙串授权。当前无需中间确认，用户无需运行测试或调试。
