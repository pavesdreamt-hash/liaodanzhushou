# 8.17 历史一次补取与界面修复验收

2026-09-17。增量承接8.16及现有未提交工作，状态 MIXED / PASS-LOCAL。不宣称真实账号全部历史或整个项目完工。

## 已实现及操作

- 首次明确关联并同意历史读取时，在一次操作内持续遍历网页可获取记录；之后只人工“刷新聊天”或“继续补齐更早记录”。不后台自动刷新，不反复打开/重载WhatsApp，不自动重试真实AI。
- 取消原45秒/60批人工补取暂停；首次和人工补取均保留30分钟/10000批保护上限，逐批落库、显示进度、可停止。重新核对导致页面位置复位时重新从最新遍历，按原生ID去重，避免跳过中间记录。成功批次和历史锚点保留，失败可人工继续。
- 对慢加载重触发并等待真实列表变化；空列表先等重新加载，再通过原严格目标校验才读正文。瞬时虚拟列表变化限定重读同批，不保存残缺批次。开始时完整账号/联系人核对一次，之后锁定页面、标题及原生远端标识；核对和建锁之间同名换聊天也会在读正文前拒绝。
- 非文字历史提供明确占位；引用/转发/编辑/图片等不再让相邻文本整批无法归档。不能完整解释的消息标记说明，属于本单的不完整上下文阻止AI拟回复/提取，变化使旧结果失效；不会冒充读懂媒体。已存正文发生冲突仍拒绝覆盖，未知时间仍拒绝猜测。
- 顶部显示最近已完成阶段✓、当前及下一阶段；窄空间保留已完成和当前，后续折叠为“…”入口，全流程8阶段仍可查看。原编号推进，字号14px，阶段目标沿用配置。
- 客户确认改为紧凑“待确认/已确认✓＋登记确认/查看依据”入口，依据和来源在可关闭弹窗中填写。不会从线索自动登记确认；草稿转换必须明确“确认并进入履约”，资料缺失及保存失败保留输入，两单隔离。
- 设置快捷按钮有12px间隔；设置×/Esc/外部关闭沿用。关联浮层定位避开原按钮，并保留原按钮可再次关闭。修复本地管理页漏供新增模块导致订单页面打不开的问题。
- 之前完成的统一聊单/正常订单结构及地图、电话身份、同排聊天/资料核对/操作记录/翻译、气泡和浅色底纹、中文对照、输入框自动高度、消息引用、资料补充入口、提示图标、独立回复与翻译缓存等继续保留，相关原业务断言复验通过；未重复从头实现。

先在菜单栏退出旧版App。双击ZIP解压，进入文件夹，直接双击“**KDocs订单助手 8.17.0.app**”，无需.command；不覆盖已安装App。首次被macOS阻止可在Finder右键App→打开，不关闭系统安全功能。关联时先人工打开专用WhatsApp网页，按需扫码并确认聊天号码和历史读取范围。

ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.17.0-x64.zip`

SHA256：`3c02d805570ac9b3fa58e26ae37b29abf4a9762dbd346d5f88875c59598ad508`

## 实际证据

| 项目 | 结果与证据 |
|---|---|
| 单元及检查 | 最终修复后319/319，0跳过；npm run check PASS。unit-final-repaired、check-final-repaired日志。沙箱内本机OAuth虚构监听EPERM失败保留，沙箱外全量通过，不当作业务失败。 |
| 历史真实DOM操作 | 一个操作72页216条，六秒空列表停顿仍继续；身份核对仅一次；再次核对复位后重新遍历增加到219并去重；取消、原锚点、同名目标切换、虚拟行变化、媒体占位、默认严格范围读取原保护通过。history-once-browser-result.json及最终包日志。虚构网页DOM，不是真实WhatsApp历史。 |
| 首次关联与人工读取 | 实际Electron点击关联、读取、停止与人工补齐；人工新消息、关闭重开、重启均无后台读取，两单草稿独立。manual-history-result.json、manual-first-stop-result.json。 |
| 最终ZIP全量 | 第一个包37项29PASS/8FAIL，保留原日志；其中两个为测试资源/末尾状态事件适配，四个为本地网页漏模块，两项为浮层挡住关闭按钮。修复应用和测试等待后重新构建ZIP并新解压，首轮36/37，仅余旧测试漏计新的客户确认✓符号；保留七项确认检查适配符号，并增强小窗口按钮滚动断言，同包复验2/2。合并37个不同案例均PASS、0跳过，不把首轮冒称全绿。zip-repaired、ui-followup日志及overall-result.json。 |
| 原业务流程 | 新建、关联、读取、资料差异/地址冲突、客户确认、分包、TXT、重启、同客第二单；正文转义、分页/引用、输入保存/剪贴板/失败/并发/退出；阶段候选依据、跳过已完成、跟进/费用/利润等原回归通过。workspace、stages等JSON。 |
| 三格式导入 | 最终App实际.xlsx/.xls/.csv各1/1，虚构1单1商品、NULL成本、重复保护和重启通过。import-results.json和三份import日志。 |
| 打包备份恢复 | 填充迁移17→24，含迁移20回复工作区及消息、缓存、历史范围/锚点、阶段和旧授权原始行逐字段保留，恢复重启通过。packaged-assistant-backup.json。 |
| 生产启动 | 新解压App正常生产模式＋隔离虚构数据，管理页显示8.17/8.17.0，手动草稿与流程重启保留，无密钥AI明确禁用。本地浏览器工作区和退出前保存通过。production-assistant-result.json。 |
| 三尺寸实际查看 | 900×700、1280×850、1440×900阶段与设置截图逐张查看，已完成✓/当前保留、折叠入口、关闭×、按钮间隔和依据弹窗可用，无横向溢出。最终截图定位修正以避免测试把标题滚到固定页头下；visual-review.json。浮层打开时遮住部分下层内容属于正常表现。 |
| ZIP一致性 | 新解压签名/架构/元数据通过，107个程序及主文档逐字节一致；8.13～8.16旧ZIP哈希保持，不覆盖已安装App。source-match.json、final-integrity.json。 |

证据目录：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/artifacts/assistant-history-once-8.17/final-verified`；全部使用虚构临时资料。真实旧聊天读取0、真实AI请求0、发送/粘贴0，正式订单/库存/Google Sheets写入0。

自动审批曾拒绝“允许空消息列表通过目标校验”的修改，理由是削弱聊天目标保护；该修改未执行。采用先等消息重载、再通过原严格校验的方案，另增加原生远端绑定保护；复验通过，无需额外授权。

## 可用、待真实核验与尚未完成

可直接使用：人工关联及一次持续历史补取、人工刷新、可读历史归档/分页/搜索、独立草稿、紧凑阶段与客户确认、停止/失败恢复、备份、表格导入、已有订单编辑/地图/TXT。部分非文字只能占位或缩略图，回原WhatsApp查看，不等于完整媒体理解。

仅虚构验证：历史长页补取覆盖、真实连接和AI网关替身流程。**网页可获取历史一次补取已实现；真实账号全部历史不能保证，也尚未完成真实覆盖验收。** WhatsApp未向网页提供、已删除、无法解密、未知时间及正文冲突的记录不能凭空恢复；达到网页可见顶部或保护上限只报告覆盖未确认。若实际聊天仍不完整，由开发检查读取结果，不把反复点击或调试交给用户。

真实账号验收缺少用户确认的具体聊天及历史起止范围、有效登录；此前仅授权20条最新虚构消息，不能扩大为自动读取真实旧历史。需要用户集中确认后由开发只读联验。两项密钥加密保存回归仍需macOS钥匙串系统授权，8.17未运行、不是PASS；等待授权不阻塞订单操作的独立测试已通过。真实AI额外额度未授权，本次不调用。

NEW-01/02/06及虚构NEW-07 PASS-LOCAL；NEW-03关闭入口与虚构配置通过、真实密钥加密保存WAIT-HUMAN；NEW-04人工历史增强PASS-LOCAL、真实全覆盖待范围/登录；NEW-05真实AI语义BLOCKED（无追加额度）。S2/S3真实语义、完整媒体与长期真实业务联验仍未完成，不宣称整个项目完工。自动回复/发送暂不实施；后台自动同步按最新决定保持停用。

本地提交使用独立分支和临时索引，只纳入本任务改动及证据，保留原HEAD、暂存内容/标记/字节、224个开工原文件快照及无关修改，不推送。提交与保护证明见git-delivery.json。

接下来做什么、由谁做、是否需要我操作：用户退出旧版、解压打开8.17；真实历史验收需用户确认具体聊天与读取范围并按需扫码，系统密钥复验需用户完成macOS授权。开发负责集中只读联验及修复，用户不用运行测试或调试。
