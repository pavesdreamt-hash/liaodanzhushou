# 聊单助手 1.8.6 消息悬浮操作验收

## 本次修复目标

- 消息悬停或键盘聚焦时，在气泡侧边横向显示“翻译、话术、识图、回复”。
- 翻译只处理当前消息；话术复用现有选择器；识图只处理已显示图片；回复保留 WhatsApp 原消息引用关系。
- 未触发时不改变消息上下间距、输入区、订单栏、导航或其他页面。

## 真实验收内容

- 在隔离 Electron 操作窗口加载虚构收发消息和 1024×1024 本机图片，逐项点击四个按钮。
- 实测收到消息的操作组位于右侧，发出消息位于左侧；按钮组与气泡中心纵向偏差小于 3 px，完整处于聊天列内。
- 实测翻译请求只含当前消息 ID；快捷话术弹层可打开；图片文字 `ORDER KY40 / AED 250.00` 显示在原消息；回复发送载荷携带被引用消息 ID，成功后引用提示消失。
- 亲自查看截图：`artifacts/message-actions-1.8.6/hover-actions.png`。
- 20 项人工发送、引用回复、翻译和图片输入单元测试通过。消息悬浮操作、图片可见读取、当前视口翻译、现有发送、系统通知、筛选徽标和新消息提示的隔离 Electron 回归分别通过。

## 未完成的端到端边界

- 为避免给真实客户产生测试消息，本轮未在真实 WhatsApp 会话执行引用发送；真实发送底层仍会在发送前核对当前账号、会话及被引用消息。
- 图片识别使用隔离模型响应完成界面和载荷验收，没有消耗真实 AI 请求。实际使用要求“连接与设置”中的当前模型支持图片输入；不支持时会在原消息显示准确失败原因，可更换支持图片的模型后重试。
- 旧手机页 P1/P2 和整页其他未涉及基准未重验。

## 核验文档和规则

- `CONFIRMED-UI-BASELINE.md`：D-101。
- `AGENTS.md`：变更边界、真实核验、版本同步和直接可用交付规则。
- 根目录 `AGENTS.md`：直接可用交付规则。
- 本轮不制作安装包，不提交 Git，直接启动本地 1.8.6 操作窗口。
