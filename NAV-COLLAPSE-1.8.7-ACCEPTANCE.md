# 1.8.7 导航折叠验收

## 本次修复目标

- 给订单详情页补齐左侧导航的折叠／展开按钮。
- 折叠后保留可操作的展开按钮，侧栏宽度为 64px。
- 与工作台及其他业务页共享既有本机折叠状态。
- 检查全部可进入页面是否还有同类遗漏，不修改订单、聊天和其他业务内容。

## 问题定位

普通业务页、商品详情和利润详情通过 `installUnifiedLayout` 装配统一按钮；聊单工作台使用自己的同名状态；订单详情单独使用 `installOrderLayout`，此前只装配宽度拖动，没有装配折叠按钮、共享状态及统一版本尾部。这是订单详情缺少按钮的直接原因。

逐页核对时还发现工作台虽然能按共享状态折叠，但首次进入时按钮仍沿用 HTML 初始的“折叠导航”文字和方向。本次只同步这项按钮状态，不改工作台其他元素。

## 真实验收内容

最终隔离 Electron 验收使用临时资料目录和虚构订单／商品／成本记录，全程没有读取或修改用户真实资料：

1. 从订单管理实际打开订单详情，确认展开态只有一个“折叠导航栏”按钮。
2. 点击后确认订单详情进入折叠态，侧栏实测 64px，按钮变为“展开导航栏”且仍可见。
3. 确认订单详情侧栏底部显示 `v1.8.7`，旧 `od-connection` 尾部为 0 个。
4. 依次实际进入聊单工作台、订单管理、商品库存、商品详情、利润核算、利润详情、助手配置和连接与设置；连同订单详情共九页，每页都只有一个可见的展开入口，并保持同一折叠状态。
5. 人工查看截图：
   - `artifacts/navigation-collapse-1.8.7/order-detail-expanded.png`
   - `artifacts/navigation-collapse-1.8.7/order-detail-collapsed.png`

最终结果：

- `npm run check`：通过，包含 TypeScript、UI 构建、共享模块与项目检查。
- `node --test --test-timeout=60000 test/navigation-collapse-audit.e2e.mjs`：1/1 通过。
- `git diff --check`：通过。
- 正式本地 `npm run start`：1.8.7 构建完成，主窗口已显示并获得焦点，原 WhatsApp 登录恢复且自动消息连接在线。

曾尝试运行两个较早的相邻 Electron 测试文件；它们在自身 30／60 秒上限后没有正常结束清理，未计入通过项。它们覆盖的导航状态、工作台初始按钮和五个普通业务页已由本次范围更完整的九页验收实际覆盖。

## 核验文档和规则

- `AGENTS.md`：直接可用交付规则。
- `CONFIRMED-UI-BASELINE.md`：D-069 全站统一导航基准、D-102 本次范围及实施核验。
- `USER-GUIDE.md`：1.8.7 本地使用说明。
- `RELEASE-NOTES.md`：1.8.7 变更说明。

## 未纳入本次结论

- 没有重验旧手机样式 P1/P2。
- 没有重验九页各自的完整业务流程或真实客户资料。
- 没有制作安装包，没有提交 Git。
