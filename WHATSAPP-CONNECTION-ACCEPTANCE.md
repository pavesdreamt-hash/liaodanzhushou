# 阶段 1：指定 WhatsApp 新消息连接核验

核验日期：2026-09-13；应用版本仍为 8.6.0。本轮只完成连接预检和已读结果的本地验证，没有修改应用代码或重新打包。

**限定范围的真实连接预检通过：已从用户确认的自用聊天读取本轮两条新虚构消息；此前 DeepSeek 真实最小样本请求通过。8.6 App 内的 WhatsApp 读取接入和完整 AI 补全尚未完成，不能将本报告作为 NEW-01～07 总体 PASS。**

## 已实际完成

| 核验 | 结果和边界 | 证据 |
|---|---|---|
| 网页登录及身份 | 个人资料电话号码与目标联系人电话号码分别核验，确认是不同账号；使用应用专用 Chrome profile；未按显示姓名绑定 | `artifacts/assistant-settings-8.6/whatsapp-identity-verification.json` |
| 浏览器沙箱 | 当前专用 Chrome 的实际参数不含 `--no-sandbox` / `--disable-sandbox`；已有登录保留 | `artifacts/assistant-settings-8.6/whatsapp-sandbox.json` |
| 真实读取 | 只定位本轮两个明确标记的虚构文本，数量 2/20；不读取旧消息和其他聊天正文 | `artifacts/assistant-settings-8.6/whatsapp-real-message-verification.json` |
| 消息依据 | 各有 WhatsApp 实际 DOM 提供的不同消息 ID；客户/商家方向由本次气泡的 `tail-in` / `tail-out` 判断；来源时间为 11:33、11:35，时区 Asia/Shanghai、精度为分钟 | 同上；公开证据只留消息 ID 和正文的 SHA-256，不保存正文 |
| 刷新稳定性 | 先确认输入框没有未发送草稿，再刷新页面、重新打开同一指定聊天；两条 ID、正文、方向和时间均一致 | 同上 |
| 实际结果重复入库 | 将刷新前后的实际虚构消息结果回放到临时订单服务；重复读取后仍只有 2 条，本单没有凭空产生客户资料、商品、包裹或金额 | `artifacts/assistant-settings-8.6/whatsapp-real-payload-ingestion-verification.json` |
| 边界和持久化 | 未关联、错误账号/聊天、范围外时间均拒绝；关联变更后的旧请求拒绝；第二单不串消息；临时数据库重启后消息和关联一致 | 同上；测试结束删除临时库，不使用正式订单库 |
| DeepSeek | 8.6 公共设置状态显示已安全保存密钥；deepseek-flash 的真实固定虚构样本校验通过，耗时 1386ms、累计 1/5 次 | `artifacts/assistant-settings-8.6/live-connection-verification.json`；请求发生于此前 10:23，本轮没有新增付费调用 |

以上重复入库等检查是“真实读取结果回放进订单服务”，没有把这个回放适配器称为 App 内的实时连接。两条 WhatsApp 消息本轮没有交给真实 AI，因此没有宣称真实聊天→AI→订单的联合链路通过。未发送任何 WhatsApp 消息、未访问正式订单数据库、未运行真实库存同步或 Google 写入。

## 尚待开发和验收

1. 将浏览器读取能力接入正常 App 的关联、读取、刷新和错误状态。当前浏览器探针与 8.6 订单助手仍是分开的。
2. 当前身份依据是可核对的账号和目标电话号码组合，未取得 WhatsApp 原生聊天 JID；不能把内部哈希标识称为平台原生聊天 ID。下一阶段需明确并验证正式绑定规则。
3. 本次两条消息都有收发气泡尾部标记；没有验证连续同方向、无尾部气泡、编辑消息、媒体和跨页历史的通用读取能力。扩大读取范围需要继续程序验证，不能从两个样本推导全部可用。
4. 本地校验器已复现一个兼容问题：约定样本中的明确 `Full name:` 标签不能通过当前姓名标签匹配。应由开发修复，不能让用户改写正确测试内容迁就实现。
5. 源码检查发现相同消息 ID 使用 `INSERT OR IGNORE`；同一消息被编辑后需要显式冲突处理，现实现尚不满足接入设计要求。本轮未要求用户编辑消息，也未读取编辑内容。
6. 真实 AI 自然语言场景及订单完整流程仍待阶段 2～4 实现、复核；其中询价、未接受报价的当前安全断言仅经过本地校验器测试，不能替代真实模型理解验收。

## 人工关口与下一阶段

此次登录、目标确认和发送两条测试消息的人工动作已经完成；不要求重新填密钥、重新发送或运行常规测试。阶段 0、1 的本轮有限连接目标已有实际证据。后续阶段 2～4 的重点是上述开发缺口与代码问题，而非继续要求用户提供相同配置；本轮按既定范围没有开始阶段 2～6 的应用功能修改。

现有专用 Chrome 窗口保留登录和指定聊天；没有开启后台消息轮询或自动发送。后续只可在已授权的新虚构消息范围内继续验证，不能因此扩大到历史或其他客户聊天。

本轮不生成新的 ZIP，也不把原 8.6 包重新包装成“已完整接通版”。应用内接入完成后，应统一升级、回归、打包并实际启动最终产物，再进入一次集中人工核验。
