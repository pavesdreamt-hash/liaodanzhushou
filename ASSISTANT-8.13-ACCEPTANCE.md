# 8.13 八阶段提醒与关联窗口关闭验收

2026-09-17。承接8.12，按AGENTS.md升次版本至8.13.0。状态：MIXED / PASS-LOCAL，不宣称S2/S3或整个项目完工。

## 本次完成

- 订单助手标题中间显示当前阶段与下一阶段，圆圈编号随设置顺序变化；点击打开完整八阶段、缺失资料、判断依据、聊天线索及提醒记录。保留8.12已完成的同排14px工具栏、双语自适应草稿、浮动说明和聊单共享地图结构。
- 默认阶段：商品意向→收货资料→配送核对→客户确认→报单准备→生成报单→收货跟进→费用结算。按已保存业务事实自动跳过完成阶段；阶段名称、动作、顺序及补充条件可设置，标准阶段必需条件不能被取消放行。原有完全自定义流程、店铺规则、范例和自动处理开关保留，只有未修改的旧默认六阶段升级。
- 配送超范围提示询问替代地址；未回复不自动结束。明确结束和恢复需要本单记录，已有包裹及财务不改写。人工关注只改变提醒，资料改变后失效；价格或收货资料在确认后变化会重新提示客户核对，重新核对依据独立记录。
- 聊天中的明确确认、全部收货关键词显示待核对线索，可定位原消息；问句、否定和泛泛OK不能完成阶段。这里是本地规则，不是新增AI语义判断；不自动确认订单或签收，不自动复制客户旧资料。
- 首次实际保存/下载有效TXT后默认七天内收货跟进，预览不计，重新生成不重置起算。旧版仅有包裹TXT时间的订单也进入提醒，采用已有报单时间；历史首次时间或实际发货时间应核对后人工修正。可记录下次跟进、等待客户和修正起算，必须有效时间与说明。软件打开、进入订单、返回列表或重新读取阶段时更新；退出后没有后台推送。
- 多包裹分别登记配送结果，未全部结束不完成收货阶段；未知成本、运费或未登记回款不会完成结算。沿用现有完整性、成本快照、分包、优惠及TXT校验，不删测试或放宽业务条件。
- 关联聊天窗口有右上角×、再次点击关联、外部点击及Esc关闭；未确认修改有放弃提示，取消关闭保留输入和原关联，确认失败保留输入。按实际可见视口和助手容器限制浮层高度，三尺寸下底部操作和滚动后关闭均可达。
- 迁移23新增各订单独立阶段状态、版本、调整历史；IPC与网页桥接接入，备份恢复及重启保留完整记录。阶段保存失败和并发版本冲突保留输入，切单异步返回有订单与界面代次保护。

## 实际证据

全部为隔离虚构临时订单、文件、模拟AI和虚构聊天；真实AI请求0、真实聊天读取0、WhatsApp发送0、正式订单/库存/Sheets写入0。操作真实Electron、浏览器和最终App，不将模拟语义冒称真实业务链路通过。

| 项目 | 实际结果与证据 |
|---|---|
| npm test / check | 292/292，0跳过；项目检查PASS。`artifacts/assistant-stages-build-final-complete.log`（最终打包前完整执行）。11项新增阶段测试含事实跳过、两单、否定/问句、延期、失败并发、费用、迁移和旧TXT兼容 |
| 源版回归 | 初跑22项21通过1旧测试模块路由失败；补齐新增模块路由后原断言1/1通过。弹窗视口、阶段及浏览器另3/3复验，最终标题/截图另1/1。`assistant-stages-source-complete.log`、`assistant-stages-browser-recheck.log`、`assistant-stages-popup-fit-recheck.log`、`assistant-stages-visual-source-release.log` |
| 最终ZIP新解压 | 最终27/27通过，0跳过；本地安全设置为虚构密钥验证，实际账号与真实AI语义仍待条件。 `artifacts/assistant-stages-delivery-zip-tests.log` |
| 三尺寸实际查看 | 900×700、1280×850、1440×900顶部当前/下一步同排、无横向溢出；关联窗口可见边界、底部操作和滚动后关闭，宽流程/核对浮层截图已实际查看。`stages-result.json`、`binding-viewport-result.json`、`visual-review.json` |
| 虚构业务链路 | 新建→关联→读虚构正文→模拟提取→冲突与依据→客户确认→分包→实际TXT→重启→同客第二单，沿用NEW-01～07 Electron回归；另验阶段、提醒入口、延期、并发和失败保留。最终包日志及`stages-result.json` |
| XLSX / XLS / CSV | 最终App实际导入各1/1，未知成本NULL、重复保护与重启。`import-results.json`和三份导入日志 |
| 迁移 / 备份恢复 | 17→23含20回复草稿、21译文、22双语/历史/流程、23阶段版本与记录；恢复两单隔离状态再重启一致。`packaged-assistant-backup.json`；22→23故障回滚和损坏阶段内容拒绝在单元测试中验证 |
| 生产模式 / 源码 | 最终App生产模式隔离临时目录启动、显示8.13/8.13.0、手动草稿与流程重启、无密钥AI不可用；未启动正式资料目录。`production-assistant-result.json`、`source-match.json`；104个源/界面/共享及主说明文件一致，包元数据一致、x86_64、签名校验通过 |

初始候选发现900尺寸浮层越界及旧生产测试仍断言schema22，均已修复并复验；第一、第二候选ZIP和记录保留在`dist/candidates/`、`artifacts/assistant-stages-8.13/candidate-1/`与`candidate-2/`。候选不作为最终交付，不覆盖旧8.11/8.12 ZIP。

## 可用与待完成

可直接使用：本地八阶段事实判断、当前/下一步提醒、完整流程查看、跳过完成项、手工补充和配送核对入口、收货截止/延期/等待/纠正记录、到期订单列表、关联窗口关闭、独立回复与TXT/导入/备份恢复。

仅模拟语义验证：AI提取、拟回复、自动翻译/补充、按业务规则起草及真实对话判断。真实开发额度已耗尽，无新增真实请求；关键字线索只供人工核对。未配置配送规则时需在地图处人工确认派送结果，不根据城市名称自行放行。

尚未完成/不纳入：真实账号全历史覆盖及媒体完整性、真实AI商业语义联合验收、模型训练、自动发送、退出后系统通知、桃园提交与回执集成。S2/S3其余清单不勾为全部完成。

钥匙串条件：本轮虚构密钥验收通过；真实AI仍需要有效配置、明确额度及受控聊天范围，不能凭模拟连接宣称完成。

## 打开方法和工作区保护

最终ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.13.0-x64.zip`。

SHA256：`70d89b96b0048883c5ee64f223a005f43ed97c37ea66f0a75863c079060b8130`。

双击ZIP解压，再双击文件夹中的`KDocs订单助手 8.13.0.app`。正常使用无需.command、终端或测试参数；若macOS首次阻止，Finder右键App选择“打开”，不关闭系统安全功能。不覆盖已安装App；正式资料沿用KDocs Order Assistant目录，升级迁移有自动备份。

本轮基线`.cache/assistant-stage-baseline/files/`共203个原文件保留；只改任务范围文件，原HEAD、暂存区及其他脏文件保留。提交前发现索引字节与开工基线不同，132个条目的路径、模式、对象、阶段和标记逐项一致，暂存区仍为空；原因未确定，保留当前索引而不恢复覆盖。使用临时Git索引建立独立本地任务提交，不git add全部、不推送。本地提交与保护证据另见`artifacts/assistant-stages-8.13/git-delivery.json`。

接下来做什么、由谁做、是否需要我操作：本地阶段功能已交付，由你按上述方法打开即可；真实AI联合验收需你明确追加额度与受控聊天范围，再由助手集中复验，当前无需参与调试。
