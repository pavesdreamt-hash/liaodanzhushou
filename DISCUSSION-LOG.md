# 聊天工作台讨论记录

状态：讨论期。除非用户明确说“集中修改”或指定实施范围，本文件只记录结论，不授权应用代码、数据或打包改动。

## 记录规则

- 每轮讨论使用连续编号；每条分为“已确认”“实现建议”“待确认”，避免把建议误当成已定需求。
- 形成一个可独立实施的功能组时，开发者提示“可汇总并进入集中修改”，并给出拟改页面、行为和保护范围。
- 集中修改前，以本文件和 `CONFIRMED-UI-BASELINE.md` 为准核对范围；未列入本文件的页面和控件默认不动。

## D-001｜聊天工作台四列结构与消息浏览

**已确认**

1. 新讨论对象为“聊天工作台 UI”，参考 Salesmartly 聊天页及用户提供截图的四列结构。
2. 第一列为可折叠的窄导航。
3. 第二列为客户／会话列表，以电话号码为主要识别信息。
4. 第三列为选中客户的完整聊天内容。
5. 第四列显示客户资料、关联订单及后续相关信息。
6. 核心目标是中间两列在滚动、选择会话和读取消息时具有连续、顺滑的使用感。

**实现建议（尚未确认）**

1. 聊天记录向上滚动到顶部时，按批补充较早消息；向下回到最新位置时，接收和显示新消息。每批读取数量、日期范围和“是否读取到最早记录”的提示另行讨论确认。
2. 补充较早消息时保留用户当前正在看的那一条消息位置，禁止整列清空后重绘导致画面跳动。
3. 会话列表使用局部更新与缓存；新消息只更新对应会话的摘要、时间和未读数，不重建整列。
4. 订单详情与聊天工作台是否共用同一份客户历史、第四列的订单卡片和操作范围，留待后续讨论。

**当前边界**

- 本条不授权修改现有订单详情、WhatsApp 真实连接、消息读取范围、发送、翻译或任何其他页面。
- 当前版本的“刷新消息”仍是最近 24 小时范围的重读；它不是本条所描述的完整历史浏览方案。

## D-002｜第一列的来源与折叠

**已确认**

1. 四列中的第一列直接沿用应用现有的全局导航列，不新建第二套导航。
2. 现有“始终展开”的导航改为可展开／可折叠。
3. 原有导航入口、语义图标与选中态保留；本条只改变导航的展开状态，不改变入口内容。

**待确认**

1. 折叠后是仅显示图标，还是保留极短标签。
2. 折叠开关的位置、默认状态，以及是否记住用户上次选择。

## D-003｜折叠态的内容与开关位置

**已确认**

1. 导航折叠后仅显示各入口的语义图标，不显示短文字标签。
2. 折叠／展开按钮放在第一列最顶部、原“我的工作台”所在的位置。
3. 折叠时“我的工作台”文字消失；它不在图标导航中额外占位。

**待确认**

无。

**补充确认**

1. 首次进入聊天工作台时，导航默认展开。
2. 折叠态下，鼠标悬停在图标上显示对应导航名称提示。

## D-004｜第二列：客户／会话列表的布局细节表

**已确认的总原则**

1. 第二列采用用户提供截图的会话列表层级与行布局作为参考，不照搬 Salesmartly 的品牌颜色、字体或图标。
2. 字体、颜色、边框、圆角、阴影和图标体系继续使用本应用已确认的样式变量与语义图标。
3. 第二列的主要识别信息是客户电话号码；聊天工作台只显示真实已读取或本地已保存的会话资料，不生成示例客户、预览内容或未读数。

| 编号 | 区域／元素 | 布局与显示规划 | 交互／数据规则 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 列本体 | 第一列右侧、第三列左侧；独立纵向滚动，顶部工具区固定。列内容不带多余外层卡框。 | 只滚动本列，会话选择不推动整窗滚动。 | 已确认结构；宽度待定 |
| 2 | 顶部工具区 | 包含会话范围标题、总数、搜索与筛选的预留位置；视觉为紧凑工具行，不挤压会话行。 | 搜索、筛选条件和是否显示总数待后续单独确认。 | 待确认 |
| 3 | 会话排序 | 会话行按最近一条真实消息时间倒序。 | 新消息只将该会话移至顶部并更新自身，不重建整列。 | 实现建议待确认 |
| 4 | 会话行高度 | 每行容纳主识别、最近消息和辅助状态三层；内容不足时保留结构留白，不任意压缩为一行。 | 长文本单行截断，不撑开整列。 | 已确认布局方向 |
| 5 | 头像 | 左侧固定头像位；有真实头像显示真实头像，无头像显示名称或电话号码首字符的应用内占位。头像右下保留 WhatsApp 渠道小标识。 | 不使用无来源的客户头像。 | 已确认 |
| 6 | 主识别 | 以完整、已规范化的客户电话号码为主要识别；存在已核对名称时可作为辅助信息。 | 号码和名称均来自已确认客户／聊天资料；显示优先级的最终文字规则待定。 | 已确认电话优先；细则待定 |
| 7 | 时间 | 行右上显示最后一条消息时间；当天显示时分，较早记录显示日期加时分或应用统一格式。 | 取真实消息时间；不以本地刷新时间伪装消息时间。 | 已确认 |
| 8 | 最近消息摘要 | 主识别下方显示最后一条可读消息的一行摘要；支持发送／接收方向或送达状态的语义小图标。 | 文本过长省略；图片、语音等没有文字时显示准确类型提示，不编造文案。 | 已确认方向 |
| 9 | 辅助状态行 | 摘要下方留一行显示接待状态、关联订单或可核实来源；层级弱于电话号码和摘要。 | 仅展示已有真实状态；具体字段和优先顺序后续确定。 | 待确认 |
| 10 | 未读数 | 行右侧提供紧凑圆形未读数位置。 | 只统计应用实际已记录、且用户尚未进入查看的消息；没有真实未读数时不显示。颜色沿用现有状态色，不照搬参考图红色。 | 已确认原则；计算规则待定 |
| 11 | 更多操作 | 行右侧可在悬停或选中时显示“更多”入口，避免每行常驻杂乱图标。 | 入口里有哪些操作另行讨论；不默认加入删除、发送等高风险操作。 | 待确认 |
| 12 | 选中与悬停 | 当前打开的会话使用现有应用的浅紫选中态；悬停使用更弱的现有中性色反馈。 | 单击整行切换第三列聊天与第四列资料；键盘焦点有清晰轮廓。 | 已确认 |
| 13 | 会话列表滚动 | 本列内部滚动条贴近右边缘，样式沿用现有细滚动条；不使用整窗滚动。 | 初始加载有限批会话，接近列表底部时增量补下一批并保持已有位置。 | 实现建议待确认 |
| 14 | 加载与缓存 | 首批和继续加载显示小型占位／加载行，不遮挡已可看的会话。 | 只更新变化的行，保留已加载会话、搜索结果和滚动位置，禁止整体闪烁重绘。 | 已确认性能目标 |
| 15 | 空、无结果与错误 | 分别提供“未连接 WhatsApp”“暂无已读取会话”“没有匹配结果”“读取失败，可重试”的明确状态。 | 不以模拟会话填充空白。 | 已确认 |
| 16 | 与第三、四列联动 | 选中一行后，第三列切换到该会话，第四列切换到该客户及其订单资料。 | 切换时各列独立更新，聊天草稿和未完成动作必须有保护规则，后续讨论。 | 已确认联动；保护细则待定 |

**下一轮建议先定的细节**

1. 第二列默认宽度、最小／最大宽度，以及是否允许用户拖动调整。
2. 顶部工具区具体保留哪些：搜索、范围标题、筛选、未读筛选、排序。
3. 号码与名称的两行显示规则，以及辅助状态行优先显示接待状态、订单号还是渠道信息。
4. 未读消息如何定义：进入会话即清零，还是滚动至最新消息才清零。

## D-005｜第二列的可调设计、顶部工具区与信息优先级

**已确认**

1. 第二列顶部可参考本轮截图：范围标题和计数、搜索框、筛选按钮组的紧凑层级可借鉴；不需要完全照抄。
2. 顶部工具区保留且仅优先放置：搜索、全部、未读、未回。其他筛选与功能先不加入，避免占用空间。
3. 第二列初期允许调节宽度；在找到合适的实际比例前，不把宽度写死。后续用户确认满意位置后再固定默认值。
4. 本轮新原则：聊天工作台新设计中，适合调节且会影响空间比例的尺寸优先做成可调，方便快速校准；不把业务行为、颜色或文字随意做成可调。
5. 会话行的信息优先级为：客户电话号码第一，接待状态第二；其余资料不在会话行常驻显示，以免挤压阅读空间。
6. 会话行仍在右侧保留最后消息时间和未读数的紧凑位置，因为它们服务于排序和待办判断；它们不属于额外资料文字。

| 区域 | 初期可调内容 | 固定不变的内容 | 后续固定条件 |
| --- | --- | --- | --- |
| 第二列 | 列宽，可通过明确分界拖动调整 | 内部滚动、行内信息优先级、现有颜色与字体体系 | 用户在常用 24 英寸外接屏和 16 英寸笔记本上确认比例满意 |
| 四列总布局 | 各列间的必要宽度分配；具体可调范围后续逐列确定 | 第一列折叠逻辑，第三列聊天主工作区，第四列资料定位 | 四列全部讨论并有真实聊天资料可评估 |
| 顶部工具区 | 不做自由大小调节，随第二列宽度自然排版 | 搜索、全部、未读、未回四项 | 用户确认实际常用宽度后只作视觉微调 |

**未读数的建议（待用户确认）**

1. 未读数不应在 30 天后自动清零；未处理消息会因此失去待办意义。
2. 用户选中会话且实际滚动至当前最新消息时，记录该条为最后已读，未读数随之清零。
3. 超过 30 天仍未读的会话可在“未读”筛选中继续存在，后续如有需要单独增加“长期未处理”标识；它不能替代未读数。

**需在下一轮确认**

1. “接待状态”具体使用哪些状态文字，以及是否配语义图标。
2. 既然会话行只常驻电话号码与接待状态，最近一条消息摘要是否完全取消，还是仅在悬停／选中时显示。
3. 是否接受上述“滚动到最新消息才清零”的未读规则。

## D-006｜最近消息摘要、接待状态与第三列完整对话

**已确认**

1. 第二列会话行保留“最近一条消息摘要”，不需要悬停才显示。摘要显示限定长度，超出后单行省略；它的作用是让用户一眼知道该客户有何消息。
2. 会话行的接待状态只根据真实 WhatsApp 发送记录判断：当前 WhatsApp 账号尚未向该客户发出任何消息时，显示灰色小人图标＋“未接待”。
3. 当前 WhatsApp 账号已经向该客户发出过消息时，显示蓝色小人图标＋该 WhatsApp 发送账号的电话号码。
4. 已接待状态不显示案例邮箱，也不以邮箱替代 WhatsApp 电话号码。
5. 第三列必须保留为选中会话的完整聊天内容区；第二列只承担列表、摘要与状态，不能替代第三列，也不能因第二列压缩信息而删减完整对话。

| 会话行层级 | 内容 | 展示规则 |
| --- | --- | --- |
| 第一行 | 客户电话号码；右侧最后消息时间／未读数 | 电话号码优先，时间与未读数保持紧凑 |
| 第二行 | 最近一条消息摘要 | 常驻显示，固定长度单行省略；保留收发方向的语义标识 |
| 第三行 | 接待状态 | 未接待：灰色小人＋“未接待”；已接待：蓝色小人＋当前 WhatsApp 发送号码 |

**第三列的本轮保护规则**

1. 只要用户选中第二列会话，第三列就显示该客户的完整逐条聊天，不以摘要替代原文。
2. 向上读取较早消息、向下显示新消息、逐条身份、时间、译文和消息方向，继续作为第三列后续讨论范围。
3. 第二列切换会话时，第三列只切换目标会话；不能清空其他会话已保存的消息或草稿。

## D-007｜第二列讨论完成与未读规则

**已确认**

1. 第二列不再增加其他常驻字段或顶部功能；以 D-004 至 D-006 的细节表为本轮集中修改依据。
2. 未读数的初期规则定为：用户进入会话并实际滚动到该会话最新消息后清零。
3. 不使用“30 天后自动清零”；超过 30 天仍未读的会话继续保留未读状态。长期未处理提示如有需要，后续另议。

**第二列集中修改前的保护范围**

1. 保留原有颜色、字体、语义图标与真实数据边界。
2. 不修改第三列完整对话、第四列资料内容、真实 WhatsApp 读取范围、发送流程或其他页面，除非对应讨论条目明确授权。

## D-008｜第三列：完整对话主区（待讨论起点）

**待确认的首要结构问题**

第三列是否采用参考图中的桌面聊天主区：无手机外框，聊天内容占满第三列，中间独立滚动，顶部为当前客户／会话信息，底部为回复区？

## D-009｜第三列改为桌面聊天工作区

**已确认**

1. 聊天工作台第三列抛弃手机样式，改为桌面聊天工作区：无手机外框，完整对话占满第三列，中部独立滚动，顶部会话栏与底部回复区固定。
2. 顶部当前客户电话号码在第三列标题区水平居中。
3. 翻译与三种回复模式使用参考图的紧凑控制样式：图标＋文字＋状态开关／选中状态，并预留下拉设置或齿轮入口。
4. 顶部控制从第三列右边缘向左排列；电话居中不随右侧控件数量偏移。
5. 中部显示双方完整逐条文字对话；图片和视频能力留待后续讨论，不作为本轮实施承诺。
6. 消息布局参考用户截图的桌面会话层级：客户消息靠左、我方消息靠右；保留时间、身份、状态、引用回复和媒体卡片的清晰位置。颜色和字体继续沿用本应用风格。

**模式的交互约束**

1. 人工、AI 辅助、AI 自动可以采用一致的“图标＋文字＋设置”视觉形式，但三者是互斥模式，任何时刻只能启用一个。
2. 不能把三种模式实现为可同时打开的三个独立开关；否则会无法判断由谁回复。
3. 翻译为独立功能，可使用总开关与展开设置面板；翻译开关不改变当前回复模式。

| 顶部区域 | 布局与行为规划 | 状态 |
| --- | --- | --- |
| 中央标题 | 当前客户电话号码居中；有已核对名称时可作为次要文字，不取代号码 | 已确认 |
| 翻译控制 | 紧凑开关＋语言图标＋“翻译”文字＋下拉箭头；点击展开设置面板 | 已确认样式；面板字段待定 |
| 翻译设置面板 | 对方语言、我方语言、接收翻译、发送翻译、翻译服务状态 | 参考图的功能方向；字段待定 |
| 人工模式 | 人形图标＋“人工”＋选中状态；可有设置入口 | 已确认外观方向 |
| AI 辅助模式 | 辅助图标＋“AI辅助”＋选中状态；可有设置入口 | 已确认外观方向 |
| AI 自动模式 | 机器人图标＋“AI自动”＋选中状态；可有设置入口 | 已确认外观方向；自动规则后议 |
| 控件排序 | 从第三列最右侧开始向左排布；具体顺序和各入口是否都要齿轮待定 | 部分待确认 |

| 消息区能力 | 首轮范围 | 说明 |
| --- | --- | --- |
| 文字与表情 | 发送、接收、完整原文 | 两侧气泡、身份、时间、送达状态与逐条中文译文保持可见 |
| 图片 | 待定 | 发送、接收、缩略图、组图与查看大图均不纳入本轮；待用户单独确认 |
| 引用回复 | 显示被回复内容的精简引用条 | 点击可定位原消息；引用不替代原消息正文 |
| 日期与历史 | 日期分隔、向上补较早消息、底部新消息提示 | 补历史时保持当前阅读位置，不整列闪烁重绘 |
| 位置与链接 | 当真实消息包含可读取的位置或链接时，显示明确的只读卡片 | 地图、外部链接不凭空生成；实际读取能力需验证 |
| 语音、视频、文件、联系人、贴纸 | 暂不承诺首轮发送／完整解析 | 有真实记录但暂不能读取时，显示真实类型与“请在 WhatsApp 核对”，不假装可播放／下载 |

**保护与待确认**

1. 本条“抛弃手机样式”只指聊天工作台第三列；订单详情页面的手机聊天保持原样，暂不修改。
2. 顶部右侧四项的具体排列顺序、翻译设置中的语言默认值、各模式的设置内容，下一轮逐项确定。
3. 底部桌面回复区的结构、快捷回复放置位置和发送确认，下一轮单独讨论；图片、视频的接收与发送另列为待定事项。

## D-010｜工作台范围与媒体能力暂定

**已确认**

1. 本轮聊天工作台改为桌面聊天工作区；只改工作台，不改订单详情页。
2. 订单详情页保留现有手机聊天样式，后续是否调整待定。
3. 图片与视频的接收、发送、预览和查看均待定，不进入当前聊天工作台的集中修改范围。

## D-011｜第三列剩余讨论清单

以下为待确认事项，不构成实施授权：

1. 顶栏右侧控件的精确顺序：翻译、人工、AI 辅助、AI 自动、各自的下拉箭头或设置齿轮。
2. 翻译设置面板：对方语言、我方语言的默认规则；接收翻译与发送翻译是否分别开关；翻译服务设置入口。
3. 三种模式：切换后各自显示什么状态与设置；人工、AI 辅助、AI 自动的可用边界和草稿保留规则。
4. 消息阅读区：气泡宽度、身份／时间／译文的位置、引用回复、日期分隔、到达最新消息的提示。
5. 历史与新消息：首次读取范围、向上加载批次、未读分隔线、底部新消息提示，以及读取完成／仍有更早记录的反馈。
6. 底部桌面回复区：中文输入与英文核对的布局、快捷回复入口、表情、清空、发送按钮及发送前确认。

图片和视频已排除在本轮；第四列资料面板在第三列讨论完成后再进入讨论。

## D-012｜第三列顶栏、翻译、模式与回复区

**已确认**

1. 第三列右侧控制从左至右排列为：人工、AI 辅助、AI 自动、翻译；翻译位于最右侧。
2. 翻译默认常开，用于把当前阅读到的客户对话翻译为中文，供用户查看。
3. 输入端翻译默认将用户输入的中文翻译为客户语言；当前常用客户语言是英语，后续允许按实际客户语言调整。
4. 人工、AI 辅助、AI 自动为单选，任何时刻只开一种；当前默认且继续使用人工模式。AI 辅助和 AI 自动的实际能力留待后续开发，不能冒充已经可用。
5. 消息展示参考用户提供的桌面会话样板：完整双方对话、左右方向、消息身份／时间／状态、引用回复、图片或位置类消息的卡片层级。颜色和字体不照抄样板，继续沿用本应用样式。
6. 底部回复区提供：中文输入、英文翻译、快捷回复、表情、清空、发送确认。
7. 中文输入与英文翻译共用同一个编辑器位置和相同尺寸，不上下堆叠两个输入框；用户在“中文输入”或“英文翻译”视图中切换查看和编辑对应内容。

| 顶部控件 | 初期状态 | 点击／状态规则 |
| --- | --- | --- |
| 人工 | 默认选中 | 人形图标＋文字；打开人工回复编辑流程 |
| AI 辅助 | 未选中，能力待开发 | 保留位置与单选语义；未开发前明确不可用或标注待启用 |
| AI 自动 | 未选中，能力待开发 | 保留位置与单选语义；未开发前不自动发送、不假装运行 |
| 翻译 | 默认开启，最右 | 语言图标＋文字＋开启状态；展开面板设置阅读与输入翻译 |

| 底部回复区 | 展示与行为 |
| --- | --- |
| 中文输入 | 同一编辑器位置显示中文草稿；快捷回复填入此视图；表情插入当前编辑内容 |
| 英文翻译 | 同一编辑器位置切换为英文结果，可人工修改；发送使用当前已核对的客户语言文本 |
| 快捷回复 | 弹出或下拉选择，具体外观后续可按空间决定；选择后不直接发送 |
| 表情 | 插入当前视图的编辑位置，不绕过翻译和发送核对 |
| 清空 | 同步清空中文草稿、英文结果与未发送预览；已发送消息不受影响 |
| 发送确认 | 仅在目标已核对、英文／客户语言内容已准备、人工确认后可用；不因切换到 AI 自动而绕过核对 |

**“历史与新消息”说明（原 D-011 第 5 项）**

1. 打开会话后先显示最新一段消息；用户向上滚到顶部时，自动补读更早一批，直到 WhatsApp 实际可提供的更早记录读完。
2. 用户正在看旧消息时，若来了新消息，画面不自动跳到底部，而是在底部显示“有新消息”提示；点击后回到最新消息。
3. 用户已经在底部看最新消息时，新消息平滑追加在底部。
4. 必须明确显示“正在读取更早消息”“没有更早消息”或“读取失败”，不能静默停住。

**待确认**

1. 翻译设置面板中，客户语言是否默认“自动识别”，以及是否允许手动选择英语等语言。
2. 人工、AI 辅助、AI 自动控件是否都需要各自设置齿轮，还是只有翻译显示下拉设置。
3. 快捷回复采用弹出面板还是下拉菜单；该面板是否沿用现有模板分组。

## D-013｜第三列设置与快捷回复确认

**已确认**

1. 翻译面板中的客户语言默认采用自动识别，并允许用户通过下拉选项手动改选实际客户语言。
2. 初期只有“翻译”控件提供下拉设置面板；人工、AI 辅助、AI 自动在未具备实际可配置能力前不显示空的设置齿轮或下拉入口。
3. 快捷回复使用弹出面板，不使用普通下拉菜单，以便后续按模板分组、管理内容和扩展。

**第三列当前状态**

1. 顶栏顺序、翻译规则、模式单选规则、消息区方向和底部回复区结构已经完成讨论。
2. 图片和视频仍为待定，不在本轮集中修改范围。
3. 历史分批读取与新消息提示的交互已提出方案，实施前可按真实 WhatsApp 可读取的历史能力验证边界。

## D-014｜第四列与客户电话号码自动关联

**已确认**

1. 工作台第四列用于显示订单的具体信息。
2. 第四列的订单依据当前选中的客户 WhatsApp 电话号码自动关联；用户在第二列切换客户后，第四列随之更新。
3. 该关联只影响聊天工作台内的资料展示，不改变现有订单详情页的结构或内容。

**关联原则（待进入细节时继续确认）**

1. 电话号码是默认主关联键，应先按统一国际号码格式比对，避免 `+971...`、`971...` 或带空格格式被当作不同客户。
2. 同一号码可能对应零个、一个或多个订单；当有多个订单时，第四列默认显示最新订单，并保留查看该客户其他订单的入口。

## D-015｜第四列宽度与待定细节

**已确认**

1. 第四列为收紧的资料面板，不能因完整订单信息而挤压第二、三列，尤其不能影响中间聊天区阅读和回复。
2. 初期第四列宽度允许拖动调整；实际使用确定合适比例后，再由用户决定是否固定宽度。

**待确认的第四列内容与行为**

1. 面板顶部是否显示客户电话、订单号、订单状态，以及“查看其他订单”入口的具体排列。
2. 默认最新订单中首屏显示哪些摘要字段：订单状态、商品／数量、应收金额、收货人、地址、配送状态等。
3. 完整订单字段采用折叠分组，还是由“查看订单详情”进入既有详情页；本面板不直接复制整张详情页。
4. 无关联订单时显示什么空状态，以及是否提供“新建订单”入口。
5. 聊天中识别到地址、商品或数量等信息时，第四列是否只提示“待核对”，还是允许一键采纳；这关系到订单数据安全，需单独确认。

## D-016｜第四列作为聊单最终校正区

**已确认**

1. 第四列基本延续此前已设计的订单信息结构，但服务于聊天工作台的聊单流程。
2. 顶部保留客户电话、订单号、订单状态与“查看其他订单”入口。
3. 第四列不跳转到现有订单详情页；它是信息校正的最终操作位置。人工完成核对后，已校正的信息自动保存并同步回订单详情页。
4. 面板必须完整覆盖既有聊单所需字段，不能遗漏：客户信息、订单信息、地址核对、报单 TXT 下载／核对。
5. 无关联订单的客户必须提供“新建订单”功能。
6. 聊天内容中识别到的地址、商品、数量等可在第四列给出采集／更新提示，但不能自动写入订单。最终更新须经人工核对；客户主动发送的信息同样需要通过地址校正等核对流程。
7. 利润计算、回款不在聊单工作台处理，后续归入订单管理；工作台只处理聊单相关事项。

**布局决定**

1. 因第四列需要收紧，默认不额外设置重复的“订单摘要”卡片，以免占用有效空间并与校正字段重复。
2. 改为按“客户信息、订单信息、地址核对、报单 TXT”分组完整展示；组内可折叠，展开后可编辑、核对和确认。
3. 重点状态（例如待客户确认、待地址核对）在顶部订单状态和对应分组标题上体现，不另复制整份订单摘要。

## D-017｜四列工作台剩余待确认项

四列主结构已完成讨论。以下仅列出仍会影响实际操作、需要继续确认的规则；已确认的四列职责、颜色字体、订单详情页不改等内容不重新讨论。

1. **第一列宽度与记忆**：展开／折叠后的具体宽度，以及用户上次折叠状态是否在下次打开时保留。已确认首次默认展开、折叠后仅图标和悬停名称提示。
2. **第二列加载节奏**：会话列表向下加载更多记录时的批次大小与“已无更多”提示；不影响已确认的真实数据、未读和接待状态规则。
3. **第三列译文展示**：每条中文译文固定在原文气泡外的独立译文行，还是通过单条消息的查看译文入口展开。已有规则是译文不得混入原文或丢失消息身份。
4. **第三列未开发 AI 的呈现**：AI 辅助、AI 自动在尚未开发时显示为禁用并标注“待启用”，还是暂时隐藏；无论哪种都不改变当前人工模式。
5. **第三列消息能力**：图片、视频仍待后续单独讨论；不纳入当前集中修改范围。
6. **第四列分组顺序与折叠策略**：客户信息、订单信息、地址核对、报单 TXT 四组的上下顺序，初始展开哪一组，以及是否允许同时展开多个组。
7. **第四列保存反馈**：人工确认某项更新后，是立即自动同步订单详情页并显示“已保存”，还是保留一个本页的“确认并保存”动作；两种都不允许未经核对自动写入。
8. **第四列冲突处理**：聊天新采集内容与订单已有值不同，需保留旧值与新值对照，提供“采纳更新／忽略”两项；地址仍须经过地址核对。
9. **第四列其他订单入口**：采用订单号列表弹层还是在面板内切换；默认最新订单不变。
10. **第四列无订单新建**：点击“新建订单”后先创建草稿，还是先填写最低必要字段再创建；不影响电话自动带入。

## D-018｜四列剩余操作规则确认

**已确认**

1. 第一列默认采用折叠状态，覆盖 D-003 中“首次进入默认展开”的结论。折叠态只显示图标，但图标的点击区不能做得过窄或拥挤；展开态保持基本导航宽度，文字右侧至少预留约一个汉字的呼吸空间。
2. 第二列自动读取并保持最新消息；没有新增消息时停止继续读取。历史消息不自动连续展开，须由用户主动向上翻阅时再逐批加载。
3. 第三列中文翻译严格独立于原消息显示，不能混入原文气泡，也不能因此丢失客户、人工或 AI 自动发送等身份和图标。
4. AI 辅助、AI 自动尚未开发时显示为“待启用”。
5. 第四列客户信息、订单信息、地址核对、报单 TXT 的分组顺序和初始折叠状态，沿用已确认的 UI 基准，不重新设计。
6. 第四列的采集更新须由人工点击“确认并保存”后，才同步回订单详情页。
7. 聊天内容与订单现有信息冲突时，由系统识别并展示待核对项，人工决定是否采纳；地址始终经过地址核对。
8. “查看其他订单”在第四列内切换订单，不采用弹层列表；默认仍为最新订单。

**仍待确认**

1. 无关联订单时，点击“新建订单”是先创建已带入客户电话号码的草稿，还是先填写最低必要字段后再创建。

## D-019｜无订单时的新建订单流程

**已确认**

1. 当前客户号码没有关联订单时，第四列提供“新建订单”。
2. 点击后直接打开与既有订单信息一致的完整订单框架，不另设计简化表单；区别仅在于新订单字段为空。
3. 当前聊天客户的电话号码自动带入新订单；其余客户、商品、地址、金额和订单字段由人工逐项补充并核对。

**本轮四列工作台讨论完成条件**

1. 四列职责、第一列折叠、第二列会话列表、第三列桌面聊天和翻译／模式／回复区、第四列订单最终校正区均已有记录。
2. 订单详情页不在本轮改动范围；图片与视频能力仍待后续单独讨论。
3. 在用户明确发出集中修改指令前，本记录不构成 UI 或功能代码修改授权。

## D-020｜集中实施的文件与版本保护

**已确认**

1. 本轮实施不直接重写旧版工作台、旧手机聊天、旧回复工具或订单详情页文件；它们作为可回看的旧版实现保留。
2. 新建独立的桌面四列工作台文件与样式，并仅在“聊单工作台”入口切换至新实现。
3. 当前应用版本号沿用，不在本轮因工作台重设而改变版本号或重新打包。
4. 本轮实施范围仍只限聊天工作台；订单详情页、图片和视频能力不改。

## D-021｜四列桌面工作台首次实施与核验

**实施范围**

1. 新增独立桌面工作台 HTML、CSS 与交互文件，并只从工作台入口加载；旧版工作台、订单详情页、手机聊天与回复工具未改写。
2. 实现默认折叠导航、号码聚合的真实订单客户列、桌面聊天区域、人工单选模式/AI 待启用、默认开启翻译设置、同位中文/英文编辑器、快捷回复、表情、同步清空、发送确认，以及第四列订单校正分组和新建订单入口。
3. 图片和视频能力未加入；利润与回款未放入工作台。

**核验结果**

1. TypeScript、Vite 前端构建、共享模块和项目检查均通过。
2. 本机浏览器预览已核对 1440×1000 四列布局；默认折叠导航、隐藏面板、底部编辑区、导航展开、中文/英文切换、快捷回复、清空和翻译面板交互通过。
3. Electron 在当前受限环境以 `SIGABRT` 退出，无法完成桌面 App 启动核验；用户要求本轮不打包、不改版本号，因此没有生成新安装包。

## D-022｜首次运行后的工作台修正

**用户实测问题与本轮修正范围**

1. macOS 窗口控制按钮与第一、二列顶部内容重叠；新工作台需为合并顶栏预留安全高度。
2. 第三列客户号码/提示与人工、AI、翻译控件重叠；改为互不覆盖的头部布局，保留人工、AI 辅助、AI 自动、翻译顺序。
3. 快捷回复弹出面板必须提供明确的关闭入口，并支持点击外部关闭。
4. 第二列“全部、未读、未回”按从左到右的紧凑顺序排列；每项预留选项后数字的位置，不能被拉散。
5. 第二列顶部提供明确的“导入订单”入口，进入现有订单管理导入流程。订单 API 自动读取目前没有已接入后端，界面只能明确标为待接入，不能假装可用。
6. 客户会话切换需在第二列标题和选中行中更明确；控制点击避免不必要的整列重绘，以改善切换的即时反馈。

**保护范围**

1. 仍只改独立桌面工作台与其入口路由；旧版工作台、订单详情页、手机聊天与图片/视频能力不动。

**D-022 核验结果**

1. TypeScript 和 Vite 构建通过。
2. 1440×1000 本机预览已核对：macOS 顶栏安全间距、聊天控件不重叠、筛选按钮紧凑靠左、快捷回复关闭按钮、底部编辑器固定均通过。
3. 已强制结束未响应的旧 Electron 单实例并重启；启动日志确认新窗口已创建、主页面加载完成、窗口显示并获得焦点。
4. “导入订单”已进入现有订单管理导入流程；API 自动读取仍未接入，显示“API 待接入”。

## D-023｜工作台第二次实测修正

1. “导入订单”和“API 待接入”不应占用聊天工作台客户列顶部；订单导入/API 连接归订单管理页面。
2. 刷新按钮必须有即时视觉反馈，读取期间显示旋转状态，结束后恢复，不论数据是否变化。
3. 客户聊天列增加可见拖动分界，用于调整第二列宽度；中间聊天列保留最低可读宽度，不能无限挤压。
4. 底部中文输入、英文翻译、快捷回复、表情、清空、发送确认均须具有明确按钮外观；发送确认不能因根容器高度溢出被遮住。
5. 修复真实订单详情调用参数，使第四列显示当前电话号码关联的真实订单。
6. 第三列增加明确“关联真实聊天”入口。读取聊天前须先登录 WhatsApp，并对当前客户号码完成核对关联；未登录/未关联时不能显示或伪造聊天信息。

**D-023 核验结果（2026-09-22）**

1. 已通过 `npm run typecheck` 与 `npm run build:ui`；构建仅有既有的脚本与体积提示，无编译错误。
2. 在 1440 × 1000 的工作台预览中实测：发送确认按钮位于底部且可见；客户聊天列可用分隔条拖动调整宽度；“关联真实聊天”入口存在；刷新按钮点击后立即出现旋转状态，至少持续 120ms，并在最短 280ms 后结束。
3. 已重新启动桌面程序，启动日志确认数据库初始化、主窗口创建并显示。
4. 订单侧栏的真实数据读取参数已修正为订单 ID 原值。由于当前无法接管已启动的桌面窗口进行账号操作，两个真实订单在当前 WhatsApp 登录态下的实际数据回显，仍需在桌面窗口中点击关联后确认；不可将这一步视为已完成实测。


## D-024｜账号级收件箱、首次视觉问题与旧规则覆盖

**用户确认的规则**

1. 工作台不能把订单列表伪装为聊天列表，也不能让每一条会话单独“关联真实聊天”。WhatsApp 在“连接与设置”登录一次后，工作台统一读取该账号的真实会话。
2. 会话以真实最近聊天时间从近到远排序，首次加载 20 条；继续向下滚动时才逐批加载更早会话。选中会话默认取最新 20 条，向上滚动才读取更早消息。
3. 订单信息只按会话电话号码自动匹配到右侧，不能反过来让订单决定聊天是否出现。
4. 列表头像只可使用真实 WhatsApp 头像；没有返回时使用中性用户图标，不能用电话尾号、姓名首字母或示例头像代替。
5. 修正 macOS 窗口控制区安全距离、计数徽标居中、第二/三列顶部边线对齐，以及导航展开/折叠图标方向。
6. D-023 的“关联真实聊天”入口与逐单核对流程不再适用于桌面聊单工作台；保留为旧订单详情页的历史能力，不借此改动订单详情页。

**实施状态**

已实施账号级会话读取接口、首次 20 条/滚动分页接口和工作台自动电话匹配。真实账号会话、真实头像 URL 与历史分页必须在用户当前已登录 WhatsApp 的桌面程序内进一步核验；未通过这一核验前，不得声称真实聊天读取完成。

**D-024 核验结果（2026-09-22）**

1. `node --check` 已检查 WhatsApp 连接模块、聊天桥接模块和主进程；`npm run typecheck`、`npm run build:ui` 均通过。
2. 人工发送/身份标记与兼容性回归共 6 项通过；本轮没有发送 WhatsApp 消息。
3. 已确认工作台源文件不再包含“关联真实聊天”入口、逐单关联弹窗或虚构“尚未读取真实消息”摘要；会话数据改为调用账号级 `inbox`、`openInbox` 和 `recent` 接口。
4. 已重新启动 1.1.5 桌面程序。启动日志确认订单数据库初始化、主窗口显示、渲染页加载完成。当前 WhatsApp 服务状态为连接重试，尚未在线；因此真实账号的会话 20 条加载、头像返回和历史滚动分页均未完成真实环境验收。工作台必须保持真实空状态，待用户在“连接与设置”完成登录后再验收。


## D-025｜设置页登录操作修正

1. 用户点击“管理登录”后只有提示、没有弹窗，属于缺陷。
2. 改为立即打开 WhatsApp 登录弹窗，显示实际连接状态；二维码生成后显示二维码，成功后显示实际账号状态。
3. 弹窗关闭不修改登录状态；不自动发送消息，不展示虚构二维码或账号。

**D-025 核验结果（2026-09-22）**

1. TypeScript 与 UI 构建通过。
2. 设置页的“管理登录”已由直接页面提示替换为立即创建的 `dialog`；弹窗调用实际 `openWhatsApp` 后每 800ms 读取一次实际连接状态，状态含二维码时才设置二维码图片地址。
3. 已重新启动桌面程序；启动日志确认渲染页和窗口显示完成。当前连接仍处于重试状态、二维码尚未返回，因此不能把扫码成功标为已验证。


## D-026｜工作台版本号恢复

1. 用户指出新工作台没有版本号。
2. 左侧栏底部恢复实际版本号，值由 `package.json` 注入；当前为 1.1.5。


## D-027｜二维码未生成的连接诊断

1. 用户看到登录弹窗但二维码未出现，原连接模块把实际异常统一改写为“连接未完成”。
2. 连接状态现保留 Chrome 缺失、连接超时等可公开原因；未知初始化失败也明确标识为 WhatsApp Web 初始化失败，不伪装为正在扫码。
3. 该修正不读取聊天、不发送消息，也不暴露登录凭据。


## D-028｜二维码前初始化失败的本机日志

1. D-027 后确认失败发生在二维码生成之前，仍需知道浏览器/库初始化原因。
2. 启动日志增加失败代码和长度限制的错误消息；不记录二维码、聊天、号码、Cookie 或凭据。


## D-029｜WhatsApp 专用浏览器残留修复

1. 实际日志显示上一轮专用 Chromium 占用 `session-local-account`，导致新会话无法启动和二维码无法生成。
2. 应用关闭逻辑改为无论库级关闭是否返回，均终止自己创建的专用 Chromium 主进程；不删除登录资料，不影响用户日常 Chrome。
3. 已清理遗留进程并重启桌面程序；启动日志实际确认连接依次进入“正在连接”“已认证”“自动消息连接在线”。由于既有登录资料有效，本次无需重新出示二维码。

## D-030｜导航默认展开与逐次版本号

1. 用户要求左侧导航调整为默认打开状态。
2. 用户要求每一次实际改动均在左下角版本号处体现。
3. 本轮允许改动范围仅为桌面工作台导航初始状态、实际版本号和对应记录；订单详情、会话布局、消息数据和其他页面不改。
4. 版本从 1.1.5 递增至 1.1.6；不重新打包。
5. 已完成 TypeScript、UI 构建、差异检查与桌面程序重启核验；窗口和渲染页正常加载，WhatsApp 已认证并在线。
