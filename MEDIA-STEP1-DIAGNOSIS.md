# 非文字消息修复：第一步真实诊断

日期：2026-09-23。检查对象：本地 1.6.17 当前代码与现有 WhatsApp 登录会话。

## 本次目标与边界

第一步先区分消息分类错误、真实媒体读取故障和本系统限制，为后续修复确定依据。本次完成的是诊断，不是图片显示修复或整页验收。

只读用户截图对应的小范围消息，复用现有浏览器连接；没有初始化新的 WhatsApp Client 登录、扫码、发送消息、调用 AI、修改订单或重启应用。真实下载仅对两个已缓存的候选图片各调用一次。没有批量下载历史图片，也没有保存图片正文、媒体密钥、下载地址或完整聊天内容到本报告。

本次只新增本报告。保留所有原有未提交改动，不提交 Git、不打包、不修改应用版本。按用户最新规则，后续版本须遵循 1.6.9 → 1.7.0 的进位顺序；本次不能把未改动的应用冒充新版本。手机样式不属于本次范围，不以旧手机底部偏差阻断诊断交付。

## 真实验收结果

下列时间均为 Asia/Shanghai。截图显示到分钟，读取结果保留秒。样本标识使用消息 ID 的 SHA-256 截断值，不记录完整 ID。

| 样本 | 实际返回 | 本次结果 |
| --- | --- | --- |
| 08-31 09:50:57，我方，样本 82d04664473c | notification_template；subtype=biz_account_type_changed_to_hosted；正文为空；hasMedia=false，无 MIME/大小/附件路径/媒体密钥 | 两次核对：原始字段检查与真实消息经过现有 normalize()。后者生成“[未知类型消息]”和 source=attachment/status=unavailable，复现错误分类 |
| 09-21 19:40:04，客户，样本 c52e5f62da28 | 与上条相同 | 同样复现错误分类；不是图片下载失败 |
| 08-25 22:41:07，我方，成功截图同分钟候选 A | image/jpeg；RESOLVED；具有附件路径和媒体密钥 | downloadMedia() 取得 81,338 字节；Base64 108,452 字符；JPEG 文件头检查通过；调用耗时 10 ms |
| 08-25 22:41:17，我方，成功截图同分钟候选 B | image/jpeg；RESOLVED；具有附件路径和媒体密钥 | downloadMedia() 取得 92,216 字节；Base64 122,956 字符；JPEG 文件头检查通过；调用耗时 5 ms |
| 09-07 18:53，截图中的另一张图片 | 在截图所指会话当前已加载缓存中未找到该分钟消息 | 未执行该图片下载，未确定它历史失败的原因。没有扩大为批量拉取历史记录 |

成功截图的同一分钟有两条图片，本次未做图像内容比对，不能把其中某一条断言为截图内的那张。两次成功取回均来自已处于 RESOLVED 状态的消息；耗时不能视作互联网下载速度，也不能证明未缓存大图、过期媒体或全部历史都可读取。JPEG 文件头检查不是完整图像解码或本系统窗口显示验收。

## 已证实的问题

### 1. 通知被误画成图片附件

两条通知的 subtype 表示商业账号类型变更到 hosted。这里确认的是消息类型与子类型，未提取 WhatsApp 官方通知的完整显示文案。

`src/orders/whatsapp-web-client.mjs` 的 mediaLabel 缺少此类型，normalize 将几乎所有非 chat/ciphertext 类型都构造为 attachment，即使 hasMedia=false。`ui/src/chat-workbench-desktop.ts` 的 appendMessageContent 又给未专门处理的非文字类型使用图片图标，所以用户看到“未知类型消息 / 请在 WhatsApp 查看”的假附件卡片。

该类型不在 needsVisibleImage 的可下载类型集合中，所以它也不会通过图片下载变成照片。增加下载并发、扩大图片上限或等待更久都不会解决这两条通知的误显示。

### 2. 真实图片存在本系统大小门槛，失败没有解释

downloadImage 限定 Base64 字符串长度不超过 1,390,000，约对应 1,042,500 字节（约 1.04 MB / 0.99 MiB）。这是本项目代码限制，不能说成 WhatsApp 禁止下载。该检查发生在 downloadMedia 返回后，因此不能避免原始图片下载时的内存消耗。

此外，当前有 20 秒等待超时、最多 100 项的适配器内存缓存及格式白名单。超过大小门槛、空返回等情况返回 null；前端 requestVisibleImage 遇到 null 或异常仍保留原占位，并已记入 mediaAttempted，当前会话不会自动再次尝试。界面没有区分“超限”“下载失败”“排队”等原因。20 秒超时本身没有向底层下载传递取消信号。

以上为源码确认的缺陷与限制，尚未证明它们就是 09-07 图片失败的具体原因；本次没有使用真实大图验证超限。

### 3. 已有可视区域触发，但实际路径缺少全局并发调度

observeVisibleImages 通过 IntersectionObserver、以聊天滚动容器为 root 触发可见消息，说明“只读当前窗口”已有代码。它按整条消息是否进入视口判断，不是只按图片像素是否可见判断。

多个可见消息会各自直接调用 requestVisibleImage；此路径没有统一下载并发上限，也没有离开视口后取消排队的机制。适配器旧 drainMedia 的并发 2 限制未约束这个直接 retryMedia 路径。retryMedia 为一张图先获取最近最多 500 条消息再查找，额外增加工作量，也存在更早图片找不到的问题。源码能证明路径设计问题，不能据此声称本次已测出拥堵或故障率。

## 当前通道核对

本机界面 → 本机聊天服务/适配器 → whatsapp-web.js 1.34.7 → Puppeteer 控制的现有 Chrome WhatsApp Web 会话 → WhatsApp 自身媒体下载与解密 → Base64/Data URL 返回界面。

本次通过现有 Chrome 调试连接复用登录，只连接与断开诊断客户端，没有关闭浏览器。库的 downloadMedia 使用 WhatsApp Web 内部下载/解密模块；项目并未直接使用官方 Cloud API 的媒体端点。本次成功样本说明该读取通道部分可用，不能评价为市场最快、最稳定或完整端到端通过。真实发送不在本次验收范围，未测试。

## 后续修复依据与验收要求

1. 先分清系统通知与媒体：这两条真实通知不再产生图片占位、下载任务或无正文翻译任务；图片仍按媒体处理，原聊天顺序与时间不变。
2. 为真实图片保留可见区域优先策略，统一下载队列、并发与取消/过期结果处理；避免每张图片重复扫描 500 条消息。
3. 修订约 1 MB 门槛及大图缓存方式，提供真实失败原因和可控重试；未取得总量时不能伪造百分比进度。
4. 最终须在本系统操作窗口核验真实图片显示、滚动到新图片、切换会话、失败重试与阅读位置稳定。单元测试和本次探针均不能替代该窗口验收。

这些是本次发现产生的后续修复要求，本报告不把后续步骤提前标为完成。

## 核验文档与规则

已实际复读 `AGENTS.md`、`CONFIRMED-UI-BASELINE.md`，重点对照 D-075、D-082、D-090、D-091 及防误改、真实验收与范围保护规则；同时核对 `DELIVERY-PLAN.md` 和 `PHASE-01-CONNECTION-DESIGN.md` 的真实/模拟结果分离原则。历史手机样式及打包规则与用户最新范围有冲突时，以用户本轮指令优先。

| 检查 | 结论 |
| --- | --- |
| 截图两条未知消息的真实类型与本地分类路径 | 已核验，并复现缺陷；尚未修复 |
| 两张同分钟候选 JPEG 通过现有库取回 | 已核验；不是模拟数据 |
| 确切失败图片、未缓存大图、过期图片 | 未验收 |
| 本系统窗口最终图片显示、队列/进度/重试 | 未验收，留待实施后 |
| 不发送、不调用 AI、不改订单、不重置登录 | 本次工具操作遵守 |
| 保留原未提交改动、不修改其他界面 | 本次只新增报告；原工作区保留 |
| Git 空白错误检查 | git diff --check 通过；不代表功能测试 |

没有应用源码改动，因此本次不运行会改写构建产物的 build/check，不打包、不重启、不增加版本。交付物为本报告，当前 1.6.17 的显示缺陷仍存在。
