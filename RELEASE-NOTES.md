# 1.9.5 — 恢复中文草稿与快捷回复的英文转换

聊天工作台现在有清晰的“转为英文”按钮：输入中文后，只有手动点击该按钮（或使用既有 Cmd/Ctrl+Enter）才会调用现有翻译服务并切到可编辑英文草稿。快捷回复继续只填入中文，需由用户再点击同一按钮才转换；没有自动翻译、自动发送，也没有改动快捷回复模板或规则。翻译中会防止重复请求；若期间修改中文、英文、快捷回复或表情，迟到结果不会覆盖新草稿。失败时保留中文和既有英文并显示准确提示。

# 1.9.4 — 修复窄窗口顶栏状态重叠

修复聊天工作台在 820×640 等窄窗口中“聊单助手”名称与 WhatsApp 状态文字重叠的问题。仅在既有的窄窗口断点内，顶栏版本徽标隐藏、状态缩为“已连接／未连接”，同时保留 WhatsApp 图标、状态圆点、完整无障碍名称和可点击状态控件；常用宽度继续显示完整“WhatsApp 已连接／未连接”文案与版本徽标。四列、顶部模式、原生拖动孔位、AI辅助、聊天、订单和其他页面不变。

# 1.9.3 — 恢复聊天工作台窗口拖动

修复聊天工作台顶部不能移动窗口的回归。工作台现在在 macOS 窗口控制按钮右侧恢复原生拖动区域，并根据 WhatsApp 状态、人工／AI辅助／AI自动按钮、窗口尺寸、导航和列宽变化实时留出不遮挡控件的空位。打开工作台弹窗时仍禁止移动窗口，关闭后恢复；聊天、订单、AI 辅助、WhatsApp 登录资料和其他页面不改。

# 1.9.2 — 修复菜单栏退出

修复 macOS 菜单栏图标的“退出”在安全收尾阶段错误恢复窗口、未结束应用的问题。现在“退出”会完成既有安全保存和资源关闭后真实结束应用；左上角红色关闭键仍只隐藏到菜单栏，菜单栏“打开管理页面”仍会恢复同一窗口。聊天、订单、AI辅助和 WhatsApp 资料不改。

# 1.9.1 — 启用聊天工作台 AI 辅助

聊天工作台顶部“AI辅助”从待启用改为可选模式；“AI自动”继续禁用。AI辅助只在人工点击“生成 AI 回复”时读取当前已核对会话的有限文字上下文，并将结果作为可编辑英文草稿与中文核对显示，不会自动请求、发送 WhatsApp、修改订单或客户资料。切换人工/AI辅助会保留当前草稿、附件和引用回复；未配置模型或 API 密钥时给出准确提示。发送仍必须经既有“发送确认”、当前账号/会话核对和 WhatsApp 回执流程。

# 1.9.0 — 订单管理小窗口排版

订单管理表格使用固定且互不挤压的列宽：应用最小窗口下通过表格横向滚动查看完整列，常用窗口下直接显示全部九列。订单编号、客户、商品、金额、来源、状态和操作不会再相互覆盖；多商品仍逐行显示，但移除商品之间多余的横线，只保留订单行分隔。页面标题操作区和筛选区可在小窗口自然换行，功能和数据不变。

# 1.8.9 — 订单详情恢复确认稿设计

订单详情正文重新采用最初已确认的业务卡设计：恢复订单阶段、顶部中文状态、客户与商品双卡、商品金额和配送地图视觉，并使用当前订单真实字段或明确的“待核对”状态。继续保留 1.8.8 的精简结果，不恢复快捷回复、客户聊天、手机聊天或订单详情发送入口；草稿编辑、电话更正、订单读取及其他页面保持原有功能。

# 1.8.8 — 订单详情精简

订单详情页移除“快捷回复”和“客户聊天”两个完整区域，不再装配该页的回复、翻译和真实聊天工具；订单详情正文使用释放出的空间。聊单工作台及其他页面不变。

# 1.8.7 — 订单详情统一导航折叠

订单详情页补齐左侧导航折叠／展开按钮，并与聊单工作台及其他业务页共享折叠状态；折叠后按钮仍可见，可直接恢复。订单详情侧栏底部改为当前版本号。已逐页检查工作台、订单管理、订单详情、商品库存、商品详情、利润核算、利润详情、助手配置和连接与设置的折叠入口。

# 1.8.6 — 消息侧边悬浮操作

聊天工作台的每条消息在悬停或键盘聚焦时，会在气泡朝向对话区中央的一侧显示同一排紧凑按钮：翻译、话术、识图、回复。翻译只处理该条文字；话术复用已有快捷回复；识图仅在图片已读取并显示后启用，调用已配置且支持图片的模型，将识别文字显示在原消息内；回复会在输入区显示被引用消息，真实发送时携带经当前会话核对的 WhatsApp 消息 ID。切换会话会清除引用对象和临时识图结果，不自动识图、不自动翻译、不自动发送。

# 1.8.5 — 真实人工发送与会话未读数

聊天工作台增加回形针附件入口，人工模式可向当前已核对 WhatsApp 会话真实发送文字、Unicode 表情、网址、JPEG/PNG/WebP 图片及常用文件；发送前验证目标号码、附件类型、文件名和大小，发送后核对 WhatsApp 回执，结果不确定时停止后续附件并保留原稿，不自动重发。修复 WhatsApp Web 新版媒体对象私有 ID 覆盖消息 ID、发送回执仍读取旧 `_serialized` 字段的问题。会话列表在日期下方显示该会话真实未读数红圈，0 隐藏，超过 99 显示 `99+`。真实验收仅向用户明确授权的测试号码进行，文字、图片和 TXT 文件均已确认送达。

# 1.8.4 — 可见图片读取与放大查看

聊天工作台仅对当前可见的图片排队读取，最多同时读取两张；离开视口的待处理图片从队列移除，切换会话后旧结果不会写入新会话。图片按消息 ID 读取，避免逐张扫描最近 500 条。单张图片的本机上限改为 16 MB，并使用最多 20 项、48 MB 的适配器内存缓存；缓存命中时不重复下载。失败会显示具体原因并提供手动重试；浏览器无法解码的坏图重试会绕过旧缓存重新下载。读取过程中仅显示不确定进度，不伪造百分比。点击缩略图可放大查看，支持关闭和切换原尺寸。纯图片占位文字不进入翻译队列，避免重绘造成阅读位置漂移。真实 WhatsApp 中未返回的旧图片仍不能凭空补出。

# 1.8.3 — 本机隐藏与恢复会话

聊天工作台的会话行增加三点菜单，可隐藏该会话；“已隐藏”入口可查看并恢复。隐藏状态按 WhatsApp 登录账号与会话 ID 保存，只过滤聊单助手列表，不删除本机消息、不改 WhatsApp 原始聊天。已按用户确认的范围精确隐藏此前误选的一条会话。其余聊天、订单、翻译和图片功能不改。

# 1.8.2 — 会话身份规则拦截

聊天工作台不再自动打开最近的一条会话。用户从列表选中会话后，平台按号码与会话 ID 核对；WhatsApp 明确返回冲突身份时停止读取或显示，临时无法复核映射时允许打开并显示待复核提示。无需 AI，不按订单、头像或消息条数猜测客户身份。此前 1.8.1 的真实来源核验误把另一号码的会话当作目标，不能据此判断目标会话的历史记录。

# 1.7.8 — 系统通知不再误显示为图片

修复商业账号变更通知和撤回消息的中文显示，不再产生图片占位或无正文翻译。保留真实图片读取及其他界面。本次为非文字消息修复的分类步骤，大图限制、下载排队和重试尚待后续处理。版本按用户要求改用三位数字逐位进位，历史 1.6.17 对应 1.7.7，本次为 1.7.8。

# 1.6.17 — 聊天翻译按视口排队

聊天工作台只翻译当前可见区域内的未译消息；滚动到其他位置后，才翻译新位置的内容。译文插入时会固定正在阅读的第一条可见消息，避免聊天内容跳动。

# 1.6.16 — 聊天图片按需读取

聊天工作台改为只在图片消息进入当前可视区域时读取这一条附件，成功后直接显示原图片；滚动到其他图片后才读取其他图片。未显示的历史图片不预加载，未知类型但带附件的可见消息会按实际 MIME 类型尝试显示。

# 1.6.15 — 订单编辑明细六列与说明提示

订单编辑说明与商品金额说明收进圆形问号提示；商品明细改为六列、表头放大，数量使用紧凑加减控件，添加产品移至标题右侧。

# 1.6.14 — 客户资料逐项确认

客户信息校正弹窗的输入区域向左收紧，每项资料新增独立确认。编辑内容变化会取消该项确认；保存时只写入已确认项目。

# 1.6.13 — 地址卡精简与配送选择

地址核对卡缩为“编辑地址”和“配送设置”两项；Google 地图入口移至编辑弹窗，配送结果在单独选择弹窗中确认。地址字段明确为街道/门牌与楼栋/单元，并移除重复说明。

# 1.6.12 — 报单独立核对状态

报单卡“已核对 ✓”改为独立本机记录：只有在报单核对弹窗点击“订单确认”后才显示；客户订单确认不会提前改变该状态，报单资料变更后会要求重新核对。

# 1.6.11 — 报单卡核对与下载

报单 TXT 卡默认显示只读完整内容；新增“核对订单”弹窗，可修正报单资料、查看商品明细并通过“订单确认”记录本机核对状态。下载按钮继续弹出系统保存位置窗口。

# 1.6.10 — 聊天筛选数字徽标

客户聊天的“全部”“未读”“未回”数字徽标改为固定圆形并水平、垂直居中；三个筛选按钮分别增加“全部对话”“客户信息未读”“未回客户信息”鼠标悬停说明。

# 1.6.9 — 地址地图确认与配送城市库

地址核对卡新增“打开 Google 地图”“确认可配送”和“标记不可配送”。打开地图后由人工判断；确认可配送时，当前订单城市写入本机配送城市库，标记不可配送时写入本机禁配名单并从城市库移除。不会自动判断配送范围，也不会修改 ShopPlus、WhatsApp 或客户地址。

# 1.6.8 — 工作台非文字媒体实际读取

- 已选且已核对的 WhatsApp 会话会自动读取可安全缓存的图片、贴纸、GIF 和视频，即使没有开启订单在线同步。
- 可下载但初始类型未知的媒体会按实际 MIME 类型识别；安全附件读取后在聊天内直接显示缩略内容。
- 无法安全缓存的附件仍显示准确类型与读取状态，不再笼统显示“非文字消息”。

# 1.6.7 — 工作台卡片状态与折叠箭头

- 右侧客户信息、订单信息和地址核对卡的标题栏新增“状态”列；客户确认后对应卡片显示绿色“已确认 ✓”，未确认显示“待确认”。
- 原来的“收起/展开”文字改为同级尺寸的向上/向下箭头，保留原有点击折叠行为。
- 不改报单 TXT、字段、确认逻辑或其他页面；直接运行本地版本，不生成安装包，不提交 Git，下一提交节点仍为 2.0.0。

# 1.6.6 — 工作台客户确认与 AI 同意提示

- 客户资料、订单和地址卡的“客户确认”会在确认后禁用；编辑相应资料后自动恢复可确认状态。
- 已发送确认请求后，客户回复 OK、Yes、Confirmed、好的等明确同意语时，工作台弹出 AI 提示；只有人工点击确认才写入本机记录。
- 不自动确认、不发送 WhatsApp，也不修改 ShopPlus 后台。

# 1.6.5 — 常驻运行与异常恢复

- 左上角红色关闭键改为隐藏到 macOS 菜单栏，后台的本地服务和 WhatsApp 连接继续运行；点击菜单栏聊单助手图标可再次显示窗口。
- 界面渲染进程异常退出时，应用会自动重建窗口；一分钟内连续失败两次才停止自动重试并提示导出诊断报告。
- `npm run start` 现在通过启动守护运行：Electron 因异常信号或非零退出码结束时自动重启；从菜单栏明确“退出”会正常结束，不会被重新拉起。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.6.4 — 工作台地址资料与位置提示

- 右侧“地址核对”卡片现在逐行显示国家/地区、省/州、城市、街道和详细收货地址，空资料明确显示“—”。
- 客户消息中的 WhatsApp 位置描述，以及 Google Maps 链接中明确携带的地址文字，会自动显示“可更新”；必须点击“更新”并再次确认，才会写入本机订单。
- 纯 Google Maps 短链、地点名或经纬度不会被猜测为详细地址；不会修改 ShopPlus 后台、WhatsApp 或订单状态。

# 1.6.3 — 订单商品编辑五列布局

- “编辑订单信息”弹窗收至适中宽度，履约状态选择器缩短为合理长度。
- 商品明细改为商品、包裹、数量、单价、操作五列，移除嵌套卡片式编辑外框；字段和保存逻辑不变。

# 1.6.2 — 工作台 AI 客户资料建议

- 在已选会话和关联订单中，可手动运行“AI 识别当前会话”，只提出有客户消息依据的资料建议。
- 建议显示在右侧客户信息卡和对应消息旁；更新前必须二次确认，确认后只写入本机并保留聊天来源。
- 不自动识别、不自动更新、不发送 WhatsApp、不修改 ShopPlus 后台。

# 1.6.1 — 工作台非文字消息显示

- 工作台保留 WhatsApp 图片、贴纸、GIF、视频、语音、文件、位置和联系人的真实消息类型，不再退化为空白或笼统提示。
- 可安全缓存的图片、贴纸和 GIF 会直接显示在消息气泡；网址提供可点击入口。未能本机展开的媒体会显示准确类型和在 WhatsApp 查看的说明。
- 只在已选择、已核对的会话中读取媒体；不会发送消息，也不会修改订单、ShopPlus 或网站后台。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.6.0 — 工作台订单商品编辑

- “编辑订单信息”支持添加、删除商品，修改数量、AED 单价和所属包裹，同时可调整履约状态。
- 保存只更新本机订单，重新计算订单金额和利润，并保留修改依据与审计记录；不会修改 ShopPlus 后台或 WhatsApp。
- 已有最终履约结果的订单会保护结算资料，不能再改商品；已报单的可编辑订单保存后需重新核对报单。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.9 — 工作台右栏编辑弹窗

- 放大客户信息校正、地址信息校正和订单信息编辑弹窗，并让输入框占满对应的可用编辑区。
- 订单履约状态下拉框改为统一的细描边、圆角和焦点样式。
- 只调整这三类弹窗的尺寸与状态选项外观；字段、文案、按钮和保存逻辑保持不变。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.8 — 工作台右侧订单信息列

- 工作台右侧新增真实订单进度卡；履约和地址状态统一显示中文业务文案。
- 客户信息、订单信息、地址核对均增加语义图标及独立的“编辑&确认 / 客户确认”操作；确认只写入本机并保留依据。
- 明确标示：AI 客户资料识别尚未接入当前工作台，不会自动更新真实客户资料。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.7 — ShopPlus 商品明细重复编号修复

- 修复真实 ShopPlus 订单同步时，商品编号在不同订单重复会导致本机数据库拒绝写入的问题。系统现在以订单号、商品来源编号和行序号组成稳定的本机明细标识，可继续同步同商品的不同订单。
- 同步窗口不再直接显示数据库技术错误；如旧版再次遇到该类错误，会给出可理解的重试提示。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.6 — ShopPlus 真实订单同步

- 订单管理顶部改为真实的 ShopPlus 连接状态和“同步最新订单”入口，不再把 AI 服务验证状态当作订单 API 状态。
- 在系统安全输入窗口保存 ShopPlus App Key 和 API Secret 后，应用会使用只读订单接口验证连接，并拉取最新订单到本机订单管理。
- 同步只新增未存在的 ShopPlus 订单号；重复订单跳过，不覆盖本地订单，也不向网站后台写入。缺少成本、商品编号或无法确定状态的资料保持待核对。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.5 — 全站统一导航尾部

- 订单管理、商品库存、利润核算、助手配置、连接与设置共用“我的工作台”旁的折叠/展开按钮，并跨页面保持状态。
- 所有共享业务页删除原侧栏底部状态信息，统一只显示版本号。
- 直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.4 — 提示点实际隐藏与向上阅读取消

- 修复 hidden 属性被 display 样式覆盖，导致点始终可见的根因；保留 7 px 紫色点。
- 向上滚动立即取消提示；迟到响应、阅读历史期间的轮询不重新唤起提示。
- 连续读取失败 6 秒显示“信息暂时无法获取”，3 秒后隐藏，同一故障不反复弹出。
- 纠正 1.5.3 的验收结论：此前仅验证 hidden 属性，未验证真实可见性。本版新增实际渲染与滚轮回归。
- 按当前约定直接运行本地版本，不生成安装包，不提交 Git；下一提交节点仍为 2.0.0。

# 1.5.3 — 新消息提示自动停止（2026-09-22）

修复聊天工作台新消息提示持续跳动的问题：新消息已渲染后提示最多显示 0.9 秒，后续成功读取无新增会立即清除残留提示。读取满 6 秒失败时显示“信息暂时无法获取”，3 秒后自动隐藏。提示点改为页面紫色并缩小到 7 px。

验证：隔离 Electron 回归实际覆盖成功渲染后的自动隐藏、6 秒失败提示与自动隐藏，以及紫色 7 px 样式。未打包、未提交 Git；下一提交节点仍为 2.0.0。

# 1.5.2 — 订单管理列表与操作修正（2026-09-22）

订单管理页已移除新建订单入口，保留聊单工作台的客户确认后新建流程。增加真实列表分页和日期范围筛选，并改为序号、上下分行日期、逐商品行、订单总金额及明确的订单详情按钮。标题后的问号集中展示页面说明和布局设置；API 状态只在最近一次真实验证通过时显示已连接。导航使用单条分界线，并可在“我的工作台”旁折叠。

验证：TypeScript、UI 构建、差异检查以及 12 条虚构订单的隔离 Electron 回归通过。直接启动本地开发版，未制作安装包、未提交 Git；下一提交节点仍为 2.0.0。

# 1.5.1 — 可见的新消息提示（2026-09-22）

- 新消息提示提高至输入区上方，改为完整可见的三个 11 px 红点与浅弧线。
- 新内容已进入视野、点击提示或滚近最新内容时立即隐藏；读取满 6 秒无结果时显示“信息暂时无法获取”3 秒后自动消失。
- 未打包、未提交 Git；下一提交节点为 2.0.0。

# 1.5.0 — 新消息提示与订单自动关联（2026-09-22）

- 新内容提示改为无底色、无外框的三个红色依次晃动点与浅弧线，完整显示在消息区与输入区之间。
- 真实 WhatsApp 会话的订单关联会持续重试：订单服务稍后可读时，阿联酋本地电话与国际 WhatsApp 电话会自动在右侧显示同一订单。
- 未打包；按五版本节点提交并推送 Git。

# 1.4.9 — 新内容提示点（2026-09-22）

- 新内容在视口下方时显示三个依次晃动的紫色提示点，取代固定跳转按钮。
- 点击或滚动到最新内容后提示自动消失；无真实新增消息不显示。
- 未打包、未提交 Git；下一个提交节点为 1.5.0。

# 1.4.8 — 客户消息头像与内容宽度（2026-09-22）

- 客户消息显示真实 WhatsApp 头像，短消息气泡随内容收窄；长消息仍不超过消息列 75%。
- 未改消息内容、译文、时间、方向、颜色、列宽、控件或其他页面。
- 未打包、未提交 Git；下一个提交节点为 1.5.0。

# 1.4.7 — 当前客户身份样式（2026-09-22）

- 当前聊天标题改为与客户列表一致的真实头像和电话号码并列样式。
- 保留真实会话与发送账号说明；未返回头像时显示中性人物图标。
- 未打包、未提交 Git；下一个提交节点为 1.5.0。

# 1.4.6 — 工作台板块边界（2026-09-22）

- 客户聊天列与聊天正文之间的可拖动分隔带增加全高浅灰紫双边线和极浅底色，板块隔离更清楚。
- 未改分隔带尺寸、拖动范围、列宽、内容、控件或其他页面。
- 未打包、未提交 Git；下一个提交节点为 1.5.0。

# 1.4.5 — 真实 WhatsApp 头像加载（2026-09-22）

- 修复本地页面 CSP 阻止 WhatsApp 头像 CDN 的问题，客户列表可显示 WhatsApp 实际返回的头像。
- 仅开放 `pps.whatsapp.net` 与 `mmg.whatsapp.net` 作为图片来源；无头像或不可读时继续显示中性人物图标。
- 按既定节点提交并推送 Git；仅直接运行本地开发版。

# 1.4.4 — WhatsApp 状态与残留浏览器恢复（2026-09-22）

- 顶部状态显示 WhatsApp 字样、绿色聊天图标及实时已连接/未连接状态，并固定在客户聊天列右侧边界内。
- 连接前仅回收异常退出后仍占用应用专属资料目录的孤立 Chrome/Chromium 进程；不删除登录资料或读取、发送聊天。
- 未打包、未提交 Git；下一个提交节点为 1.4.5。

# 1.4.3 — 中间聊天列内的顶部工作模式（2026-09-22）

- 三枚顶部模式按钮限定在中间聊天列内，等宽、居中、间隔 12 px；列宽变化时同步居中和收窄。
- 未改模式功能、按钮高度、聊天列拖动、标题栏其它内容、聊天同步或数据。
- 未打包、未提交 Git；下一个提交节点为 1.4.5。

# 1.4.2 — 顶部工作模式按钮（2026-09-22）

- “人工”“AI辅助”“AI自动”移至顶部标题栏，增大为 36 px 高、13 px 字和 16 px 图标。
- 翻译按钮留在聊天标题区；未改模式功能、翻译、聊天同步、列宽、其它布局或数据。
- 未打包、未提交 Git；下一个提交节点为 1.4.5。

# 1.4.1 — 顶部 WhatsApp 状态与导航折叠控制（2026-09-22）

- WhatsApp 状态移至标题栏：已连接显示 10 px 绿色状态球与“已连接”，未连接显示灰色状态球与“未连接”。
- 折叠按钮移至“我的工作台”标题同一行并增大；折叠状态仍可见且图标朝右。
- 未改连接逻辑、聊天同步、导航入口、列宽、其它布局、功能或数据；未打包、未提交 Git。

# 1.4.0 — 聊天与订单标题区横线对齐（2026-09-22）

- 中间“请选择客户”标题区下边线与右侧“订单信息”标题区下边线重合。
- 未改左侧客户聊天栏、标题内容、空白提示、同步逻辑、列宽、导航、按钮、图标、功能或数据。
- 未打包；按约定在此版本提交并推送 Git。

# 1.3.9 — 工作台空白状态标题统一（2026-09-22）

- “客户聊天”“请选择客户”“订单信息”三处空白状态主标题统一为 16 px。
- 未改空白提示、聊天同步、列宽、布局、导航、按钮、图标、功能或数据。
- 未打包、未提交 Git；下一个提交节点为 1.4.0。

# 1.3.8 — 工作台拖拽分隔条单线显示（2026-09-22）

- 客户聊天栏/聊天正文、聊天正文/订单信息列之间的两个可拖动分隔处均改为一条居中的紫色指示线。
- 保留原有拖动范围、拖拽命中面积和悬停反馈；未改列宽、导航、聊天和订单内容、文字、图标、功能或数据。
- 未打包、未提交 Git；下一个提交节点为 1.4.0。

# 1.3.7 — 订单信息列可调宽（2026-09-22）

- 聊天正文和订单信息列之间新增可拖动分隔条；最右列默认约占窗口四分之一，可在五分之一至三分之一范围内调整。
- 窗口放大、全屏或拉伸时恢复默认比例；未改聊天、订单信息内容、文字、图标、功能或数据。
- 未打包、未提交 Git；下一个提交节点为 1.4.0。

# 1.3.6 — 五个业务页导航对齐工作台（2026-09-22）

- 订单管理、商品库存、利润核算、助手配置和连接与设置的导航图标、文字、菜单行和分组间距统一为聊天工作台样式。
- 仅改五页导航；顶栏、业务内容、客户聊天栏、聊天正文、订单列、文字、功能和数据未改。
- 未打包、未提交 Git；下一个提交节点为 1.4.0。

# 1.3.5 — 工作台首项与订单管理页对齐（2026-09-22）

- 工作台“我的工作台”标题下方留白调整为 9 px，与订单管理页一致；首项“聊单工作台”文字和图标随之对齐。
- 未改标题位置、导航宽度、导航项高度、客户聊天栏、聊天正文、订单列、文字、功能或数据。
- 未打包；按约定在此版本提交并推送 Git。

# 1.3.4 — 工作台左侧整体宽度随窗口变化（2026-09-22）

- 放大、全屏或拉伸窗口时，导航栏和客户聊天栏整体默认保持窗口宽度约三分之一。
- 客户聊天栏仍可拖动；窗口尺寸变化后恢复当前窗口的默认占比。未改聊天正文、订单列、文字、功能或数据。
- 未打包，Git 提交留待 1.3.5 节点。

# 1.3.3 — 工作台导航顶部间距对齐（2026-09-22）

- 工作台从共享顶栏到“我的工作台”的导航留白调整为 22 px，与其它业务页面一致。
- 未改变客户聊天栏、导航宽度、行距、图标、文字、功能或数据；未打包，Git 提交留待 1.3.5 节点。

# 1.3.2 — 工作台统一顶栏与导航图标（2026-09-22）

- 聊天工作台顶部显示与业务页一致的“聊单助手 1.3.2”标识，窗口拖动区随之对齐为 56 px。
- 导航与客户聊天列继续由原有细线分隔；导航图标按确认截图调整为聊天、归档订单、库存、利润、助手和设置。
- 客户聊天宽度、消息/订单列、文字、功能和数据未改；未打包，Git 提交留待 1.3.5 节点。

# 1.3.1 — 客户聊天列默认位置（2026-09-22）

- 客户聊天列默认右边界对齐窗口约三分之一处，仍可拖动调宽。
- 导航展开或收起时，聊天内容列宽不变，仅客户聊天列自动补偿。
- 未改其他列、文字、数据或功能；未打包，Git 提交留待 1.3.5 节点。

# 1.3.0 — 全局按钮悬停反馈（2026-09-22）

- 所有可用操作按钮和角色按钮悬停时显示小手、轻微上浮、颜色加深和浅阴影。
- 禁用按钮与布局拖动分隔条不变；未改元素尺寸、位置、功能或数据。
- 本次按五个版本节点提交 Git，未制作安装包。

# 1.2.9 — 客户列表文字与头像更清晰（2026-09-22）

- 客户列表头像、电话和日期时间各增大一号。
- 客户聊天标题区文字各增大一号，“刷新”显示为紫色。
- 仅改工作台 CSS；未打包，Git 提交留待 1.3.0 节点。

# 1.2.8 — 中文译文更易阅读（2026-09-22）

- 聊天气泡下方的中文译文改为深紫色，提高阅读对比度。
- 未改译文位置、字号、翻译、消息、订单或其他页面；未打包，Git 提交留待 1.3.0 节点。

# 1.2.7 — 消息加载提示自动收起（2026-09-22）

- “已显示最新消息”和“已读取更早消息”会显示约 3 秒后向上收起。
- 重新打开会话或再次读取较早消息会重新出现；读取中和失败提示保留。
- 未改变消息、排序、订单、发送或其他页面；未打包，Git 提交留待 1.3.0 节点。

# 1.2.6 — 真实头像与接待状态（2026-09-22）

- 客户聊天列表会裁切展示 WhatsApp 实际返回的联系人头像；未返回或无法加载时显示通用头像。
- 最后一条消息来自我方时显示绿色“已接待”，来自客户时显示橙色“待接待”。
- 不改会话排序、聊天内容、订单、发送流程和其他页面；未打包，Git 提交留待 1.3.0 节点。

# 1.2.5 — 当前页聊天自动翻译（2026-09-22）

- 打开真实会话、刷新最新消息或加载一页较早消息时，工作台会先读取本机已保存的译文，再自动翻译该页尚未翻译的文字。
- 工作台右上角“翻译”菜单中，“自动翻译当前页”默认开启并会记住开关；“阅读翻译为中文”只控制是否显示译文。逐条“翻译成中文”和“翻译未译消息”仍可手动使用。
- 译文按账号、会话、消息内容和模型配置保存于本机；不会写回 WhatsApp、订单或聊天原文。本次直接运行本地开发版，不制作下载或安装包。

# 1.2.4 — 修复工作台订单自动关联（2026-09-22）

- 聊天工作台右侧订单区现在使用订单已保存的国家/地区规则统一识别电话号码：阿联酋本地写法与 WhatsApp 的国际写法会关联到同一真实订单。
- 未能按已有国家规则核实的号码仍保持原有的精确匹配与“新建订单”兜底，不修改订单资料、聊天内容或其它页面。
- 本次直接运行本地开发版，不制作下载或安装包。

# 1.2.3 — 工作台侧栏对齐（2026-09-22）

- 工作台展开侧栏恢复“我的工作台 / 助手管理”分组，统一导航字体、行高、图标与间距；订单管理入口保留。
- 折叠按钮不再显示为“我的工作台”紫色菜单项，改为侧栏底部图标入口。
- 本次直接运行本地开发版，不制作下载或安装包。

# 1.2.2 — 明确显示已保存的 API Key（2026-09-22）

- “连接与设置”的 API Key 已保存时，改为显示“密钥已安全保存”实字和浅绿色状态，不显示密钥原文，也不再出现会被误解为异常的输入焦点框。
- 切换服务商会按该服务商的实际保存状态同步更新；未保存时仍显示安全输入提示。密钥加密存储和其他业务功能未改。
- 按当前开发方式仅更新本地运行版至 1.2.2，没有制作新的安装包。

# 1.2.1 — 修复工作台顶部右半无法拖动（2026-09-22）

- 工作台拖动区从 macOS 控制按钮右侧延伸至窗口右边缘，高度限定在既有 48 px 顶部空白区；不再沿用旧布局右侧 600 px 的预留。
- 切换页面后返回工作台仍使用完整拖动范围；模态弹窗打开时停用拖动层，关闭后恢复。下方导航、搜索、聊天与订单操作保持原样。
- 应用、工作台、设置页与安装包统一为 1.2.1。本轮只验收顶部局部修复，完整真实聊天业务未重新核验；订单详情旧手机底部 P1/P2 偏差未改。

# 1.1.5 — 恢复六个业务页面（2026-09-21）

- 以 1.1.4 为基础，恢复商品库存、利润核算、助手配置、连接与设置、产品详情、每日利润详情的确认稿结构、页签与可用操作。资料来自本机库存、订单、商品档案、利润、助手及连接服务；空库不填演示客户、售价、金额或连接状态。
- 商品档案编辑、库存同步、每日成本保存、利润导出、AI 密钥安全输入与测试、订单数据库备份与确认恢复接入现有服务。原稿纯演示而没有独立服务接口的开关、筛选与导出明确禁用，不伪称已保存。
- 保留 1.1.4 的 ShopPlus 导入、新建订单、工作台卡片及 WhatsApp 关联边界；正式用户资料与旧版 ZIP 不动。手机底部 P1/P2 的既有偏差不在本轮修改范围，工作台与订单详情仍不算整页通过。

# 1.1.4 — 工作台真实订单卡片与点击边界（2026-09-21）

- 工作台近期订单以独立卡片显示；选中卡片使用浅紫背景。真实订单状态显示中文、有色标签和对应图标；“需注意”数量显示在有色圆形徽标中。
- 点击订单卡片只选中，不进入订单详情；只有明确点击“订单详情”按钮才跳转。卡片内容继续来自本机真实订单库，不补造聊天、未读数或客户资料。
- 订单管理、订单详情业务区和其他页面未改。手机底部旧版大英文框与“待发送”仍不符合确认基准。

# 1.1.3 — 修复订单电话误判（2026-09-21）

- 订单电话使用项目已有的国家区号识别规则核对 WhatsApp 号码。已知国家的本地写法与完整国际写法指向同一号码时可以关联；真正不同或不能核实的号码仍拦截。
- 关联、重新打开聊天、刷新和发送前都使用同一规则；不改订单原始电话、已导入数据或 WhatsApp 登录状态。本次没有读取或发送真实客户消息。
- 手机底部大英文框与“待发送”行仍不符合确认基准，工作台和订单详情整页不标记通过。

# 1.1.2 — 恢复真实订单入口与订单聊天关联（2026-09-21）

- 订单管理恢复 ShopPlus 文件导入预览、逐单核对、确认写入、列表搜索和真实订单详情；手动新建为空白草稿，不预置客户资料。工作台近期订单也从本机订单库读取。
- 所选订单的 WhatsApp 聊天须核对账号和号码后关联，号码与订单不一致或更正后未重新核对时禁止读取／发送；读取限定最近 24 小时、最多 100 条。
- 本次以隔离空资料目录验证界面与启动，未接触正式订单、未扫码、未读取或发送真实 WhatsApp 消息。ShopPlus API 自动同步、库存和利润业务页未恢复。
- 手机底部大英文框与“待发送”行仍不符合确认基准，工作台和订单详情整页不标记通过。

# 1.1.1 — 页面板块隔离（2026-09-21）

- 聊天布局和快捷回复样式只装入工作台及订单详情；商品图片按钮只装入库存与产品详情。图片对话框样式单独共享，避免以后改一处波及无关页面。
- 1.1.0 的十一张固定尺寸页面截图在改动前后逐像素一致；本版保持空白资料与真实 WhatsApp 关联范围，未接入 Shoplus API 或恢复订单业务页。
- 手机底部大英文框与“待发送”行仍不符合确认基准；这次未获该部位修复要求，工作台与订单详情整页仍标记未通过。

# 1.1.0 — 独立空白版与真实 WhatsApp 连接入口（2026-09-21）

- 保留 1.0.12 包和资料；1.1.0 使用新的应用标识、本地端口与空白资料目录，不复制旧订单、登录或草稿。
- 工作台清除示例客户、任务和聊天。其他页面暂显示明确空状态，避免把示例订单、商品、利润和连接状态当成真实资料。
- 工作台及空白订单详情保留 WhatsApp 关联入口。扫码后还须核对发送账号、目标号码并确认最近 24 小时的读取；本版联验不发送消息。
- 订单导入、新建及其他业务页尚未接入空白版；手机底部仍有 1.0.12 已记录的设计偏差。本版不能作为九页业务完成或真实读取已通过的证明。

# 1.0.12 — 修复手机聊天身份与逐条译文（2026-09-21）

- 订单详情与工作台手机聊天保留客户、人工发送和 AI 自动发送的身份文字、语义图标与时间；无法核实来源的旧我方消息显示“我方发送”。
- 中文译文固定在各自原文气泡外下方；显示开关、单条／批量翻译及刷新行为统一，图片占位不造译文。
- 真实聊天的译文按账号、聊天、消息正文和模型配置存入本机，重启后复用；仍需人工点击才向翻译服务提交未译原文。
- 手机顶栏、底部输入区、快捷回复、订单业务卡和其他独立页面保持原有结构。

# 1.0.11 — 恢复聊天记录逐条中文翻译（2026-09-21）

- 恢复订单详情和工作台中每条聊天消息的中文译文及显示／隐藏开关。
- 真实聊天支持逐条翻译或按按钮翻译尚未翻译的消息；沿用翻译服务，结果按消息原文缓存，刷新不重复调用。
- 仅修改消息翻译；底部英文输入框、快捷回复、模板、发送流程和布局参数保持不变。

# 1.0.10 — 订单弹窗与回复细节修复（2026-09-21）

- 订单 TXT 改为弹窗预览，支持复制、导出；新增独立订单模板设置，字段顺序、标签和换行可保存。
- 快捷回复设置支持中英文编辑、增删、排序、启用隐藏、字段和商品图片调用；已有草稿选择替换或追加。
- 状态按钮加大字号、加深颜色并增加间距；人工模式三个按钮去掉多余外围边框。
- 增加小扫帚，中英文、中文对照及手机英文同步清空，保留图片，支持短暂撤销。
- 手机底部增加固定英文编辑框，与左侧双向同步；未发送内容不再显示为聊天气泡，发送成功后进入历史。
- 真实连接范围不变，开发验证使用虚构客户，不发送真实消息。

# 1.0.9 — 左侧回复编辑与手机同步预览（2026-09-21）

- 工作台和订单详情将快捷回复、模式选择、中译英、英文核对及发送工具集中到手机左侧；支持调宽、收起和小窗口浮动展开。
- 手机移除回复编辑器，同步预览待发送英文、表情和图片，不附带中文翻译；历史与待发送内容明确区分。
- 「本地预览」改为「发送」，需主动扫码并核对真实客户后启用。仅读取该客户最近 24 小时消息；其他订单资料继续为演示内容。
- 成功后清理本次草稿；重复点击保护、部分成功记录及结果不确定时停止重发。真实订单尚未关联，演示订单确认模板不会用于真实聊天。
- 保留翻译服务、图片库与四种表情；AI 辅助和自动回复仍不启用。
- 发送流程使用虚构客户端验证，开发期间真实消息发送数为 0，真实 WhatsApp 端到端发送尚未实测。

# 1.0.8 — 恢复人工回复中译英（2026-09-21）

- 恢复订单详情和工作台的“转为英文”按钮，接入已有翻译服务；输入中文，点击翻译后核对可编辑的英文及中文对照。
- 保留已有中英文草稿直至翻译成功。失败或超时不清空内容、不自动重试；编辑草稿或切换客户后丢弃过时结果。
- 表情随翻译保留，图片、快捷回复和 1.0.7 布局继续保留。
- 增加“翻译服务设置”入口，沿用本机服务配置及安全密钥存储；不会自动开启 AI 辅助或自动回复。
- 翻译只在人工点击后提交本次中文；预览仍不向 WhatsApp 发送消息。

# 1.0.7 — 全页布局统一与人工回复工具（2026-09-21）

- 九个页面去除外框和外围留白，固定合并顶栏、导航及底部状态，内容区域内部滚动。
- 各页面共用按显示器保存的导航宽度、导航和内容字号；工作台同步订单详情的手机尺寸、字号及回复区调节。
- 手机左侧增加固定 120px 快捷回复栏，支持收起；小于 1100px 窗口自动收起，通过按钮打开。
- 订单确认模板读取当前页面客户、商品、数量、配送费及应付金额；其他快捷回复包括地址、资料、选品、配送及跟进。只填入草稿。
- 放大并着色表情和附件按钮，支持四种表情、图片上传、缩略图、预览、移除及商品图片库选取。
- 库存及产品详情新增本机商品图片管理入口；图片与库存、价格数据分开保存。已存档商品图片也可读取选用。
- 工作台切换客户时分别保留当前页面内各客户的草稿，避免混入另一位客户内容。
- AI 辅助未深入开发。自由输入中文不假装已翻译；英文可编辑，快捷回复自带英文模板。真实 WhatsApp 发送仍未连接，按钮明确显示“本地预览”。

# 1.0.6 — 订单详情固定外框与内部滚动（2026-09-21）

- 整个页面不再滚动；顶部固定，中间订单、右侧手机区域分别滚动。
- 导航背景延伸到窗口底部，连接状态固定在导航底部。
- 聊天与回复之间增加浅紫色可拖动横条，支持键盘、双击重置与按屏幕保存。
- Mac 订单详情合并原生标题栏与品牌栏，保留红黄绿按钮；其他页面保留原内容尺寸。
- 保留 1.0.5 的默认字号、手机比例和已有屏幕设置。

# 1.0.5 — 订单详情独立布局设置（2026-09-21）

- 默认导航和内容字号 15px、聊天正文 13px；字号独立于窗口尺寸。
- 三栏宽度、手机四边与四角、三个区域字号可调，手机默认锁定长宽比。
- 按内置屏和外接屏分别保存布局，支持恢复本屏幕默认。
- 手机高度不再为了适应矮窗口而压缩；保留各区域独立滚动。
- 仅修改订单详情布局，其他八页的确认稿和业务交互保留。

# 1.0.4 — 订单详情比例校准（2026-09-21）

- 按 UI-028 的桌面构图重新分配订单详情三栏比例：加宽导航和聊天区，收紧中间订单内容区。
- 放大左侧导航文字与图标，增加项目高度和横向留白。
- 页面外框与侧栏延伸至窗口底部，消除大尺寸窗口底部的白色空区。
- 保持订单卡片、按钮位置、手机内部结构和窄屏响应方式不变。

# 1.0.3 — 打包界面渲染修复（2026-09-21）

- 修复打包版安全策略阻止确认稿内联样式和交互脚本的问题。
- 增加使用正式本地服务响应头的渲染验证，防止开发预览正常而 App 退化为默认样式。

# 1.0.2 — 确认稿直接实现（2026-09-21）

- 九个主要页面改为直接加载最终确认稿的结构与样式，取消通用组件近似重画造成的比例和细节偏差。
- 恢复确认稿中的按钮层级、分隔、圆圈说明、状态指示、侧栏图标和业务图标。
- 统一“订单管理”“利润核算”“助手配置”等最终导航名称，并打通页面间跳转。
- 修复订单详情右侧手机固定位置；左侧滚动时手机保持在可视区域。
- 保留确认稿的页签、弹层、编辑、联动、确认后折叠和详情跳转交互。

# 1.0.1 — 确认稿视觉修复与演示交互（2026-09-20）

- 按最终确认稿重新校准九个主要页面的结构、比例、间距、颜色和卡片层级。
- 恢复侧栏、导航、模式、订单状态、库存状态及业务操作所需的语义图标。
- 补齐圆圈问号说明、浮层、状态提示和操作反馈。
- 产品详情恢复商品图片、产品资料、价格与库存来源、聊单可用状态等确认稿结构。
- 订单详情加入订单推进阶段、状态组、固定手机预览和完成后自动折叠。
- 助手配置与连接设置改为真实页签交互，加入 WhatsApp 扫码登录演示弹层。
- 内置 24 个订单、12 个聊单用户、8 个商品和 7 日利润数据，用于独立交互核对。
- 演示模式不访问真实 WhatsApp、金山文档或 AI API。

# 1.0.0 — 独立重构首版（2026-09-20）

- 新建独立 React + TypeScript + Electron 工程，与旧软件代码和数据彻底隔离。
- 保留已验证的订单、库存同步、利润、WhatsApp 与 AI 业务后端。
- 应用直接在 Electron 窗口中运行。
# 1.7.9 — 聊天历史保留与缺口提示

刷新现在会合并消息，不再用较短的最新记录覆盖已显示的旧聊天。已读取的会话文字和消息信息会保存在本机，重启后按账号与客户会话恢复；图片本体仍按可视区域读取。WhatsApp Web 暂未提供更早记录时显示明确提示，稍后刷新可重新尝试。截图中另一条加密通知也不再显示成假图片。

# 1.8.0 — 无订单聊天持续留存

已核对的一对一 WhatsApp 新消息在到达时就写入独立本机历史库，不再要求先关联订单或打开工作台会话。已保存消息在浏览器重启、刷新返回较少记录后仍可显示；群聊和未核实电话身份不会被写入。此改动无法追溯恢复升级前未保存、且 WhatsApp Web 当前未提供的旧聊天。仅直接运行本地窗口，不制作安装包或提交 Git。

# 1.8.1 — 会话来源对照与历史保存补齐

打开已核对会话时，本机保存 WhatsApp Web 实际返回的可用记录，而非只保存屏幕最后 20 条；向上翻页取得的消息也继续保存。后续来源返回占位内容或暂时读取失败，不再覆盖已保存原文或清空可读历史。当前指定会话在 WhatsApp Web 和本机来源都只有一条通知，旧消息仍须由可核实的另一来源补回；本版不声称已恢复该缺口。只运行本地操作窗口，图片显示问题留待后续处理。
