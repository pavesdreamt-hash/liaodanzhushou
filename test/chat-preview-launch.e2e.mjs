import test from 'node:test';import assert from 'node:assert/strict';import {_electron as electron} from 'playwright-core';import {mkdtemp,rm,access,readFile} from 'node:fs/promises';import os from 'node:os';import path from 'node:path';
test('versioned packaged isolated launcher keeps database separate and starts usable empty draft UI',async()=>{assert.ok(process.env.KDOCS_TEST_EXECUTABLE);const dir=await mkdtemp(path.join(os.tmpdir(),'chat-preview-launch-'));let app;try{app=await electron.launch({executablePath:process.env.KDOCS_TEST_EXECUTABLE,args:[`--isolated-user-data=${dir}`],env:{...process.env,NODE_ENV:'test'}});const identity=await app.evaluate(({app})=>({version:app.getVersion(),packaged:app.isPackaged,userData:app.getPath('userData')}));assert.deepEqual(identity,{version:JSON.parse(await readFile('package.json','utf8')).version,packaged:true,userData:dir});const page=await app.firstWindow();await page.getByRole('button',{name:'订单管理',exact:true}).click();await page.getByRole('button',{name:'新建订单',exact:true}).click();await page.getByRole('heading',{name:'草稿 #1',exact:true}).waitFor();await page.getByRole('button',{name:'订单助手',exact:true}).click();await page.getByText('WhatsApp 尚未连接',{exact:true}).waitFor();await page.getByText('AI 尚未连接',{exact:true}).waitFor();await access(path.join(dir,'orders','orders.sqlite'));}finally{await app?.close();await rm(dir,{recursive:true,force:true});}});
