# 图片剩余三步修复核验 — 1.8.4

日期：2026-09-24。交付方式：现有本地 Electron 操作窗口；不制作安装包，不提交或重置 Git，保留全部已有未提交改动。

## 本次修复目标

按 `MEDIA-STEP1-DIAGNOSIS.md` 的后续要求完成图片读取的三步：①已选、已核对会话只读取可见图片，统一排队最多两张并行，离开视口的排队任务移除，按单条消息 ID 查找而不逐张扫描 500 条；②把原约 1 MB 自设上限调至单张 16 MB，使用有界内存缓存、准确失败提示与手动重试；③在实际工作台窗口检查解码、滚动、重试、切换会话和阅读位置，并增加点击缩略图放大、关闭与原尺寸查看。纯图片占位不送翻译，以免无意义翻译引发聊天整段重绘。正常文字翻译及其他业务页面未改。

## 验收结果

| 检查 | 结果及证据 |
| --- | --- |
| UI 可见区域与队列 | 隔离 Electron 窗口放入 40 条虚构图片消息：初始只请求底部可见图片，滚动到第 0 条后才请求它；记录到的最大并发 ≤2。排队图片离开视口后移除。|
| 精确读取 | 隔离适配器读取较早图片时只用一次 `fetchMessages({limit:1})` 核对聊天，随后 `getMessageById` 直取指定消息；没有为这张图片扫描最近 500 条。WhatsApp 库 1.34.7 提供该方法。|
| 大图、安全上限和缓存 | 隔离适配器接受 1.2 MB 图片数据，超过 16 MB 给出明确错误；3 张各 14 MB 的附件读入后最旧缓存被淘汰，主进程缓存维持 ≤48 MB、≤20 项；再次打开缓存中的图片不重复下载。此测试检查传输与缓存限制，不等于真实 WhatsApp 大图成功。|
| 失败、重试、切换 | 隔离窗口模拟一张图片首次失败，显示失败原因与“重试读取”；点击后消息变为真实解码图片。另模拟浏览器无法解码的坏图，重试请求明确带 `force=true`，绕过旧缓存并获得可解码的 1024×1024 图片。切换到第二个虚构会话后旧图片、旧放大框均不进入新聊天。连接代际变化时已排队图片被拒绝，迟到下载不进入缓存。|
| 放大和阅读位置 | 隔离窗口使用项目内真实 1024×1024 PNG；消息缩略图与放大框均由 Chromium 解码，`complete=true`、`naturalWidth=1024`、`naturalHeight=1024`。亲自查看最终截图，图片在弹窗内完整显示、底部不裁切；点击图片切换原尺寸，关闭按钮可用。单图失败重试后首条可见消息锚点偏移 <12 px。截图：`artifacts/media-step3/image-zoom-fixture.png`，SHA-256 `63226bdd7d6cdac960c8a1a06e2e1b2cc0cddb05265d4011e5a5639a113548a7`。该截图使用虚构会话和本地 PNG，**不是 WhatsApp 客户图片**。|
| 相邻功能 | `npm run check` 通过；最终 33 项相关单元测试通过；图片窗口、可见文字翻译、系统通知、隐藏会话共 4 项隔离 Electron 回归串行通过。并行运行曾出现图片阅读锚点断言失败与翻译测试元素重绘时脱离 DOM，单独图片复测及四项串行复测均通过；并行稳定性不记为通过。过期的 `chat-workbench-auto-translation.e2e.mjs` 仍硬编码 1.6.17 且依赖已废弃的自动选中行为，本次没有把它的失败作为当前版本通过；正常文字翻译另由新视口回归通过。|
| 本地直接可用 | 正式本地窗口已以 1.8.4 启动并置于前台；实屏可见顶部和左下角版本 1.8.4、WhatsApp 已连接。窗口当前未选聊天，没有主动读取用户指出的正确客户号码，也没有发送消息。|

## 尚未通过的真实来源验收

本轮**没有从 WhatsApp 实际取回并在正式窗口展示一张客户聊天图片**。此前成功的真实诊断只证明两个 08-25 候选 JPEG 能由 `downloadMedia()` 取回，并没有在 1.8.4 工作台中完成端到端显示；09-07 18:53 那张具体图片仍未在可用的已核对消息源中定位。因而不能承诺所有历史客户图片都已恢复。若 WhatsApp Web 对某条旧消息本身不返回图片，系统会保留原消息、给出明确失败提示和重试入口；不会用截图或模拟数据伪造图片。已开始的底层 WhatsApp 下载无法真正取消，切换会话时只丢弃过期结果；等待超时并非底层取消。

此交付是 D-098 范围的局部修复，未把整个聊天工作台或旧手机页面标成全面通过。全页基准未重验；旧手机页 P1/P2 已按用户计划弃用，仍列为未检查。没有读取或更改正确客户的聊天、电话号码、订单、WhatsApp 原始资料，也未重新扫码、发送、调用 AI。

## 核验文档与规则

实施前已读 `AGENTS.md`、`CONFIRMED-UI-BASELINE.md` 的 D-075、D-082、D-090～D-098，以及 `MEDIA-STEP1-DIAGNOSIS.md`、`MEDIA-STEP2-ACCEPTANCE.md`；交付前再复读本文件对应的项目规则。用户最新指令只需本地操作窗口，不要安装包，优先于旧版文档的 ZIP 打包规则。旧 CSS 在图片规则之前存在缺失括号，若直接修复会激活后续非图片规则；因此本轮仅把新图片样式放在该规则前，没有改变其他页面样式。`git diff --check` 已通过。
