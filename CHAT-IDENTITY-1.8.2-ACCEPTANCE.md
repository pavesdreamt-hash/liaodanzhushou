# 聊单助手 1.8.2：会话身份拦截验收

## 本次修复目标

防止工作台把“最近的一条 WhatsApp 会话”自动当成目标客户；只有用户点选会话后才读取。平台用确定性号码和会话 ID 规则核对身份，不使用 AI，不根据订单、头像、消息数量或系统通知猜测身份。明确冲突时停止读取或显示；暂时无法复核 LID 映射时不误拦，提示待复核。过期的列表映射如果能从 WhatsApp 直接号码入口重新核实为同一客户，允许打开。

## 真实验收与边界

- `npm run check` 通过：TypeScript、UI 构建、WhatsApp 兼容性和项目检查均通过。
- 23 项相关单元测试通过：包括正确 LID 映射可打开、冲突映射被拦、暂时缺失映射不误拦、过期列表映射可通过号码入口恢复、聊天服务在冲突时不读取历史、不保存消息。
- 隔离 Electron 工作台使用虚构号码通过：屏幕版本显示 `1.8.2`；列表加载后不自动打开最新会话；明确身份冲突的会话不显示消息；另一条正确会话仍显示消息。隔离测试未连接真实 WhatsApp。
- `git diff --check` 通过。现有未提交改动保留；本轮未制作安装包、未提交 Git。
- 1.8.2 本地操作窗口已启动，日志确认主窗口加载、显示、获得焦点，原有 WhatsApp 登录资料恢复认证并在线。**没有打开或读取用户指出的正确号码对应的真实聊天**，因此尚未验收该特定号码的真实映射与历史消息；不把虚构测试当成真实客户验收。旧手机页 P1/P2 和全工作台基准未在本轮重验。

## 规则核对

核对 `AGENTS.md` 与 `CONFIRMED-UI-BASELINE.md` 的 D-096：只改会话选择、身份核对及必要的版本、测试和文档；原有聊天、订单、图片、翻译和其他页面不因本轮判定为已验收。D-095 的错误目标会话结论已在基准和原验收文档中附纠错。
