# 聊单助手：已确认界面基准与防误改规则

更新日期：2026-09-21。状态：**设计与验收基准，不代表当前程序已经全部达到**。本文件持续记录已确认目标、当前偏差和逐版本验收结果；不能把某一项测试通过写成整个页面达标。

## 1. 怎样使用这份基准

1. 用户本轮明确提出的要求优先于本文件；本文件优先于旧设计图、旧验收记录和当前代码。新要求与本文件冲突时，先把对应条款改成新要求，并在末尾登记。`USER-GUIDE.md` 和历史发布说明描述当时**实际交付**，不能推翻这里的**目标设计**。讨论而未明确要求改代码时，只讨论或更新文档，不动应用。
2. 表中标为“已确认”的内容来自用户明确要求或确认；“当前参考值”是现有样式/布局模型里的实测参数，只作为实施起点，不冒充用户亲自指定的像素值。未确认的数值不得凭感觉写成不可变标准。
3. 修改前列出**允许修改的页面、组件、行为和文件**，并列出邻近但必须保留的元素。修改后检查实际变更范围、界面结构、尺寸、交互、数据状态；无关改动立即撤回。不能以“整体更协调”为由连带重画。
4. 验收以可直接打开的成品为准。开发者自己核对文档和实际运行效果，不能要求用户制作对照截图或替开发者找误改。文档和自动检查不能替代实际运行检查。
5. **“本轮改动通过”与“整页符合基准”是两种结论**。只修其中一项时，其他已知偏差必须继续标为未通过，并在交付开头说明；不得以新版本号、局部测试通过或“已修复”的笼统措辞暗示整页完成。未经用户明确要求，不借局部修复擅自改其他区域。

## 2. 全局结构与显示

| 项目 | 已确认的规则 | 当前参考值 / 核对点 |
| --- | --- | --- |
| 使用场景 | 一台电脑，常在 24 英寸外接显示器或 16 英寸笔记本上**最大化**使用；1024 × 1000 实验视窗不作为日常默认设计尺寸。1280 × 820、1440 × 1000 等窗口下仍需检查字号、手机比例和可用空间。 | 不能因窗口变宽/变窄自动把字号或手机高度压得失真；宽度不够时先按既定响应规则布局。 |
| 外框与顶栏 | 去掉包住整页的外层圆角框；应用内容延伸到窗口底部。合并软件顶栏与内容顶栏，主顶栏固定在顶部。保留品牌、帮助、用户等原有元素及 macOS 窗口控制区；不制造第二条重复标题栏。 | 当前共享顶栏高约 56 px；这属于现有参考值。 |
| 滚动归属 | 整个窗口不跟着订单内容滚动。导航、订单正文、聊天记录各自在内部滚动；滚动条靠相应内容的内边缘，不能跑到整窗最外侧。导航和内容需延伸至窗口底部。顶部固定，手机顶部固定，聊天记录在手机内部滚动。 | 内容滚动槽约 8 px；右侧聊天内部滚动，底部输入区保持可见。 |
| 页面保护 | 九个已确认页面的现有结构、导航、文案、业务控件、语义图标及交互属于保护范围。某页需求不授权顺带修改别页；订单详情先核对，再明确同步的部分才应用于其他页。 | 原始结构分别见 `ui/src/confirmed/` 的九个 HTML 文件；这些文件是**未被本文件新要求覆盖部分**的细节参考，不可用其中旧设计覆盖本文件。 |
| 设计元素 | 图标保持语义和身份；按钮的主次层级、选中/悬停/禁用状态、边框、圆角、留白、标题和内容层次保留。 | 现有确认稿使用 Lucide 语义 SVG；不要用无关字符代替、删除或换成泛用图标。 |

九页保护索引（新任务只触及明确指定的页面）：

| 页面 | 不经明确要求不得改掉的主要结构 |
| --- | --- |
| 工作台 `workbench.html` | 统计卡、任务行、状态与模式图标、手机聊天区及导航。 |
| 订单管理 `orders.html` | 页签、日期、编号、客户列、状态提示、行内操作。 |
| 商品库存 `inventory.html` | 面包屑、同步区、商品行、概览编辑、方形商品图、缺货提示。 |
| 利润核算 `profit.html` | 汇总卡、图表、说明、汇率表单、滚动明细。 |
| 助手配置 `assistant.html` | 模式卡、规则状态、页签。AI 辅助暂不深度开发。 |
| 连接与设置 `settings.html` | 各设置页签、连接状态、备份与清理、登录弹层。 |
| 产品详情 `product.html` | 方形主图和缩略图、编辑、导入区。 |
| 订单详情 `order.html` | 订单阶段、状态组、客户/商品/配送等业务卡、快捷回复、手机聊天。下文对此页有更高优先级的细化。 |
| 每日利润详情 `profit-detail.html` | 标题、成本卡、逐单明细。 |

## 3. 订单详情三栏、字号和调整

| 区域 | 已确认的形态与交互 | 当前参考值（可调，非固定标准） |
| --- | --- | --- |
| 左侧导航 | 宽度与字体可调；有拖动分界，导航名称/图标/选中态保留。连接状态留在导航底部。 | 默认按窗口宽约 15% 计算，初值限制 151–260 px；用户设置允许 120–340 px；导航文字默认 15 px、可调 13–22 px；导航图标约 18 px，条目最小高 46 px。 |
| 中间订单内容 | 宽度与字体可调；内容独立滚动，订单卡片内容不因右栏变化被挤成难读窄列。 | 内容文字设置默认 15 px、可调 13–22 px；一级标题现约 20 px、卡标题约 15 px、字段值约 15 px、字段标签约 13 px，随中栏字号设置同比调整。主内容内边距约 14 × 18 px。 |
| 右侧手机与快捷回复 | 手机可上下、左右、对角调整；可锁定或解锁长宽比；手机字体可调。快捷回复在手机左侧，有固定初始宽度、可调宽并可收起。两块连接位置和拖动把手要清楚可见。 | 手机默认宽至少 333 px，初始高按宽 × 2.1 计算；允许宽 280–650 px、高 560–1500 px；字号默认 13 px，可调 12–20 px。快捷回复默认宽 300 px，可调 280–420 px。手机高度需符合比例，不能因缩放显示为明显矮胖的“压缩手机”。 |
| 窄窗口 | 仍能操作订单和聊天；优先保住可读字号、手机合理比例和可滚动内容。 | 当前布局宽小于约 980 px 时导航收为 64 px 图标栏；这只是既有响应阈值，修改须自检 1280 × 820 和 1440 × 1000。 |

默认字号遵循用户在 24 英寸外接屏所认可的那版界面；上表记录的是代码中的默认 CSS 像素值。**物理屏幕尺寸本身不能推出用户实际看到的毫米大小**，还受系统缩放和屏幕分辨率影响。以后调整用应用内字号控件，不以窗口缩放强制改默认字号；不擅自把 1024 × 1000 的实验比例套用到常用屏幕。

## 4. 订单详情业务区

| 元素 | 位置、外观和行为 |
| --- | --- |
| 订单抬头/返回 | 位于中栏顶部，订单编号、返回、阶段信息保留；顶部固定区不能挤掉状态操作。 |
| 状态按钮 | “待确认、待报单、待收货、拒单、联系不上”等保留原意；文字加深、按钮间距加大。当前参考：14 px 文字、至少 36 px 高、水平内边距 13 px、间隔 8 px；选中为紫底白字。空间不够时换行，不能让最后按钮截断。 |
| 订单 TXT | “生成订单 TXT”的预览使用**弹窗**，不在详情页正文长期展开。格式/字段/样式的配置应有专门设置入口；生成条件和未满足原因仍可见。快捷回复模板与订单 TXT 模板是不同配置，不能混用。 |
| 已有客户、商品、配送、签收、回款结构 | 保持卡片顺序、数据、核对/编辑/状态流转及既有图标；聊天区改动不构成改这些业务卡的授权。 |

## 5. 快捷回复面板：位置、每个控件和流程

快捷回复固定在**手机左侧**，视觉上是一块清晰的独立工作区域，不占手机消息内容；与手机之间的交界和可调宽把手清楚可见。可收起后给手机和订单内容让位。以下为用户确认的目标状态；数值是现有参考，除非用户已明确指定，后续可在不改结构的前提下校准。

| 顺序 / 元素 | 位置与外观 | 点击/输入后的行为 |
| --- | --- | --- |
| 标题行 | 顶部“快捷回复”，左侧闪电图标（紫色）；右侧设置齿轮、收起箭头。标题当前约 15 px，闪电约 18 px，行高至少 42 px。 | 齿轮打开快捷回复内容设置；箭头收起/展开，不丢草稿。 |
| 快捷语 | 标题下方**两列三行**，依次为确认订单、询问地址、补充资料、确认商品、配送说明、礼貌跟进；每个是独立按钮。当前按钮最小高 33 px、字约 12 px、列/行间隔约 6 px。 | 模板支持单独设置中文内容、英文内容/翻译规则、变量及调用；点击填入人工回复的中文输入，进入核对流程，不直接发送。不可把订单 TXT 当快捷语显示。 |
| 模式行 | 快捷语下为“人工 / AI辅助 / AI自动”三个按钮，**外面不再包一层多余边框**。当前各约 32 px 高、字 12 px、图标约 14 px；未选文字用较深色，选中紫底白字。 | 切换模式的可用状态、草稿和提示必须明确；AI 辅助当前不深度开发，不应假装已完成能力。不得在切换时清掉人工草稿或触发发送。 |
| 中文输入标题 | “中文输入/用中文写回复”在输入框上方；右侧小扫帚，紫色，当前点击区约 30 × 28 px、图标约 19 px。 | 小扫帚一次**同步清空中文、英文和中英核对内容及手机未发送预览**；已选图片和已经发送的聊天记录不跟着清空。按钮禁用/可用状态应准确。 |
| 中文输入框 | 白底有清楚边框，当前约 90 px 高、最低 65 px，正文默认约 13 px、行高 1.55。 | 输入中文，或由快捷语填入；编辑中文时旧英文不可冒充对应新文案。 |
| 翻译操作 | 中文框下标明发送语言（英语），“转为英文”紫色操作清晰可见。 | 用户明确触发转换；不能把中文原句直接显示为英文完成态，也不能自动发送。 |
| 英文内容 | 标明“英文内容·可编辑”；白色编辑框，当前约 120 px 高、最低 90 px，默认约 13 px、行高 1.55。 | 转换后可人工校对/编辑；用户真正准备发送的是核对后的英文。英文改动要反映到手机预览；中文比较区保留原中文用于核查，不假称自动回译。 |
| 中文对照/反馈 | 英文框下以浅紫/浅灰背景单独显示中文对照及翻译反馈，当前约 11 px。 | 辅助核查英文含义，**不混入手机英文气泡，不作为发送文字**。失败时保留输入并给明确错误，不填假译文。 |
| 底部工具 | 左侧笑脸为暖黄、曲别针为紫色；各当前约 36 × 36 px、图标约 21 px，比旧灰色小图标更明显。右侧主按钮紫色。 | 笑脸弹出少量常用表情：友好、疑问、抱歉、OK。曲别针可上传本机图片，或从库存选产品图片；选图可预览、移除。主按钮的文案应与真实动作一致：连接/核对前是本地预览，只有明确满足发送条件时才标“发送/确认发送”；点击前核对目标、英文与附件。 |

**真实发送边界**：用户要求的“点击发送”不能被“仅本地预览”冒名顶替，也不能误把预览按钮做成无需核对即向外发送。账号与客户聊天要明确绑定、英文草稿和附件经过人工核对，状态才能表述为可发送；未具备真实发送能力时明确标注预览/未发送。历史聊天和草稿按订单隔离。AI 自动发送不因快捷回复的按钮存在而默认开启。

## 6. 手机聊天：身份、翻译、底部

| 元素 | 已确认的细节 |
| --- | --- |
| 手机轮廓与顶端 | 维持手机外形、深色边框和圆角；顶部有听筒槽/状态时间、Wi‑Fi/电池、返回箭头、头像、客户号码/名称、更多操作；绿色聊天顶栏固定，不随消息滚走。宽高按第三节调整，不能单独纵向压扁到不合理比例。 |
| 译文工具条 | 联系人顶栏下方保留“显示中文译文”开关与翻译按钮。工具条是独立区域，不和消息气泡合并。开关仅决定**已有中文译文显隐**；“翻译”按钮才触发翻译。默认显示状态遵循保存设置，不因切页随意翻转。 |
| 聊天画布与方向 | 手机消息区为淡米色背景，可独立滚动；客户消息在左、白色气泡，我方消息在右、浅绿色气泡。原文必须完整留在原气泡中。默认正文约 13 px、行高 1.55；小字随手机字号控件同步变化。 |
| 身份图标与文字 | **每条消息**的气泡内底部有身份图标和文字，时间位于同一行右侧。客户为人形轮廓图标＋“客户”；已知人工发出为人形图标＋“人工发送”（或既有等义人工作者标签）；有明确 AI 来源标记的消息为机器人图标＋“AI自动发送”。WhatsApp 旧我方消息若没有可核实的来源标记，只能标“我方发送”，不得猜成由人工或 AI 发送。人、AI、客户不能合并成无身份的纯文本气泡，图标不能丢。当前参考：身份行文字约 10 px、时间约 9 px、图标约 13 px。 |
| 逐条中文译文 | **每条外文消息对应一条中文译文**。译文以灰色小字放在该消息气泡**外侧下方**，跟随原消息一组排列；不能塞进气泡正文，不能与原文混成一段，也不能集中列在别处。客户与我方均适用。参考字号约 11 px。隐藏译文只隐藏这行，不隐藏原消息和身份。 |
| 新消息与预览 | 新消息仍按时间顺序进入手机聊天，身份、时间、图标和逐条译文规则完全一致。左侧正在编辑的英文可在手机同步**本地预览**，预览要明显区分未发送和已发送；不要复制一个完整英文编辑器到手机底部。 |
| 手机最底部 | 保留类似真实聊天软件的**白色短输入框＋紫色小纸飞机按钮**及底部 home 指示条，用于完整视觉效果。这里不展示整段英文，不与左侧英文框争夺编辑主入口；不暗示点击纸飞机就绕过左侧核对流程发送。底部固定，聊天滚动只发生在上方消息区。**明确排除**：大块多行“英文回复”编辑框、“英文回复会同步到这里，也可以直接输入”提示和单独的“待发送”行。英文全文只在左侧可编辑框核对。 |

逐条翻译适用范围：当前显示且有正文的外文消息，包括较早历史和可见引用/编辑正文；英语、阿拉伯语以及模型可识别的其他语言可请求译为中文。图片或待解密等**没有正文**的占位自身不造译文。翻译与订单阶段、客户电话是否一致、历史是否完整是不同问题，不得把后者当作拒绝字面翻译整屏消息的借口。每次最多处理 20 条新译文，处理后允许继续补齐；按消息 ID、正文版本、语言及模型配置缓存并在重启后复用，不重复付费翻同一正文。翻译失败不抹原文、不覆盖已有草稿；不能用固定演示译文冒充真实翻译成功。

## 7. 目前已知偏差：不能写成“已验收”

以下是 1.0.12 完成手机消息身份与逐条译文修复后仍需**单独获准修复**的偏差。记录它们是为了阻止以后抄错现状。消息身份、气泡外译文及重启后译文复用已在隔离虚构聊天与解压 App 中验证；真实 WhatsApp 的端到端翻译质量未因此宣称通过。

| 位置 | 当前已知问题 | 正确基准 |
| --- | --- | --- |
| 手机最底部 | **1.0.12 未通过**：截图中仍有大块多行“英文回复”框和“待发送”；代码由 `ui/src/manual-chat-ui.ts` 添加 `.phone-draft`，`ui/src/reply-tools.css` 给它较大高度。此前验收只测了消息身份和译文，没有检查这里。 | 第六节短白色单行视觉输入框和紫色纸飞机；不显示整段英文，不设置第二个英文编辑入口；左侧编辑和真实发送流程保持原样。此项未获明确代码修复指令时，不改应用，但任何交付都不得称手机整页符合基准。 |
| 其他控件 | 存在旧确认稿/旧 CSS 与新要求冲突的层叠样式，例如模式行多余外框。 | 以本文件第五节最终呈现为准；修复时限定范围。 |

## 8. 每次改动的执行与纠错记录

新需求到来时，在动应用代码**之前或同一任务内先**修改本文件对应条款；变更登记必须写出新旧差异、允许触及的页面/文件、必须保留的邻近结构、验收动作。若用户明确要求“只讨论”，只讨论，不实施。项目规则 `AGENTS.md` 要求每次 UI 工作读取本文件。

开发者自行完成以下核对，不能转交给用户：

1. **改前**：读最新用户要求与本文件，查旧设计和实际代码；区分“目标”和“当前”；记录允许改动边界。
2. **改中**：只改必要组件；保留其余页面的结构、文案、图标、数据与交互。不可因修一个翻译功能，顺手重画手机底部或状态栏。
3. **改后**：按本文件逐项检查字号、图标、颜色、大小、位置、滚动/固定、点击结果、键盘/焦点、草稿和翻译状态；至少在常用两类最大化窗口和 1280 × 820、1440 × 1000 的空间条件下检查受影响区域。检查实际文件改动清单和最终运行成品；无关改动撤回，必要时重新检查。用户无需提供对照截图。
4. **交付**：清楚列出“已满足 / 待修复 / 未触碰”；只对亲自验证的项目写“通过”。应用代码有变化时，仍执行项目既有构建、打包、启动验证与版本规则；文档修改无需冒充新版本。
5. **完成时的责任声明**：每次任务结束前重新阅读本文件与 `AGENTS.md`，按本次范围核对实际结果和成品，记录未通过/未检查项。只有真正做完这项核验，最终回复最后一句才写“已真实核验基准文档和项目规则”；没有核验不能写，也不能称任务已完成。该句不是自动添加的模板。

### 手机底部的发布拦截项（1.0.12 漏检补录）

这组检查适用于下一次**获准修改手机底部**的应用版本；只写规则不等于已经修好。开发者须在最终 ZIP 的新解压 App 中，于订单详情和工作台、1280 × 820 与 1440 × 1000 两种窗口，分别检查空草稿和左侧已输入英文两种状态。每项必须记录实际结果；任何“未检查”按未通过处理。

| 编号 | 必须在成品中成立 | 1.0.12 现状 |
| --- | --- | --- |
| P1 | 手机底部只有短白色单行视觉输入框、紫色纸飞机和既有 home 指示条；顶部与聊天记录区域位置不被改动。 | **失败**：显示多行英文编辑框。 |
| P2 | 手机内不显示“英文回复会同步到这里，也可以直接输入”、整段英文和单独的“待发送”状态行；左侧英文编辑框仍能输入、核对。 | **失败**：前两种文字与“待发送”可见。 |
| P3 | 手机小飞机不得形成绕过左侧核对的第二条真实发送路径；图片、身份图标、时间与逐条气泡外译文保持原有行为。 | **未做对应成品验收**。 |
| P4 | 检查最终 ZIP 解压版的**实际可见结果**、控件层级与两种窗口尺寸；旧版或源码运行截图不能代替。若 P1～P3 任一失败，不得报告手机页面已完成。 | **失败**：此前测试只覆盖身份/译文，未覆盖底部。 |

今后每次界面发版都必须在本文件的变更登记中写出受影响页面的“整页基准状态：通过／未通过／未核验”和对应证据。已有偏差不会因被写进“待修复”而自动变成合格项。发现与基准冲突时，若属于误改立即恢复；若是旧版遗留且本轮未获准修复，则明确保持“未通过”，不能让用户再次从成品里才发现它。

### 变更登记模板

| 日期 / 用户原话要点 | 基准条款的新旧差异 | 允许改的页面/组件/文件 | 必须保持的邻近内容 | 自检证据、误改恢复及未解决项 |
| --- | --- | --- | --- | --- |
| 2026-09-21：建立已确认 UI 基准；以后新要求更新文档、改后自检并恢复误改 | 新增本文；当前代码偏差单列，未视为新基准 | 仅本文与 `AGENTS.md` 的流程规则 | 所有应用代码、现有页面、包与版本 | 本轮核对文件改动范围；未做应用改动 |
| 2026-09-21：修复手机信息翻译和身份信息 | 第六节补充无法核实的旧我方消息标记；修复演示/实时消息的身份图标、时间和气泡外译文；按账号/聊天/原文/配置持久缓存 | `ui/src/manual-chat-ui.ts`、`ui/src/message-translations.ts`、`ui/src/confirmed/workbench.html`、`ui/src/reply-tools.css`、`src/manual-chat.mjs`、`src/manual-translation.mjs`、`src/main.mjs`；相关测试与版本交付文档 | 手机顶栏、底部输入区、快捷回复、订单业务卡、其他页面的独立结构和行为不变 | 1.0.12：376 项测试、项目检查通过；最终 ZIP 解压版在工作台与订单详情核对演示/虚构实时消息的客户、人工、AI 身份和图标、逐条译文位置、显隐与刷新，两种窗口尺寸通过；缓存跨重启及消息/配置隔离单元测试通过。真实 AI 语义和真实 WhatsApp 消息未测试，手机底部既有偏差保留待单独处理。 |
| 2026-09-21：用户指出 1.0.12 手机底部仍不符合确认设计，并质询验收失效 | 第六节明确排除大英文框；第七节将 1.0.12 标为未通过；第八节新增 P1～P4 成品发布拦截项和整页状态判定 | 仅本文及 `AGENTS.md` 的验收规则；本轮没有应用代码修改授权 | 1.0.12 ZIP、已解压 App、所有应用代码及用户资料不变 | 文档已明确“身份/译文局部通过，手机整页未通过”；下次获准修底部时须在最终 ZIP 新解压版逐项核验。 |
| 2026-09-21：每完成一次，最后补充“已真实核验基准文档和项目规则” | 第八节与 `AGENTS.md` 加入实际核验后才可使用的责任声明 | 仅本文与 `AGENTS.md` | 应用代码、安装包、版本和用户资料不变 | 本轮重新阅读两份规则并检查只改文档；任何后续完成都须重新核验，不能沿用本轮结论。 |
| 2026-09-21：暂停界面修复，清除模拟数据并开始接入真实 WhatsApp | 新目标是让示例订单、聊天与真实资料明确分离，确认清理范围后让指定聊天进入真实关联；不得把示例数据当成真实客户资料 | 当前仅登记要求与核查数据来源，尚未获知本轮指定账号/聊天范围 | 保留旧版 WhatsApp 登录资料、现有真实订单与用户数据；不发送消息，不借此修手机底部等 UI 偏差 | 1.0.12 的示例主要内嵌于界面代码，删除数据库不能消除；当前应用资料库无法在本环境只读打开，尚未执行清理或真实连接，待用户确认清理与聊天范围。 |
| 2026-09-21：保留旧版，另做空白版开始真实 WhatsApp 连接 | 新版不展示示例订单、客户、聊天、库存和利润；建立独立的空白资料目录及 WhatsApp 登录会话，扫码并明确核对目标后才读取。空白不等于已登录或已读取 | 新版身份、资料目录、连接入口及九页示例内容；真实读取限已确认聊天与时间，不能发送消息 | 1.0.12 ZIP、解压 App、资料目录和旧版会话不动；已确认的布局、字号、图标和交互不因清空示例而重画；手机底部旧偏差继续标为未通过 | 新版须验证首次空目录、无假客户/假消息/假连接状态、扫码入口、目标确认与范围控制；真实扫码／读取须由用户完成扫码并确认目标后单独验收。 |
| 2026-09-21：1.1.0 空白版成品核验 | 无新设计变更；按上一行实施并保留旧版 | 1.1.0 新 App、独立资料目录与九页空状态 | 1.0.12 ZIP、解压 App、资料和旧登录保持原样；手机底部未改 | 376/376 单元测试、项目检查通过；最终 ZIP 新解压 App 的 3/3 聚焦验收通过，含生产模式启动、1.1.0/Bundle ID、空订单库、九页无示例、连接入口和未核对前禁止读取；1280×820 工作台及 1440×1000 空订单页截图已人工检查。**整页基准状态：工作台/订单详情未通过（手机底部 P1、P2 仍失败；P3 未全量复核；P4 已看成品）；其余七页为空状态、业务未接通，未核验为完整业务页。真实扫码及聊天读取未核验，发送 0。** |
| 2026-09-21：用户授权从 1.1.0 开始做板块隔离以减少反复误改 | 无新的视觉设计；把回复区、订单／工作台布局与商品图片样式限定在实际使用它们的页面，禁止宽泛选择器作用于任意页面元素 | 仅界面样式的装配和作用范围、商品图片对话框所需样式、固定窗口对照脚本及版本交付文件；交付 1.1.1 | 九页现有外观、比例、字号、交互及 1.1.0 空白资料和 WhatsApp 连接流程不变；不借隔离修手机底部 P1／P2、接入 Shoplus、恢复订单导入或移动旧版资料 | 修改前的 1.1.0 与修改后的源码，同尺寸 11 张截图逐像素一致；最终 ZIP 新解压 1.1.1 的 11 张截图与 1.1.0 仅版本号小标签有像素差，九页几何数据相同。376/376 业务测试、项目检查、最终解压 App 空白版验收 2/2 通过；版本在包内为 1.1.1。**整页基准：工作台及订单详情未通过（手机底部 P1／P2 仍失败，P3 未完整复核；P4 已检查最终成品）；其他七页空状态可见，完整业务页未核验。真实 WhatsApp 和 Shoplus 未调用，真实发送 0。** |
| 2026-09-21：用户要求恢复导入订单、新建订单、订单列表与详情，然后关联真实 WhatsApp；用户本人导入真实订单，不要虚拟资料 | 在保留九页确认外观的前提下，订单页和工作台只显示本机实际订单库；导入须先预览、逐单核对后确认写入；手动新建为空白草稿；订单详情显示所选订单；聊天须对本单核对目标后关联。真实扫码、目标和读取范围单独确认，不以程序模拟宣称真实连接通过 | 订单管理、工作台待办及订单详情的真实数据装配、导入与新建对话框、订单聊天关联以及必要桥接；1.1.2 版本及针对性测试 | 商品库存、利润、助手配置、连接设置等未授权业务页的结构与行为；导航、顶栏、字号、手机外形、翻译和快捷回复；1.1.1 及旧版本和现有正式资料；手机底部 P1／P2 不借此修改 | 1.1.2 最终 ZIP 新解压 App 的隔离空库验收 4/4 通过：空列表、新建空白草稿、保存、工作台统计和订单列表、无效文件写入前拒绝、生产模式启动和显示版本。项目检查通过；11 张固定尺寸截图已检查，未触及六页及工作台／订单详情的外框几何与 1.1.1 相同，订单管理的内容变化属于本轮范围。真实 ShopPlus 文件成功导入、真实 WhatsApp 扫码／读取／发送均**未核验**，发送 0；用户稍后自行导入真实订单。**整页基准状态：工作台及订单详情未通过（手机底部 P1、P2 仍失败；P3 未完整复核；P4 已检查最终成品）；订单管理只通过本轮空库入口，真实文件数据呈现未核验；其余六页结构未改，完整业务页未核验。** |
| 2026-09-21：用户真实订单的电话与 WhatsApp 号码关联时被误判 | 1.1.2 只去符号逐位比较，未识别已知国家的本地号码；1.1.3 改为复用现有订单电话／国家区号核对规则。只有同一完整号码才能关联，真正不同或不能核实的号码仍拦截 | 仅订单聊天的关联、恢复、刷新和发送前电话号码检查；必要的针对性测试与版本交付文档 | 不修改已导入订单原值、WhatsApp 登录、聊天正文、发送动作、其他页面 UI、手机底部 P1／P2 和旧版 ZIP | 1.1.3：订单电话核对相关单元检查 11/11、项目检查通过；最终 ZIP 新解压 App 隔离空库验收 4/4 通过并显示 1.1.3。11 张固定尺寸截图的几何数据与 1.1.2 全部相同。真实订单的实际关联／消息读取**未核验**，发送 0；用户须在登录 WhatsApp 的正式资料环境中重试。**整页基准状态：工作台及订单详情未通过（手机底部 P1、P2 仍失败；P3 未完整复核；P4 已检查最终成品）；订单管理仅保留 1.1.2 本轮范围结果；其他六页未改，完整业务页未核验。** |
| 2026-09-21：用户对照设计图指出工作台订单条缺少卡片、状态颜色，点击整条误入详情，且“需注意 2”应为彩色圈数 | 工作台真实订单保留图 1 的卡片层次与有色状态标签；需注意数用彩色圆形计数；点击卡片仅选中，只有明确的“订单详情”按钮进入详情 | 仅工作台真实订单列表的 DOM／局部样式、选中与详情路由、必要回归测试及 1.1.4 交付文件 | 订单管理、订单详情业务区、快捷回复、手机、其他页面及已导入真实数据；不恢复示例客户、消息或假未读数 | 1.1.4：项目检查通过；最终 ZIP 新解压 App 隔离空库验收 4/4 通过，点击卡片保持工作台并选中，点击“订单详情”才进入详情。1280×820 与 1440×1000 最终包截图已核对独立圆角卡片、浅紫选中态、有色草稿标签和有色圆形“需注意 0”；实际“需注意 2”需有真实数据后再核对。11 张固定尺寸截图的外框几何与 1.1.3 全部相同。未写入虚拟客户／订单资料；用户真实订单状态显示和真实 WhatsApp 仍待正式环境核验，发送 0。**整页基准：工作台和订单详情仍未通过（手机底部 P1／P2 旧偏差未修，P3 未完整复核；P4 已检查最终成品）。** |
| 2026-09-21：以 1.1.4 为基准一次恢复空白版误删的功能 | “清理模拟数据”仅影响数据记录和真实连接状态；商品库存、利润核算、助手配置、连接与设置、产品详情、每日利润详情恢复原有结构、页签、按钮、编辑和弹层。真实资料为空时只在具体数据位置显示空状态；旧版纯演示操作不能冒充真实已接入。 | 六个被 `installEmptyWorkspace` 整页替换的页面及其专用数据装配、必要的页面路由/服务桥接、产品图片入口读取商品编号的必要修正、针对性验收与新版本交付文件。 | 1.1.4 成品、导入／新建／列表／订单详情、工作台订单卡片与跳转、WhatsApp 关联、聊天回复工具、导航/顶栏/字号/手机外形不变；真实资料和旧版安装包不动；不借机修手机底部 P1/P2。 | 1.1.5：改前已归档 1.1.4 源码，旧 ZIP SHA-256 保持 47cabc0f…762d2db。376/376 业务检查、项目检查、最终 ZIP 新解压 App 的 5/5 Electron 验收通过；六页在 1280×820、1440×1000 共 12 张成品截图核对了空数据、主要结构与状态，商品档案及每日成本以隔离虚构资料验证读取、图片编号和详情路由。工作台/订单详情、翻译和订单服务等保护文件与改前快照哈希一致；新解压包显示 1.1.5。历史演示开关、订单明细状态筛选和部分导出因缺真实接口明确禁用，不能算完整业务能力；真实 WhatsApp、KDocs、AI 密钥测试、正式订单利润与恢复数据库动作未在用户资料上核验，发送 0。手机底部 P1/P2 旧偏差仍未通过，P3/P4 本轮未完整复核，工作台/订单详情整页仍未通过。 |

## 8. 聊单工作台桌面四列（2026-09-22 实施基准）

本节只约束 `workbench` 的新桌面工作台；订单详情页及其手机聊天、图片和视频能力不在本轮范围。

| 列 | 已实施规则 |
| --- | --- |
| 第一列 | 默认折叠，只显示语义图标；图标有足够点击区和悬停名称提示。顶部按钮可展开，展开后显示原有导航名称并留有文字右侧空白。 |
| 第二列 | 以真实订单中存在的客户电话号码聚合会话；保留搜索、全部、未读、未回。号码优先、状态和最近可知信息次之。没有实际聊天内容时明确显示未读取，不制造示例消息。 |
| 第三列 | 使用桌面聊天区而非手机壳；顶栏右侧顺序固定为人工、AI 辅助、AI 自动、翻译。人工为选中模式，两个 AI 模式显示待启用，翻译默认开启且提供客户语言自动识别/手动改选入口。中文与英文在同一个编辑位置切换，快捷回复、表情、同步清空和发送确认在同一底部区域。译文在消息气泡外独立显示，身份、图标、时间保留。 |
| 第四列 | 以当前电话号码自动选取最新订单；可在面板内切换其他订单。完整信息按客户信息、订单信息、地址核对、报单 TXT 分组，允许折叠。客户资料只有人工点击“确认并保存”才同步到订单详情页；无订单时可创建带入当前电话的空白完整订单草稿。利润与回款不显示。 |

实施文件：新建 `ui/src/chat-workbench-desktop.html`、`ui/src/chat-workbench-desktop.css`、`ui/src/chat-workbench-desktop.ts`，并仅在 `ui/src/main.tsx` 的工作台入口引用。旧版 `ui/src/confirmed/workbench.html`、手机聊天/回复工具及订单详情页面未改写。

本轮实际核验：2026-09-22 已通过 TypeScript、Vite 构建、共享模块与项目检查；本机浏览器预览在 1440×1000 下已核对默认折叠、四列边界、底部编辑区固定、初始隐藏面板，以及导航、编辑器切换、快捷回复、清空和翻译面板交互。Electron 在受限环境启动时以 `SIGABRT` 终止，未能完成桌面 App 启动验证；依用户要求未打包、未变更版本号。

### 2026-09-22 首次实测修正规则

1. 合并 macOS 顶栏下，桌面工作台顶部必须预留窗口控制安全高度，第一、二列标题不能与窗口控制按钮重叠。
2. 第二列客户聊天宽度可通过其与第三列之间的可见竖向拖动把手调整；第三列最小宽度保持可读，不能被拖没。
3. 第二列只保留会话检索、筛选和刷新。订单导入及未来 API 连接属于订单管理；未接入的 API 不得显示成可用动作。
4. 刷新读取期间按钮图标至少旋转约 280 ms，提供可感知反馈。
5. 此条已由 D-024 覆盖：第三列不得出现“关联真实聊天”或逐单关联流程；未登录时工作台仅提示前往连接与设置，不能显示或编造聊天数据。
6. 底部的中文输入、英文翻译、快捷回复、表情、清空和发送确认都应呈现为可辨识按钮；发送确认在正常窗口高度下保持可见。
7. 当前订单详情必须以订单 ID 原值调用既有订单服务，不能将 `{id}` 再包装为参数对象；否则第四列空白属于缺陷。

### D-023 实施核验（2026-09-22）

已完成静态构建和浏览器工作台布局核验，并已启动桌面程序。真实订单数据与 WhatsApp 历史消息依赖当前账号登录、客户号码绑定和第三方连接状态；它们必须在桌面程序中以真实账号再次确认，不能用模拟预览代替。


### D-024｜账号级 WhatsApp 收件箱与首次运行视觉修正（2026-09-22）

1. **数据入口**：WhatsApp 登录只在“连接与设置”完成。工作台读取该账号的真实会话，不能要求用户逐个客户或逐张订单关联。
2. **会话加载**：按真实聊天最近时间倒序，初始显示 20 条；第二列向下滚动到末端时才追加更早会话。第三列初始显示选中会话最新 20 条，向上滚动才继续取更早消息。无登录、无会话或读取失败时明确显示真实状态，绝不补演示消息、订单或头像。
3. **订单自动匹配**：右侧仅按当前真实会话电话号码自动匹配本机订单，默认最新订单；没有匹配订单时提供新建订单，不改变会话本身。
4. **头像**：仅使用 WhatsApp 返回的真实头像 URL；头像缺失或不可读时显示中性人物图标，不能生成姓名首字母或虚构照片。
5. **布局**：macOS 顶栏安全区下移；第一、二、三列顶部动作不与窗口按钮相贴。第二列和第三列标题区的底边必须对齐。数量徽标采用固定圆形和水平、垂直居中。
6. **导航**：默认折叠时展开图标指向展开方向；展开后切换为折叠方向。文字“我的工作台”只在展开态显示。

此节覆盖 D-023 中关于“关联真实聊天”的入口、逐单绑定和相关核验表述；旧订单详情页的历史关联规则不受本节影响。

### D-024 实施核验（2026-09-22）

已完成语法、TypeScript、UI 构建以及 6 项聊天发送/身份兼容回归。桌面程序已重新启动并显示。当前 WhatsApp 自动连接处于重试状态，真实会话、头像和分页不能宣称已通过；工作台在此状态只能显示连接提示和空状态。


### D-025｜设置页真实 WhatsApp 登录弹窗（2026-09-22）

“连接与设置”内的“管理登录”必须立即打开真实状态弹窗，不能只显示页面提示。弹窗轮询实际连接状态；二维码可用时显示实际二维码，在线时显示实际账号并说明工作台将统一读取会话。关闭弹窗只停止界面轮询，不退出账号、不发送消息、不伪造登录结果。

### D-025 实施核验（2026-09-22）

已通过 TypeScript 与 UI 构建，并重启桌面程序确认窗口/渲染页加载。实际二维码的出现和扫码成功取决于当前 WhatsApp 服务状态，仍待用户在弹窗中完成真实登录后验收。


### D-026｜工作台版本号（2026-09-22）

桌面聊单工作台的左侧栏底部必须显示来自 `package.json` 的实际应用版本号；不能写死示例版本，也不能因工作台重构消失。


### D-027｜WhatsApp 登录失败状态（2026-09-22）

二维码未生成时，设置页登录弹窗必须显示实际可公开的连接状态；不能把依赖缺失、连接超时和初始化失败全部改写成同一句泛化提示。内部错误细节只记录在本机启动状态，不向页面暴露凭据或网页内容。


### D-028｜WhatsApp 初始化本机诊断（2026-09-22）

当 WhatsApp Web 在二维码生成前失败时，启动日志必须记录失败代码和经过长度限制的失败消息，以便修复初始化问题；该诊断不记录二维码、会话正文、电话号码、Cookie 或其他凭据。


### D-029｜WhatsApp 专用浏览器清理（2026-09-22）

应用创建的 WhatsApp 专用 Chromium 在初始化失败、重连或应用退出后必须被关闭；库级关闭返回后仍存活的子进程也必须终止。清理范围仅限带应用专用登录目录的浏览器，不触及用户日常 Chrome 或登录资料。

### D-029 实施核验（2026-09-22）

已清理先前遗留的应用专用浏览器进程并重新启动桌面程序。启动日志实际确认 WhatsApp 从“正在连接”进入“已认证”与“自动消息连接在线”；登录资料被保留，因此本次状态为在线而非二维码。管理登录弹窗应据此显示在线账号状态，工作台可读取该账号的真实会话。

### D-030｜导航初始状态与逐次版本显示（2026-09-22）

桌面聊单工作台左侧导航首次进入时默认展开，顶部按钮显示“折叠导航”的语义图标与文字；点击后才收起为仅图标状态。每次实际代码改动均递增 `package.json` 中的应用版本，并让工作台左下角与设置页显示同一个实际版本；本轮由 `v1.1.5` 递增为 `v1.1.6`。不因版本号递增而创建压缩包或改动未授权页面。

### D-030 实施核验（2026-09-22）

已通过 TypeScript、UI 构建和差异范围检查；桌面程序已重新启动并加载渲染页。当前 WhatsApp 已认证且在线。改动文件仅涉及版本来源、桌面工作台导航初始类/按钮语义、设置页版本显示及两份规则记录；未修改聊天、订单或其他页面。

### D-031｜真实会话同步与连接检测反馈（2026-09-22）

已登录的 WhatsApp 账号在工作台必须读取其真实个人会话，不能因 WhatsApp 当前使用 LID 会话标识而显示 0 条。LID 仅能通过 WhatsApp 返回的 LID/电话号码映射还原为号码；映射失败的会话不猜测号码、不显示虚构记录。设置页“检测连接”点击后，按钮依次显示“检测中…”及实际结果“已连接”/“未连接”/“检测失败”，同时更新页面状态提示。版本仍按每次实际改动递增，本轮为 `v1.1.7`；Git 提交改为每累计 5 个版本号执行一次，下一提交节点为 `v1.2.0`。

### D-032｜连接完成后的会话自动加载（2026-09-22）

工作台若在 WhatsApp 尚未在线时首次读取会话失败或为空，连接状态从非在线变为在线后必须自动重新读取第一页真实会话；无需用户重新进入页面或手动刷新。连接再次中断后重置该状态，下一次在线时再读取。该规则不重复追加已加载的分页记录。本轮版本为 `v1.1.8`，仍不提交 Git。

### D-031/D-032 实施核验（2026-09-22）

已通过 7 项聊天/兼容性回归、TypeScript、UI 构建和差异检查，并重新启动桌面程序。对已登录 WhatsApp 页面执行只读计数：共有 53 个个人会话，其中 52 个为 LID；52 个 LID 均由 WhatsApp 自身映射为电话号码。当前应用已认证并在线。未读取或记录消息正文、客户号码或聊天内容。

### D-033｜全局悬停提示与鼠标指针（2026-09-22）

本轮只增加全局悬停提示，不改变任何页面的位置、尺寸、文案、功能、数据或交互流程。鼠标经过可点击按钮、图标按钮，或已有说明的卡片时，显示深色浮动提示；提示文字只能来自现有 `data-tooltip`、`title` 或无障碍标签，不编造说明。可点击控件显示小手指针；静态卡片仅在已有说明时显示提示，不伪装成可点击。提示应位于目标附近，避开窗口边缘，并支持键盘焦点。版本为 `v1.1.9`，仍累计至 `v1.2.0` 再提交 Git。

### D-034｜移除 API 人工调用次数限制（2026-09-22）

移除 AI 连接测试与手动调用的 5 次验证、每日 20 次等应用内部次数上限，以及对应的额度提示和翻译设置中的“验证模式／日常使用”选择。API 的实际可用性、费用和服务商限流由用户配置的服务商账户决定；应用只保留每次点击才发起请求、同一时刻只执行一个请求、密钥安全存储和测试使用虚构资料的规则。AI 自动功能当前仍未启用。本轮只改 API 限制及相关提示，版本为 `v1.2.0`。

### D-034 实施核验（2026-09-22）

已通过 28 项 API 设置、翻译、聊天和 WhatsApp 兼容性回归，TypeScript 与 UI 构建通过。验证连接和手动请求在超过原有 5 次/20 次阈值后仍可继续完成；密钥没有出现在公共状态或测试输出中。改动未启用 AI 自动功能。


### D-035｜工作台完整顶部拖动区（1.2.1，2026-09-22）

用户要求修复聊天窗口顶部左半能拖、右半不能拖。桌面工作台现有 48 px 顶部空白安全区，在 macOS 窗口控制按钮右侧应一直延伸至窗口右边缘，均可拖动窗口；切换其他页面再返回、调整窗口大小后仍须成立。拖动层不能覆盖下方按钮、搜索、聊天、订单或编辑区域；工作台模态弹窗打开时停用拖动层，关闭后恢复。

允许修改：`ui/src/main.tsx` 中工作台的窗口拖动层范围和弹窗生命周期，以及版本、交付文档和针对性验收。必须保持：四列位置、48 px 安全区高度、字号、颜色、导航、聊天与订单业务、其他页面原有顶栏可点击区、用户资料和历史安装包。设置页版本改为读取 package.json，避免与应用版本不一致。

验收计划：1.2.1 最终 ZIP 新解压 App 启动及内外版本；1280×820、1440×1000 和现有显示器最大化下顶部左右范围、48 px 边界、下方按钮/输入、页面往返、弹窗开关。仅声明顶部局部修复，工作台完整真实聊天业务本轮未核验；订单详情旧手机底部 P1/P2 偏差仍未通过且不在本次范围。

### D-035 成品核验（2026-09-22）

- 已实际重新阅读 AGENTS.md 与本基准，并逐项对照本轮变更范围。最终 ZIP 新解压 App 在 production 模式、隔离空资料目录成功启动；包内、工作台和设置页版本均为 1.2.1。
- 原生鼠标事件分别按住顶部左侧和右侧拖动；两侧均使窗口 x 增加 40、y 增加 35，尺寸不变。不是仅核对 CSS 属性。
- 1280×820、准确 1440×1000、内屏最大化 1453×930、外屏最大化 1920×1050，以及设置页返回工作台：拖动区域从 x=96 到窗口最右侧，y=0 到 48；下方不被覆盖。导航按钮、搜索输入、模态弹窗打开停用/关闭恢复通过；渲染页无脚本异常。成品截图已查看。
- TypeScript、Vite、共享模块检查、项目检查、签名打包、git diff --check 通过。完整测试 376/377 通过；旧的 assistant-live-sync.test.mjs 的进程清理测试期待单次 SIGTERM，实际两次。相关实现和测试均与 HEAD 1.2.0 相同，单独运行仍复现，未扩大本轮范围修改。
- 工作台整页基准状态：未全量核验（真实登录、会话/订单、翻译/发送不在本轮范围）；本轮顶部局部修复通过。订单详情旧手机底部 P1/P2 仍未通过且未触碰。正式订单、WhatsApp 登录、聊天和旧 ZIP 未修改。Git 修改保留未提交，遵循每 5 个版本提交一次的既有规则。

### D-036｜已保存 API Key 的明确状态（1.2.2，2026-09-22）

用户指出 API Key 已保存时仍以灰色占位文字和系统焦点色呈现，容易被误解为未保存或异常。本轮只允许改“连接与设置”中 API Key 这一行的状态呈现与版本号：已保存时显示非敏感的“密钥已安全保存”实字、浅绿色状态底和边框，输入框不再接受焦点；未保存时保留原有安全输入提示。切换服务商时必须同步刷新当前服务商的保存状态。不得显示密钥原文，不改密钥存储、服务商、模型、测试、WhatsApp、订单、聊天或其他页面。
### D-036 实施核验（2026-09-22）

已通过 TypeScript、UI 构建和差异检查。实际启动的本地 Electron 开发版进程使用 `liaodan-assistant-next` 的当前 1.2.2 源码；未制作安装包。已保存状态只写入固定说明文字，不读取、显示或记录 API Key 原文；未保存与切换服务商路径继续由真实 `hasApiKey` 状态驱动。本轮未触碰设置页其余控件、WhatsApp、订单、聊天或布局。

### D-037｜工作台侧栏分组与字体一致性（1.2.3，2026-09-22）

用户确认工作台展开侧栏应与已确认页面导航使用同样的视觉层级：顶部“我的工作台”为分组文字，业务入口在其下；“助手管理”为第二分组，置于助手配置和连接与设置之前。现有订单管理入口必须保留。展开态使用统一的 15 px 项目文字、约 44 px 行高和 18 px 语义图标；折叠控制改为侧栏底部的图标入口，不能再伪装成“我的工作台”紫色菜单项。允许修改仅限工作台侧栏结构、样式、版本和核验记录；聊天、订单、按钮业务、四列内容与其他页面不改。本轮直接启动本地应用，不制作下载或安装包。
### D-037 实施核验（2026-09-22）

已通过 TypeScript、UI 构建、共享模块检查、项目检查和差异检查。静态结构核对确认工作台侧栏恰有“我的工作台 / 助手管理”两个分组，订单管理入口保留，展开态导航项目使用 15 px 文字、44 px 行高和 18 px 图标；折叠控制位于侧栏底部。1.2.3 Electron 本地开发版已实际重新启动，启动日志确认主窗口加载、显示并获得焦点。未制作安装包；工作台其余三列、聊天和订单业务未触碰，整页其他既有偏差不因此标记为通过。

### D-038｜工作台真实订单的号码自动关联（1.2.4，2026-09-22）

用户确认某个真实 WhatsApp 会话已有真实订单，但工作台右侧订单区显示“暂无关联订单”。根因是工作台此前只比较去除符号后的原始号码；订单使用阿联酋本地号码 `05…`、WhatsApp 使用国际号码 `+971 5…` 时不能相等。允许修改仅限订单列表为工作台提供按已有国家/地区规则标准化后的电话号码，以及工作台右侧关联时使用该值。订单原始号码、订单资料、聊天记录、排序、页面位置、文案、按钮和其它页面不得修改。无法依据已有国家/地区规则核验时，继续使用原精确比较；没有匹配订单时继续显示原有“新建订单”状态。本轮版本为 1.2.4，直接运行本地应用，不制作安装包，不提交 Git。

### D-038 实施核验（2026-09-22）

订单服务针对阿联酋本地号码和同一国际号码的测试通过，确认向工作台提供同一标准号码；未知国家或不能标准化的记录仍使用原始精确号码。`npm run check`（TypeScript、UI 构建、WhatsApp 兼容性及项目检查）、订单服务 10 项测试和 `git diff --check` 均通过。1.2.4 Electron 本地开发版已重新启动，主窗口加载、显示并获得焦点。启动时 WhatsApp Web 初始化仍在自动重试，因此没有把该用户的真实会话读取结果标记为已复验；本轮没有改动 WhatsApp 连接或聊天读取逻辑。

### D-039｜聊天工作台当前页自动翻译（1.2.5，2026-09-22）

用户要求聊天工作台默认批量翻译当前读取的消息页，不再逐条手动触发；逐条翻译和“翻译未译消息”必须保留，供关闭自动翻译或重试时使用。允许修改仅限工作台的翻译设置、当前页翻译调度、已有翻译缓存读取和版本/验收记录。每次读取当前页、刷新最新消息或加载一页更早消息时，先读取本机已有译文，再只提交该页尚未翻译的文字；每批仍受既有 20 条、12,000 字符限制。译文继续按账号、会话、消息内容和模型配置保存在本机翻译缓存中，绝不写回 WhatsApp、订单或聊天原文。默认“自动翻译当前页”开启，可在工作台右上角“翻译”菜单关闭；“阅读翻译为中文”只控制显示。不得改动消息顺序、聊天内容、订单、发送流程、其它页面的位置、文字或功能。本轮版本为 1.2.5，不打包；按五个版本节点提交 Git。

### D-039 实施核验（2026-09-22）

桌面端回归测试使用隔离的虚构会话验证：工作台先从本机缓存显示已有译文，只向翻译服务提交本页缺失的消息，默认自动开关为开启，原有手动批量入口仍存在。缓存测试验证译文重启后仍可命中，且不会跨会话、消息正文或模型配置复用。4 项翻译/工作台测试、`npm run check`（TypeScript、UI 构建、WhatsApp 兼容性及项目检查）和 `git diff --check` 均通过。1.2.5 Electron 本地开发版已重新启动，主窗口加载、显示并获得焦点。未读取、输出或修改真实聊天内容；启动时 WhatsApp Web 初始化仍在自动重试，本轮未改动连接逻辑。

### D-040｜真实头像与接待状态（1.2.6，2026-09-22）

工作台客户聊天列表只使用 WhatsApp 返回的联系人头像 URL；头像图片必须在既有圆形容器中完整裁切呈现。WhatsApp 未返回头像、链接失效或加载失败时保留原有通用头像，绝不伪造客户头像。接待状态按会话最后一条可读取消息方向判断：最后消息来自我方时显示绿色“已接待”，来自客户时显示橙色“待接待”；两种状态均有对应图标和悬停说明。允许修改仅限工作台客户聊天列表的头像呈现、状态呈现、版本和针对性验收；不改聊天内容、排序、订单、发送、其他页面、布局尺寸或真实数据。

### D-040 实施核验（2026-09-22）

隔离虚构会话回归确认：WhatsApp 资料头像 URL 传入工作台后使用 `object-fit: cover` 在既有圆形头像中呈现；最后消息为客户时显示橙色“待接待”及方向说明，数据层也验证客户方向和 HTTPS 头像 URL 不被丢弃。`node --test test/manual-chat.test.mjs` 6 项、工作台 Electron 回归、`npm run check` 和 `git diff --check` 通过。1.2.6 Electron 本地开发版已启动并显示。启动时发现上次异常退出的应用专用无头浏览器占用了 WhatsApp 登录目录；仅结束该专用进程、未删除登录资料后，当前应用重新认证并在线。未读取、输出或修改真实聊天内容。整页其他既有偏差不因此标记为通过；本轮不打包、不提交 Git，下一提交节点仍为 1.3.0。

### D-041｜消息加载提示自动收起（1.2.7，2026-09-22）

聊天工作台的“已显示最新 20 条消息”和“已读取更早消息”提示应在成功显示约 3 秒后向上收起，不再长期占用消息区。重新打开真实会话，或再次读取更早消息时，提示应重新出现并再次自动收起。读取中的提示保持显示，读取失败提示继续保留以便用户查看原因。只允许修改这两种加载提示的显示寿命与动画、版本和针对性验收；不改提示文字、消息内容、会话排序、订单、发送、其他页面或布局。

### D-041 实施核验（2026-09-22）

工作台 Electron 回归使用隔离的虚构会话验证：成功读取更早消息后提示进入收起状态；重新选择会话后，“已显示最新 20 条消息”再次出现。回归还保留了头像、消息方向及自动翻译已有断言。`npm run check`、`node --test test/manual-chat.test.mjs` 6 项和 `git diff --check` 通过。1.2.7 Electron 本地开发版已启动，未制作安装包、未提交 Git。整页其他既有偏差不因此标记为通过；下一提交节点仍为 1.3.0。

### D-042｜中文译文对比度（1.2.8，2026-09-22）

聊天工作台消息气泡下方的中文译文仅将文字颜色由浅灰紫加深为 `#4c1d95`，提高白色与浅绿色气泡背景上的可读性。红色保留给错误或警告语义，不用于正常译文。不得改译文位置、字号、内容、翻译触发、聊天、订单或其他页面。本轮版本为 1.2.8，不打包、不提交 Git；下一提交节点仍为 1.3.0。

### D-042 实施核验（2026-09-22）

工作台回归新增对已缓存中文译文的计算颜色断言，确认其为 `rgb(76, 29, 149)`；其它既有自动翻译、头像、方向和提示收起断言保留。`npm run check` 与 `git diff --check` 通过。1.2.8 Electron 本地开发版已启动，未制作安装包、未提交 Git。整页其他既有偏差不因此标记为通过；下一提交节点仍为 1.3.0。

### D-043｜客户列表一号字号调整（1.2.9，2026-09-22）

仅使用工作台 CSS 调整客户列表：头像由 38 px 增至 42 px；电话文字由 13 px 增至 14 px；日期时间由 11 px 增至 12 px。客户聊天标题区的标题、未读数、搜索输入和筛选文字各增大一号；“刷新”文字改为已有主题紫色。不得修改 TypeScript、数据、交互、其他页面或未列出的元素。本轮版本为 1.2.9，不打包、不提交 Git；下一提交节点仍为 1.3.0。

### D-043 实施核验（2026-09-22）

静态范围核对确认本轮只改工作台 CSS、版本与记录：头像 42 px，电话 14 px，日期时间 12 px；标题区文字各增加 1 px，“刷新”为主题紫色。`npm run check` 与 `git diff --check` 通过。1.2.9 Electron 本地开发版已启动，未制作安装包、未提交 Git；下一提交节点仍为 1.3.0。

### D-044｜全局按钮悬停反馈（1.3.0，2026-09-22）

页面中所有可用的操作按钮及角色按钮在鼠标悬停时必须显示小手、向上浮动 1 px、轻微加深原有颜色并显示浅阴影；离开后平滑复原。禁用按钮保持无反馈；用于拖动布局宽度的分隔条不作为操作按钮处理。只允许修改全局悬停样式、版本和针对性验收，不改元素尺寸、静态位置、文案、数据、功能或交互流程。本轮版本为 1.3.0，按五个版本节点提交 Git。

### D-044 实施核验（2026-09-22）

静态范围核对确认全局悬停样式只作用于可用按钮与角色按钮：使用小手、1 px 视觉上浮、颜色加深和浅阴影；禁用按钮及 `.up-divider` 分隔条被明确排除。`npm run check`（TypeScript、UI 构建、WhatsApp 兼容性、共享模块和项目检查）与 `git diff --check` 通过。未制作安装包；本轮按 1.3.0 节点提交 Git。

### D-045｜客户聊天列默认位置与导航补偿（1.3.1，2026-09-22）

客户聊天列保持可拖拽。默认状态下，其右边界应位于整个窗口约三分之一处：导航展开时客户聊天列使用“窗口三分之一减去 208 px 导航和 8 px 分隔条”；导航折叠时使用“窗口三分之一减去 64 px 导航和 8 px 分隔条”。用户拖拽过客户聊天列后，再切换导航时，客户聊天列补偿 144 px，使聊天内容列的左边界和宽度保持不变。只允许修改客户聊天列默认宽度、导航切换时的客户聊天列补偿、版本和验收；不改聊天内容列的宽度、其它列、文字、数据、功能或交互。

### D-045 实施核验（2026-09-22）

工作台 Electron 回归在隔离虚构会话中记录导航切换前后的聊天内容列边界，断言其左边界差不超过 1 px；客户聊天列继续使用既有拖拽分隔条。`npm run check` 与 `git diff --check` 通过。1.3.1 Electron 本地开发版已启动，未制作安装包、未提交 Git；下一提交节点为 1.3.5。

### D-046｜工作台统一顶栏与导航图标（1.3.2，2026-09-22）

聊天工作台在原有 48 px 顶部留白处接入与订单、库存等业务页相同的 56 px 应用顶栏，只显示品牌图标、“聊单助手”和当前 `1.x.x` 版本。左侧导航继续作为第一列；客户聊天列仍紧邻其右侧，并以工作台已有的 1 px 细线作分界。导航图标采用用户提供的顺序与语义：聊天、订单归档、库存、利润、助手、设置。只允许修改工作台顶栏、导航图标、与标题栏对应的拖动高度、版本和针对性验收；不改客户聊天列宽度、消息/订单列、任何业务文字、数据、按钮功能或其他页面内容。

### D-046 实施核验（2026-09-22）

工作台 Electron 回归确认顶栏显示“聊单助手 1.3.2”，订单入口使用归档图标；回归同时保留客户聊天列拖拽、导航切换后聊天内容列起点不变、自动翻译、头像和消息状态断言。`npm run check`、工作台 Electron 回归和 `git diff --check` 通过。1.3.2 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点为 1.3.5。整页既有未核验项不因本次局部视觉统一而标记通过。

### D-047｜工作台导航顶部留白对齐（1.3.3，2026-09-22）

工作台左侧导航从共享顶栏到“我的工作台”分组标题的顶部内边距调整为 22 px，与其它业务页导航一致。只允许修改这一处导航顶部留白、版本和针对性验收；不改导航宽度、分组内行距、图标、客户聊天列、聊天正文、订单列、文字、功能、数据或其它页面。

### D-047 实施核验（2026-09-22）

工作台隔离 Electron 回归断言导航顶部内边距为 22 px，并保留顶栏高度、导航与客户聊天列 1 px 分界、导航切换后聊天内容列起点、自动翻译、头像与接待状态断言。`npm run check`、工作台 Electron 回归和 `git diff --check` 通过。1.3.3 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点为 1.3.5。整页既有未核验项不因本次局部间距修复而标记通过。

### D-048｜工作台左侧区域随窗口占比（1.3.4，2026-09-22）

工作台放大、全屏或拉伸窗口后，左侧“导航栏 + 客户聊天栏 + 分隔条”的默认右边界保持在窗口宽度约三分之一处；不再只让聊天正文列吸收新增宽度。客户聊天栏原有拖拽调整保留，但在窗口尺寸变化时恢复当前窗口的三分之一默认值。只允许修改这一默认宽度计算、窗口尺寸变化时的复位、版本和针对性验收；不改导航宽度、客户聊天栏内容、聊天正文、订单列、文字、图标、功能、数据或其它页面。

### D-048 实施核验（2026-09-22）

工作台隔离 Electron 回归先写入客户聊天栏的临时手动宽度，再派发窗口尺寸变化；断言临时宽度被清除，并验证聊天正文左边界等于导航与客户聊天栏整体的三分之一默认边界（窄窗口受既有 240 px 最小客户聊天宽度保护）。`npm run check`、工作台 Electron 回归和 `git diff --check` 通过。1.3.4 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点为 1.3.5。整页既有未核验项不因本次局部宽度修复而标记通过。

### D-049｜工作台首项与订单页对齐（1.3.5，2026-09-22）

工作台与订单管理页的“我的工作台”标题已经对齐；工作台标题下方到首项“聊单工作台”的间距必须同步为订单管理页的 9 px，使首项文字与图标垂直对齐。只允许修改工作台分组标题下边距、版本和针对性验收；不改标题位置、导航宽度、导航项高度、图标、客户聊天栏、聊天正文、订单列、文字、功能、数据或其它页面。

### D-049 实施核验（2026-09-22）

工作台隔离 Electron 回归断言“我的工作台”分组标题下边距为 9 px，并保留顶栏高度、导航顶部留白、导航与客户聊天列分界、窗口缩放后三分之一默认边界、自动翻译、头像与接待状态断言。`npm run check`、工作台 Electron 回归和 `git diff --check` 通过。1.3.5 仅直接运行本地开发版，不制作安装包；按用户约定在此五个版本节点提交并推送 Git。整页既有未核验项不因本次局部间距修复而标记通过。

### D-050｜业务页导航复刻工作台（1.3.6，2026-09-22）

订单管理、商品库存、利润核算、助手配置、连接与设置五页的左侧导航以聊天工作台为唯一标准：同一套聊天、归档订单、库存、利润、助手、设置图标；15 px 文字、18 px 图标、44 px 菜单行、12 px 左右内边距、10 px 图文间距、9 px 分组标题下边距和 22 px 分组间距。只允许修改这五页的导航图标与导航样式结构、版本和针对性验收；不改顶栏、导航以外的业务内容、客户聊天栏、聊天正文、订单列、页面文字、功能、数据或其它页面。

### D-050 实施核验（2026-09-22）

隔离 Electron 导航回归逐页打开订单、库存、利润、助手和设置页面，断言六个导航入口均使用目标图标，并验证“聊单工作台”条目为 44 px 高、15 px 字、12 px 左内边距、10 px 图文间距，分组标题下边距为 9 px。工作台既有回归继续覆盖顶栏、左侧三分之一默认宽度、自动翻译、头像和接待状态。`npm run check` 与 `git diff --check` 通过。1.3.6 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.0。整页既有未核验项不因本次导航统一而标记通过。

### D-051｜订单信息列拖动宽度（1.3.7，2026-09-22）

工作台聊天正文和最右侧订单信息列之间增加与客户聊天栏相同的可拖动紫色分隔条。订单信息列默认占窗口约四分之一，拖动范围为窗口宽度的五分之一至三分之一；窗口放大、全屏或拉伸时恢复默认比例。只允许修改最右侧订单信息列的分隔条、宽度计算、拖动行为、窗口尺寸复位、版本和针对性验收；不改左侧导航和客户聊天栏、聊天正文、订单信息内容、文字、图标、功能、数据或其它页面。

### D-051 实施核验（2026-09-22）

工作台隔离 Electron 回归验证订单信息列默认宽度处于窗口五分之一至三分之一之间；拖动新增分隔条后其宽度增大且不超过三分之一；窗口尺寸变化后客户聊天栏和订单信息列的临时宽度均被清除并恢复默认比例。既有回归继续覆盖左侧三分之一默认边界、顶栏、自动翻译、头像与接待状态。`npm run check` 与 `git diff --check` 通过。1.3.7 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.0。整页既有未核验项不因本次局部拖动功能而标记通过。

### D-052｜工作台拖拽分隔条单线显示（1.3.8，2026-09-22）

客户聊天栏/聊天正文、聊天正文/订单信息列两处保留原有拖拽命中区域与拖动范围，但移除内容列重复的右边框和分隔条底色，只显示一条居中的 2 px 拖动线。只允许修改这两处视觉分隔、版本和针对性验收；不改导航、列宽、聊天正文、订单信息、文字、图标、功能、数据或其它页面。

### D-052 实施核验（2026-09-22）

工作台隔离 Electron 回归断言客户聊天栏和聊天正文右边框均为 0 px；两条可拖动分隔条均为透明命中区并只保留 2 px 指示线。既有回归继续验证订单列的五分之一至三分之一拖动范围、窗口尺寸复位、左侧三分之一默认边界、顶栏、自动翻译、头像和接待状态。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.3.8 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.0。整页既有未核验项不因本次局部分隔线修复而标记通过。

### D-053｜工作台空白状态三列标题统一（1.3.9，2026-09-22）

工作台空白状态的“客户聊天”“请选择客户”“订单信息”三处主标题统一使用“客户聊天”的 16 px 标题样式与字号。只允许修改后两项标题字号、版本和针对性验收；不改空白状态文字、同步逻辑、列宽、布局、导航、按钮、图标、功能、数据或其它页面。

### D-053 实施核验（2026-09-22）

工作台隔离 Electron 回归断言三处主标题的计算字号均为 16 px；既有回归继续验证两处单线可拖动分隔、订单列拖动范围、窗口尺寸复位、左侧三分之一默认边界、顶栏、自动翻译、头像与接待状态。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.3.9 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.0。整页既有未核验项不因本次局部标题统一而标记通过。

### D-054｜聊天与订单标题区下边线对齐（1.4.0，2026-09-22）

右侧“订单信息”标题区下边线为标准；中间“请选择客户”标题区下边线必须与其重合。中间标题区固定为 66 px，可调整顶部内边距及窄列时副标题的单行省略规则，以确保内容不越过下边线。只允许修改这一标题区、版本和针对性验收；不改左侧客户聊天栏、标题内容、空白提示、同步逻辑、列宽、布局、导航、按钮、图标、功能、数据或其它页面。

### D-054 实施核验（2026-09-22）

工作台隔离 Electron 回归读取中间聊天标题区和订单信息标题区的实际边界，断言两条下边线相差不超过 1 px；既有回归继续验证三处 16 px 标题、两处单线可拖动分隔、订单列拖动范围、窗口尺寸复位、左侧三分之一默认边界、顶栏、自动翻译、头像与接待状态。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.0 仅直接运行本地开发版，不制作安装包；按用户约定在此五个版本节点提交并推送 Git。整页既有未核验项不因本次局部横线对齐而标记通过。

### D-055｜顶部 WhatsApp 状态与导航折叠控制（1.4.1，2026-09-22）

工作台底部的 WhatsApp 连接状态移入应用标题栏，持续按真实状态显示：在线为更大的绿色状态球和“已连接”，非在线或状态读取失败为灰色状态球和“未连接”。导航折叠按钮移至“我的工作台”标题同一行，尺寸为 36 px；折叠态仍显示，图标朝右。只允许修改该状态显示、该折叠控制、版本和针对性验收；不改连接逻辑、聊天同步、导航入口、列宽、其它布局、文字、图标、功能、数据或其它页面。

### D-055 实施核验（2026-09-22）

工作台隔离 Electron 回归模拟在线状态，断言标题栏显示“已连接”、对应无障碍标签和 10 px 状态球；断言底部旧状态已移除、折叠控制位于“我的工作台”标题区，折叠后仍显示朝右图标。既有回归继续验证标题区横线重合、三处标题、分隔条、列宽、翻译、头像、接待状态和五页导航。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.1 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.5。整页既有未核验项不因本次局部状态显示与控制位置调整而标记通过。

### D-056｜顶部工作模式按钮（1.4.2，2026-09-22）

“人工”“AI辅助”“AI自动”三个既有工作模式按钮移入应用标题栏，统一增大至 36 px 高、13 px 字和 16 px 图标；原有选中、禁用和待启用状态保持。聊天标题区只保留翻译控制。只允许修改这三枚按钮的位置与尺寸、版本和针对性验收；不改按钮功能、翻译控制、标题栏其余内容、聊天同步、列宽、其它布局、文字、图标、功能、数据或其它页面。

### D-056 实施核验（2026-09-22）

工作台隔离 Electron 回归断言标题栏中恰有三枚工作模式按钮，聊天标题区不再包含该按钮，模式按钮计算最小高度为 36 px；既有回归继续验证顶部连接状态、导航折叠控制、标题区横线、三处标题、分隔条、列宽、翻译、头像、接待状态和五页导航。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.2 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.5。整页既有未核验项不因本次局部模式按钮调整而标记通过。

### D-057｜中间聊天列内的顶部工作模式（1.4.3，2026-09-22）

顶部“人工”“AI辅助”“AI自动”三枚模式按钮限定在中间聊天列范围内，三等分且保留 12 px 间隔。按钮组最大宽度为 474 px、可用聊天列宽度的 84%；聊天列拉宽时组始终居中，拉窄时三枚按钮等比收窄并居中。只允许修改该按钮组的横向布局、版本和针对性验收；不改按钮功能、按钮高度、聊天列宽度规则、标题栏其余内容、聊天同步、其它布局、文字、图标、功能、数据或其它页面。

### D-057 实施核验（2026-09-22）

工作台隔离 Electron 回归读取中间聊天列和顶部模式按钮组实际边界，断言按钮组完全处在该列范围内，并断言三枚按钮宽度相等；既有回归继续验证顶部连接状态、导航折叠控制、标题区横线、三处标题、分隔条、列宽、翻译、头像、接待状态和五页导航。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.3 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.5。整页既有未核验项不因本次局部按钮分布调整而标记通过。

### D-058｜客户聊天列内的 WhatsApp 状态与残留浏览器恢复（1.4.4，2026-09-22）

顶部状态显示“WhatsApp 已连接/未连接”，采用绿色聊天图标，并固定在客户聊天列右侧边界内。连接启动前只识别并结束父进程已退出、仍占用应用专属 WhatsApp 浏览器资料目录的 Chrome/Chromium 进程；不删除登录资料、不结束正常运行的应用进程、不读取或发送任何聊天。只允许修改该状态显示、残留浏览器恢复、版本和针对性验收；不改 WhatsApp 账号、登录资料、聊天同步内容、列宽、其它布局、功能、数据或其它页面。

### D-058 实施核验（2026-09-22）

工作台隔离 Electron 回归断言 WhatsApp 状态文字、绿色聊天图标、10 px 状态球及其客户聊天列右边界；单元测试模拟已孤立的应用资料目录浏览器，断言它在新客户端初始化前被回收，并验证正常重连继续初始化。既有回归继续验证模式按钮、导航、标题区横线、分隔条、列宽、翻译、头像、接待状态和五页导航。`npm run check`、工作台 Electron 回归、孤立浏览器恢复测试、五页导航回归和 `git diff --check` 通过。1.4.4 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.4.5。整页既有未核验项不因本次局部状态与恢复修复而标记通过。

### D-059｜真实 WhatsApp 头像加载（1.4.5，2026-09-22）

客户聊天列表继续只展示 WhatsApp 返回的真实头像 URL。桌面本地页面的内容安全策略必须允许 `pps.whatsapp.net` 和 `mmg.whatsapp.net` 返回的头像图片加载；其它图片来源仍维持既有本地与 data URL 限制。WhatsApp 未返回头像、头像隐私不可见或 URL 失效时显示中性人物图标，不生成或替换虚构头像。只允许修改真实头像的加载许可、版本和针对性验收；不改聊天内容、联系人资料、排序、头像尺寸、列宽、其它布局、功能、数据或其它页面。

### D-059 实施核验（2026-09-22）

本地网页服务测试验证 CSP 仅额外允许两项 WhatsApp 头像 CDN，不放开任意外部图片；既有工作台 Electron 回归继续验证头像在圆形容器中 `object-fit: cover` 呈现，并保留连接、模式按钮、导航、标题区横线、分隔条、列宽、翻译和接待状态断言。`npm run check`、本地网页服务测试、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.5 仅直接运行本地开发版；按既定节点提交并推送 Git。整页既有未核验项不因本次局部头像加载修复而标记通过。

### D-060｜客户聊天与聊天正文的板块边界（1.4.6，2026-09-22）

客户聊天列和右侧聊天正文之间的既有可拖动分隔带，使用贯穿全高的两条浅灰紫边线与极浅底色，清楚表达两个独立板块。保留原有 8 px 可拖动命中区域、短紫色拖动提示、列宽与所有元素的位置、尺寸、文字、图标、功能和数据；不新增其它边框或修改其它板块。

### D-060 实施核验（2026-09-22）

工作台隔离 Electron 回归读取分隔带的计算背景色和双侧内阴影，确认板块边界清楚且不占用任一列宽；既有回归继续验证头像、连接状态、模式按钮、导航、标题区横线、列宽、翻译和接待状态。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.6 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.5.0。整页既有未核验项不因本次局部分隔带视觉加强而标记通过。

### D-061｜当前客户标题与客户列表同款身份样式（1.4.7，2026-09-22）

当前聊天标题中的选中客户，使用客户列表同款 42 px 圆形头像与电话号码并列展示；头像只使用该客户已经从 WhatsApp 返回的真实头像 URL，缺失或加载失败时使用中性人物图标。保留“真实 WhatsApp 会话 · 发送账号”说明、标题区高度、位置、翻译按钮、列宽、文字内容、功能、数据与其它页面；只调整该客户身份块的内部呈现。

### D-061 实施核验（2026-09-22）

工作台隔离 Electron 回归选中含头像的虚构会话，断言列表和当前客户标题均显示相同来源的头像图片、42 px 容器和 `object-fit: cover`；既有连接、模式按钮、导航、标题区横线、分隔带、列宽、翻译和接待状态断言继续通过。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.7 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.5.0。整页既有未核验项不因本次局部身份样式统一而标记通过。

### D-062｜客户消息头像与内容宽度（1.4.8，2026-09-22）

聊天正文中客户发来的消息，使用当前客户已返回的真实 WhatsApp 头像，并按文字实际宽度显示气泡；短消息不能扩展到聊天列的固定大宽度，长消息仍受既有 75% 最大宽度约束。头像缺失或加载失败时显示中性人物图标。保留消息内容、时间、发送者、译文、客户消息左侧与我方消息右侧的方向、颜色、所有列宽、控件、功能、数据和其它页面；只调整客户消息的头像与气泡宽度呈现。

### D-062 实施核验（2026-09-22）

工作台隔离 Electron 回归使用含头像的虚构客户消息，断言客户消息头像为 32 px、短消息总宽度小于消息列 75%，并保留现有真实头像、翻译、连接、模式按钮、导航、标题区横线、分隔带、列宽和接待状态断言。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.8 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.5.0。整页既有未核验项不因本次局部消息呈现调整而标记通过。

### D-063｜新内容的依次晃动提示点（1.4.9，2026-09-22）

当新消息已缓存但仍在当前视口下方时，原固定紫色“有新消息”跳转按钮改为三个 8 px 紫色提示点，依次向上晃动。提示持续到用户点击、滚动到最下方、或新内容已进入视野后自动消失；没有真正新增消息时不出现。保留既有点击滚到最新消息、缓存轮询、消息内容、消息布局、列宽、控件、功能、数据和其它页面。

### D-063 实施核验（2026-09-22）

工作台隔离 Electron 回归验证提示控件含三个 8 px 点、使用依次晃动动画，点击后自动隐藏；既有客户消息头像与宽度、翻译、连接、模式按钮、导航、标题区横线、分隔带、列宽和接待状态断言继续通过。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.4.9 仅直接运行本地开发版，不制作安装包、不提交 Git；下一个提交节点为 1.5.0。整页既有未核验项不因本次局部新内容提示调整而标记通过。

### D-064｜完全可见的新消息提示与订单自动补关联（1.5.0，2026-09-22）

聊天工作台中，仍在视口下方的新消息提示改为截图所示的三个红色提示点与浅弧线：无色块、无外框、完整可见，三个 9 px 点依次上移；缓存的新内容完全进入视野前持续显示。保留既有点击提示或滚到底部即隐藏的行为。

右侧“订单信息”须按当前真实 WhatsApp 会话的国际电话号码自动显示已存在订单。订单服务在会话首次读取后才就绪、或之后才返回订单时，工作台须重新按已有国家/地区规则关联并自动补充右栏，无需用户重新选择会话；多个订单继续保留最新订单默认显示和切换入口。只允许修改该提示视觉、工作台订单关联重试、版本和针对性验收；不改消息内容、电话号码、订单资料、消息滚动、列宽、其他页面或业务流程。

### D-064 实施核验（2026-09-22）

工作台隔离 Electron 回归使用虚构阿联酋订单电话 `971500000002`，确认右侧自动显示该订单的联系电话而非“暂无关联订单”；并验证无底色的三个 9 px 红色依次晃动点、浅弧线和点击隐藏交互。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.5.0 仅直接运行本地开发版；按五个版本节点提交并推送 Git。整页既有未核验项不因本次局部修复而标记通过。

### D-065｜新消息提示的可见性与停止条件（1.5.1，2026-09-22）

聊天工作台的新消息提示点必须完整露在输入区上方，不能被输入区、消息、底色或外框遮住；使用三个 11 px 红点与浅弧线。只在有新内容尚未完整进入视野时依次晃动；用户滚动到距离最新内容 70 px 内、点击提示或新内容进入视野后立即隐藏。

一次最新消息读取启动后，如 6 秒仍无法取得结果，停止点动画并临时显示“信息暂时无法获取”；该提示 3 秒后自动消失。不得持续晃动或长期停留。只允许修改这项工作台提示的显示、时序、版本和针对性验收；不改消息、订单、列宽、输入区、其它页面或业务流程。

### D-065 实施核验（2026-09-22）

工作台隔离 Electron 回归验证三个无底色的 11 px 红点、浅弧线和可见层级；模拟最新消息读取失败，确认 6 秒后显示“信息暂时无法获取”，并在自动停留期结束后隐藏。`npm run check`、工作台 Electron 回归、五页导航回归和 `git diff --check` 通过。1.5.1 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点为 2.0.0。整页既有未核验项不因本次局部提示修复而标记通过。

### D-066｜订单管理页的操作和列表修正（1.5.2，2026-09-22）

本条只允许修改订单管理页、其订单列表的真实读取与分页呈现、该页导航分界/折叠控件、版本与核验记录；聊单工作台的新建订单流程、订单详情、其他业务页、订单数据和聊天数据均受保护。已确认的 13 项为：

1. 顶部显示 API 连接状态；只有最近一次真实接口验证通过时才显示“API 已连接”，其他状态显示“API 未连接”，不可把已配置密钥伪装为已连接。
2. 订单管理页不显示“新增订单”；新建订单仍固定在聊单工作台，先由客户确认。
3. 查找订单输入框加宽。
4. 增加实际可筛选的下单日期起止范围。
5. 导航和正文之间保留与工作台一致的单条细分界线及拖动命中区。
6. 在“我的工作台”标题旁提供始终可见的导航折叠/展开按钮。
7. “订单管理”使用与客户聊天标题同级的标题字级。
8. 表格首列是有内边距的序号；下单日期按日期在上、时间在下显示。
9. 金额列表头写为“订单总金额”。
10. 每个商品单独一行，不压成一条逗号文本。
11. “订单详情”采用清晰的行内按钮样式。
12. 说明文字和原顶栏内部布局标识不在页面常驻；圆形问号紧跟标题，点击后可查看说明和布局设置并“应用”。
13. 同商品数量的订单采用相同的固定行高，商品每增加一行才等量增加该订单行高。

订单数量超出一页时在列表底部显示可操作的上一页/下一页与页码；筛选和检索会重置到第一页。此处的分页基于订单管理已读取的真实订单列表，不插入演示订单。

### D-066 实施核验（2026-09-22）

已通过 TypeScript、UI 构建、`git diff --check` 和隔离 Electron 回归。回归用 12 条虚构订单确认：订单管理页没有“新增订单”或 UI-040 内部标识；问号可打开说明和“应用”；第 1 页显示 10 条、可进入第 2 页；日期范围实际筛空；两商品订单渲染两行商品；导航可折叠。API 状态在测试中未冒充已连接。1.5.2 仅直接运行本地开发版，不制作安装包、不提交 Git，下一提交节点仍为 2.0.0。该次是订单管理页的 13 项针对性修复，其他页面及既有未核验项不因本条标记通过。

### D-067｜新消息提示停止规则的第三次修复（1.5.3，2026-09-22）

只允许修改聊天工作台的新消息提示的生命周期、紫色视觉、版本、测试和核验记录；不改消息内容、会话数据、列宽、输入区、翻译、订单区或其它页面。提示点必须在新消息已渲染到消息区后自行停止并隐藏，不能在后续成功轮询中遗留。读取 6 秒仍不能得到消息时，点动画停止并显示“信息暂时无法获取”；该提示 3 秒后自动隐藏。提示点按页面紫色 `#7c3aed` 呈现，从 11 px 缩小为 7 px，并同步缩短弧线、间距和晃动幅度。

### D-067 实施核验（2026-09-22）

隔离 Electron 回归在有新消息且未在底部的路径中确认新内容已渲染，1.1 秒后提示点为隐藏状态；同时模拟 `recent` 读取持续失败，确认第 6 秒出现失败提示、之后自动隐藏。回归也读取 7 px 紫色点、紫色弧线和紫色背景色。TypeScript、UI 构建和 `git diff --check` 通过。1.5.3 仅直接运行本地开发版，不制作安装包、不提交 Git，下一提交节点仍为 2.0.0。该次仅验收新消息提示，不表示工作台其它既有未核验项已通过。

### D-068｜纠正提示点隐藏样式与验收漏洞（1.5.4）

1.5.3 的隐藏验收结论撤回：旧测试只断言 hidden 属性，未断言实际渲染是否消失。加入实际可见性检查后，旧版 Electron 稳定复现 hidden=true、display=flex、仍有布局矩形。只允许修复提示点隐藏样式、向上阅读时的取消行为及关联测试、版本和文档；保留 7 px 紫色点、消息数据、翻译、布局、订单管理和其他页面。

隐藏状态必须 display:none 且无布局矩形；向上滚动时立即清除提示和等待提示的计时，阅读历史期间不被迟到响应或后台轮询重新唤起。连续失败仅临时提示一次，成功读取、主动刷新或重新选择会话后才允许下一次失败提示。验收须覆盖实际可见性、真实滚轮操作、迟到响应及连续轮询，不能仅断言 hidden 属性。

### D-068 实施核验

修改前先运行新增可见性断言，实际 Electron 失败并输出 hidden=true / display=flex / rects=1；修改后原工作台回归通过。新增独立回归用虚构消息在 Electron 中运行 61 秒，通过首次加载实际隐藏、7 px 紫色点本体可见、真实向上滚轮后隐藏、1.8 秒迟到响应不唤起、至少两次 10 秒后台轮询不唤起、失败 6 秒提示/3 秒隐藏、继续观察 20 秒不重复提示及恢复读取后隐藏。隐藏检查同时断言 display:none、零布局矩形、零运行中动画与 isVisible=false。

已查看 artifacts/message-cue-1.5.4 下显示点、向上滚轮隐藏、失败显示与失败隐藏的截图。TypeScript、UI 构建、共享兼容检查及项目检查通过。保持本地直接启动方式，不制作安装包、不提交 Git，下一提交节点仍为 2.0.0。此为提示点局部验收；未对真实聊天内容做故障注入，也未做全页验收，其他既有未核验项仍未核验。

### D-069｜全站统一导航折叠与版本尾部（1.5.5）

订单管理、商品库存、利润核算、助手配置、连接与设置，以及共享导航容器承载的详情页，使用和聊单工作台一致的“我的工作台”标题旁折叠/展开按钮。折叠状态包含工作台并跨页面保持；折叠后按钮继续显示，并朝右提供展开操作。各页旧的底部 WhatsApp 状态、连接提示及页面特有尾部信息全部移除，统一为侧栏底部版本号 `v1.5.5`。不改业务内容、页面正文、标题栏、导航条目、数据、其他控件或业务流程。

### D-069 实施核验

隔离 Electron 回归从订单管理实际点击折叠，依次切换商品库存、利润核算、助手配置、连接与设置和聊单工作台。所有目标页均保持折叠状态、保留“展开导航栏”按钮、显示 `v1.5.5`，且业务页没有旧 `.connection`、`.wa` 或 `.up-status` 底部状态元素。订单管理原有分页、日期筛选、逐商品行、说明问号与无新增订单入口回归继续通过。此为导航与尾部的局部验收；未重新验收各页面业务正文和其它既有未核验项。保持本地直接启动方式，不制作安装包、不提交 Git，下一提交节点仍为 2.0.0。

### D-070｜订单管理的真实 ShopPlus 同步（1.5.6，2026-09-22）

本条只允许修改订单管理页的 ShopPlus 连接状态、订单同步入口和弹窗、对应的本机加密凭据与只读订单读取服务、版本、测试和核验记录。原先显示 AI 服务状态的“API 已连接/未连接”按钮不再用于订单管理，改为紧凑的 ShopPlus 状态与“同步最新订单”主操作；状态只可反映已配置、正在验证、真实验证成功或同步失败，不能把 AI 配置或仅保存密钥显示成 ShopPlus 已连接。

同步弹窗应清楚显示连接状态、最近成功同步时间、配置 ShopPlus API、开始同步和从文件导入入口。App Key 与 API Secret 只经系统安全输入窗口保存到本机加密存储，页面不显示其内容。同步只调用 ShopPlus 的读取订单接口；将新读到且本机不存在的订单加入订单管理，已有相同 ShopPlus 订单号只跳过和计数，不覆盖本机订单或向店铺后台写入、修改订单。缺少成本、业务商品编码或无法确定的履约状态要保留真实原始资料并标为待核对，不能补造资料或以零成本替代。保留订单管理既有搜索、筛选、分页、逐商品行、订单详情和文件导入；不改聊单工作台、订单详情、其他业务页、聊天数据或既有订单资料。

### D-070 实施核验（2026-09-23）

隔离单元测试使用虚构 App Key、Secret、接口返回与 SQLite 数据库，确认签名按 ShopPlus 参数排序生成、请求体不含 Secret；首次同步只新增一单，第二次相同订单号仅跳过；缺成本商品保持 NULL 成本和待核对状态。订单管理隔离 Electron 回归实际打开“同步最新订单”弹窗，确认不再显示“API 已连接”、未配置状态提供“配置 ShopPlus API”、同步按钮在未配置时禁用，并继续通过原有分页、日期筛选、逐商品行、导航折叠和版本检查。`npm run check`、TypeScript、UI 构建、ShopPlus 单元测试、订单管理 Electron 回归和 `git diff --check` 通过。1.5.6 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点仍为 2.0.0。未用真实凭据发起接口请求，用户配置后首次同步的真实返回字段仍需在其店铺资料环境中核对；其他页面及既有未核验项不因本条标记通过。

### D-071｜ShopPlus 商品明细重复编号写入修复（1.5.7，2026-09-23）

本条只允许修改 ShopPlus API 同步的本机商品明细标识、同步弹窗的错误文字、版本、测试和核验记录。真实 ShopPlus 商品的原始 `id` 可能在不同订单中重复，不能单独写入具有全局唯一索引的 `shopplus_sub_order_id`，否则一批有效订单会整体失败。本机 API 导入改为用“订单号 + 原始商品来源编号 + 该订单商品行序号”构造稳定标识；这只是本机去重键，不修改店铺后台、原订单号、金额、商品资料、成本或状态。相同订单号仍整个跳过；其他订单管理、聊天、订单详情、文件导入和既有资料受保护。若旧版错误再次出现，弹窗必须显示可理解的重试说明，不能把 SQLite 技术错误直接给用户。

### D-071 实施核验（2026-09-23）

隔离 SQLite 测试使用两个不同的虚构订单，并让它们返回相同的商品 `id`。两单均成功写入，生成的本机明细标识分别包含各自订单号；再次同步时两单均按既有订单号跳过。缺成本仍为 NULL 并保持待核对。`npm run check`、TypeScript、UI 构建、ShopPlus 同步单元测试、订单管理 Electron 回归和 `git diff --check` 通过；Electron 回归还确认同步弹窗、原有分页、日期筛选和逐商品行保持可用。已直接启动 1.5.7 本地窗口并确认订单数据库初始化、主窗口显示和既有 WhatsApp 认证连接恢复。未再用真实订单发起同步请求；实际店铺的下次同步仍应由用户在当前窗口点击后核对结果。未重新验收其他页面或既有手机底部 P1/P2 偏差。

### D-072｜聊单工作台右侧订单信息列（1.5.8，2026-09-23）

本条只允许修改桌面聊单工作台的右侧订单信息列、其本机确认记录和必要桥接、版本、测试和核验记录。订单编号旁、订单信息卡和地址核对卡中的履约/核对状态一律使用中文业务文案，不向用户显示 `shipped_pending`、`unusable` 等内部英文枚举。客户信息之前增加独立的“当前进度”卡，按真实订单确认、地址核对与履约事实显示当前阶段；不可用或资料待补时必须明确提示待核对，不能捏造已确认状态。

“客户信息”“订单信息”“地址核对”标题前必须各自显示语义图标。三张卡均有并排的“编辑&确认”和“客户确认”按钮：客户资料编辑保留原有本机收件资料人工覆盖，客户资料确认只记录确认事件；订单编辑只允许人工更正本机履约状态且保留原因/审计，订单客户确认使用既有订单确认；地址编辑进入收货地址编辑，地址客户确认以人工确认地址记录为准。所有写入只发生在本机，必须有人工点击与确认，不能修改 ShopPlus 或 WhatsApp。现有 AI 客户资料识别尚未接入这个新版右栏，不能显示成已启用、不能自动修改资料；仅可明确标注为“未接入当前工作台”。保留左侧会话、聊天消息、翻译、右栏订单切换、报单 TXT、列宽和其它页面。

### D-072 实施核验（2026-09-23）

隔离 Electron 工作台回归使用临时虚构订单和伪造聊天接口，实际验证右栏进度卡显示“待补齐订单资料”、客户/订单/地址三张卡的 Lucide 图标、六个“编辑&确认 / 客户确认”按钮、订单编辑弹窗、地址编辑弹窗，以及客户资料确认写入后的“已由人工确认”反馈。断言右栏不显示 `shipped_pending`，并明确显示“AI资料识别：未接入当前工作台，不会自动更新客户资料”。隔离服务回归确认客户资料确认写入本机事件、订单状态更正携带依据并更新包裹审计；ShopPlus 重复商品编号同步回归继续通过。`npm run check`、TypeScript、UI 构建、ShopPlus 单元测试、工作台 Electron 回归、订单管理 Electron 回归和 `git diff --check` 通过。已直接启动 1.5.8 本地窗口，订单数据库初始化、主窗口显示及既有 WhatsApp 认证连接恢复均已确认。未调用真实订单同步，未重新验收工作台其它内容、其它页面或既有手机底部 P1/P2 偏差。

### D-073｜工作台右栏编辑弹窗局部尺寸与状态选项（1.5.9，2026-09-23）

本条只允许修改聊单工作台右栏“客户信息校正”“地址信息校正”“编辑订单信息”三种弹窗的可用宽度、字段宽度和订单履约状态下拉选项外观。三个弹窗在常规窗口中扩大到更舒适的阅读宽度，客户与地址字段和核对备注必须在标签右侧占满可用输入区；窄窗口可自然收为单列。订单履约状态选择器必须使用与应用一致的浅色、圆角、细描边、清晰焦点与下拉提示，不能呈现浏览器默认的粗糙样式。字段、文案、按钮、保存逻辑、本机写入、其它弹窗和其他页面一律不改。

### D-073 实施核验（2026-09-23）

TypeScript、UI 构建、共享兼容与项目检查、`git diff --check` 均通过。隔离 Electron 工作台回归实际打开客户、订单和地址三种弹窗，确认客户与地址弹窗宽度不小于 640 px、相应输入框宽度不小于 400 px；确认订单状态选择器使用 `appearance:none`、9 px 圆角和不小于 42 px 的点击高度。订单管理隔离 Electron 回归继续通过。1.5.9 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点仍为 2.0.0。本条只验收这三类弹窗，未重新验收工作台其它内容、其他页面或既有手机底部偏差。

### D-074｜工作台右栏订单商品编辑（1.6.0，2026-09-23）

本条只允许替换聊单工作台右栏“订单信息”的编辑弹窗和必要的本机订单编辑桥接。编辑器必须能显示现有商品、添加商品、删除商品、修改数量、修改 AED 单价及所属包裹，并同时保留履约状态与明确的修改依据。保存时只更新本机订单明细、订单总额、包裹/利润重新计算与审计记录；不得修改 ShopPlus 后台、原始 API 资料或 WhatsApp。商品数量必须为正整数，价格必须为非负 AED 金额，订单至少保留一件商品且每个现有包裹至少保留一件商品。已有签收、拒收或取消等最终履约结果的订单不允许修改商品，防止篡改结算记录；已生成报单的可编辑订单保存后必须重新核对报单。保留客户资料、地址核对、聊天、其它订单页和所有其他页面。

### D-074 实施核验（2026-09-23）

TypeScript、UI 构建、共享兼容与项目检查、`git diff --check` 均通过。隔离服务回归以虚构 ShopPlus 订单实际验证：将原商品改为 2 件、AED 45.50，新增一件 AED 10.00 商品后订单总额为 AED 101.00；删除原商品并将保留商品改为 3 件、AED 11.00 后订单总额为 AED 33.00，数据库只保留一条商品并写入两条本机编辑审计。隔离 Electron 工作台回归实际打开编辑器，确认状态选择器、商品行、新增商品按钮、商品名称/编号、数量和单价输入框均可见。订单管理隔离 Electron 回归继续通过。1.6.0 仅直接运行本地开发版，不制作安装包、不提交 Git；下一提交节点仍为 2.0.0。本条只验收订单编辑器与本机订单重算，未重新验收工作台其它内容、其他页面或既有手机底部偏差。

### D-075｜聊天工作台非文字消息读取与显示（1.6.1，2026-09-23）

本条只允许修改桌面聊单工作台的真实 WhatsApp 消息标准化、可安全缓存的媒体读取、消息气泡呈现、版本、测试和核验记录。图片、贴纸和 GIF 在 WhatsApp 库能返回安全尺寸的本机附件时，必须在对应消息气泡内显示缩略内容；未缓存时显示准确的媒体类型和“正在读取/请在 WhatsApp 查看”的状态，不能显示为空白或泛化的“非文字消息”。网址保留原文字并提供安全的可点击 HTTP/HTTPS 链接。语音、视频、文件、位置和联系人等不能在本机展开的类型，应显示各自中文类型、文件名或说明；不假称已取得其内容。

保留文字内容、消息方向、客户头像、身份与时间、逐条翻译、消息滚动、翻译调用、发送、右侧订单信息、导航、布局和其他页面。媒体读取只在已选择并已核对的 WhatsApp 会话内发生，不发送消息、不修改 ShopPlus 或订单资料，也不自动读取未选择的真实聊天。

### D-075 实施核验（2026-09-23）

隔离单元测试使用虚构的 WhatsApp 贴纸和 GIF 附件，确认读取前保留“贴纸 / GIF 动图”类型，安全媒体写入本机缓存后带有相应 MIME 数据地址。隔离 Electron 工作台回归使用虚构贴纸、网址和文件消息，实际确认贴纸缩略图显示在对应气泡，HTTP/HTTPS 网址提供安全链接，文件卡显示“文件 · invoice.pdf”和 WhatsApp 查看说明；既有客户头像、身份与时间、翻译、订单编辑、导航和新消息提示断言继续通过。`npm run check`、`git diff --check`、手动聊天单元测试和工作台 Electron 回归通过；已直接启动 1.6.1 本地窗口，确认主窗口显示并恢复既有 WhatsApp 认证连接。未对真实聊天实际媒体逐条读取，也未重新验收其他工作台区域、其他页面或既有手机底部 P1/P2 偏差；语音、文件、位置和联系人在 WhatsApp 未提供可安全展开附件时仅显示准确类型和说明。

### D-076｜工作台 AI 聊天资料建议与人工更新（1.6.2，2026-09-23）

本条只允许修改工作台当前已选会话的 AI 资料识别入口、对应消息的“可更新”提示、右侧客户资料卡的建议行与必要本机桥接。用户明确点击“AI 识别当前会话”后，才可把当前已读取的该会话文字消息交给已配置的 AI；AI 只可提出姓名、电话、国家/地区、城市、街道和详细地址建议，并且每项必须有客户原文消息依据。没有 AI 配置、没有关联订单、没有建议或证据不足时明确说明，不假造建议。

每条建议在右栏显示当前值、新值、来源消息和“更新”；点击更新必须再显示“是否更新”的是/否确认。只有点击“是，更新”才写入本机订单，并记录“AI 聊天建议经人工确认”及消息来源；“否”只关闭确认，不写入。不得自动更新、发送 WhatsApp、修改 ShopPlus 后台或改变其他聊天、订单、布局、翻译、订单信息卡、地址核对卡和页面元素。

### D-076 实施核验（2026-09-23）

隔离 Electron 工作台回归确认客户资料卡存在“AI 识别当前会话”手动入口，并继续通过客户头像、消息身份与译文、媒体显示、订单编辑、导航和新消息提示的既有断言。TypeScript、UI 构建、共享兼容检查、项目检查、手动聊天单元测试与 `git diff --check` 通过；1.6.2 本地窗口已启动，主窗口显示且既有 WhatsApp 认证连接恢复在线。未用真实聊天或真实 AI 配置发起识别请求，因此不宣称某条真实客户资料已被识别；用户首次点击入口时，只有已配置 AI、当前文字消息和明确客户原文依据同时具备才会显示建议。未重新验收其他工作台区域、其他页面或既有手机底部 P1/P2 偏差。

### D-077｜订单商品编辑弹窗的紧凑五列明细（1.6.3，2026-09-23）

本条只允许修改工作台右栏“编辑订单信息”弹窗的整体宽度、履约状态选择器长度和商品明细的内部排版。弹窗从过宽状态收至常规桌面下舒适的中等宽度；履约状态选择器不再横跨整行，长度以选择状态文字和下拉操作为宜。商品明细改为一行五列：商品、包裹、数量、单价（AED）、删除；列标题清楚，编辑项作为同一张明细表中的列，不再在外层面板中嵌套一张张圆角框。新增商品仍在商品列内填写名称和编号。保留所有字段、商品新增/删除、数量/价格/包裹编辑、履约状态、修改依据、保存逻辑、本机写入、按钮、文案和其他页面。

### D-077 实施核验（2026-09-23）

TypeScript、UI 构建和 `git diff --check` 通过。隔离 Electron 工作台回归实际打开订单编辑器，确认弹窗宽度在 720–800 px、履约状态选择器宽度不超过 540 px，商品明细含商品、包裹、数量、单价、操作五个列标题；既有状态选择器、添加产品、商品名称/编号、数量和单价输入框断言继续通过。只验收此弹窗内部排版，未重新验收工作台其他区域、其他页面或既有手机底部 P1/P2 偏差。

### D-078｜聊天工作台地址资料与位置更新提示（1.6.4，2026-09-23）

本条只允许修改聊天工作台右侧“地址核对”卡片、该卡的本机地址编辑字段、位置与 Google Maps 地址提示、必要的 WhatsApp 位置描述读取、版本、测试和核验记录。卡片在原有“核对状态”和“说明”之间，按一行一个字段显示国家/地区、省/州、城市、街道、详细收货地址；空值明确显示“—”。原有图标、折叠、核对状态、两个按钮及相邻客户、订单、报单区保持原样。

客户发来的 WhatsApp 位置描述，或 Google Maps 链接中已经明确携带的地址文字，可在读取会话时自动形成“可更新”建议，且在对应消息显示提示。用户必须点击更新并再次确认，才写入本机订单；不修改 WhatsApp、ShopPlus 后台、订单状态或地址核对结果。只有短链、地点名或经纬度而没有可核实详细地址时，不能编造省市街道或门牌；须在 Google Maps 展开或由客户补充地址后再建议更新。

### D-078 实施核验（2026-09-23）

`npm run check`、`git diff --check`、手动聊天单元测试和隔离 Electron 工作台回归通过。Electron 回归使用虚构客户消息中的 Google Maps 地址查询，确认地址卡逐行显示五个地址字段、地图消息有“! 可更新”、地址卡有更新项；点击更新并确认后，详细收货地址写入虚构本机草稿。未读取真实客户聊天、位置或地图链接，未向 Google Maps、WhatsApp 或 ShopPlus 发请求。只验收本条范围；其他工作台区域、其他页面和既有手机底部 P1/P2 偏差仍未重新验收。

### D-079｜工作台确认按钮与客户同意提示（1.6.6，2026-09-23）

本条只允许修改聊天工作台右侧客户信息、订单信息、地址核对三张卡的“客户确认”状态与聊天中的确认建议弹窗，及必要本机确认记录、版本、测试和核验记录。每张卡确认完成后，该卡的“客户确认”按钮变为不可用；编辑并保存该卡相应资料后，确认作废且按钮重新可用。三张卡的图标、字段、布局、其他按钮及其他页面不得改变。

客户在本机已发送消息后回复明确同意词（如 `OK`、`Yes`、`Confirmed`、`好的`）时，可显示“AI 提示：客户可能已确认”的弹窗。建议必须说明所推断的确认对象和原回复，并提供“暂不确认”和“确认”两项；只有点击确认才写入本机对应确认记录。模糊回复、没有前序我方消息、已经确认的卡片或确认失败时都不能假称已确认，更不能发送 WhatsApp、修改 ShopPlus 后台或修改聊天原文。

### D-079 实施核验（2026-09-23）

TypeScript、UI 构建、共享兼容和项目检查、`git diff --check`、手动聊天单元测试通过。隔离 Electron 工作台的虚构流程确认：人工确认客户资料后按钮禁用；编辑并保存客户资料后按钮恢复可用；随后虚构我方确认请求和客户 `Yes` 触发 AI 提示，只有点击“确认客户资料”后按钮再次禁用。未读真实聊天或调用真实 AI。完整工作台回归在其原有的新消息失败提示计时断言处未通过；该路径不在本条修改范围，本条不将其标记为通过。其他页面、工作台其他区域和既有手机底部 P1/P2 偏差仍未重新验收。

### D-080｜桌面应用常驻运行与异常恢复（1.6.5，2026-09-23）

用户反馈 Electron 软件会中断而需反复重新打开，要求尽可能持续运行。本条只允许修改桌面主进程的窗口关闭、渲染进程恢复、直接本地启动守护、版本、文档和针对性回归。点击 macOS 左上角红色关闭键时，主窗口必须隐藏到菜单栏而不能销毁；后台本地服务、已启动的 WhatsApp 连接和资料不应因此停止。菜单栏图标点击必须重新显示同一窗口；只有菜单栏“退出”才可终止应用。

渲染进程异常退出时，应用在后台自动重建窗口，最多在一分钟内自动尝试两次；若连续恢复失败，清楚提示用户从菜单栏退出后重新打开并导出诊断报告。`npm run start` 需在外层守护 Electron：子进程因异常信号或非零退出码终止后延迟重启；正常退出不重启。不得改工作台、订单、聊天、WhatsApp 业务数据、菜单栏原有“打开管理页面 / 退出”语义或其他页面元素。

### D-080 实施核验（2026-09-23）

实际隔离 Electron 回归调用主窗口关闭，确认同一窗口未被销毁且隐藏；等待关闭动作产生的激活抑制窗口结束后，发送 macOS `activate`，确认同一窗口重新显示。该回归曾暴露出 Electron 在 `close` 事件内调用 `hide()` 会把窗口自动恢复显示的行为；已改为在关闭事件返回后 100 ms 隐藏，并复测通过。`npm run check`、TypeScript、UI 构建、共享兼容检查、项目检查和 `git diff --check` 通过。

已直接启动当前 1.6.6 本地应用，日志确认订单数据库初始化、主窗口显示及 WhatsApp 自动消息连接在线；进程树确认 `npm run start`、`scripts/run-desktop.mjs` 和 Electron 子进程均在运行。未制作安装包、不提交 Git，下一提交节点仍为 2.0.0。渲染进程自动恢复和外层守护的异常重启逻辑已作代码与进程链核对，但不能以人工制造主进程崩溃干扰当前用户正在使用的真实连接；该路径会在真实的异常非零退出时执行。

### D-081｜工作台订单卡片状态标题与折叠箭头（1.6.7，2026-09-23）

本条只允许修改聊天工作台右侧“客户信息”“订单信息”“地址核对”三张订单卡片的标题栏、版本、测试和核验记录。三张卡右上角原有的“收起/展开”可见文字改为同级大小、清晰可点击的 Lucide 向上/向下箭头；箭头左侧增加“状态”标题和对应状态值。客户确认完成后，对应卡片标题栏状态值显示绿色“已确认 ✓”；未确认时显示中性的“待确认”。报单 TXT、卡片字段、按钮、确认写入逻辑、聊天、订单数据、布局和其他页面保持不变。

### D-081 实施核验（2026-09-23）

TypeScript、UI 构建、共享兼容和项目检查、手动聊天单元测试、相邻的新消息提示 Electron 回归及 `git diff --check` 通过。隔离 Electron 工作台的虚构订单流程实际确认：客户信息、订单信息、地址核对三张卡均显示“状态 / 待确认”和 Lucide 向上箭头；点击卡片标题后内容收起且箭头切为向下；人工确认客户资料后，对应标题状态显示绿色“已确认 ✓”，同时原有确认按钮禁用。完整工作台回归仍在既有“新消息失败提示计时”断言处未通过；该路径不在本条修改范围，本条不将其标记为通过。报单 TXT、其他字段、其他工作台区域、其他页面及既有手机底部 P1/P2 偏差未重新验收。

### D-082｜聊天工作台非文字媒体的实际自动读取（1.6.8，2026-09-23）

本条只允许修改已选、已核对 WhatsApp 会话中的非文字媒体读取和呈现所需适配器、工作台消息状态、版本、测试和核验记录。图片、贴纸、GIF 和可安全缓存的视频，即使当前会话未开启订单在线同步，也必须在首次读取后自动下载并在后续刷新中显示实际缩略内容；不得因该会话不是后台在线同步目标而永久停留在“请在 WhatsApp 查看”。未知媒体类型但具有可下载附件时，应尝试读取并根据返回 MIME 类型显示；不支持或无法安全缓存的内容须显示准确类型和原因，不得笼统显示“非文字消息”。

只读取当前已选、已核对会话的既有消息附件，不发送 WhatsApp、不修改订单、ShopPlus、聊天原文或其他页面；保留文字、网址、头像、消息方向、时间、翻译、右侧订单栏和布局。

### D-082 实施核验（2026-09-23）

第一轮隔离单元测试使用未加入订单在线同步目标的虚构已核对会话和 `unknown + hasMedia` 消息，确认首次返回“图片正在读取”，后台安全下载后下一次读取返回 `image / cached` 与 PNG 数据地址。第二轮隔离 Electron 测试使用同一条先不可用、后缓存的虚构图片，确认聊天气泡先显示“图片”读取状态，刷新后出现实际缩略图，且不再保留“[图片]”或“非文字消息”占位。未读取真实客户聊天、未发送 WhatsApp、未修改订单或 ShopPlus。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过；本条不将该不相关路径标记为通过。其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-083｜地址核对的地图确认与本机配送城市库（1.6.9，2026-09-23）

本条只允许修改工作台右侧地址核对卡、必要本机配送城市库、Google Maps 打开操作、版本、测试与核验记录。卡片须提供“打开 Google 地图”“确认可配送”“标记不可配送”三个动作；后两项必须由人工确认后才写入本机。确认可配送时将订单城市加入配送城市库并移出禁配名单；标记不可配送时将城市加入禁配名单并从配送城市库移出。城市库供后续地址范围核对使用。不得自动判断配送范围，不得发送 WhatsApp、修改 ShopPlus 或修改客户地址；其它卡片、字段和页面保持不变。

### D-083 实施核验（2026-09-23）

`npm run check`、`git diff --check` 和地址范围单元测试通过。隔离 Electron 回归实际创建虚构订单并验证：地址核对卡显示“打开 Google 地图”“确认可配送”“标记不可配送”三项操作；地图操作在测试模式返回成功，确认可配送后当前城市写入临时本机 `orders/delivery-zones.json` 的配送城市库，随后标记不可配送后该城市从配送城市库移除并写入禁配名单，订单地址状态改为不可配送。未打开真实 Google Maps，未读取真实客户地址或聊天，未发送 WhatsApp，未修改 ShopPlus、客户地址或订单内容。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过；该路径不在本条修改范围，本条不将其标记为通过。其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-084｜客户聊天筛选徽标居中与提示文字（1.6.10，2026-09-23）

本条只允许修改聊天工作台左侧“全部”“未读”“未回”筛选按钮的数字徽标对齐、三项鼠标悬停提示、版本、测试与核验记录。每个可见红色圆形数字徽标内的数字必须在水平和垂直方向居中，单数和双数数字都不能偏移；按钮原有大小、颜色、筛选逻辑、数字含义和相邻搜索/刷新/会话列表保持不变。“全部”的提示为“全部对话”，“未读”为“客户信息未读”，“未回”为“未回客户信息”。

### D-084 实施核验（2026-09-23）

`npm run check`、`git diff --check` 和隔离 Electron 回归通过。回归使用 20 条虚构会话（其中 2 条未读且待回复），实际确认“全部 / 20”“未读 / 2”“未回 / 2”三种徽标均为 18×18 px 圆形，文字几何中心与圆心的水平、垂直偏差均不超过 1 px；三枚按钮的原生悬停说明分别为“全部对话”“客户信息未读”“未回客户信息”。只验收筛选按钮及徽标，未重新验收会话列表、聊天、订单栏、其他页面或既有手机底部 P1/P2 偏差。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过，本条不将该不相关路径标记为通过。

### D-085｜报单 TXT 卡片的只读内容、人工核对与下载（1.6.11，2026-09-23）

本条只允许修改聊天工作台右侧“报单 TXT”卡、其本机报单预览/核对交互、版本、测试与核验记录。报单卡默认展开，显示当前完整报单 TXT 的只读内容；尚不能生成时显示明确待补项目，不能编造报单。卡片下方必须有“核对订单”和“下载报单 TXT”两个动作：核对订单打开含报单资料列表的弹窗，资料可在本机修正，点击“订单确认”后该卡按钮改为绿色“已核对 ✓”；下载报单 TXT 必须继续使用系统指定位置保存窗口。不得修改 ShopPlus、WhatsApp、客户聊天或报单原文；原有客户、订单、地址和其他页面保持不变。

### D-085 实施核验（2026-09-23）

`npm run check`、`git diff --check`、草稿与订单确认单元测试（24 项）以及隔离 Electron 回归通过。Electron 回归使用虚构会话和订单，确认报单卡默认展开并以 `pre` 呈现不可编辑的内容/待补项，显示“核对订单”和“下载报单 TXT”；核对弹窗实际显示 8 个可编辑资料字段、商品明细、“编辑商品明细”和“订单确认”，确认后卡片操作变为不可用的“已核对 ✓”。订单服务单元测试覆盖虚构完整订单从草稿、人工确认、地址确认、报单生成到保存的实际路径。下载操作继续调用已有本机系统保存位置对话框；未向 ShopPlus、WhatsApp 或真实客户数据写入。只验收报单卡及其弹窗，未重新验收客户/订单/地址卡、聊天、其他页面或既有手机底部 P1/P2 偏差。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过，本条不将该不相关路径标记为通过。

### D-086｜报单核对状态独立于客户订单确认（1.6.12，2026-09-23）

本条只允许修正 D-085 的报单核对状态语义、必要本机事件、版本、测试与核验记录。“已核对 ✓”只能由报单卡“核对订单”弹窗的“订单确认”产生，不能因为客户信息卡或订单信息卡已确认而提前显示。报单资料、商品或地址任一修改导致报单失效后，报单核对状态必须重新变为“核对订单”。不得改变客户订单确认、ShopPlus、WhatsApp、聊天或其他卡片逻辑。

### D-086 实施核验（2026-09-23）

`npm run check`、`git diff --check`、草稿与订单确认单元测试（24 项）和隔离 Electron 回归通过。Electron 以虚构草稿订单实际打开报单核对弹窗并点击“订单确认”，确认系统写入独立 `report_order_reviewed` 本机事件，报单卡才变为不可用的“已核对 ✓”；该动作不调用客户订单确认接口。报单失效路径仍由既有 `report_invalidated` 事件清除该状态。未读取或写入真实聊天、ShopPlus、客户资料。只验收 D-086 的状态语义，其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收；完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过，本条不将该不相关路径标记为通过。

### D-087｜地址卡精简操作与地图入口（1.6.13，2026-09-23）

本条只允许修改聊天工作台右侧地址核对卡和地址编辑弹窗、版本、测试与核验记录。地址卡底部只保留“编辑地址”和“配送设置”两项操作；删除卡内“客户确认”和直接地图/可配送/不可配送按钮。打开 Google 地图入口移至地址编辑弹窗；配送设置必须先打开选择弹窗再让人工选可配送或不可配送。卡片不再同时显示重复的“说明”字段；地址字段明确显示“街道 / 门牌”和“楼栋 / 单元”，两项共同作为 Google Maps 的详细地址查询。不得更改配送城市库写入逻辑、ShopPlus、WhatsApp、客户地址数据、其他卡片或其他页面。

### D-087 实施核验（2026-09-23）

`npm run check`、`git diff --check` 与隔离 Electron 回归通过。回归实际创建虚构订单，确认地址卡仅有“编辑地址”“配送设置”两个操作，不再直接显示地图、可配送、不可配送或客户确认按钮，也不再重复显示“说明”。地址卡和编辑弹窗均显示“街道 / 门牌”“楼栋 / 单元”；地址编辑弹窗内有“在 Google 地图查看当前完整地址”入口，配送设置弹窗要求填写地图核对依据并提供“设为可配送”“设为不可配送”。同一隔离流程实际确认城市先写入临时本机配送城市库，随后被移出并写入禁配名单。未打开真实 Google Maps，未读取或写入真实客户地址、WhatsApp、ShopPlus 或订单资料。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过；本条不将该不相关路径标记为通过。其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-088｜客户资料逐项确认（1.6.14，2026-09-23）

本条只允许修改聊天工作台“客户信息校正”弹窗中的字段布局、逐项确认交互、必要测试、版本和核验记录。客户资料编辑行的输入区域须相对当前布局向左移动；姓名、联系电话、国家/地区、省/州、城市、街道、详细地址各行末尾须有独立“确认”操作。输入内容变动后该行确认自动作废；点击确认后显示“已确认 ✓”。点击底部“编辑&确认”时，只写入已逐项确认的客户资料；没有任何逐项确认时提示用户。地址校正弹窗、地址卡、订单卡、客户确认卡片操作、客户资料字段、ShopPlus、WhatsApp 和其他页面不得改变。

### D-088 实施核验（2026-09-23）

`npm run check`、`git diff --check` 与两项隔离 Electron 回归通过。客户资料回归实际创建虚构订单，确认客户资料弹窗有 7 条资料编辑行及 7 个“确认”按钮，输入框相对弹窗左边界小于 190 px；点击确认后显示“已确认 ✓”，再次修改内容会恢复为“确认”。该回归实际保存一个确认后的姓名与一个未确认的电话，确认只有姓名写入本机订单。地址配送回归再次通过，确认本次改动未影响地址卡、地图入口和本机城市库/禁配名单写入。未读取或写入真实客户资料、WhatsApp、ShopPlus 或订单。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过；本条不将该不相关路径标记为通过。地址校正弹窗、地址卡、订单卡、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-089｜订单编辑说明收纳与六列商品明细（1.6.15，2026-09-23）

本条只允许修改聊天工作台“编辑订单信息”弹窗的标题说明、商品明细表头与行内控件、版本、测试和核验记录。标题“编辑订单信息”后与“商品明细”后各有一个可点击圆形问号；原有两段对应说明只在各自问号提示中显示，不能占据弹窗正文。商品明细表头改为“序号、编号、商品、商品数量、包裹数量、单价”，六列均匀排布，字号较原表头增大 2 px。已有商品的编号和商品名称以非编辑文字显示；商品数量使用紧凑减号、数值、加号控件，单价保持可编辑。包裹归属继续使用已有包裹选择，但显示为包裹数量与单位，不能伪造可保存的包裹数量数据。保留删除商品能力，但不新增第七个表头。添加产品按钮置于商品明细标题行的右侧。不得修改订单金额计算、商品新增/删除/保存语义、包裹归属、履约状态、ShopPlus、WhatsApp、其他订单卡或页面。

### D-089 实施核验（2026-09-23）

`npm run check`、`git diff --check` 与三项隔离 Electron 回归通过。订单编辑回归实际打开虚构订单的编辑弹窗，确认“订单编辑说明”“商品金额说明”两个圆形问号存在，订单说明默认隐藏，点击问号后才显示。商品明细实际显示“序号、编号、商品、商品数量、包裹数量、单价”六列，表头为 12 px；添加产品按钮几何位置位于标题行右侧。点击添加产品后，新行保留商品名称、商品编号和单价编辑能力，商品数量显示减号、数值、加号控件。客户资料与地址配送相邻回归再次通过。未改变订单计算、保存服务、包裹归属、ShopPlus、WhatsApp 或真实订单资料。完整工作台旧回归仍在既有的新消息失败提示计时断言处未通过，本条不将该不相关路径标记为通过。其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-090｜聊天窗口可视图片按需读取（1.6.16，2026-09-23）

本条只允许修改已选、已核对 WhatsApp 聊天中图片附件的读取调度、聊天消息实际图片呈现、必要本机桥接、版本、测试和核验记录。打开或刷新会话不得再为未显示的历史消息批量下载图片；仅当当前聊天窗口内的图片消息实际进入可视区域时，才读取该单条附件，并将成功取得的安全图片直接显示在原消息气泡。滚动到另一张可见图片后才读取该张；当前视口外的图片不预加载。未知类型但带附件的可见消息应尝试一次实际读取，以便依据 MIME 转成图片显示。下载失败、类型不是安全图片或图片超过现有限制时，保留准确状态，不冒充图片，不自动无限重试。不得发送 WhatsApp、读取非当前已选会话、修改订单、ShopPlus、聊天原文、卡片或其他页面。

### D-090 实施核验（2026-09-23）

`npm run check`、`git diff --check` 与手动聊天单元测试（9 项）通过。隔离 Electron 工作台显示版本 `1.6.16`，并以 40 条虚构图片消息验证：初次进入会话后只对底部当前视口内的消息发起图片读取，读取数小于 40；未进入视口的历史图片没有一并读取。滚动到最早的一条 `unknown + hasMedia` 消息后，才读取该条，并按实际 PNG MIME 作为图片直接显示。适配器单元测试还确认 `recent` 返回图片读取状态而不自动缓存，只有通过当前已核对会话的单条 `retryMedia` 请求才读取附件。未读取真实客户聊天、未发送 WhatsApp、未修改订单或 ShopPlus。完整工作台旧回归仍在既有“新消息失败提示计时”断言处未通过；本条不将该不相关路径标记为通过。其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-091｜聊天翻译按当前视口排队与视口稳定（1.6.17，2026-09-23）

本条只允许修改聊天工作台自动翻译的候选范围、排队时机、翻译结果写入后的滚动锚定、版本、测试和核验记录。自动翻译只可读取当前聊天消息滚动区域内实际可见的未译文本；会话初次打开、刷新或读取更早记录时，屏幕外的消息不得进入缓存查询或翻译请求。用户滚动到其他消息位置后，才把新进入当前视口的未译文本排入下一次翻译。保留现有手动逐条翻译和“翻译当前可见内容”入口。

翻译开始、缓存译文写入和翻译结果返回导致消息高度变化时，必须保持当前视口第一条可见消息及其相对顶部位置不变；用户正在阅读的内容不得因译文插入而跳动、漂移或被挤离当前位置。不得修改聊天原文、WhatsApp、订单、ShopPlus、图片读取、消息样式、右侧卡片或其他页面。

### D-091 实施核验（2026-09-23）

`npm run check`、`git diff --check`、隔离 Electron 翻译视口回归及相邻图片按视口读取回归通过。翻译回归显示版本 `1.6.17`，使用 40 条虚构长文本消息确认：初次会话只把实际可见消息送入缓存查询和自动翻译，屏幕外消息不在请求内；滚动到最早消息后，该消息才进入翻译请求。回归在译文写入前后测量当前第一条可见消息的几何位置，顶部偏移不超过 1 px。未调用真实 AI、未读取真实客户聊天、未发送 WhatsApp、未修改订单或 ShopPlus。完整工作台旧回归仍在既有“新消息失败提示计时”断言处未通过；本条不将该不相关路径标记为通过。其他工作台区域、其他页面和既有手机底部 P1/P2 偏差未重新验收。

### D-092｜非文字消息第二步：通知与附件分类（1.7.8，2026-09-23）

仅修复 WhatsApp 系统通知和撤回消息的分类、工作台显示与无正文翻译排除；相应适配器、消息渲染、测试、版本和核验文档允许修改。已核实的 notification_template / biz_account_type_changed_to_hosted 显示中文账号变更提示，其他未识别通知显示中性系统通知，不猜测具体含义；系统通知不得构造图片附件或进入下载/翻译。保留真实消息 ID、顺序、时间、正文；真实图片、贴纸、GIF、网址及现有身份、头像、布局、订单、导航均不改变。旧缓存通知即使携带错误附件元数据，也须在显示侧正确识别。

本次仍不宣称所有图片读取故障已修复：大图上限、下载队列、失败重试留待后续步骤。只交付直接运行的本地窗口，不制作安装包、不提交 Git。按用户要求三位数字逐位进位：历史 1.6.17 对应顺序 1.7.7，本次递增为 1.7.8；历史记录保留原版本号，不伪改历史安装包。旧手机样式不在本轮范围。

### D-092 实施核验（2026-09-23）

两条真实通知通过修复后的适配器返回中文账号变更提示、media=[]，消息 ID 与时间不变。npm run check、11 项单元测试与通知显示/图片视口读取/翻译视口稳定共三项隔离 Electron 回归最终通过；通知显示检查覆盖 1280×820 和 1440×1000，已查看最终截图。正常启动的本地 1.7.8 窗口和工作台均 visible，WhatsApp 已连接，保留原登录；未在正式工作台滚动复验历史通知，不宣称真实显示全流程通过。没有修改其他界面、订单、下载门槛或队列。详细结果与测试首次失败原因见 MEDIA-STEP2-ACCEPTANCE.md。

### D-093｜聊天记录不因刷新缩短而消失（1.7.9，2026-09-23）

仅修复已核对会话的聊天历史读取、工作台刷新合并、缺口提示和必要测试/版本/文档。定时刷新或手动刷新只追加、更新实际取回的消息，不得用更短的“最新 20 条”结果覆盖已经显示的旧消息；向上翻页已取得的消息须保留。对已读取的会话内容建立本机持久记录，重启后在同一账号与同一客户会话中恢复；不得把其他会话或账号资料串入，不得伪造未读到的消息。图片二进制内容不进入聊天历史数据库，仍按既有可视区域机制读取。WhatsApp Web 没有提供更多历史时须明确提示“暂未核实”，不能暗示手机记录已删除；刷新可以重新尝试。消息时间、方向、正文、订单、发送、翻译、图片读取、布局与其他页面保持原样。用户明确要求保留现有未提交改动，只打开本地操作窗口，不制作安装包。

### D-093 实施核验（2026-09-23）

真实指定会话的 WhatsApp Web 缓存及限 100 条的读取均只返回一条 `e2e_notification / encrypt`，没有正文或媒体；订单聊天档案两表均无该会话记录，所以手机截图里的 09-07 旧消息本次无法真实补回。当前本地 1.7.9 窗口直接运行、WhatsApp 已连接、会话显示一条“WhatsApp 加密通知。”及清楚的历史缺口提示，新历史库已保存这条实际读到的消息，文件权限 0600。`npm run check`、13 项相关单元测试及 4 项隔离 Electron 回归通过；隔离窗口确认加载 25 条后刷新只回 1 条时 25 条仍在，图片/翻译/通知相邻行为通过。真实旧记录恢复未通过、全页面未验收；详细边界见 `CHAT-HISTORY-1.7.9-ACCEPTANCE.md`。无安装包、无 Git 提交。

### D-094｜无订单会话也持续保存新消息（1.8.0，2026-09-23）

用户确认聊天历史保存不得以是否存在或关联订单为条件。WhatsApp 账号已连接时，已核对为当前账号的一对一电话会话，无论是否选中工作台、是否关联订单，都应在新消息到达时写入本机独立聊天历史；隐藏窗口后也须保留。保存只包含实际收到的消息文本、方向、时间、类型和必要元数据；图片二进制仍不持久化。不读取群聊或无法核对电话身份的内容，不改订单、发送、翻译、图片按视口读取、聊天布局及其他页面。刷新仍合并本机已保存消息，不得伪造 WhatsApp Web 未提供的旧消息。用户只要直接运行本地操作窗口，不制作安装包、不提交 Git。下一版本从 1.7.9 按逐位进位为 1.8.0。

### D-094 实施核验（2026-09-23）

已接入无订单一对一会话的新消息持久化，并在正常退出时等待写入队列完成。31 项相关单元测试、3 项隔离 Electron 回归、`npm run check` 与 `git diff --check` 通过。正式本地窗口显示 `v1.8.0`、WhatsApp 已连接，指定会话仍只见 1 条通知与历史缺口提示；没有伪造 09-07 的旧消息。真实新消息到达路径未通过实收验收，旧消息恢复未通过；详见 `CHAT-HISTORY-1.8.0-ACCEPTANCE.md`。布局与相邻操作未改，旧手机页 P1/P2 未重验，无安装包、无 Git 提交。

### D-095｜来源对照与可核实历史补齐（1.8.1，2026-09-23）

本轮只处理聊天记录缺失与防止再次丢失。打开已核对的会话时，应从 WhatsApp Web 一次读取并本机保存其当下可获取的近期记录，而不是仅保存屏幕显示的最后 20 条；后续翻页也保存实际读取到的消息。相同消息 ID 的弱占位内容不得覆盖此前保存的真实正文。WhatsApp 临时读取失败时，显示本机已保存记录及准确的故障提示。历史始终按已核对账号和客户号码隔离，不与订单存在与否挂钩；没有从来源读到的旧消息不得从截图推断写入。界面布局、图片读取、翻译、订单、发送和其他页面不在本轮范围。继续直接运行本地窗口，不制作安装包、不提交 Git。

### D-095 实施核验（2026-09-23）

目标号码的当前 WhatsApp Web 内存及其本机 IndexedDB 消息库均只含一条 09-23 通知；现有独立聊天历史 SQLite 对应会话也仅一条，旧订单档案备份未找到该会话。真实 1.8.1 操作窗口已打开该号码，显示一条通知及历史缺口提示，故 09-07 旧历史尚未补回，不能宣称本轮完整验收。隔离单元回归用虚构 37 条来源记录验证首开后全数入库、来源缩为一条后仍保留 37 条；还验证占位消息不能覆盖原文、临时读取失败时从本机恢复。相关单元测试通过；完整测试集在本轮未修改的订单确认、OAuth 等路径仍有失败，隔离 Electron 测试等待超时，详见 `CHAT-HISTORY-1.8.1-ACCEPTANCE.md`。真实图片显示与旧手机页 P1/P2 未重验。

### D-095 纠错（2026-09-23）

D-093 至 D-095 真实来源核验中所称“指定号码／目标号码”，后来被用户明确指出并非其指定客户的正确号码。上次仅一条通知的来源核验属于另一条会话，不能用来判断指定客户的聊天历史是否存在或能否补齐。该次误判的原因是工作台自动选中最近会话，而核验时未先比对用户提供的完整号码。本条保留原始记录并附纠错，不改动任何客户号码或聊天数据。

### D-096｜会话身份确定性拦截（1.8.2，2026-09-23）

仅允许修改聊天工作台的初始会话选择、会话身份核对、必要的只读 WhatsApp 适配器与手动聊天入口、版本、测试和文档。列表仍按最新消息排序，但初始不自动打开第一条；用户明确点击后才读取该会话。程序只在列表号码、实际会话号码或实际 WhatsApp LID 映射出现明确冲突时阻止进一步读取与显示。LID 映射临时失败或暂未返回时不得误拦正确会话，应允许人工点开并显示待复核提示。不得用订单关联、头像、消息条数或系统通知推断身份，不使用 AI 判断，不改真实号码、聊天记录、订单、发送逻辑、图片与翻译功能。保留全部现有未提交改动；本轮只直接运行本地操作窗口，不制作安装包。

### D-096 实施核验（2026-09-24）

`npm run check`、23 项相关单元测试、使用虚构号码的隔离 Electron UI 回归和 `git diff --check` 通过。隔离窗口显示 1.8.2，验证列表不会自动打开首条、号码冲突不显示消息、号码一致的其他会话仍可读取；单元回归确认映射临时缺失不拦、列表缓存过期但号码入口可核实时放行。本地 1.8.2 操作窗口已重新启动，主窗口显示、获得焦点，原 WhatsApp 登录资料恢复在线。未读取用户指出的正确号码对应真实聊天，故该客户真实映射和历史消息仍未验收；其他工作台区域与旧手机页 P1/P2 未重验。详见 `CHAT-IDENTITY-1.8.2-ACCEPTANCE.md`。

### D-097｜本机隐藏会话与恢复入口（1.8.3，2026-09-24）

用户确认仅在聊单助手隐藏误选的那条会话，参考 Salesmartly 会话行的三点菜单形式。仅允许修改工作台会话列表的三点菜单、“隐藏此会话／恢复会话”操作、已隐藏会话入口、必要的本机隐藏状态保存、版本和验收文档。隐藏按已登录 WhatsApp 账号与会话 ID 精确记录，只从聊单助手列表移除；本机聊天历史和 WhatsApp 原始会话、消息均不删除，也不加入黑名单、不结束会话。恢复入口必须可直接使用；隐藏或恢复不会改订单关联、接待状态、翻译、图片与发送逻辑。只对用户已指出的误选会话执行一次精确隐藏，不查询其指出的正确客户聊天。保留现有未提交改动；直接运行本地窗口，不制作安装包。

### D-097 实施核验（2026-09-24）

代码实现后执行类型、UI 构建、共享模块与项目检查；隔离单元测试使用虚构会话验证隐藏状态跨重启保留、账号隔离、原消息不删除、恢复及正常／已隐藏列表独立分页；隔离 Electron 窗口验证三点菜单隐藏、恢复及恢复后仍能读取原消息。真实本机只对先前误选的会话 ID 记录一条隐藏标记，消息表不修改。旧手机页 P1/P2、真实正确客户的消息、图片显示与全工作台基准未在本轮重新验收；本轮只交付 D-097 范围的局部修复。详见 `CHAT-HIDE-1.8.3-ACCEPTANCE.md`。

### D-098｜可见图片队列、大图读取、失败重试与点击放大（1.8.4，2026-09-24）

继续完成 `MEDIA-STEP1-DIAGNOSIS.md` 中通知分类之后的三项图片修复：已选且已核对会话的可见图片限流读取、按单条消息 ID 定位而不重复扫描 500 条、大图安全上限与有界缓存、准确失败说明及手动重试、在原消息气泡点击缩略图后放大查看。排队中的视口外图片和切换会话后的旧任务不显示；不可取消的已开始 WhatsApp 下载只丢弃过期结果，不伪称底层取消。未知总字节数时只显示读取中，不伪造百分比。只允许修改图片链路、消息气泡图片本身、必要桥接、版本、测试和验收文档；其他文字、列表、订单、发送、布局和其他页面不改；纯图片占位文字不送翻译，正常文本翻译保持原行为。真实图像验收必须区分 WhatsApp 实际读取与虚构隔离数据；未取得真实图时不能宣称真实端到端成功。保留现有未提交改动，直接运行本地操作窗口，不制作安装包。

### D-098 实施核验（2026-09-24）

本地 1.8.4 操作窗口已经启动，实屏显示版本 1.8.4、WhatsApp 已连接；未选取用户指出的正确客户聊天。隔离 Electron 窗口用 1024×1024 PNG 验证实际解码、点击放大、原尺寸、关闭、可见区域读取、最多两张并行、失败重试、坏图强制重新下载、阅读锚点与切换会话不串图；已亲自查看最终截图。适配器单元测试验证单消息 ID、1.2 MB 与 16 MB 限制、48 MB 有界缓存及过期任务丢弃。`npm run check`、33 项相关单元测试和 4 项隔离窗口串行回归通过；并行滚动测试未稳定通过，未计入通过范围。真实 WhatsApp 客户图片在正式工作台尚未端到端验收，整页其他基准与旧手机页未重验；本轮只认定 D-098 局部修复。详细证据见 `MEDIA-STEP3-4-1.8.4-ACCEPTANCE.md`。

### D-099｜真实人工消息发送与附件（1.8.5，2026-09-24）

本条只允许修改聊天工作台人工发送链路、输入区必要的附件选择和预览、WhatsApp 发送适配器、版本、测试和验收记录。保持现有聊天、订单、翻译、导航和页面布局；输入工具行只增加与既有表情按钮等大的回形针入口，所选附件仅在输入区内部显示，可逐项移除。人工发送支持文字、Unicode 表情、网址原文、JPEG/PNG/WebP 图片，以及 PDF、TXT、CSV、Word、Excel、ZIP 等常用文件；文件名、MIME、单项和总大小必须在本机发送前验证。文字/链接/表情不得被改写，文件不得当作图片发送。发送前继续核对当前 WhatsApp 账号、目标完整号码和真实会话身份；一次发送拆为文字与附件多个有回执的部分，任何部分结果不确定时停止后续发送、保留草稿且不得自动重试。不得向用户未授权的号码发送。本轮真实发送验收只允许用户指定的测试号码（已脱敏），使用明确标注为聊单助手 1.8.5 验收的无业务测试内容，不调用 AI、不修改订单或其他客户资料。保留全部现有未提交改动，只运行本地操作窗口，不制作安装包、不提交 Git。

### D-100｜会话行新消息数量红圈（并入 1.8.5，2026-09-24）

聊天列表的每条会话须在右侧日期下方显示该会话的真实未读消息数量，使用与现有“全部／未读／未回”统计相同的红色圆形数字样式；未读数为 0 时不显示，1～99 显示实际数字，超过 99 显示 `99+`。刷新会话列表后数字随 WhatsApp 返回的 `unreadCount` 更新；不得根据订单、消息总数或本机猜测生成。只允许增加该会话行数字和必要测试，不改变头像、号码、摘要、接待状态、日期、筛选统计、行高、会话顺序及其他页面。

### D-099 / D-100 实施核验（2026-09-24）

真实发送只对用户授权测试号码（已脱敏）执行。发送前的号码与会话双重核对通过；只读回查确认文字、图片和 TXT 文件分别真实存在于目标会话，最终唯一文字测试由发送接口直接返回 `sent` 和合法回执。媒体首次失败的实际原因是 WhatsApp Web 新版 MediaData 私有 `__x_id` 覆盖外层消息 ID；发送虽成功但回执曾为 `undefined` 的原因是库仍用旧 `_serialized` 查询新建消息。兼容补丁只作用于审核过哈希的 `whatsapp-web.js 1.34.7`，删除媒体私有冲突字段并用统一消息键读取器查询新建消息。未知结果从未自动重发，后续部分在不确定时停止。

隔离 Electron 窗口验证文字、表情、网址原文、图片和 TXT 附件预览及发送载荷；会话行用真实字段值 3、120、0 验证红圈分别显示 `3`、`99+`、隐藏，并亲自查看截图 `artifacts/send-1.8.5/conversation-unread-badges.png`。`npm run check`、36 项相关单元测试及未读红圈、发送、图片、隐藏会话、系统通知、可视翻译共 6 项串行 Electron 回归通过。完整 `npm test` 仍包含本轮范围外的旧订单确认依据断言失败，以及受沙箱回环端口权限影响的本地服务/OAuth 测试失败，不计为本轮通过。正式 1.8.5 本地窗口已启动、显示并获得焦点，启动日志确认 WhatsApp 自动消息连接在线。详细边界见 `REAL-SEND-1.8.5-ACCEPTANCE.md`。本轮不制作安装包、不提交 Git。

### D-101｜消息侧边悬浮操作组（1.8.6，2026-09-24）

聊天工作台中的每条真实消息在鼠标悬停或键盘聚焦时，于消息气泡朝向对话区中央的一侧显示一排紧凑按钮；按钮与气泡横向并列，不占用消息上下空间，尺寸略小于消息正文。固定顺序为“翻译、话术、识图、回复”：翻译只处理当前消息；话术打开既有快捷回复选择；识图仅在该消息已有可用图片时启用，点击后由已配置的支持图片模型识别图片文字并显示在该消息内；回复记录当前消息为引用对象，输入区仅在引用期间显示可取消的回复提示，真实发送时把该消息 ID 作为 WhatsApp 引用回复目标。图片识别和引用回复都必须继续核对当前账号、会话和消息，不得跨会话复用；切换会话后清空回复对象和临时识图结果。未悬停时保持原消息、翻译、图片、输入区、订单栏、导航和其他页面不变。本轮不改变快捷话术内容，不自动调用 AI，不自动发送消息，不制作安装包、不提交 Git。

### D-101 实施核验（2026-09-24）

隔离 Electron 窗口用虚构文字和本机图片逐项验证：收到消息的操作组在气泡右侧，发出消息的操作组在气泡左侧，四个按钮同排且完整位于聊天列内；翻译只提交当前消息 ID，话术打开原快捷回复，图片文字结果回写原消息，回复发送载荷包含原消息 ID，切换或成功发送后清理临时状态。适配器单元测试验证引用消息必须属于当前已核对会话，且多段发送只在第一段携带引用；图片识别输入限制为 PNG/JPEG/WebP、16 MB，并只在人工点击后调用已配置模型。亲自查看 `artifacts/message-actions-1.8.6/hover-actions.png`。未向真实 WhatsApp 发送验收消息，未调用真实图片模型，因此真实引用展示及当前模型的图片识别兼容性尚未端到端验收；旧手机页与整页其他基准未重验。本轮直接运行本地窗口，不制作安装包、不提交 Git。

### D-102｜订单详情补齐统一导航折叠（1.8.7，2026-09-24）

订单详情页必须在左侧“我的工作台”标题旁显示与其他业务页相同的导航折叠按钮；点击后左栏缩为仅图标，按钮继续显示并改为“展开导航栏”，再次点击恢复。折叠状态按既有 `liaodan-unified-nav-collapsed` 本机设置与聊单工作台、订单管理、商品库存、利润核算、助手配置、连接与设置、商品详情和利润详情共享，跨页面切换保持；页面初次载入时按钮文字与箭头也必须立即对应已保存状态。订单详情侧栏底部使用当前实际版本号，移除旧的页面专有连接提示。只允许调整订单详情的统一导航入口及折叠所需宽度适配，以及修正工作台初始折叠按钮的状态同步；订单资料、聊天、卡片、回复、其他页面内容与业务行为不改。本轮同时逐页检查所有九个可进入页面，每页只能有一个可见的折叠／展开入口。

### D-102 实施核验（2026-09-24）

隔离 Electron 窗口以虚构订单、商品和成本记录实际进入九个页面，验证每页仅一个折叠入口，订单详情展开／折叠可点击、折叠宽度 64px、按钮持续可见、状态跨页面保持，订单详情侧栏底部显示 v1.8.7 且不再保留旧连接尾部；工作台首次载入的按钮文字和箭头也与已保存折叠状态一致。已人工查看 1440×900 的订单详情展开及折叠截图。`npm run check`、最终九页 Electron 验收与 `git diff --check` 通过；正式本地 1.8.7 窗口已重新构建、显示并获得焦点，启动日志确认 WhatsApp 自动消息连接在线。旧手机页 P1/P2、真实订单业务内容及各页完整业务流程未重验；本轮不制作安装包、不提交 Git。详细证据见 `NAV-COLLAPSE-1.8.7-ACCEPTANCE.md`。

### D-103｜订单详情移除快捷回复与客户聊天（1.8.8，2026-09-24）

仅在订单详情页彻底移除“快捷回复”面板和“客户聊天”区域，包括关联真实聊天按钮、手机样式聊天窗口及其输入区；进入订单详情时不再装配回复、翻译或真实聊天工具。原订单详情正文使用释放出的页面宽度。导航、折叠按钮、顶栏、布局设置入口、订单概览、客户信息、商品与金额、配送与包裹及其他页面保持原有结构和行为；聊单工作台的聊天和回复能力不受影响。本轮不得以隐藏样式保留两个区域，最终订单详情 DOM 中相关区域应为 0 个。

### D-103 实施核验（2026-09-24）

已打包的 Mac x64 1.8.8 App 使用隔离虚构订单实际打开订单详情；快捷回复、客户聊天、手机框、聊天输入区及“关联真实聊天”的 DOM 数量均为 0。订单正文为单列宽屏，订单概览、客户信息、商品与金额、配送与包裹均保留；导航折叠和布局设置入口保留。返回聊单工作台后，聊天、输入和快捷回复均仍存在。九页导航回归、`npm run check` 与 `git diff --check` 通过，已人工查看 1440×900 实际截图。本轮未重验其他页面的全部 UI 基准；原订单详情手机 P1/P2 随用户指定的客户聊天区整体删除，不再属于该页元素。详见 `ORDER-DETAIL-CLEANUP-1.8.8-ACCEPTANCE.md`。

### D-104｜订单详情恢复已确认业务卡设计（1.8.9，2026-09-24）

用户要求将项目最初已确认的 `ui/src/confirmed/order.html` 订单业务区样式迁移到当前真实订单详情，覆盖 1.8.8 临时素面的动态卡片。页面应恢复确认稿的阶段卡、顶部中文状态组、客户信息与商品金额双卡布局、语义 Lucide 图标、商品图形占位、金额合计分隔及配送地图占位风格。所有文字和状态必须来自当前订单真实字段或明确的“待核对”，不得拷贝确认稿的演示客户、商品、金额、进度或地址。

保留 1.8.8 已完成的范围：订单详情不得恢复快捷回复、客户聊天、手机框、聊天输入区或关联真实聊天入口。现有真实订单读取、草稿编辑与保存、联系电话更正、导航折叠、布局设置、订单数据、其他页面和聊单工作台不改。验收须在 1280×820 与 1440×1000 的最终 ZIP 新解压 App 中检查动态真实字段、卡片结构、图标、双卡布局、滚动及 1.8.8 删除项 DOM 为 0。

订单详情顶栏不得继续显示“固定聊天与自动折叠稿”等已失效的聊天稿说明，统一显示“UI-028 · 订单详情”。

### D-104 实施核验（2026-09-24）

最终 Mac x64 ZIP 全新解压后，以虚构隔离订单实际打开订单详情：1280×820 与 1440×1000 均保持订单阶段卡、客户/商品双卡、商品视觉、金额合计和配送地图风格；顶部状态为中文，页面显示当前订单字段，确认稿中的 Avery Example、KY02、Villa 12 未混入。快捷回复、客户聊天、手机框、聊天输入区和关联真实聊天入口的 DOM 均为 0；返回聊单工作台后聊天、输入和快捷回复仍存在。九个可进入页面的导航折叠回归一并通过。已人工查看两张最终包截图；正式 1.8.9 App 已启动，日志确认 `packaged=true`、`x64`、主窗口已显示并获得焦点、WhatsApp 自动消息连接在线。完整项目检查及 `git diff --check` 通过。交付 ZIP SHA-256 为 `4b0cfd7197dd8d241341c740935639a249f04c4dfe70afe4a6e8a72d2b82599c`。本轮未重验其他页面的全部业务和视觉基准。

### D-105｜订单管理小窗口表格排版与商品分隔线（1.9.0，2026-09-24）

本轮只允许调整订单管理页的格式和分隔线。窗口缩至应用允许的 820×640 时，表格各列必须保持独立宽度，订单编号、客户电话和姓名、商品、金额、来源、状态及操作不得彼此覆盖；宽度不足时由表格容器提供水平滚动，不压缩文字到相邻列。表头、列顺序、内容、字号层级、订单行高度、搜索筛选、分页、ShopPlus 状态与同步、导入和订单详情入口不改。

多商品订单仍在同一个商品单元格内逐行显示全部商品，但商品行之间不得出现额外横线；每个订单之间原有的单条横向分隔线继续保留。允许在小窗口下让页面标题操作区和筛选区自然换行，防止控件互相挤压或超出可视宽度，不改变任何按钮文字、数量、顺序和功能。其他页面和业务数据受保护。最终须在 820×640 与 1280×820 的全新解压 Mac x64 App 中验收表格无重叠、多商品无内部横线、订单行分隔仍为一条，并确认筛选、分页和订单详情入口可用。

### D-105 实施核验（2026-09-24）

最终 Mac x64 ZIP 全新解压后，以包含三条商品的虚构订单实际打开订单管理页。820×640 下表格保持固定列宽并提供横向滚动，文本使用单元格边界裁切而未覆盖相邻列，ShopPlus 状态、同步和导入按钮均完整可见；1280×820 下九列和订单详情入口全部直接显示。三条商品的顶部边框计算值均为 0 px，各订单单元格底部边框为 1 px。ShopPlus 同步弹窗、搜索、日期筛选、分页、导航折叠和跨页版本显示回归通过。已人工检查最终包两张整窗截图。正式 1.9.0 App 已启动，日志确认 `packaged=true`、`x64`、主窗口已显示并获得焦点、WhatsApp 自动消息连接在线。`npm run check` 与 `git diff --check` 通过。交付 ZIP SHA-256 为 `ce666c7c23cbb585972f67aa7da90c0bb2213630057a0a38984dd67a7599df64`。本轮未重验订单管理页之外的完整业务和视觉基准。

### D-106｜聊天工作台 AI 辅助草稿（1.9.1，2026-09-24）

用户明确要求启用聊天工作台顶部“AI辅助”。本轮只允许修改该模式、其明确人工触发的草稿动作、受当前会话令牌限制的主进程草稿接口、必要的 AI 输入校验、版本、测试和交付文档。顶部模式顺序、位置、36 px 高度和三分布局保持“人工、AI辅助、AI自动”；仅 AI辅助从“待启用”变为可选，AI自动继续禁用并显示“待启用”。

进入 AI辅助或切回人工不得自动请求 AI、发送 WhatsApp、修改订单或客户资料，也不得清空中文/英文草稿、附件或引用回复。只有用户在 AI辅助中明确点击“生成 AI 回复”时，才可将当前已选且仍有效的 WhatsApp 会话的有限文字上下文、当前引用客户消息（如有）和用户输入的中文意图交给已配置的服务商；不得读取其他会话、图片或附件，不得记录或显示 API 密钥。结果仅以可编辑英文草稿及中文核对文本返回；人工仍须核对并通过既有“发送确认”身份与回执流程发送。无有效会话、没有可用客户文字、未配置服务商/模型/密钥、输入过长或服务失败时，必须保留已有草稿并给出准确提示；不得重试或降级为自动发送。

保护邻近元素：中文输入/英文翻译切换、快捷回复、附件、表情、清空、发送确认、WhatsApp 状态、会话列表、消息操作、右侧订单栏、列宽、订单详情及其他页面均不在本轮范围。开发与最终独立 Mac x64 App 验收只使用虚构会话、虚构模型结果和隔离用户数据；不得请求真实 AI、读取真实客户聊天或发送真实消息。最终须构建到独立 `dist/聊单助手1.9.1-x64/mac/` 输出目录，并从其中的 Mac x64 `.app` 直接实际启动，验证显示 1.9.1、AI辅助可选、草稿生成不发送、切换保留草稿、未配置失败、AI自动仍禁用，以及相邻输入和人工发送确认入口仍可用；本轮不生成 ZIP。

### D-107｜菜单栏“退出”真实结束应用（1.9.2，2026-09-24）

用户反馈 macOS 菜单栏图标的“退出”没有反应，导致旧版与新版同时运行。本轮只允许修改退出请求、退出前安全保存/停止以及必要的生命周期测试、版本和交付文档。点击窗口左上角红色关闭键继续仅隐藏到菜单栏；点击菜单栏“打开管理页面”继续恢复同一窗口；只有菜单栏“退出”才在完成已有的工作区保存、在线聊天停止、WhatsApp 浏览器关闭和本机数据库关闭后真实结束应用进程。退出过程中重复点击不得启动第二份清理；若安全保存失败，必须取消退出并恢复窗口，不能丢失资料或假称已经退出。

保护范围：聊天、订单、AI辅助、WhatsApp 登录资料、订单数据库内容、工作台布局、其他页面及现有菜单文案均不改。本轮验证只使用隔离虚构资料目录；不得关闭或读写当前正式版本的真实 WhatsApp 会话。最终须构建独立 Mac x64 1.9.2 App 并直接启动，实际验证窗口关闭隐藏/激活恢复、菜单栏退出对应的应用退出，以及退出后可独立重新启动；本轮不生成 ZIP。

### D-107 实施核验（2026-09-24）

根因是顶层 `before-quit` 安全收尾访问了仅在 `bootstrap()` 局部声明的 `manualChat`，抛错后走既有失败回退逻辑，把窗口重新显示并取消退出。修复将该实例提升到主进程共享作用域；窗口红色关闭、菜单栏“打开管理页面”、既有安全收尾顺序及菜单文字不变。安全收尾失败时现在会记录明确错误、恢复窗口并显示失败提示，不会无声假称退出。

隔离 Electron 生命周期回归 3/3 通过：红色关闭键隐藏后 `activate` 恢复；`app.quit()` 正常退出；测试模式下调用与原生 `Menu.buildFromTemplate` 同一闭包的“打开管理页面／退出”回调，确认恢复隐藏窗口后在 12 秒内以 `code=0`、`signal=null` 结束。最终独立 Mac x64 1.9.2 App 从其实际可执行文件启动，生产模式虚构会话验收报告记录 `packaged=true`、版本 `1.9.2`、`arch=x64`、`menuBarQuit={code:0,signal:null}`、`errors=[]`；退出后又从该 App 直接启动独立隔离实例，启动日志确认主窗口可见。所有验收资料目录、号码、会话和 AI 响应均为临时虚构数据；正式 1.9.0 的真实 WhatsApp 进程未关闭或读写。详情见 `QUIT-LIFECYCLE-1.9.2-ACCEPTANCE.md`。

### D-108｜聊天工作台恢复原生窗口拖动（1.9.3，2026-09-24）

用户确认排查结论后要求修复“聊天助手又不能移动”。根因是工作台被 `page !== 'workbench'` 排除在父级原生拖动层之外；iframe 内的 `-webkit-app-region: drag` 不能替代 macOS 的可靠窗口命中区。工作台必须恢复从窗口控制按钮右侧 x=96 到右边缘、现有 56 px 顶栏内的原生可拖区域。

父级拖动层不得覆盖同源 iframe 内的 WhatsApp 状态和“人工／AI辅助／AI自动”控件。实现须把父级拖动层物理分成多个片段，按 `#cwb-connection-status` 与 `.cwb-top-modes` 的实时几何范围留出带边距的孔位；窗口大小、导航展开/收起、客户聊天列和订单列宽度变化、状态文字或类变化后必须重新计算。工作台模态弹窗打开时仍须完全停用拖动层，关闭后恢复。

允许修改：`ui/src/main.tsx` 的工作台拖动几何与模态生命周期、原生拖动针对性测试/成品验收脚本、版本与交付文档。必须保持：顶栏 56 px 高度、四列位置、字号、颜色、导航、AI 辅助流程、WhatsApp 状态、聊天和订单业务、其他页面顶栏和用户资料。最终只交付独立 Mac x64 `.app`，不生成 ZIP；所有自动化与最终验收使用临时虚构资料，不能读写正式 WhatsApp、客户或订单。

验收必须包含：最终 App 的 `packaged=true`、1.9.3、x64 和直接启动；1280×820、820×640、1440×1000 下左右原生鼠标拖动均实际移动窗口且不改变尺寸；AI 辅助按钮可点击、顶栏控件不被父级拖动片段覆盖；切换业务页面再返回、调整窗口、模态弹窗打开停用/关闭恢复；页面无脚本错误。本条只验收工作台顶部拖动局部修复，不代表工作台整页或其他页面完整基准通过。

### D-108 实施核验（2026-09-24）

最终独立 Mac x64 1.9.3 App 从实际 `.app` 启动的隔离验收记录 `packaged=true`、版本 `1.9.3`、`arch=x64`、`errors=[]`。工作台顶栏由父级物理原生拖动片段承接；片段在 `#cwb-connection-status` 与 `.cwb-top-modes` 周围保留实时 4 px 孔位，并在窗口、iframe、导航、客户聊天列／订单列宽度、状态或 DOM 变化及模态开关时更新。真实 macOS `CGEvent` 在 1280×820、820×640、1440×1000 下分别从左右拖动验证窗口移动而宽高不变；模态打开时不移动、关闭后恢复。AI辅助点击、连接状态和三种模式控件均未被遮挡，AI／人工发送和菜单栏退出回归通过。所有验收均为临时虚构资料，`realMessagesSent=0`、`realAiCalls=0`；未读写正式 WhatsApp、客户或订单资料。详细证据见 `WORKBENCH-DRAG-1.9.3-ACCEPTANCE.md`。**整页基准状态：工作台未核验；D-108 顶部拖动局部通过。** 最终 820×640 截图仍显示品牌与 WhatsApp 状态文字的既有重叠，故该窄窗口视觉项未通过；本轮没有修改它，也没有重新检查历史手机底部 P1–P4。另没有修改订单管理页范围外的既有内嵌脚本错误，也没有重验其它页面完整基准；不生成 ZIP，当前批次第 3 个版本未进行 GitHub 提交或推送。

### D-109｜窄窗口顶栏品牌与 WhatsApp 状态防重叠（1.9.4，2026-09-24）

用户明确要求修复 820×640 等窄窗口中“聊单助手”名称与 WhatsApp 状态文字重叠。仅在工作台宽度不超过既有 980 px 响应阈值时，顶栏保持 56 px 高、品牌图标和“聊单助手”名称，以及可辨识的 WhatsApp 图标、状态球和实际“已连接／未连接”文字；可隐藏重复的“WhatsApp”产品前缀与顶栏版本徽标，为二者留出明确间距。状态的完整无障碍名称仍为“WhatsApp 已连接／未连接”，状态控件继续位于客户聊天列右侧且可点击。宽度大于 980 px 时保留原有完整状态文案与版本徽标。

允许修改：`ui/src/chat-workbench-desktop.css` 的这条窄窗口响应式规则，以及相应工作台原生拖动回归／成品验收脚本、版本与交付文档。必须保持：状态 DOM 与更新逻辑、顶栏其余布局、模式按钮位置和 36 px 高度、连接状态图标／颜色／真实状态语义、父级原生拖动孔位、四列、导航、聊天、AI辅助、订单及其它页面均不变；不得读取、写入或发送真实 WhatsApp、客户或订单资料。

验收必须在隔离虚构资料的最终 Mac x64 1.9.4 `.app` 中实际启动并检查：820×640 下品牌与状态边界不相交、状态仍显示实际短状态文字并保留完整无障碍名称；1280×820 与 1440×1000 下完整状态文案和版本徽标保持；三种顶部模式、连接状态和原生拖动片段仍可用且不互相覆盖。此条只验收窄窗口顶栏防重叠，不代表工作台整页或历史手机底部 P1–P4 已通过。

### D-109 实施核验（2026-09-24）

最终独立 Mac x64 1.9.4 App 从实际 `.app` 启动的隔离验收记录为 `packaged=true`、版本 `1.9.4`、`arch=x64`、`errors=[]`、`fictionalClient=true`、`realMessagesSent=0`、`realAiCalls=0`。只在既有窄窗口 CSS 规则中隐藏顶栏版本徽标、缩小状态 pill 并以状态类伪元素显示短“已连接／未连接”；状态原始 DOM、`aria-live`、动态 `aria-label`、图标、状态圆点、连接逻辑及父级拖动几何均保持。最终 820×640 成品几何实际为品牌 x=104..211、状态 x=227..304，不相交；版本徽标 `display:none`，短状态可见。1280×820 与 1440×1000 版本徽标均为 `display:block`，完整 WhatsApp 状态文案可见，三张成品截图已人工查看。原生 `CGEvent` 拖动、连接状态和模式控件孔位、AI辅助点击、业务页往返、模态停用/恢复、窗口生命周期和人工发送入口回归均通过。详细证据见 `TOPBAR-RESPONSIVE-1.9.4-ACCEPTANCE.md`。**整页基准状态：工作台未核验；D-109 窄窗口顶栏局部通过，D-108 拖动回归通过。** 历史手机底部 P1–P4、工作台其它业务区域及其它页面完整基准仍未核验；当前批次第 4 个版本，不进行 GitHub 提交或推送。

### D-110｜工作台中文草稿与快捷回复的手动英文转换恢复（1.9.5，2026-09-24）

用户反馈：在聊单工作台输入中文后无法转换英文；从既有快捷回复填入中文后也没有可见转换路径。本轮只允许恢复工作台人工回复区的明确“转为英文”操作、其草稿状态保护、必要样式与针对性测试/交付文档。保留“中文输入／英文翻译”两个编辑标签的查看和手工编辑语义；不得把标签本身改为隐藏的翻译或发送动作。

在中文草稿非空时，“转为英文”紫色按钮必须清晰可见；点击才调用现有的手动翻译接口，转换中防止重复请求，完成后切换到可编辑英文草稿。用户在请求期间修改中文、选择快捷回复、添加/清空文字或切换编辑状态时，过期请求结果不得覆盖当前英文草稿。失败、未配置或无效结果必须保留当前中文与既有英文，并显示准确反馈；空中文不得发起请求。保留既有 Cmd/Ctrl+Enter 快捷键作为同一明确转换入口。

快捷回复的模板、内容、变量、规则、按钮顺序和发送逻辑均不在本轮范围：点击快捷回复仍只填入中文并清空旧英文，绝不自动调用 AI 或发送；用户必须再明确点击同一个“转为英文”按钮。现有主进程翻译的输入限制、20 秒单次调用、无自动重试、无读取聊天/附件、无写订单/客户资料和无自动发送边界保持不变。库存设计只记录在讨论日志，本轮不改库存页面、ShopPlus 商品接口或网站上下架。

最终必须以隔离临时资料、虚构会话和模拟翻译结果验证：普通中文和快捷回复均在点击后精确调用一次翻译并得到可编辑英文；未点击时调用数为 0；空中文、重复点击、迟到响应、失败/未配置均保留草稿且不发送消息。最终构建独立 Mac x64 `dist/聊单助手1.9.5-x64/mac/` `.app` 并直接实际启动，确认包内版本、人工转换、快捷回复转换、人工发送入口和零真实数据访问；不生成 ZIP。此条只验收输入转换局部修复，不代表工作台整页、历史手机底部 P1–P4、库存页或其他页面完整基准通过。
