# 聊单助手 1.0.12 回溯说明

当前应用：`聊单助手 1.0.12.app`，Bundle ID：`com.liaodan.assistant.rebuild`，资料目录：`~/Library/Application Support/Liaodan Assistant Rebuild`，端口：43876。

1.0.12 只修复手机消息身份与译文显示，并在资料目录 `orders/manual-chat/chat-translations.json` 增加逐条译文缓存；不迁移订单数据库，不改变发送记录。1.0.11 安装包保留，旧版会忽略新缓存。回退前先退出新版 App，勿清理或覆盖本机资料目录。

1.0.11 只恢复聊天记录翻译显示及消息翻译入口，不改变订单数据库、模板或布局参数；1.0.10 安装包保留。

1.0.10 新增本机订单模板和快捷回复模板设置，不迁移订单数据库。1.0.9 安装包继续保留；旧版忽略新增模板设置。

1.0.9 增加回复栏宽度设置及人工发送记录 orders/manual-chat/manual-outbox.json，不更改订单数据库。旧版忽略新增字段；1.0.7、1.0.8 安装包保留。请先退出当前 App 再打开旧版，勿手动删除发送记录（用于重复发送保护）。

本次统一九页布局并增加人工回复工具，不更改订单数据库结构。新增商品图片与索引保存在资料目录 orders/product-media 中，旧版忽略新增图片索引。1.0.4、1.0.5、1.0.6 ZIP 保留；如需对照，先退出当前 App，再打开旧版。1.0.6 沿用 1.0.5 的布局设置，新增回复区高度；旧版忽略该新增字段。

以下为此前产品的历史回溯记录，不是当前版本信息。

# 版本隔离与回溯

## 当前版 8.8.0

- 应用：`KDocs订单助手 8.8.0.app`
- Bundle ID：`com.kdocs.orderassistant`
- 数据目录：`~/Library/Application Support/KDocs Order Assistant`
- 本地管理端口：`43874`

8.8.0 不读写旧版的 `KDocs Inventory Sync` 目录。两个应用可同时保留，回到旧版不会改变新版订单。

8.4 至 8.8 使用同一个新版资料目录；版本号用于辨认 App，不代表每次升级清空订单。8.7 ZIP 继续保留，7.6 回溯版的资料目录独立。

## 创建订单功能之前的存档

回溯基线是 7.6.0，Git 提交为 `480c0a4`（`Workspace baseline before NEW-01 through NEW-07`）。存档目录同时保留可直接打开的 Intel x64 App ZIP 和该提交的原始源码 ZIP。可运行 App 的订单与库存业务代码保持 7.6.0，仅为可并存回溯而使用独立身份、目录和端口：

- 应用：`KDocs创建订单前回溯 7.6.0.app`
- Bundle ID：`com.kdocs.inventorysync.rollback76`
- 数据目录：`~/Library/Application Support/KDocs Inventory Sync Rollback 7.6`
- 本地管理端口：`43872`

回溯时解压 7.6.0 App ZIP，双击其中的 `.app`。该 App 不会打开当前旧版 `KDocs Inventory Sync` 或 8.8.0 的数据目录，可与两者同时保留。

8.8 使用数据库结构 19，升级前自动保存结构 18 的备份。回到 8.7 程序时需要使用升级前匹配的备份；不要用旧程序直接打开新版结构或覆盖新版数据。7.6 回溯基线仍使用独立资料目录。

1.0.8 恢复人工中译英入口，沿用已有加密服务设置，不更改订单数据结构。1.0.7 安装包保留。
# 聊单助手 1.1.0 空白连接版与 1.0.12 并存

1.1.0：`聊单助手 1.1.0.app`，Bundle ID `com.liaodan.assistant.live`，资料目录 `~/Library/Application Support/Liaodan Assistant Live`，本地端口 43877。首次为空，WhatsApp 需要重新扫码。1.0.12 的 ZIP、解压 App、`~/Library/Application Support/Liaodan Assistant Rebuild` 资料目录和端口 43876 保留原样。

两个版本的订单、登录资料、草稿、翻译缓存互不迁移。回到 1.0.12 时直接退出 1.1.0 后打开旧 App；不要把两版资料目录互相覆盖。

## 1.1.1 板块隔离版

1.1.1 与 1.1.0 使用相同的 Bundle ID、端口及 `Liaodan Assistant Live` 资料目录；没有数据库结构变更。回到 1.1.0 前先退出 1.1.1，直接打开保留的 1.1.0 App 即可。1.0.12 的独立资料目录仍不受影响。

## 1.1.2 订单恢复版

1.1.2 与 1.1.1 使用相同的 Bundle ID、端口及 `Liaodan Assistant Live` 资料目录，不会自动导入示例数据。升级前可先备份该目录。回到 1.1.1 前请退出 1.1.2；旧版业务界面不会显示新版导入的订单，正式资料请保留并勿用旧版改写。1.0.12 的独立资料目录仍不受影响。

## 1.1.3 订单电话核对修复版

1.1.3 与 1.1.2 使用同一资料目录，不迁移或改写已导入订单电话。退回 1.1.2 会重新遇到本地号码与国际号码被误判的问题；旧版本 ZIP 保留。

## 1.1.5 六页业务恢复版

1.1.5 延用 1.1.4 的资料目录，恢复六个业务页面，不迁移或清空订单。若要退回 1.1.4，请退出 App 并直接打开保留的 1.1.4 ZIP 中的 App；原有六页会再次显示空状态，已保存的订单与商品档案不会由回退操作删除。对备份执行恢复前仍须核对目标文件，避免覆盖近期订单。

## 1.1.4 工作台订单卡片版

1.1.4 与 1.1.3 使用同一资料目录，不迁移或改写已导入订单。退回 1.1.3 时工作台会重新显示原来的无卡片订单行及点击整行跳转行为；旧版本 ZIP 保留。
