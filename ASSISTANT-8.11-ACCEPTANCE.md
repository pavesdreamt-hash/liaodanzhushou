# 8.11 对话完善验收

2026-09-17，承接8.10，保留既有工作区、原HEAD与暂存区。自动发送继续关闭。

状态：MIXED / PASS-LOCAL。可交付本地功能和安装包；打包安全存储 WAIT-HUMAN，真实聊天完整性与AI语义未通过，不宣称S2/S3或整个项目完成。

## 本轮范围

标签与短翻译按钮同一行、16px字号；单条回复选中/引用/聚焦；移除译文前缀；文本随内容与宽度展开/收缩；聊天可拖动高度并保存。

译文30/90天未访问清理或永久保留，不删除来源和已保存中英文回复。英文草稿带中文对照，修改后旧译文失效；按意图起草与忠实翻译分开。流程条件可设置，保存批准范例辅助后续起草，不等同模型训练。

客户历史独立归档、受控网页读取、TXT导入、分页/搜索；本单提取仍限制在订单时间范围。同客另一单共享历史查看、不共享回复或购买范围。图片支持可识别网页缩略图本地保存/预览/备份。

自动同步/翻译/核对/空草稿起草均默认关闭；自动AI需明确日常额度，验证额度不能用于自动请求。现有草稿和人工编辑受保护；不自动确认客户、付款或发送。

## 不能宣称通过的内容

本轮新增真实AI请求0、真实聊天读取0、正式订单/库存/Sheets写入0、WhatsApp消息发送0。

网页不能证明覆盖账号聊天的第一条；滚动停止仍显示未证明读全。TXT仅证明文件内容已导入，不证明账号全部历史。网页DOM和媒体选择器仅以虚构页面验收，真实WhatsApp身份、完整性与图片下载仍待授权范围核验。图片为缩略图，原图、其他附件/语音和图片AI识别未完成。AI译文、拟回复与提取的真实语义仍待额外额度和核验，不将虚构输出冒称真实模型通过。

## 验收证据

证据目录：`artifacts/assistant-mature-8.11/final-delivery/`。测试均使用虚构隔离数据；正常交付App不用测试参数、评估数据库或.command。

| 验收 | 实际结果与证据 |
|---|---|
| 全部单元 | 281/281通过，0跳过；`artifacts/assistant-mature-unit-final.log` |
| 项目检查 | PASS，共享模块与源码检查通过；`artifacts/assistant-mature-check-final.log` |
| 源版Electron回归 | 18/18通过；`artifacts/assistant-mature-regression-final.log`。最后新增大对话保护另有单元覆盖，并重新构建最终包验收 |
| 最终ZIP实际操作 | 22项中20通过、2失败，0跳过；`artifacts/assistant-mature-final-zip-tests.log`。2失败均为钥匙串授权等待，不能写成22/22 PASS |
| 正常生产启动 | 新解压App、NODE_ENV=production、手动输入与流程设置保存/重启、未配置AI禁用通过；`production-assistant-result.json`，另有`artifacts/assistant-mature-production-retest.log` |
| XLSX/XLS/CSV实际导入 | 各1/1通过；`artifacts/assistant-mature-import-{xlsx,xls,csv}-final.log`。每份虚构文件1订单1商品，成本NULL、未写0，重启和重复导入阻止通过 |
| 消息/草稿/失败 | 165条分页、正文转义、引用、同客两单隔离、剪贴板、保存故障、版本并发、关闭重开、退出重启通过；`workspace-result.json` |
| 布局和新功能 | 900×700、1280×850、1440×900同一行/16px/无横向溢出；35行中文、30行英文自适应展开收缩，无文本框内部截断；聊天280px保存及240～1600px调整；`experience-result.json` |
| 截图实际查看 | 最终包三尺寸chat/reply、900长输入、历史导入搜索和生产模式截图已打开查看；`visual-review.json`记录文件哈希。长内容通过页面滚动到达，不宣称一屏展示全部 |
| 自动处理 | 虚构开关启用后同步→译文→核对→空草稿起草通过，人工草稿不覆盖，关闭停止定时器；`automation-result.json`。生产默认全关闭，验证额度不能自动请求 |
| 历史和媒体 | 实际浏览器虚构DOM的受控滚动、取消、身份变化保护、20条开发限制，以及缩略图读取/持久化通过；`history-crawler-result.json`、`image-dom-result.json`。真实WhatsApp未读 |
| 完整虚构订单 | 新建→关联→读取→模拟提取→地址冲突选择→客户确认→分包→TXT→重启→同客第二单通过；原业务断言保留；`chat-drafts.e2e.mjs`在最终ZIP实测通过 |
| 打包备份恢复 | 从已填数据迁移17到22，升级前备份；关联、消息依据、确认、成本、中英文回复、缓存、历史、流程和批准范例恢复及重启通过；`packaged-assistant-backup.json` |
| 源与包一致 | 100个运行资源逐字节匹配、包版本8.11.0、界面8.11、x86_64、ad hoc签名严格验证通过；`source-match.json`。ad hoc不等于Apple公证 |
| 打包密钥设置 | WAIT-HUMAN；`gear-availability.json`明确安全存储false、调用0、授权等待。没有替换成假加密，未删测试或把上一版通过冒称本版通过 |

初次候选包22项19通过3失败，重启测试因读取旧启动地址已修复并在最终包通过；记录保留在`artifacts/assistant-mature-zip-tests-initial.log`。当前2项授权阻塞的完整失败日志亦保留。超大对话在进入模型请求或提取计数前拒绝、原稿与业务数据保留，新增单元复验通过。

补拍生产模式完成态时新增断言查出了历史窗口关闭后进度提示残留；已修复通用操作成功后的进度清理，保留明确通知/错误并校验当前订单。修复前失败记录`artifacts/assistant-mature-production-retest-before-feedback-fix.log`、候选包完整测试`artifacts/assistant-mature-final-zip-tests-before-feedback-fix.log`保留；修复后重新构建ZIP并复验，不把中间候选包作为交付。

## NEW-01～NEW-07 本阶段

| 项目 | 本轮结论 |
|---|---|
| NEW-01 | PASS-LOCAL：新建、保存、重启、列表和两单隔离 |
| NEW-02 | PASS-LOCAL：商品、NULL成本、系列分包、优惠、业务恢复 |
| NEW-03 | PASS-LOCAL：助手界面、引用、可伸缩聊天、长输入、中文对照与规则入口；打包安全密钥 WAIT-HUMAN |
| NEW-04 | PASS-LOCAL：本单分页、独立历史归档、TXT导入/搜索、连续检查和缩略图。账号全部历史、真实媒体覆盖及其他附件未完成 |
| NEW-05 | 本地差异、冲突、来源和保存保护通过；AI提取/意图/双语/自动处理仅虚构网关通过，真实语义 BLOCKED：额外额度未授权 |
| NEW-06 | PASS-LOCAL：确认/TXT/迁移20～22/备份恢复和草稿持久化 |
| NEW-07 | 虚构完整流程 PASS-LOCAL；真实聊天+AI+订单联合链路未通过 |

流程判断是按已保存事实和可编辑完成条件计算；批准范例作为后续请求上下文，不是自动训练模型或自主修正规则。历史和本单范围分离，已有人工资料的冲突需核对；自动处理不会跳过客户确认、付款或报单。大型上下文当前按单次限制明确拒绝，尚无全历史语义压缩/分批总结。不同来源的历史编号可能不同，不保证TXT与网页交叉来源自动合并同一消息。

## 交付

最终ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.11.0-x64.zip`。

SHA-256：`769afd96c6e315db1b2679d4d1ba4a8dd5a0de9c17f0dba73dac896c835b6f55`。

双击ZIP解压，进入`KDocs库存同步8.11.0`，双击`KDocs订单助手 8.11.0.app`；自动打开默认浏览器本地工作区，点击“订单管理”，打开订单后点击“订单助手”。不运行.command，不自动替换已安装App。若macOS提示未验证开发者，右键App→打开；若钥匙串要求授权，仅由用户处理系统窗口。

本地可直接用：手动订单/核对/分包/TXT、消息与草稿管理、界面调整、规则与缓存设置、已导入历史搜索。需要真实连接才可用且未核验：聊天读取、AI翻译/拟回复/提取及可选自动处理。未接通时禁用或解释原因，不制造模型输出。自动发送、原图/附件/语音/图片理解、全历史覆盖证明和S2/S3真实联合语义仍未完成。

Git采用临时索引和独立交付分支`codex/assistant-dialogue-8.11`；本轮差异相对190份冻结原文件区分，26份有任务改动、164份未变，10个新任务文件；其他既有修改保留。原HEAD `c511f84dea2c31527cb652c92eb554070ba0e62b`和暂存内容保留，不推送。Git状态刷新造成索引元数据哈希与本轮基线不同，不声称从基线起字节始终相同；交付提交前后原索引字节不变。准确提交及保护核验见`artifacts/assistant-mature-8.11/git-delivery.json`。

接下来做什么、由谁做、是否需要我操作：本轮本地修复、复验和ZIP交付由开发完成，用户休息期间无需操作。后续用户仅在使用AI设置时完成macOS钥匙串授权并点击“重新检查安全存储”；真实核验另需明确有限AI额度和指定自用聊天范围，由开发负责复验，用户不负责调试或运行测试。没有Codex后台持续执行任务。
