# 8.16 首次关联补取与人工刷新验收

2026-09-17。按最新用户要求取消8.15后台在线同步，承接现有代码增量修改。状态MIXED / PASS-LOCAL；不宣称全部历史或整个项目完成。

## 行为与使用方法

- 关闭主进程后台同步、助手每30秒读取及3秒状态轮询。旧在线授权、continuousSync设置恢复后也不生效；拒绝重新开启。App启动、重启、切单、关闭/重开助手不会自动打开WhatsApp或读取新消息。
- 首次确认关联时，确认读取当前客户可获取历史后分批归档。首次最多180秒/500批，有进度及停止同步；失败保留关联和成功批次，后续只人工继续，不后台重试。已尝试客户历史的再次关联不重跑初始化。受限验证连接不扩大读取。
- 此后只点击“刷新聊天”更新；未读完可点“继续补齐更早记录”。本地保存记录用“加载较早的聊天记录”分页查看。关闭专用WhatsApp网页后需人工打开，登录失效需扫码，不自动反复拉起。
- 历史补取开始时完整核对账号/联系人一次，期间锁定页面、标题与原生远端聊天标识并监测变化，不每批进入资料页面或重选聊天。同名联系人换原生标识、页面重载/切换均丢弃未保存批次；慢列表等待真实消息变化。
- 旧归档、草稿、迁移24授权原始行、备份与业务配置保留；订单草稿独立。本单范围仍控制回复/提取依据，不自动更新地址、客户确认或发送。翻译/提取选项仅按已确认设置在人工刷新后运行，默认关闭，真实AI仍需安全密钥和额度。

先从菜单栏退出旧版App。双击ZIP解压，进入文件夹后双击 `KDocs订单助手 8.16.0.app`，无需.command，不覆盖已安装App。首次被macOS阻止时在Finder右键App→打开，不关闭系统安全功能。

ZIP：`/Users/apple/Documents/ChatGPT/网站料单Skill/kdocs-diagnostics/kdocs-inventory-app/dist/KDocs库存同步8.16.0-x64.zip`

SHA256：`951f8ea288d492951d9b555fa0d25a53d067e64cefe6dd257b210a32703d5c07`

## 验收证据

全部为虚构临时订单、聊天与表格。真实操作Electron、浏览器DOM和最终App不等同于真实WhatsApp业务联验；真实聊天读取0、真实AI请求0、发送/粘贴0，正式订单/库存/Sheets写入0。

| 项目 | 实际结果 |
|---|---|
| 最终构建 | npm run dist内314/314单元、0跳过；检查PASS，x64 ZIP生成。assistant-manual-build.log |
| 原问题复现 | 新浏览器回归在旧逻辑下FAIL，完整核对7次而预期1次；不是仅根据截图猜测。assistant-manual-browser-reproduction.log |
| 慢加载与目标保护 | 修复后真实虚构DOM三页9条、一次核对、同名原生目标切换拒绝，原游标/停止/覆盖未确认断言保留。manual-history-browser-result.json、refresh-browser-result.json |
| 新单元6项 | 首次同意才补取、旧范围不作本单依据、手动重试去重、部分失败保留、受限连接不扩大、旧开关启动/重启无效且不打开网页、切绑定丢弃。原在线算法兼容性9项保留，但生产服务不能开启。assistant-manual-unit-targeted.log |
| 源版界面 | 4/4，包括旧人工刷新、可选模拟AI流程与新首次关联；新增首次停止1/1单独复验。source-ui-recheck、first-stop-recheck日志 |
| 最终ZIP回归 | 首轮30项29PASS/1FAIL，为关联提示文案变化后的旧精确文字等待；更新测试文案，保留原业务断言。原关联案例及两项人工流程最终包集中复验3/3。合并31个不同案例均PASS，不把首轮日志冒称全通过。assistant-manual-zip-tests.log、assistant-manual-zip-followup.log |
| 首次关联/停止 | 实际点击确认关联读取旧历史；实际停止首次补取，关联和成功批次保留、等待期间不重试，人工刷新补齐；手动新消息、关开助手、重启无消息轮询、两单英文独立。manual-history-result.json、manual-first-stop-result.json |
| 原业务 | 分页、正文转义、引用、剪贴板校验、失败/并发/退出保存、资料冲突、确认、分包TXT、阶段利润、同客第二单、正常重启等原断言通过，见最终两轮日志和workspace/stages等JSON |
| 三格式导入 | 最终App实际.xlsx/.xls/.csv各1/1，重复保护、NULL成本、重启，import-results.json及三份日志 |
| 备份恢复 | 17→24、回复/缓存/历史范围与锚点/阶段/旧授权原始行逐字段恢复及重启；旧授权仍无效。packaged-assistant-backup.json |
| 生产启动 | 新解压App生产模式＋虚构隔离资料打开管理页，显示8.16/8.16.0，无密钥AI禁用，手动输入和流程重启保留。production-assistant-result.json |
| 三尺寸 | 900×700、1280×850、1440×900截图实际查看，设置关闭入口和自主滚动可用、无在线开启按钮；浮层打开会遮住部分下层内容。visual-review.json |
| 包与保护 | 新解压签名/架构/元数据验证，106个源文件与主文档一致；8.13/8.14/8.15 ZIP哈希未变，不覆盖已安装App。source-match.json、final-integrity.json |

初次npm test沙箱监听本机OAuth虚构回调端口EPERM，沙箱外314/314通过。新原生标识回归曾发现变化捕获太晚，加入变化监测及保存后检查，原断言通过。新源版首次关联测试初次选错浮层选择器，修正后4/4；新增停止测试初次误用数字订单ID，改为实际创建返回ID后1/1，不删业务检查。完整失败及复验日志保留。

8.15的两项加密密钥保存测试仍为钥匙串待授权。8.16本轮不重复触发这两项，未验收、不能记PASS；等待授权不阻塞订单操作的最终包测试通过。需用户完成macOS系统授权及“重新检查安全存储”后由开发复验。

## 范围与未完成部分

可用：人工关联/历史补取、人工刷新、本地历史分页、独立草稿、停止与失败恢复、备份和表格导入。仅虚构验证：真实账号历史补取覆盖及完整媒体。首次读取上限、页面不提供的历史或不支持的消息会保留成功批次并提示覆盖未确认，不能保证从第一条开始全读到。自动后台在线功能按用户要求停用，旧代码/数据仅兼容保留；原8.15后台定时测试文件未删除，本轮由新人工及无定时读取案例验收替代。

NEW-01/02/06和虚构NEW-07 PASS-LOCAL；NEW-03关闭入口PASS-LOCAL、密钥设置WAIT-HUMAN；NEW-04人工历史增强PASS-LOCAL、真实覆盖待用户确认范围及登录；NEW-05真实AI语义BLOCKED（无额外额度）。自动发送、云托管、全部媒体、S2/S3剩余功能不宣称完成。

本地提交采用独立分支和临时索引，保留原HEAD、暂存内容/标记/字节及全部无关修改，不推送；220个开工原文件快照完整。提交证明见git-delivery.json。

接下来做什么、由谁做、是否需要我操作：用户退出旧版、解压打开8.16，首次关联需用户确认聊天与历史读取范围、必要时扫码；真实联验和系统授权后的密钥复验由开发集中完成，用户不用运行测试或调试。
