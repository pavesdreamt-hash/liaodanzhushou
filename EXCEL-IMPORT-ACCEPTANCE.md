# 8.0.0 Excel 导入修复验收

## 问题与修复

7.9.0 的管理页在默认浏览器中运行，但文件选择由隐藏 Dock 且没有可见窗口的 Electron 菜单栏进程打开。系统选择窗口可能留在后台，页面则一直等待，表现为两个导入按钮持续变灰。

8.0.0 在浏览器模式中由当前页面直接打开文件选择器。选中的 `.xlsx` 通过只绑定 `127.0.0.1`、要求应用令牌和同源请求的本机通道交给主进程。Electron 内置窗口模式仍保留原生文件选择方式。

导入业务没有改变：仍使用原有预览令牌、文件 SHA-256 复核、重复订单过滤、商品及成本校验和整批事务导入。

## 验收要求

- 点击导入后由当前浏览器出现文件选择器。
- 取消后按钮恢复，可以再次点击。
- 重新选择虚构 ShopPlus `.xlsx` 后显示预览与“准备导入 1 个订单”。
- 最后确认后显示“成功导入 1 个订单”，订单出现在列表。
- 未授权的本机上传被拒绝；仅接受 `.xlsx`，上限 50MB。
- 临时文件在导入成功、重新选择或应用退出时清理，不将 Excel 内容写入日志。

自动验收使用临时隔离数据库、虚构客户及虚构 Excel，不读取正式订单库，不触发 KDocs 采集或 Google Sheets 写入。证据保存在 `artifacts/excel-import-8.0`。

## 8.2.0 XLS 与 CSV 扩展验收

- 浏览器文件选择器接受 `.xlsx`、`.xls`、`.csv`，取消后仍可重新选择。
- 三个虚构文件分别完成上传、表头识别、预览、成本匹配、确认导入和列表显示。
- `.xls` 使用真实旧版 Excel 二进制工作簿；`.csv` 覆盖逗号、引号、两层表头和带分组前缀的单层表头。
- 文件扩展名与内容签名不符时拒绝；`.ods` 等其他扩展名拒绝。
- 三种格式的临时文件均在导入成功、重新选择或应用退出时清理。

打包应用的生产模式证据保存在 `artifacts/import-formats-8.2`。所有订单、客户、地址与商品均为虚构数据。

## 8.3.0 缺失成本导入修复验收

- 文件识别成功且没有阻断错误时，库存成本未匹配不再造成无法导入。
- 必须逐单明确确认成本暂时留空；未确认仍会阻止导入。
- 空成本保存为数据库 `NULL`，不得保存为 0，订单继续标记为需要核对并不计入最终利润。
- 使用用户指定的 ShopPlus `.xlsx` 在临时隔离数据库完成打包应用的文件选择、预览、逐单核对、确认导入、重启持久化和重复导入保护。
- 真实文件只在本机临时验收中读取，不复制到项目、不生成包含客户内容的截图、不写正式订单数据库。
