export const migration017=Object.freeze({version:17,foreignKeysOff:true,name:'chat_drafts_and_order_assistant',statements:[
"CREATE TABLE orders_nullable (\n      id TEXT PRIMARY KEY,\n      source TEXT NOT NULL CHECK (source IN ('shopplus_api','shopplus_excel','manual')),\n      shopplus_order_no TEXT,\n      order_sequence INTEGER,\n      currency TEXT NOT NULL DEFAULT 'AED',\n      order_status TEXT NOT NULL DEFAULT 'new',\n      payment_status TEXT NOT NULL DEFAULT 'unknown',\n      fulfillment_status TEXT NOT NULL DEFAULT 'unfulfilled',\n      delivery_status TEXT NOT NULL DEFAULT 'unshipped',\n      customer_first_name TEXT,\n      customer_last_name TEXT,\n      customer_full_name TEXT,\n      customer_phone TEXT,\n      customer_email TEXT,\n      country TEXT,\n      province TEXT,\n      city TEXT,\n      street TEXT,\n      residence TEXT,\n      postal_code TEXT,\n      order_total INTEGER,\n      product_total INTEGER,\n      discount_amount INTEGER DEFAULT NULL,\n      buyer_note TEXT,\n      order_note TEXT,\n      shopplus_created_at TEXT,\n      shopplus_paid_at TEXT,\n      shopplus_shipped_at TEXT,\n      shopplus_cancelled_at TEXT,\n      profit_status TEXT NOT NULL DEFAULT 'pending' CHECK (profit_status IN ('final','pending','incomplete')),\n      total_profit INTEGER,\n      created_at TEXT NOT NULL,\n      updated_at TEXT NOT NULL\n    , projected_profit INTEGER, realized_profit INTEGER, legacy_total_profit INTEGER, profit_review_required INTEGER NOT NULL DEFAULT 0 CHECK (profit_review_required IN (0,1)), exchange_rate_rule_id TEXT, exchange_rate_version TEXT, exchange_rate_scaled INTEGER, exchange_rate_scale INTEGER, projected_rmb_settlement INTEGER, realized_rmb_settlement INTEGER, rmb_settlement_status TEXT NOT NULL DEFAULT 'pending' CHECK (rmb_settlement_status IN ('final','pending','incomplete')), import_batch_id TEXT REFERENCES import_batches(id) ON DELETE RESTRICT, source_file_name TEXT, source_file_sha256 TEXT, imported_at TEXT, shopplus_site_id TEXT, shopplus_order_sequence_text TEXT, aftersale_status TEXT, repaid_status TEXT, channel_source TEXT, payment_method TEXT, website_domain TEXT, customer_shipping_fee INTEGER, tax_amount INTEGER, shipping_insurance INTEGER, tip_amount INTEGER, fixed_discount_amount INTEGER, door_number TEXT, customer_data_fallback INTEGER NOT NULL DEFAULT 0 CHECK (customer_data_fallback IN (0,1)), billing_first_name TEXT, billing_last_name TEXT, billing_full_name TEXT, billing_phone TEXT, billing_email TEXT, billing_country TEXT, billing_province TEXT, billing_city TEXT, billing_street TEXT, billing_residence TEXT, billing_postal_code TEXT, billing_door_number TEXT, order_confirmed INTEGER NOT NULL DEFAULT 0 CHECK (order_confirmed IN (0,1)), order_confirmed_at TEXT, order_confirmation_note TEXT, fixed_sequence INTEGER CHECK (fixed_sequence IS NULL OR fixed_sequence>0), net_remittance INTEGER CHECK (net_remittance IS NULL OR net_remittance>=0), remittance_registered_at TEXT, workflow_completed_at TEXT, report_date TEXT, report_sequence INTEGER CHECK (report_sequence IS NULL OR report_sequence>0), report_number TEXT)",
"INSERT INTO orders_nullable SELECT * FROM orders",
"DROP TABLE orders",
"ALTER TABLE orders_nullable RENAME TO orders",
"CREATE UNIQUE INDEX orders_shopplus_order_no_unique\n      ON orders(shopplus_order_no)\n      WHERE shopplus_order_no IS NOT NULL AND trim(shopplus_order_no) <> ''",
"CREATE UNIQUE INDEX orders_fixed_sequence_unique ON orders(fixed_sequence)",
"CREATE UNIQUE INDEX orders_report_day_sequence_unique ON orders(report_date,report_sequence) WHERE report_date IS NOT NULL AND report_sequence IS NOT NULL",
"CREATE UNIQUE INDEX orders_report_number_unique ON orders(report_number) WHERE report_number IS NOT NULL",

 `ALTER TABLE orders ADD COLUMN entry_state TEXT NOT NULL DEFAULT 'active' CHECK(entry_state IN ('draft','active'))`,
 `ALTER TABLE orders ADD COLUMN draft_revision INTEGER NOT NULL DEFAULT 0`,
 `ALTER TABLE orders ADD COLUMN draft_request_id TEXT`,
 `CREATE UNIQUE INDEX orders_draft_request_unique ON orders(draft_request_id) WHERE draft_request_id IS NOT NULL`,
 `CREATE TABLE order_draft_items(id TEXT PRIMARY KEY,order_id TEXT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,position INTEGER NOT NULL,sku TEXT NOT NULL,quantity INTEGER NOT NULL CHECK(quantity>0),price_fils INTEGER CHECK(price_fils IS NULL OR price_fils>=0),discount_fils INTEGER CHECK(discount_fils IS NULL OR discount_fils>=0),cost_fils INTEGER CHECK(cost_fils IS NULL OR cost_fils>=0),cost_source TEXT,source TEXT NOT NULL,UNIQUE(order_id,sku))`,
 `CREATE TABLE order_field_sources(order_id TEXT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,field TEXT NOT NULL,source TEXT NOT NULL,evidence TEXT,updated_at TEXT NOT NULL,PRIMARY KEY(order_id,field))`,
 `CREATE TABLE order_assistant(order_id TEXT PRIMARY KEY REFERENCES orders(id) ON DELETE CASCADE,account_id TEXT,chat_id TEXT,scope_start TEXT,scope_end TEXT,binding_revision INTEGER NOT NULL DEFAULT 0,result TEXT,last_read_at TEXT)`,
 `CREATE TABLE order_chat_messages(order_id TEXT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,binding_revision INTEGER NOT NULL,message_id TEXT NOT NULL,direction TEXT NOT NULL CHECK(direction IN ('customer','merchant')),sent_at TEXT NOT NULL,body TEXT NOT NULL,PRIMARY KEY(order_id,binding_revision,message_id))`,
 `CREATE TABLE order_assistant_changes(id TEXT PRIMARY KEY,order_id TEXT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,before_state TEXT NOT NULL,after_revision INTEGER NOT NULL,created_at TEXT NOT NULL,undone INTEGER NOT NULL DEFAULT 0)`
]});
